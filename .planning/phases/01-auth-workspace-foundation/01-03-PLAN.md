---
phase: 01-auth-workspace-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Button.module.css
  - src/components/ui/Card.tsx
  - src/components/ui/Card.module.css
  - src/components/ui/Form.tsx
  - src/components/ui/Form.module.css
  - src/components/ui/Input.tsx
  - src/components/ui/Input.module.css
  - src/app/globals.css
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/signup/page.tsx
  - src/app/(auth)/signup/signup-form.tsx
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/dashboard/loading.tsx
  - src/app/(main)/dashboard/error.tsx
  - src/components/workspace/WorkspaceCard.tsx
  - src/components/workspace/WorkspaceCard.module.css
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The login screen (E1) submits email+password via Auth.js signIn and, on failure, shows the single generic message 이메일 또는 비밀번호가 올바르지 않습니다. (never revealing which field was wrong)"
    - "The signup screen (E2) validates password min-8 client-side (D-01) with the copy 비밀번호는 8자 이상이어야 합니다. using the SAME shared zod schema the server uses"
    - "Both auth forms switch their CTA label to a processing state and disable the button while submitting (로그인하는 중… / 가입하는 중…)"
    - "The dashboard (E3) renders one workspace card per membership; with the default workspace always present there is no zero-card render path (AUTH-03, D-12)"
    - "While the dashboard's server fetch is pending, loading.tsx shows a Suspense skeleton; if the fetch fails, error.tsx shows 워크스페이스 목록을 불러오지 못했어요. with a 다시 시도 button calling reset()"
    - "A long workspace name on a card truncates to one line with an ellipsis (overflow handled, ui-kit .kit-item-name pattern)"
    - "Accent color #2563eb is used only for the reserved elements (form submit CTA, input focus ring) — active-card emphasis uses weight/lightness, not color (UI-SPEC anti-slop)"
  artifacts:
    - path: src/app/(auth)/login/page.tsx
      provides: "E1 login surface"
    - path: src/app/(auth)/signup/page.tsx
      provides: "E2 signup surface (polished; replaces Plan 02 minimal form)"
    - path: src/app/(main)/dashboard/loading.tsx
      provides: "Suspense skeleton for the card grid (D-11 loading state)"
    - path: src/app/(main)/dashboard/error.tsx
      provides: "error boundary with reset() retry (E3 error state)"
    - path: src/components/ui/Button.tsx
      provides: "ported ui-kit Button (CSS Modules)"
    - path: src/components/workspace/WorkspaceCard.tsx
      provides: "single workspace card with one-line ellipsis title"
  key_links:
    - from: src/app/(auth)/login/login-form.tsx
      to: src/auth.ts
      via: "signIn('credentials', {...}) from the client"
      pattern: "signIn"
    - from: src/app/(auth)/signup/signup-form.tsx
      to: src/lib/validation.ts
      via: "client imports signupSchema for parity with the server (Pitfall 5)"
      pattern: "signupSchema"
    - from: src/app/(main)/dashboard/page.tsx
      to: src/components/workspace/WorkspaceCard.tsx
      via: "maps memberships to WorkspaceCard"
      pattern: "WorkspaceCard"
  backstops:
    - statement: "A 100-character user name renders without breaking card/member layout (E2 long-text)"
      verification: backstop
  assumptions:
    - "AUTH-03's acceptance phrase 사이드바에 표시된다 is satisfied in Phase 1 by the default workspace appearing on the card dashboard (D-12) — the persistent sidebar is Phase 4; a missing sidebar is NOT a Phase-1 failure."
---

<objective>
Polish the auth surfaces the tracer left minimal into the approved UI contract: port the six-of-relevant ui-kit components as CSS Modules (Button/Card/Form/Input this plan; Modal/Confirm Dialog are Plan 05), build the E1 login screen, replace the Plan-02 minimal signup form with the E2 signup screen (shared-schema client validation + exact copy + processing states), and complete the E3 card dashboard with its loading.tsx skeleton and error.tsx retry boundary. This is horizontal expansion on the proven tracer — it adds validation, states, and visual contract without changing the architecture (AUTH-01/02/03 surfaces).

Purpose: deliver the actual signed-off UI (UI-SPEC approved 6/6) and the shape-rooted UI state coverage (loading/error/overflow/long-text).
Output: E1/E2/E3 screens matching the UI-SPEC Copywriting + Color + Typography + Spacing contracts.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md
@docs/ui-kit.html

<interfaces>
<!-- From Plan 02: signIn/signOut/auth exported by src/auth.ts; signupSchema exported by src/lib/validation.ts; listMembershipsForUser(userId) in src/lib/db-membership.ts returning {id,name,role}[]. -->
<!-- ui-kit source (docs/ui-kit.html), search by id: 'button' (#4), 'card' (#5), 'form' (#12), 'input-field' (#15). Port the CSS-variable tokens 1:1 into CSS Modules. Button internal padding stays verbatim (btn 8px, form-submit 9px 14px) — NOT re-rounded to the 8-point layout scale (UI-SPEC Spacing note). -->
<!-- UI-SPEC tokens: font IBM Plex Sans/Mono; accent #2563eb (--accent), accent-strong #1d4ed8 (hover), destructive #dc2626; body 13/400, label 12/600, heading 16/600, display 20/600. Page paddings from the 4-multiple scale (xs4/sm8/md16/lg24/xl32/2xl48). -->
<!-- Reading order (UI-SPEC Visual Hierarchy): login = 로그인 title → email → password → CTA → signup link; signup = 가입하기 title → name → email → password → CTA → login link; dashboard = 내 워크스페이스 title → (Plan 05 adds create CTA) → card grid. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port ui-kit Button/Card/Form/Input (CSS Modules) + E1 login screen</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md (Design System, Spacing, Typography, Color, Copywriting), docs/ui-kit.html (ids button/card/form/input-field)</read_first>
  <files>src/components/ui/Button.tsx, src/components/ui/Button.module.css, src/components/ui/Card.tsx, src/components/ui/Card.module.css, src/components/ui/Form.tsx, src/components/ui/Form.module.css, src/components/ui/Input.tsx, src/components/ui/Input.module.css, src/app/globals.css, src/app/(auth)/login/page.tsx, src/app/(auth)/login/login-form.tsx</files>
  <action>
    Port the four ui-kit components from docs/ui-kit.html into CSS Modules, transplanting the CSS-variable tokens verbatim (accent #2563eb, surfaces, border, IBM Plex fonts) into globals.css :root and each *.module.css. Button internal padding stays as the ui-kit original (not re-rounded). Button supports a disabled/pending visual state. Input supports an error variant (destructive #dc2626 border + message). Accent is applied ONLY to the reserved elements: the primary submit CTA background and the input focus ring (2px accent outline) — nothing else (UI-SPEC Color).
    Build src/app/(auth)/login/page.tsx (E1) centered auth card, background --bg solid, reading order title(로그인) → email → password → CTA(로그인) → link to /signup. The client login-form.tsx calls signIn("credentials", { email, password, redirect... }) targeting /dashboard; on failure it shows the single generic message "이메일 또는 비밀번호가 올바르지 않습니다." (do NOT branch the message by cause — info-disclosure). While submitting, the CTA label becomes "로그인하는 중…" and the button is disabled (D-06: no remember-me checkbox anywhere).
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test e2e/login.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/{Button,Card,Form,Input}.module.css exist and reference the ui-kit CSS variables; no Tailwind classes used.
    - E1 login submits via signIn; on wrong credentials the page shows exactly "이메일 또는 비밀번호가 올바르지 않습니다." and no field-specific hint.
    - The submit button shows "로그인하는 중…" and is disabled during submission; there is no remember-me control.
    - e2e/login.spec.ts drives a successful login → /dashboard and a failed login → generic error.
  </acceptance_criteria>
  <done>E1 login screen works against the Plan-02 auth backend using ported ui-kit components; generic error copy verified.</done>
</task>

<task type="auto">
  <name>Task 2: E2 signup polish + E3 dashboard with loading/error boundaries</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md (Copywriting Contract, UI Considerations, Visual Hierarchy), src/lib/validation.ts, src/lib/db-membership.ts</read_first>
  <files>src/app/(auth)/signup/page.tsx, src/app/(auth)/signup/signup-form.tsx, src/app/(main)/dashboard/page.tsx, src/app/(main)/dashboard/loading.tsx, src/app/(main)/dashboard/error.tsx, src/components/workspace/WorkspaceCard.tsx, src/components/workspace/WorkspaceCard.module.css</files>
  <action>
    Replace the Plan-02 minimal signup form with the E2 surface: same centered auth card, identical position/width to login (UI-SPEC hierarchy), reading order title(가입하기) → name → email → password → CTA(가입하기) → link to /login. The client signup-form.tsx imports signupSchema from src/lib/validation.ts and validates client-side BEFORE posting (Pitfall 5 — same source of truth as the server): password under 8 chars shows "비밀번호는 8자 이상이어야 합니다."; a duplicate-email 409 from the server maps to "이미 사용 중인 이메일이에요. 다른 이메일로 시도하거나 로그인해 주세요."; a network/500 maps to "일시적인 오류가 발생했어요. 잠시 후 다시 시도해 주세요.". While submitting, the CTA becomes "가입하는 중…" disabled. On success, sign in and go to /dashboard (D-02).
    Complete the E3 dashboard: page.tsx is an RSC that calls listMembershipsForUser and renders a page title "내 워크스페이스" plus a card grid mapping each membership to WorkspaceCard. WorkspaceCard shows the workspace name one line with white-space:nowrap; overflow:hidden; text-overflow:ellipsis (overflow coverage), and active emphasis via weight/lightness not color. Add loading.tsx (Suspense skeleton cards for the initial server fetch) and error.tsx (a client boundary showing "워크스페이스 목록을 불러오지 못했어요." and a "다시 시도" button that calls reset()). Because AUTH-03 guarantees the default workspace membership, there is no zero-card empty state to design (UI-SPEC Empty state row).
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test e2e/dashboard.spec.ts e2e/signup.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - signup-form.tsx imports signupSchema from src/lib/validation.ts (client/server parity — grep confirms the import).
    - The three signup error cases render their exact UI-SPEC copy; CTA shows "가입하는 중…" disabled during submit.
    - dashboard renders one WorkspaceCard per membership, always ≥1 (default workspace); loading.tsx and error.tsx exist; error.tsx wires a reset() retry button.
    - A workspace name longer than the card width truncates to one line with an ellipsis.
    - e2e/dashboard.spec.ts asserts the default workspace card is visible after signup; e2e/signup.spec.ts still green with the polished form.
  </acceptance_criteria>
  <done>E2 signup and E3 dashboard match the UI-SPEC copy, states, and overflow contract; loading + error boundaries in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + functional verification of E1/E2/E3</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md</read_first>
  <what-built>The E1 login, E2 signup, and E3 card dashboard screens wired to the Plan-02 auth backend, using ported ui-kit CSS-Module components, with loading/error boundaries and the exact Korean copy.</what-built>
  <how-to-verify>
    Run pnpm dev and, against the seeded dev DB:
    1. /signup → create an account with an 8+ char password → confirm immediate landing on /dashboard showing a "기본 워크스페이스" card (AUTH-01/AUTH-03).
    2. Reload /dashboard → still authenticated (AUTH-02).
    3. /login with the wrong password → confirm the single message "이메일 또는 비밀번호가 올바르지 않습니다." and no field-specific hint.
    4. /signup with a 7-char password → confirm "비밀번호는 8자 이상이어야 합니다." client-side; re-use an existing email → confirm the duplicate-email copy.
    5. Confirm accent #2563eb appears only on the submit CTAs and input focus rings; card active emphasis is weight/lightness, not color; a long workspace name truncates with an ellipsis; and (backstop) a 100-char user name does not break layout.
    6. Temporarily force the dashboard fetch to throw → confirm error.tsx copy + "다시 시도" reset works; observe the loading skeleton on a slow load.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all six checks pass, or lists specific deviations from the UI-SPEC.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → server | Client validation is UX only; the server (Plan 02) re-validates via the same schema |
| login error surface | UI must not reveal account existence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Login failure copy | high | mitigate | Single generic message "이메일 또는 비밀번호가 올바르지 않습니다."; the form never branches by cause (mirrors T-02-01 server side) |
| T-03-02 | Tampering | Client-only validation bypass | medium | mitigate | Client imports the SAME signupSchema; the server remains the real gate (Plan 02, Pitfall 5) — UI validation is convenience, not the boundary |
</threat_model>

<verification>
- E1/E2/E3 build and pass their E2E specs.
- Client signup validation uses the shared zod schema (parity with server).
- Login error copy is the single generic message.
- Loading + error boundaries present on the dashboard; overflow ellipsis on card titles.
</verification>

<success_criteria>
- The approved UI-SPEC surfaces E1/E2/E3 are implemented with exact copy, color reservation, and state coverage (loading/error/overflow/long-text).
- AUTH-01/02/03 are exercisable through the real UI, not just the API.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/components/ui/{Button,Card,Form,Input}.tsx` + `.module.css` | new files | ported ui-kit, CSS Modules |
| `src/app/(auth)/login/page.tsx` + `login-form.tsx` | new files | E1 |
| `src/app/(auth)/signup/page.tsx` + `signup-form.tsx` | modified/new | E2 (replaces Plan 02 minimal form) |
| `src/app/(main)/dashboard/page.tsx` | modified | E3 polished |
| `src/app/(main)/dashboard/loading.tsx` + `error.tsx` | new files | loading skeleton + retry boundary |
| `src/components/workspace/WorkspaceCard.tsx` + `.module.css` | new files | one-line ellipsis card |
| `e2e/login.spec.ts`, `e2e/dashboard.spec.ts` | new files | E1/E3 E2E |

<output>
Create `.planning/phases/01-auth-workspace-foundation/01-03-SUMMARY.md` when done.
</output>
