---
phase: 01-auth-workspace-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - tsconfig.json
  - next.config.ts
  - .eslintrc / eslint.config.mjs
  - .env.example
  - .env.local
  - src/db/index.ts
  - src/db/schema.ts
  - src/db/seed.ts
  - drizzle.config.ts
  - drizzle/ (generated migrations)
  - vitest.config.ts
  - playwright.config.ts
  - tests/db/seed.test.ts
  - tests/global-setup.ts
autonomous: false
requirements: [AUTH-03, WS-01]
user_setup:
  - service: postgresql-16
    why: "Every Phase 1 integration test and the signup/seed transaction write to a live PostgreSQL 16 database. Research Environment Audit found NO local Postgres and NO Docker on this machine."
    env_vars:
      - name: DATABASE_URL
        source: "Local: brew install postgresql@16 && brew services start postgresql@16, then createdb markdown_kms → postgresql://localhost:5432/markdown_kms. OR a hosted dev Postgres 16 (Neon/Supabase free tier) connection string."
      - name: DATABASE_URL_TEST
        source: "A separate disposable database/schema for the test suite (e.g. createdb markdown_kms_test) so integration tests never touch dev data."
      - name: AUTH_SECRET
        source: "Generate with: pnpm dlx auth secret (writes to .env.local), or openssl rand -base64 33"

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "Running pnpm build succeeds on the freshly scaffolded Next.js 15 project with no Tailwind config present"
    - "A single system default workspace exists after seeding, named '기본 워크스페이스', with is_default = true (D-08, D-10)"
    - "Re-running the seed never creates a second is_default = true row (idempotent, Pitfall 4)"
    - "The workspace_member table rejects any role value outside OWNER/ADMIN/EDITOR/VIEWER at the database level (D-09, WS-01 substrate)"
    - "DATABASE_URL_TEST is migrated (the 3 tables) and seeded (the default workspace) automatically before every Vitest integration suite via tests/global-setup.ts — so Plan 02/04 integration tests find real tables and a default workspace to join"
    - "pnpm vitest run and pnpm exec playwright test both discover their config and run (empty suites pass) — the TDD harness exists before any implementation"
  artifacts:
    - path: src/db/schema.ts
      provides: "1:1 Drizzle port of TRD §3 Phase-1 tables: user, workspace, workspace_member"
      contains: "workspace_member_role_check"
    - path: src/db/seed.ts
      provides: "Idempotent default-workspace seed exposing seedDefaultWorkspace(db) (check-then-insert on is_default=true), so it can target either DATABASE_URL or DATABASE_URL_TEST"
      contains: "기본 워크스페이스"
    - path: src/db/index.ts
      provides: "drizzle(postgres(DATABASE_URL)) instance exported as db"
      exports: ["db"]
    - path: drizzle.config.ts
      provides: "drizzle-kit config: dialect postgresql, schema ./src/db/schema.ts, out ./drizzle"
    - path: vitest.config.ts
      provides: "Vitest runner config (greenfield — none existed)"
    - path: playwright.config.ts
      provides: "Playwright E2E config (greenfield — none existed)"
    - path: tests/db/seed.test.ts
      provides: "Seed idempotency unit test (runs against DATABASE_URL_TEST)"
    - path: tests/global-setup.ts
      provides: "Vitest globalSetup: programmatically migrates (drizzle-orm migrator over ./drizzle — NOT push) then idempotently seeds DATABASE_URL_TEST before the suite"
  key_links:
    - from: src/db/seed.ts
      to: src/db/schema.ts
      via: "insert into workspace where not exists is_default=true"
      pattern: "isDefault"
    - from: drizzle.config.ts
      to: src/db/schema.ts
      via: "schema path drives generate/migrate"
      pattern: "src/db/schema"
    - from: tests/global-setup.ts
      to: drizzle/
      via: "migrate(testDb, { migrationsFolder: './drizzle' }) then seedDefaultWorkspace(testDb) against DATABASE_URL_TEST"
      pattern: "migrationsFolder|seedDefaultWorkspace"
    - from: vitest.config.ts
      to: tests/global-setup.ts
      via: "test.globalSetup runs the test-DB migrate+seed once before the suite"
      pattern: "globalSetup"
---

<objective>
Stand up the greenfield project skeleton and the database foundation every later Phase-1 plan reads from: scaffold Next.js 15 (App Router, TypeScript, CSS Modules — no Tailwind) via the official create-next-app, install the TRD-locked pinned dependencies, wire Drizzle over PostgreSQL 16, express TRD §3's three Phase-1 tables 1:1 in src/db/schema.ts, and generate → migrate → idempotently seed the single shared default workspace (D-08/D-09/D-10). Also lay the Vitest + Playwright test harness so the TDD-first plans that follow have somewhere to commit failing tests.

Purpose: This is the foundation half of the Walking Skeleton (see SKELETON.md). Without the schema applied to a live DB and the default workspace seeded, AUTH-03 (auto-join as EDITOR) and WS-01 (role-constrained membership) have no substrate and every DB-touching test is a false positive. This plan also provisions the TEST database (DATABASE_URL_TEST) — a Vitest globalSetup migrates+seeds it before any integration suite — so the Plan 02/04 integration tests (which hit DATABASE_URL_TEST) are not false positives against a table-less DB.
Output: A buildable Next.js project, a migrated + seeded PostgreSQL 16 database, and a running (empty) test harness.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-workspace-foundation/SKELETON.md
@docs/TRD.md
@.claude/skills/scaffold/SKILL.md

<interfaces>
<!-- TRD §3 DDL — src/db/schema.ts must be the column-for-column Drizzle expression of this. Source: docs/TRD.md lines 47-68. -->
<!-- user: id uuid PK default gen_random_uuid, email text UNIQUE NOT NULL, password_hash text (nullable — OAuth accounts in R3), name text NOT NULL, created_at timestamptz NOT NULL default now -->
<!-- workspace: id uuid PK, name text NOT NULL, is_default boolean NOT NULL default false, created_at timestamptz NOT NULL default now -->
<!-- workspace_member: workspace_id uuid NOT NULL REFERENCES workspace(id) ON DELETE CASCADE, user_id uuid NOT NULL REFERENCES user(id) ON DELETE CASCADE, role text NOT NULL CHECK (role IN OWNER/ADMIN/EDITOR/VIEWER), created_at timestamptz NOT NULL default now, PRIMARY KEY (workspace_id, user_id) -->
<!-- Drizzle pg-core notes (confirm against node_modules/drizzle-orm/pg-core during this plan): a timestamptz column is timestamp("...", { withTimezone: true }), NOT a separate timestamptz export. Composite PK via primaryKey({ columns: [...] }); CHECK via check(name, sql`...`). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy sign-off before install (SUS cluster)</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-RESEARCH.md (## Package Legitimacy Audit)</read_first>
  <what-built>Nothing yet — this gate precedes the dependency install in Task 2. The research Package Legitimacy Audit flagged next, react, react-dom, typescript, next-auth as SUS purely on a "too-new latest-publish-date" heuristic — a documented false-positive cluster for the most-downloaded packages on npm (5M–254M weekly), all resolving to their canonical org repos (vercel, react, microsoft, nextauthjs). bcrypt, drizzle-orm, drizzle-kit, postgres, zod all verdict OK.</what-built>
  <how-to-verify>
    For each flagged package confirm it resolves to its canonical org with expected download volume:
    - npmjs.com/package/next → github.com/vercel/next.js
    - npmjs.com/package/react and /react-dom → github.com/facebook/react
    - npmjs.com/package/typescript → github.com/microsoft/TypeScript
    - npmjs.com/package/next-auth → github.com/nextauthjs/next-auth
    This is a fast rubber-stamp for this specific mega-package group, not a deep investigation. No [SLOP] packages are present; none are removed.
  </how-to-verify>
  <acceptance_criteria>
    - Human types "approved" (or names a specific package to reject) before Task 2 installs anything.
    - This checkpoint is never auto-approved (legitimacy gates ignore workflow.auto_advance).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to install, or name the package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 15 + install pinned deps + Drizzle wiring + test harness</name>
  <read_first>.claude/skills/scaffold/SKILL.md, .planning/phases/01-auth-workspace-foundation/01-RESEARCH.md (## Standard Stack, ### Scaffold command), docs/TRD.md (§11 directory structure)</read_first>
  <precondition>pnpm is on PATH (research Environment Audit confirmed pnpm 10.18.3) and the working directory is the empty repo root.</precondition>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, next.config.ts, eslint.config.mjs, .env.example, .env.local, src/db/index.ts, drizzle.config.ts, vitest.config.ts, playwright.config.ts</files>
  <action>
    Run the official scaffolder (scaffold skill principle 1 — never hand-roll a file tree): pnpm create next-app@latest into the repo root with TypeScript, ESLint, App Router, src-dir, import-alias "@/*", use-pnpm, and Tailwind DISABLED (the --no-tailwind negation; confirm the flag with create-next-app --help first, and if it does not take, manually delete any tailwind/postcss config and strip the Tailwind import from globals.css — TRD §11 mandates CSS Modules, UI-SPEC confirms shadcn/Tailwind are not adopted). Pin Next to the 15.5.x line, NOT 16.x: after scaffold, verify package.json next resolves to 15.5.x (registry latest has moved to 16 which renames middleware.ts to proxy.ts); if the scaffold pulled 16, downgrade to next@15.5.22 and re-run install.
    Install the runtime deps at the RESEARCH-pinned versions: next-auth@5.0.0-beta.32, bcrypt@6.0.0, drizzle-orm@0.45.2, postgres@3.4.9, zod@4.4.3, lucide-react. Install dev deps: drizzle-kit@0.31.10, @types/bcrypt@6.0.0, vitest@4.1.10, @playwright/test@1.62.1, and tsx (to run seed.ts). Re-run the npm view version checks from RESEARCH before pinning next-auth@beta in case a newer beta shipped.
    Wire the DB client in src/db/index.ts: export a db built from drizzle(postgres(process.env.DATABASE_URL)) using the postgres-js adapter (per RESEARCH db/index.ts example). Create drizzle.config.ts with defineConfig: dialect postgresql, schema "./src/db/schema.ts", out "./drizzle", dbCredentials.url from process.env.DATABASE_URL.
    Create vitest.config.ts and playwright.config.ts (no watch mode — CI-style single-run). Do NOT wire test.globalSetup yet — the DATABASE_URL_TEST migrate/seed hook is added in Task 4 once the ./drizzle migrations and the seed exist (wiring it now would break this empty-suite run because there are no migrations to apply). Add package.json scripts if the scaffold did not: dev, build, and confirm pnpm vitest run + pnpm exec playwright test resolve. Do NOT add a project-wide "test" script that runs Playwright automatically in unit runs.
    Generate AUTH_SECRET into .env.local (pnpm dlx auth secret) and record DATABASE_URL, DATABASE_URL_TEST, AUTH_SECRET placeholders in .env.example. Confirm .gitignore covers .env.local, node_modules, .next, and the postgres.js has no secrets in source.
    Do NOT create schema.ts, auth.ts, routes, or UI here — those are Task 4 and later plans. This task ends at a buildable empty skeleton with the test harness present.
  </action>
  <verify>
    <automated>pnpm build && pnpm vitest run && test ! -f tailwind.config.ts && test ! -f tailwind.config.js && node -e "const p=require('./package.json');const v=p.dependencies.next;if(!/^\D*15\.5/.test(v)){console.error('next not pinned to 15.5.x:',v);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build completes with exit 0 on the scaffolded project.
    - No tailwind.config.* exists; globals.css has no Tailwind directives.
    - package.json dependencies.next matches 15.5.x (not 16.x); next-auth, bcrypt, drizzle-orm, postgres, zod present at RESEARCH-pinned versions.
    - pnpm vitest run exits 0 (empty suite) and playwright.config.ts is discoverable.
    - src/db/index.ts exports db; drizzle.config.ts points schema at ./src/db/schema.ts.
  </acceptance_criteria>
  <done>A buildable Next.js 15 App Router project (CSS Modules, no Tailwind) with pinned deps, a Drizzle client, and a runnable Vitest + Playwright harness — no application code yet.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm default-workspace seed shape before baking it in (D-08)</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-CONTEXT.md (D-08, D-09, D-10), docs/PRD.md (§3 last paragraph)</read_first>
  <decision>Bake the single shared system-seeded default workspace (is_default=true, name "기본 워크스페이스", every signup auto-joins as EDITOR) into src/db/seed.ts and the migration, per locked decision D-08.</decision>
  <context>D-08 is rated **costly**: once this seed runs in a production database, reversing it (e.g. switching to per-user personal default workspaces) requires a data migration plus rewriting the signup membership logic and the AUTH-03 tests. This checkpoint surfaces that weight before the agent writes the migration + seed in Task 4 — it is a confirmation of an already-locked door, not a re-opening of D-08. If the developer has changed their mind about the shared-vs-personal model, this is the last cheap moment to say so.</context>
  <options>
    <option id="proceed">
      <name>Proceed with D-08 as locked (single shared default workspace)</name>
      <pros>Matches CONTEXT.md D-08/D-09/D-10 exactly; AUTH-03 auto-join semantics are already designed for it.</pros>
      <cons>Costly to reverse after a production seed.</cons>
    </option>
    <option id="revisit">
      <name>Revisit the default-workspace model before writing the seed</name>
      <pros>Cheapest possible moment to change the shared-vs-personal decision.</pros>
      <cons>Re-opens a locked decision; ripples into signup transaction and AUTH-03 tests across Plans 02-03.</cons>
    </option>
  </options>
  <acceptance_criteria>
    - Human selects proceed or revisit before Task 4 writes seed.ts / runs migrate.
  </acceptance_criteria>
  <resume-signal>Select: proceed or revisit.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: [BLOCKING] Schema tables + generate → migrate → idempotent seed</name>
  <read_first>docs/TRD.md (§3 DDL lines 47-68), .planning/phases/01-auth-workspace-foundation/01-RESEARCH.md (### db/schema.ts, Pattern 3, Pitfall 4), CLAUDE.md (schema single-source invariant)</read_first>
  <precondition>PostgreSQL 16 is reachable at BOTH DATABASE_URL (dev) and DATABASE_URL_TEST (test) — user_setup provisions both (local brew postgresql@16 with two databases, or hosted dev + a separate disposable test DB). A read-only reachability check on each (e.g. drizzle-kit migrate connecting / a globalSetup connect) confirms it; if either is unreachable, HALT — do not partial-apply.</precondition>
  <reversibility rating="costly">Seeding a production default workspace requires a data migration to undo (D-08); the schema tables themselves are the contract every later phase's rows reference.</reversibility>
  <files>src/db/schema.ts, src/db/seed.ts, drizzle/ (generated), tests/db/seed.test.ts, tests/global-setup.ts, vitest.config.ts</files>
  <action>
    Author src/db/schema.ts as the column-for-column Drizzle expression of TRD §3's three Phase-1 tables ONLY — user, workspace, workspace_member. Match every column, nullability, default, the email UNIQUE constraint, password_hash nullable, name NOT NULL, workspace.is_default boolean default false, the workspace_member composite primary key (workspace_id + user_id) via primaryKey({ columns: [...] }), the ON DELETE CASCADE foreign keys, and the role CHECK constraint named workspace_member_role_check restricting role to the four allowed values (D-09, WS-01 substrate). Use timestamp(col, { withTimezone: true }) for the timestamptz columns — confirm this against node_modules/drizzle-orm/pg-core before committing (do not import a nonexistent timestamptz symbol). Do NOT add folder, document, tag, draft, join-request, invitation, or any soft-delete/closure columns — those belong to Phases 3-8.
    Author src/db/seed.ts (Pattern 3, idempotent) exporting seedDefaultWorkspace(db) that takes a Drizzle instance so it can seed EITHER database: query for an existing workspace with is_default true; if one exists return it; otherwise insert one with name "기본 워크스페이스" (D-10) and is_default true (D-08). Never bare-insert. Add a CLI entry (run-directly guard) that calls seedDefaultWorkspace with the default DATABASE_URL-backed db from src/db/index.ts, so pnpm tsx src/db/seed.ts still seeds the dev DB. Do NOT add a partial unique index on is_default unless TRD §3 is updated first (CLAUDE.md schema-single-source invariant) — the check-then-insert guard is sufficient this phase.
    Write tests/db/seed.test.ts FIRST verifying: after two consecutive seed runs, exactly one row has is_default=true and its name is "기본 워크스페이스". Point the test at DATABASE_URL_TEST (its tables are provisioned by the globalSetup below).
    Author tests/global-setup.ts (Vitest globalSetup): connect to DATABASE_URL_TEST via postgres.js, apply the committed migrations with migrate(drizzle(sql), { migrationsFolder: "./drizzle" }) from drizzle-orm/postgres-js/migrator (this is the generate→migrate flow applying the SAME ./drizzle SQL — NOT drizzle-kit push), then call seedDefaultWorkspace(testDb). Both steps are idempotent (migrate tracks applied migrations; seed is check-then-insert), so it is safe to run before every suite. This runs BEFORE any Vitest integration test in Plans 01/02/04, guaranteeing the test DB always has the 3 tables + the default workspace. Wire it into vitest.config.ts as test.globalSetup: ["./tests/global-setup.ts"] (added now, in Task 4 — the ./drizzle migrations it applies exist only after the generate step below).
    Run drizzle-kit generate to produce the migration SQL in drizzle/, then drizzle-kit migrate against DATABASE_URL to apply it to the live DEV DB (NOT drizzle-kit push — CLAUDE.md invariant / RESEARCH anti-pattern: push has no history and can silently drop columns). Then run the seed (pnpm tsx src/db/seed.ts) against the dev DB. The TEST DB is migrated + seeded by the globalSetup, which fires when the seed test runs.
  </action>
  <verify>
    <automated>pnpm drizzle-kit generate && pnpm drizzle-kit migrate && pnpm tsx src/db/seed.ts && pnpm vitest run tests/db/seed.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Migration applied to the DEV DB: user, workspace, workspace_member tables exist (drizzle-kit migrate exits 0).
    - The TEST DB (DATABASE_URL_TEST) has the same 3 tables + the seeded default workspace before any integration suite: tests/db/seed.test.ts targets DATABASE_URL_TEST and passes only because the globalSetup migrated+seeded it first (its green run proves Plans 02/04 preconditions are met — no missing-tables error).
    - vitest.config.ts declares test.globalSetup pointing at ./tests/global-setup.ts, and tests/global-setup.ts applies ./drizzle via the drizzle-orm migrator (migrationsFolder: "./drizzle") — NOT drizzle-kit push.
    - Exactly one workspace row has is_default=true, name "기본 워크스페이스"; a second seed run leaves it at exactly one (tests/db/seed.test.ts green).
    - Inserting a workspace_member with role 'MANAGER' (or any value outside OWNER/ADMIN/EDITOR/VIEWER) is rejected by the DB CHECK constraint.
    - schema.ts declares exactly three tables — `grep -c 'pgTable(' src/db/schema.ts` returns 3 (user, workspace, workspace_member only); no Phase 2-8 tables are present.
  </acceptance_criteria>
  <done>The three Phase-1 tables are migrated onto live PostgreSQL 16 (both the dev DB and DATABASE_URL_TEST), the single shared default workspace is seeded idempotently into each, and the seed test is green against the test DB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party package code enters the build (supply chain) |
| app → PostgreSQL | The seed/migration writes rows other systems will read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | pnpm install of next/react/react-dom/typescript/next-auth (SUS by too-new heuristic) | high | mitigate | Task 1 blocking-human legitimacy checkpoint before install; verify each resolves to its canonical org repo; no [SLOP] packages present |
| T-01-01 | Denial of Service (data integrity) | Default-workspace seed run twice → two is_default=true rows, ambiguous signup join | medium | mitigate | Idempotent check-then-insert seed (Pattern 3); tests/db/seed.test.ts asserts exactly one row across repeated runs (Pitfall 4) |
| T-01-02 | Tampering | Role string typo bypassing the four-value role set | medium | mitigate | Postgres CHECK constraint workspace_member_role_check as defense-in-depth (ASVS V4); enforced at the DB, not only app layer |
| T-01-03 | Tampering | SQL injection via future name fields | low | accept | Drizzle parameterized query builder; no raw string concatenation. No user-supplied strings reach the DB in this plan (seed name is a constant) — full mitigation lands with the input surfaces in Plans 02/04 |
</threat_model>

<verification>
- pnpm build green on the scaffold (no Tailwind).
- drizzle-kit migrate applied the three tables to the live dev Postgres 16.
- The test DB (DATABASE_URL_TEST) is auto-migrated + seeded by tests/global-setup.ts before the Vitest suite (proven by the seed test passing against it) — Plans 02/04 integration tests inherit a ready test DB.
- Seed idempotent: exactly one is_default workspace named "기본 워크스페이스" after repeated runs.
- Test harness runs: pnpm vitest run (empty + seed test) and pnpm exec playwright test both resolve their config.
</verification>

<success_criteria>
- A buildable Next.js 15 App Router project exists with CSS Modules (no Tailwind), pinned TRD-locked deps, and a Drizzle client.
- The three Phase-1 tables are migrated onto live PostgreSQL 16 — the dev DB (via drizzle-kit migrate) and DATABASE_URL_TEST (via the Vitest globalSetup).
- The single shared default workspace is seeded idempotently into both DBs (D-08/D-09/D-10).
- Vitest + Playwright harness is present and runs; every Vitest integration suite (this plan and Plans 02/04) finds a migrated + seeded test DB.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/db/schema.ts` | new file | Drizzle: `user`, `workspace`, `workspaceMember` tables; `workspace_member_role_check` |
| `src/db/index.ts` | new file | exports `db` (drizzle + postgres.js) |
| `src/db/seed.ts` | new file | `seedDefaultWorkspace(db)` — idempotent, DB-parameterized |
| `drizzle.config.ts` | new file | drizzle-kit config |
| `drizzle/` | new dir | generated migration SQL |
| `vitest.config.ts`, `playwright.config.ts` | new files | test harness (`vitest.config.ts` wires `test.globalSetup`) |
| `tests/global-setup.ts` | new file | Vitest globalSetup — migrates (drizzle-orm migrator, not push) + seeds `DATABASE_URL_TEST` before the suite |
| `tests/db/seed.test.ts` | new file | seed idempotency (targets `DATABASE_URL_TEST`) |
| `.env.example` | new file | `DATABASE_URL`, `DATABASE_URL_TEST`, `AUTH_SECRET` |
| scaffold files | new | Next.js 15 project (package.json, tsconfig, next.config, eslint, src/app scaffold) |

<output>
Create `.planning/phases/01-auth-workspace-foundation/01-01-SUMMARY.md` when done.
</output>
