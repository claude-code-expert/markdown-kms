---
phase: 01-auth-workspace-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - src/components/ui/Modal.tsx
  - src/components/ui/Modal.module.css
  - src/components/ui/ConfirmDialog.tsx
  - src/components/ui/ConfirmDialog.module.css
  - src/components/workspace/CreateWorkspaceModal.tsx
  - src/components/workspace/DeleteWorkspaceDialog.tsx
  - src/components/workspace/CreateWorkspaceButton.tsx
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/w/[wsId]/page.tsx
  - e2e/workspace-create.spec.ts
  - e2e/workspace-delete.spec.ts
autonomous: false
requirements: [WS-02]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The dashboard shows a 워크스페이스 만들기 CTA that opens a modal with a single name field (D-13); submitting it POSTs to /api/workspaces and, on success, navigates to /w/[newId] (D-14)"
    - "The create modal enforces the same 100-char name cap as the server via the shared workspaceSchema (O2), and its confirm button shows 만들기 → 만드는 중… disabled while submitting"
    - "A workspace card exposes a delete affordance only for workspaces the user owns; clicking it opens a GitHub-style confirm dialog that keeps the 삭제 button disabled until the typed text exactly matches the workspace name (D-15)"
    - "The delete dialog's confirm button shows 삭제 → 삭제하는 중… while deleting; on server failure it shows the inline destructive copy 삭제하지 못했어요. 다시 시도해 주세요. and stays open"
    - "On successful delete the workspace disappears from the dashboard immediately; on success create navigates away to the new workspace's placeholder screen"
    - "/w/[wsId] renders a minimal empty placeholder for a freshly created workspace (D-14) — no 3-pane sidebar (that is Phase 4)"
    - "Hiding the delete affordance for non-owners is UX only — the DELETE route's OWNER requireRole gate (Plan 04) is the real boundary (NFR-3.2)"
  artifacts:
    - path: src/components/workspace/CreateWorkspaceModal.tsx
      provides: "E4 create modal — single name field, shared-schema validation, navigate on success"
    - path: src/components/workspace/DeleteWorkspaceDialog.tsx
      provides: "E5 re-type-name confirm dialog (D-15)"
    - path: src/app/(main)/w/[wsId]/page.tsx
      provides: "D-14 placeholder screen for a created/active workspace"
    - path: src/components/ui/Modal.tsx
      provides: "ported ui-kit Modal (#19)"
    - path: src/components/ui/ConfirmDialog.tsx
      provides: "ported ui-kit Confirm Dialog (#33)"
  key_links:
    - from: src/components/workspace/CreateWorkspaceModal.tsx
      to: src/app/api/workspaces/route.ts
      via: "POST /api/workspaces then router.push(/w/[newId])"
      pattern: "/api/workspaces"
    - from: src/components/workspace/DeleteWorkspaceDialog.tsx
      to: src/app/api/workspaces/[id]/route.ts
      via: "DELETE /api/workspaces/:id after name match"
      pattern: "DELETE"
    - from: src/components/workspace/CreateWorkspaceModal.tsx
      to: src/lib/validation.ts
      via: "client imports workspaceSchema for the 100-char cap (O2)"
      pattern: "workspaceSchema"
  backstops:
    - statement: "A 100-character workspace name entered in the create modal renders and submits without breaking the modal or the resulting card layout (E4 long-text)"
      verification: backstop
  assumptions:
    - "Workspace name max length = 100 chars (O2, research-recommended) — enforced in workspaceSchema (client + server, Plan 04), NOT as a DB column constraint. UI-SPEC flagged this ⚠ unresolved and delegated the cap to the planner; recorded here as the explicit assumption."
    - "No success toast for create/delete in Phase 1 — the UI-SPEC leaves ui-kit Toast (#35) adoption to the planner; deferred (list mutation is the feedback). Not a scope gap."
---

<objective>
Add the two workspace mutation surfaces on top of the dashboard and the Plan-04 API: the E4 create modal (single name field, D-13; navigate to /w/[newId] on success, D-14) and the E5 delete dialog (GitHub-style re-type-name confirm, OWNER-visible, D-15), plus the /w/[wsId] placeholder screen. This is the last horizontal slice — it wires the workspace-CRUD UI to the already-tested create/delete routes (WS-02) and completes the shape-rooted UI states for E4/E5 (loading/error).

Purpose: make WS-02 usable through the product, with the destructive delete guarded by the strong confirm the user asked for (D-15) while the real authorization stays server-side (Plan 04).
Output: E4 create modal, E5 delete dialog, /w/[wsId] placeholder, and their E2E specs.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md
@docs/ui-kit.html

<interfaces>
<!-- From Plan 03: ported Button/Card/Form/Input (CSS Modules), WorkspaceCard, dashboard page.tsx (title 내 워크스페이스 + card grid). This plan adds the create CTA to the dashboard and a delete affordance to owned cards. -->
<!-- From Plan 04: POST /api/workspaces { name } → { id } (creator OWNER); DELETE /api/workspaces/:id (OWNER-only, 403 "이 작업을 수행할 권한이 없습니다.", is_default not deletable). workspaceSchema (name ≤100) in src/lib/validation.ts. -->
<!-- ui-kit source (docs/ui-kit.html): id 'modal' (#19), id 'confirm-dialog' (#33). Port tokens 1:1; modal padding lg 24px, action button gap sm 8px (UI-SPEC Spacing). Destructive #dc2626 on the delete confirm button. -->
<!-- Copywriting (UI-SPEC): create CTA "워크스페이스 만들기" / modal confirm "만들기" → "만드는 중…"; create fail "워크스페이스를 만들지 못했어요. 잠시 후 다시 시도해 주세요."; delete dialog title "워크스페이스를 삭제할까요?", body "'{이름}'과(와) 그 안의 모든 문서·폴더가 영구적으로 사라지며 되돌릴 수 없습니다. 계속하려면 워크스페이스 이름을 정확히 입력하세요.", confirm "삭제" → "삭제하는 중…" (disabled until typed name matches), delete fail inline "삭제하지 못했어요. 다시 시도해 주세요." (dialog stays open). -->
<!-- membership role is available per card (from listMembershipsForUser {id,name,role}); the delete affordance shows only when role === 'OWNER'. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port ui-kit Modal + Confirm Dialog; E4 create modal wired to POST</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md (Copywriting, UI Considerations E4, Spacing), docs/ui-kit.html (ids modal, confirm-dialog), src/lib/validation.ts</read_first>
  <files>src/components/ui/Modal.tsx, src/components/ui/Modal.module.css, src/components/ui/ConfirmDialog.tsx, src/components/ui/ConfirmDialog.module.css, src/components/workspace/CreateWorkspaceModal.tsx, src/components/workspace/CreateWorkspaceButton.tsx, src/app/(main)/dashboard/page.tsx</files>
  <action>
    Port docs/ui-kit.html Modal (#19) and Confirm Dialog (#33) into CSS-Module components (transplant tokens verbatim; modal box padding lg 24px, action gap sm 8px; a lucide-react close icon on the modal). ConfirmDialog is generic enough for Task 2's delete flow.
    Build CreateWorkspaceButton (the "워크스페이스 만들기" accent CTA) and add it to the dashboard header row, right-aligned next to the "내 워크스페이스" title (UI-SPEC hierarchy — smaller footprint than the grid). Build CreateWorkspaceModal: a single name field (D-13), client-validated with workspaceSchema imported from src/lib/validation.ts (100-char cap, O2 — parity with the server). Submit POSTs to /api/workspaces; the confirm button label goes "만들기" → "만드는 중…" disabled while pending (E4 loading); on a non-2xx it shows the inline copy "워크스페이스를 만들지 못했어요. 잠시 후 다시 시도해 주세요." and stays open; on success it closes and router.push to /w/[newId] (D-14).
    The dashboard change is additive (header CTA + rendering owned-card delete affordance is Task 2) — keep the Plan-03 card grid intact.
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test e2e/workspace-create.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - CreateWorkspaceModal imports workspaceSchema from src/lib/validation.ts (grep confirms) and rejects a >100-char name client-side (O2).
    - The create CTA reads "워크스페이스 만들기"; the modal confirm reads "만들기" and switches to "만드는 중…" disabled while submitting.
    - On success the app navigates to /w/[newId]; on failure the modal shows the create-fail copy and stays open.
    - e2e/workspace-create.spec.ts: create a workspace → land on /w/[newId] → the new workspace appears when returning to the dashboard.
  </acceptance_criteria>
  <done>E4 create modal works against the Plan-04 create route with shared-schema validation and correct states; navigates to the new workspace on success.</done>
</task>

<task type="auto">
  <name>Task 2: E5 delete dialog (re-type confirm) + /w/[wsId] placeholder</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-CONTEXT.md (D-14, D-15), .planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md (Copywriting delete rows, UI Considerations E5), CLAUDE.md (UI hiding is not security)</read_first>
  <files>src/components/workspace/DeleteWorkspaceDialog.tsx, src/app/(main)/dashboard/page.tsx, src/app/(main)/w/[wsId]/page.tsx, e2e/workspace-delete.spec.ts</files>
  <action>
    Add a delete affordance to WorkspaceCard rendering ONLY when the card's membership role is OWNER (UX convenience — the real gate is Plan 04's DELETE requireRole; do not treat hiding as security). Wire it to DeleteWorkspaceDialog built on the ported ConfirmDialog: title "워크스페이스를 삭제할까요?", body with the exact D-15 copy interpolating the workspace name, a text input, and a destructive (#dc2626) "삭제" button that stays disabled until the typed value exactly equals the workspace name (D-15). While deleting: "삭제" → "삭제하는 중…" disabled. On success: DELETE /api/workspaces/:id, close the dialog, remove the card from the dashboard immediately (no success toast — deferred). On failure: show inline destructive "삭제하지 못했어요. 다시 시도해 주세요." and keep the dialog open. The seeded default workspace (is_default) is not owned by anyone and shows no delete affordance (D-09) — and the server rejects it anyway (Plan 04).
    Build src/app/(main)/w/[wsId]/page.tsx: a minimal placeholder screen for an active/just-created workspace (D-14) — show the workspace name and an empty-state message; NO persistent folder sidebar (Phase 4). Guard it with auth() and a membership check (non-members should not read it).
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test e2e/workspace-delete.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The "삭제" button is disabled until the typed text exactly matches the workspace name (D-15); it reads "삭제하는 중…" disabled while deleting.
    - The delete affordance appears only on OWNER cards; the default workspace shows none.
    - On failure the dialog stays open with the inline "삭제하지 못했어요. 다시 시도해 주세요."; on success the card is removed immediately.
    - /w/[wsId] renders a placeholder (name + empty state), no sidebar; non-members cannot view it.
    - e2e/workspace-delete.spec.ts: OWNER creates then deletes a workspace via the re-type dialog → card disappears.
  </acceptance_criteria>
  <done>E5 delete dialog enforces the re-type confirm and OWNER-only affordance against the Plan-04 route; the /w/[wsId] placeholder exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual + functional verification of E4/E5 and full Phase-1 flow</name>
  <read_first>.planning/phases/01-auth-workspace-foundation/01-UI-SPEC.md, .planning/ROADMAP.md (Phase 1 Success Criteria)</read_first>
  <what-built>The E4 create modal and E5 delete dialog wired to the Plan-04 workspace routes, plus the /w/[wsId] placeholder — completing the Phase-1 success criteria end to end.</what-built>
  <how-to-verify>
    Run pnpm dev against the seeded dev DB and walk the full Phase-1 flow:
    1. Sign up → land on dashboard with the default "기본 워크스페이스" card (criteria 1 + 3).
    2. Reload → still logged in (criteria 2).
    3. Click "워크스페이스 만들기" → enter a name → confirm the modal shows "만드는 중…" then navigates to /w/[newId] placeholder; return to the dashboard and see the new card, on which you are OWNER (criteria 4 create half).
    4. On your OWNER card, open delete → confirm the "삭제" button stays disabled until you type the exact name → delete → the card disappears (criteria 4 delete half).
    5. Confirm the default workspace card has NO delete affordance; try (e.g. via devtools) DELETE on a workspace you do not own and confirm a 403 "이 작업을 수행할 권한이 없습니다." (criteria 5, server gate).
    6. Backstop: create a workspace with a 100-char name → confirm the modal and resulting card render cleanly.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all five ROADMAP Phase-1 success criteria are demonstrable, or lists specific deviations.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe the issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owned-card delete affordance | Client visibility gate — must NOT be the security boundary |
| create/delete modal → API | Untrusted name and delete intent cross to the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | Non-owner deletes by unhiding the affordance | high | mitigate | Hiding is UX only; DELETE /api/workspaces/:id enforces OWNER via requireRole server-side (Plan 04, T-04-02) — verified in Task 3 step 5 |
| T-05-02 | Denial of Service (accidental) | Fat-finger workspace deletion (irreversible) | medium | mitigate | GitHub-style re-type-exact-name confirm keeps 삭제 disabled until match (D-15); default workspace not deletable |
| T-05-03 | Tampering | Oversized/injected workspace name | low | mitigate | Shared workspaceSchema (≤100) client-side + server re-validation (Plan 04); Drizzle parameterized |
</threat_model>

<verification>
- E4 create: single field, shared-schema cap, states, navigate to /w/[newId] (WS-02, D-13/D-14).
- E5 delete: re-type-name confirm, OWNER-only affordance, inline error keeps dialog open (D-15).
- /w/[wsId] placeholder renders (D-14), no sidebar.
- Full Phase-1 flow demonstrable (all five ROADMAP success criteria).
</verification>

<success_criteria>
- Any member can create a workspace via the modal and lands on its placeholder; the OWNER can delete it via the re-type confirm; non-owners cannot (server-enforced) (WS-02).
- The five Phase-1 ROADMAP success criteria are all demonstrable end to end.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|----------|------|-------|
| `src/components/ui/Modal.tsx` + `.module.css` | new files | ported ui-kit Modal (#19) |
| `src/components/ui/ConfirmDialog.tsx` + `.module.css` | new files | ported ui-kit Confirm Dialog (#33) |
| `src/components/workspace/CreateWorkspaceModal.tsx` | new file | E4 |
| `src/components/workspace/CreateWorkspaceButton.tsx` | new file | dashboard CTA |
| `src/components/workspace/DeleteWorkspaceDialog.tsx` | new file | E5 (D-15) |
| `src/app/(main)/w/[wsId]/page.tsx` | new file | D-14 placeholder |
| `src/app/(main)/dashboard/page.tsx` | modified | adds create CTA + owned-card delete affordance |
| `e2e/workspace-create.spec.ts`, `e2e/workspace-delete.spec.ts` | new files | WS-02 UI E2E |

<output>
Create `.planning/phases/01-auth-workspace-foundation/01-05-SUMMARY.md` when done.
</output>
