---
phase: 03-folder-tree-closure-table
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/lib/closure.ts
  - tests/folder/closure.test.ts
  - tests/folder/query-count.test.ts
autonomous: true
requirements: [TREE-02, TREE-03]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "getSubtree(folderId, dbOrTx)가 closure join으로 자신 포함 서브트리를 단일 쿼리로 반환하고, 깊이와 무관한 고정 쿼리 수다(TREE-02)."
    - "moveFolder는 사이클 체크(움직일 폴더 → 새 부모 closure 행 존재 여부)를 rewiring과 같은 트랜잭션의 첫 문장으로 수행하고, 존재하면 CycleError를 던져 아무 행도 재작성하지 않는다."
    - "자기 자신으로의 이동(newParentId == folderId)도 self 행 때문에 사이클로 걸린다."
    - "moveFolder는 서브트리 내부 링크는 보존하고 외부(조상) 링크만 삭제 후 새 부모 조상 × 서브트리로 재작성한다."
    - "newParentId가 다른 워크스페이스 폴더면 이동을 거부한다(크로스 워크스페이스 차단)."
    - "softDeleteFolder는 서브트리 전체에 is_deleted=true·deleted_at을 set하고 삭제 대상에만 is_trash_root=true를 set하며, closure 행은 보존한다(복원의 역연산 전제)."
  artifacts:
    - src/lib/closure.ts
    - tests/folder/closure.test.ts
  key_links:
    - "moveFolder 사이클 체크 SELECT + DELETE + INSERT + folder.parentId UPDATE가 하나의 db.transaction 안에 있다(TOCTOU 차단)."
    - "softDeleteFolder는 getSubtree(folderId, tx)로 서브트리 id를 tx 스코프에서 읽어 read-skew를 피한다(RESEARCH Pitfall 4)."
---

<objective>
검증된 생성 슬라이스(03-02)에서 나머지 closure 연산을 확장한다: getSubtree(조상 기준 서브트리 단일 쿼리), moveFolder(동일 트랜잭션 사이클 체크 + rewiring + 크로스 워크스페이스 거부), softDeleteFolder(cascade + closure 보존). TDD red 먼저.

Purpose: TRD §4의 이동·삭제 연산은 사이클(TOCTOU)·read-skew·closure 보존이라는 미묘한 불변식을 갖는다. 이를 통합 테스트로 못박아 route(03-04)·UI(03-05)가 안전하게 얹히게 한다.
Output: `closure.ts`의 `getSubtree`/`moveFolder`/`softDeleteFolder`/`CycleError`와 그 통합 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/TRD.md
@.planning/phases/03-folder-tree-closure-table/03-RESEARCH.md
@src/lib/closure.ts
@src/db/schema.ts
</context>

<artifacts_produced>
## Artifacts this plan produces
- **`src/lib/closure.ts`** — `getSubtree(folderId, dbOrTx?)`, `moveFolder(folderId, newParentId, dbOrTx?)`, `softDeleteFolder(folderId, dbOrTx?)`, `class CycleError extends Error`
- **테스트** `tests/folder/closure.test.ts`(확장), `tests/folder/query-count.test.ts`(getSubtree 고정 쿼리 수 확장)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — getSubtree / moveFolder(사이클·크로스ws) / softDeleteFolder 통합 테스트</name>
  <files>tests/folder/closure.test.ts, tests/folder/query-count.test.ts</files>
  <read_first>
    - docs/TRD.md §4 (서브트리 조회 / 이동 DELETE+CROSS JOIN INSERT / 삭제 cascade)
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Pattern 2·4·5, Pitfall 1(사이클·rewiring 동일 트랜잭션)·2(크로스 워크스페이스)·4(getSubtree tx 주입 read-skew)
    - src/lib/closure.ts (03-02의 createFolder/getWorkspaceFolders 시그니처·db/tx 주입 관례)
    - tests/folder/closure.test.ts (03-02가 만든 파일 — 여기에 describe 블록 추가), tests/rbac/helpers.ts
  </read_first>
  <behavior>
    - getSubtree(A) (트리 A>B>C, A>D): {A,B,C,D} 반환; getSubtree(B): {B,C}; is_deleted=true 행은 제외.
    - moveFolder(B, D): B 서브트리(B,C)의 외부 조상 링크((A,B),(A,C)) 삭제, 내부 링크((B,B),(C,C),(B,C)) 보존, 새 부모 D의 조상 × B서브트리로 (D,B),(D,C),(루트조상,...) 재작성, folder.parentId(B)=D.
    - 사이클: moveFolder(A, C) — C는 A의 자손 → CycleError, folder_closure/folder 어떤 행도 변경 없음(호출 전후 closure 스냅샷 동일).
    - 자기 이동: moveFolder(B, B) → CycleError(self 행 때문).
    - 루트로 이동: moveFolder(C, null) → C의 외부 조상 링크 삭제, 새 조상 INSERT 없음, parentId=null.
    - 크로스 워크스페이스: 다른 워크스페이스 폴더 X를 newParentId로 moveFolder(B, X) → 거부(에러 던짐), 변경 없음.
    - softDeleteFolder(A) (A>B>C): A·B·C 모두 is_deleted=true·deleted_at 채워짐, A만 is_trash_root=true, folder_closure 행 수는 삭제 전과 동일(보존).
    - query-count: getSubtree가 서로 다른 depth에서 동일 고정 statement 수.
  </behavior>
  <action>
    `tests/folder/closure.test.ts`에 getSubtree·moveFolder·softDeleteFolder describe 블록을 추가하고, `tests/folder/query-count.test.ts`에 getSubtree 고정 쿼리 수 케이스를 추가한다. `@/lib/closure`에서 아직 없는 `getSubtree`/`moveFolder`/`softDeleteFolder`/`CycleError`를 import하므로 red다. 사이클/크로스ws 케이스는 "호출이 throw" + "closure 스냅샷 불변"을 함께 단언한다(rewiring 전 거부 증명). moveFolder는 db/tx 주입 시그니처를 테스트로 고정한다. helpers.ts factory·afterEach cleanup 재사용.
  </action>
  <verify>
    <automated>pnpm vitest run tests/folder/closure.test.ts tests/folder/query-count.test.ts 2>&1 | grep -qiE 'fail|error|getSubtree|CycleError' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - closure.test.ts에 getSubtree·moveFolder·softDeleteFolder 블록과 사이클/자기이동/크로스ws 케이스가 존재: `grep -c 'CycleError' tests/folder/closure.test.ts` >= 1.
    - 테스트가 red (getSubtree/moveFolder/softDeleteFolder 미해결) — RED-OK.
    - 사이클 케이스가 "closure 스냅샷 불변"을 단언한다(호출 전후 folder_closure 행 비교).
  </acceptance_criteria>
  <done>이동·삭제·서브트리 통합 테스트가 커밋되고 red. TDD red 확립.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: getSubtree + moveFolder (동일 트랜잭션 사이클 체크 + rewiring + 크로스 워크스페이스 거부)</name>
  <files>src/lib/closure.ts</files>
  <read_first>
    - docs/TRD.md §4 (이동 원문 SQL)
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Pattern 2·4 (원문 SQL 이식), Pitfall 1·2·4
    - src/lib/closure.ts (createFolder db.transaction 관례)
  </read_first>
  <behavior>
    - getSubtree(folderId, dbOrTx = db): folder innerJoin folderClosure ON descendant_id=folder.id WHERE ancestor_id=folderId AND is_deleted=false. dbOrTx 주입 시 그 클라이언트로 실행(cascade 삭제가 tx를 넘김).
    - moveFolder(folderId, newParentId, dbOrTx = db): db.transaction 안에서 (1) newParentId 있으면 사이클 체크 SELECT((folderId → newParentId) closure 행 존재?) → 존재 시 CycleError throw(첫 문장). (2) newParentId 있으면 그 folder 행 SELECT해 workspace_id가 folderId의 workspace_id와 다르면 throw(크로스ws). (3) 서브트리 외부 조상 링크만 DELETE. (4) newParentId 있으면 새 부모 조상 × 서브트리 CROSS JOIN INSERT. (5) folder.parentId=newParentId, updatedAt=now.
  </behavior>
  <action>
    `src/lib/closure.ts`에 `CycleError extends Error`, `getSubtree`, `moveFolder`를 추가한다. TRD §4 / RESEARCH Pattern 2·4의 SQL을 Drizzle `sql` 템플릿(파라미터 바인딩)으로 그대로 이식하고 재설계하지 않는다. 사이클 체크·크로스ws 검증·DELETE·INSERT·UPDATE를 하나의 db.transaction 콜백에 순서대로 둔다(콜백 throw = 자동 롤백). 크로스ws는 CycleError와 구분되는 에러(예: `CrossWorkspaceError` 또는 명시적 Error)로 던져 route가 400으로 매핑할 수 있게 한다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/folder/closure.test.ts -t "moveFolder" && pnpm vitest run tests/folder/closure.test.ts -t "cycle"</automated>
  </verify>
  <acceptance_criteria>
    - moveFolder·getSubtree·cycle 관련 테스트 green.
    - 사이클/크로스ws 호출 후 folder_closure 스냅샷이 호출 전과 동일(rewiring 전 거부) — 해당 테스트 통과로 증명.
    - `pnpm exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>getSubtree·moveFolder가 TRD §4대로 동작하고, 사이클·크로스ws가 트랜잭션 안에서 rewiring 전에 거부된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: softDeleteFolder (cascade + closure 보존, tx 스코프 서브트리 조회)</name>
  <files>src/lib/closure.ts</files>
  <read_first>
    - docs/TRD.md §4 (삭제 cascade), PRD §2-2 (하위 일괄 소프트삭제, 직접 삭제 대상만 trash_root)
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Pattern 5, Pitfall 4 (getSubtree를 tx로 호출)
    - src/lib/closure.ts (getSubtree — Task 2 산출, db/tx 주입)
  </read_first>
  <behavior>
    - softDeleteFolder(folderId, dbOrTx = db): db.transaction 안에서 getSubtree(folderId, tx)로 서브트리 id 목록을 tx 스코프로 읽고(read-skew 방지), folder에서 그 id들에 is_deleted=true·deleted_at=now, folderId에만 is_trash_root=true. folder_closure는 건드리지 않는다(보존).
    - Phase 4의 document cascade는 이 phase 범위 밖(테이블 없음) — 주석으로만 자리 표시, 실제 쿼리 없음.
  </behavior>
  <action>
    `src/lib/closure.ts`에 `softDeleteFolder`를 추가한다. getSubtree(folderId, tx)로 서브트리 id를 tx 안에서 조회한 뒤 inArray로 is_deleted/deleted_at을 set하고 folderId에만 is_trash_root를 set한다. closure 행은 남긴다(복원 역연산 전제).
  </action>
  <verify>
    <automated>pnpm vitest run tests/folder/closure.test.ts -t "softDelete"</automated>
  </verify>
  <acceptance_criteria>
    - softDelete 테스트 green: 서브트리 전체 is_deleted=true, 대상만 is_trash_root=true, folder_closure 행 수 불변.
    - `pnpm vitest run tests/folder` 전체 green (03-02·03-03 통합).
    - `pnpm exec tsc --noEmit` clean.
  </acceptance_criteria>
  <done>소프트삭제가 서브트리를 cascade하고 closure를 보존하며, 서브트리 조회가 삭제와 같은 트랜잭션 안에서 수행된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| closure lib → DB | 이동/삭제 벌크 SQL이 트랜잭션 경계 안에서 여러 테이블을 바꾼다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-CYCLE | Tampering | moveFolder 사이클 | high | mitigate | (folderId → newParentId) closure 존재 여부를 rewiring과 같은 트랜잭션의 첫 문장으로 검사, 존재 시 CycleError로 중단 — 트리가 순환 그래프로 변형되는 것을 rewiring 전에 차단. self 이동도 self 행으로 걸린다. |
| T-03-03-TOCTOU | Tampering | 사이클 체크/rewiring 분리 | high | mitigate | 체크·DELETE·INSERT·UPDATE를 단일 db.transaction으로 원자화 — 두 요청 사이 레이스로 사이클이 끼어들 여지 제거(Pitfall 1). |
| T-03-03-XWS | Tampering | 크로스 워크스페이스 이동 | high | mitigate | newParentId의 workspace_id를 트랜잭션 안에서 대상과 비교, 불일치 시 거부(Pitfall 2). |
| T-03-03-SKEW | Information Disclosure | cascade read-skew | medium | mitigate | 삭제 대상 서브트리를 getSubtree(folderId, tx)로 트랜잭션 스코프에서 읽어 동시 변경과의 스냅샷 어긋남 방지(Pitfall 4). |
| T-03-03-SQLI | Tampering | closure sql 템플릿 | high | mitigate | 파라미터 바인딩만 사용, 사용자 입력 원문 삽입 없음. |
| T-03-03-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 없음. |
</threat_model>

<verification>
- `pnpm vitest run tests/folder` green (closure + query-count).
- `pnpm vitest run` 전체 green (회귀 없음).
- `pnpm exec tsc --noEmit` clean.
</verification>

<success_criteria>
getSubtree가 단일 고정-쿼리로 서브트리를 반환하고, moveFolder가 사이클·크로스워크스페이스를 rewiring 전 거부하며 내부 링크를 보존하고, softDeleteFolder가 서브트리를 cascade 소프트삭제하면서 closure를 보존한다.
</success_criteria>

<output>
Create `.planning/phases/03-folder-tree-closure-table/03-03-SUMMARY.md` when done
</output>
