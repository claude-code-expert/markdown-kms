---
phase: 03-folder-tree-closure-table
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/
  - tests/folder/schema.test.ts
autonomous: false
requirements: [TREE-02, TREE-03]
user_setup:
  - service: postgres-dev
    why: "folder/folder_closure 마이그레이션을 개발 DB(Homebrew PG16 @ 5433, DATABASE_URL)에도 적용해야 이후 wave의 Playwright e2e가 동작한다. 테스트 DB(DATABASE_URL_TEST)는 tests/global-setup.ts가 자동 migrate한다."
    env_vars:
      - name: DATABASE_URL
        source: ".env.local (로컬 PG16 @ localhost:5433, 무비번)"

estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "folder 테이블이 workspace_id(FK cascade)·parent_id(self-FK nullable)·name·is_deleted·is_trash_root·deleted_at·created_at·updated_at 컬럼으로 존재한다."
    - "folder_closure 테이블이 (ancestor_id, descendant_id) 복합 PK + depth로 존재하고, descendant_id 인덱스가 있다."
    - "folder에는 (parent_id, name) 등 형제-유일성 unique 제약이 없다(CONTEXT.md 잠금)."
    - "활성 조회 부분 인덱스 folder_active_idx ON folder(workspace_id, parent_id) WHERE is_deleted = false 가 존재한다(TRD §3)."
  artifacts:
    - src/db/schema.ts
    - drizzle/0002_*.sql
    - tests/folder/schema.test.ts
  key_links:
    - "folder.workspace_id → workspace.id (ON DELETE CASCADE): 워크스페이스 소프트삭제와 별개로, 물리 삭제 시 폴더 정리."
    - "folder_closure.ancestor_id/descendant_id → folder.id (ON DELETE CASCADE)."
---

<objective>
Phase 3 폴더 트리의 데이터 기반을 만든다. TRD §3 DDL을 Drizzle 스키마로 이식하고 마이그레이션을 생성·적용한다. 이 마이그레이션은 이후 모든 wave가 상속하는 **일방향(one-way) 스키마 변경**이므로, 적용 전 `checkpoint:decision`으로 확정한다.

Purpose: closure 연산(03-02~04)과 트리 UI(03-05)가 읽고 쓸 folder/folder_closure 테이블이 없으면 이 phase의 어떤 것도 시작할 수 없다. 스키마는 TRD §3에서 잠긴 계약이라 재설계가 아니라 **충실한 이식**이다.
Output: `src/db/schema.ts`의 `folder`/`folderClosure` 정의, `drizzle/0002_*.sql` 마이그레이션 파일, 스키마 정합성 smoke 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/TRD.md
@src/db/schema.ts
</context>

<artifacts_produced>
## Artifacts this plan produces
- **테이블** `folder` (컬럼: id, workspace_id, parent_id, name, is_deleted, is_trash_root, deleted_at, created_at, updated_at)
- **테이블** `folder_closure` (컬럼: ancestor_id, descendant_id, depth; 복합 PK)
- **인덱스** `folder_active_idx` (부분 인덱스), `folder_closure(descendant_id)` 인덱스
- **Drizzle export** `folder`, `folderClosure` (`src/db/schema.ts`)
- **마이그레이션** `drizzle/0002_*.sql`
- **테스트** `tests/folder/schema.test.ts`
</artifacts_produced>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: folder/folder_closure 마이그레이션 적용 게이트 (one-way)</name>
  <decision>TRD §3의 folder/folder_closure DDL을 지금 마이그레이션으로 생성·적용할지 확정한다.</decision>
  <context>이 마이그레이션은 일방향 문(one-way door)이다. Phase 4가 document.folder_id로, 이후 export(Phase 6)가 closure 서브트리로 이 스키마를 상속하며, 되돌리려면 데이터 마이그레이션이 필요하다. DDL 형태 자체는 TRD §3에서 이미 잠겼으므로 이 게이트는 "형태를 다시 정하는 것"이 아니라 "비가역 DB 변경을 적용해도 되는지"를 사람이 확인하는 지점이다. 개발 DB(5433)에도 적용된다.</context>
  <options>
    <option id="apply-now">
      <name>지금 TRD §3 DDL 그대로 생성·적용</name>
      <pros>Phase 3 전체가 이 테이블 위에서 진행된다. TRD §3 계약과 1:1.</pros>
      <cons>비가역 — 이후 되돌리려면 drop + 데이터 마이그레이션.</cons>
    </option>
    <option id="hold">
      <name>보류 (스키마 재검토 필요)</name>
      <pros>TRD §3에 이의가 있으면 먼저 문서 개정.</pros>
      <cons>Phase 3 전체가 블록된다.</cons>
    </option>
  </options>
  <resume-signal>선택: apply-now 또는 hold</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: folder/folder_closure 스키마 + 마이그레이션 + 정합성 smoke 테스트</name>
  <reversibility rating="one-way">folder/folder_closure는 Phase 4(document.folder_id)·Phase 6(zip export)가 상속하는 저장 스키마 — 형태 변경은 기존 폴더/문서 데이터 마이그레이션을 요구한다.</reversibility>
  <files>src/db/schema.ts, drizzle/0002_*.sql, tests/folder/schema.test.ts</files>
  <read_first>
    - docs/TRD.md §3 (folder / folder_closure DDL — LOCKED 계약, 그대로 이식) 및 §3 line 110 (folder_active_idx 부분 인덱스)
    - src/db/schema.ts (기존 workspace/workspaceMember 패턴: uuid PK defaultRandom, boolean default, timestamp withTimezone defaultNow, 복합 PK, check 제약)
    - .planning/phases/03-folder-tree-closure-table/03-PATTERNS.md §"src/db/schema.ts" (컬럼/제약 관례, Pitfall 6 = 형제-유일성 unique 금지)
    - drizzle.config.ts (drizzle-kit이 .env.local의 DATABASE_URL로 마이그레이션)
    - tests/global-setup.ts (테스트 DB는 ./drizzle에서 자동 migrate — smoke 테스트가 이걸 태운다)
    - tests/rbac/helpers.ts (createTestWorkspace 등 — smoke 테스트 시드에 재사용 가능)
  </read_first>
  <behavior>
    - folder: id uuid PK defaultRandom / workspaceId uuid FK(workspace.id, onDelete cascade) notNull / parentId uuid self-FK(folder.id) nullable / name text notNull / isDeleted boolean notNull default(false) / isTrashRoot boolean notNull default(false) / deletedAt timestamptz nullable / createdAt·updatedAt timestamptz notNull defaultNow.
    - folderClosure: ancestorId·descendantId uuid FK(folder.id, onDelete cascade) notNull, depth integer notNull, 복합 PK(ancestorId, descendantId), descendantId 인덱스.
    - folder에 형제-유일성 unique 제약을 절대 추가하지 않는다(CONTEXT.md 잠금). 부분 인덱스 folder_active_idx(workspace_id, parent_id) WHERE is_deleted = false 는 추가한다.
    - smoke: db.select().from(folder) 와 db.select().from(folderClosure) 가 각각 [] 를 반환(테이블 존재 + Drizzle 모델 정합).
  </behavior>
  <action>
    `src/db/schema.ts`에 `drizzle-orm/pg-core`의 `integer`, `index`를 추가 import하고 `folder`, `folderClosure` 두 pgTable을 정의한다. parentId self-FK는 순환 타입을 피하려 `references((): AnyPgColumn => folder.id)` 형태로 선언한다(`AnyPgColumn`을 `drizzle-orm/pg-core`에서 import). 컬럼·제약은 위 behavior와 TRD §3 DDL을 그대로 따른다. 형제 이름 유일성 제약은 넣지 않는다(CONTEXT.md 잠금, PATTERNS.md Pitfall 6). folder_active_idx 부분 인덱스는 pgTable의 두 번째 인자 콜백에서 `index("folder_active_idx").on(t.workspaceId, t.parentId).where(sql\`${t.isDeleted} = false\`)`로 선언한다.
    이어서 `pnpm drizzle-kit generate`로 `drizzle/0002_*.sql`을 만들고, `pnpm drizzle-kit migrate`로 개발 DB에 적용한다(테스트 DB는 global-setup이 적용).
    마지막으로 `tests/folder/schema.test.ts`를 작성한다: `db.select().from(folder)` / `db.select().from(folderClosure)`가 각각 `[]`을 반환함을 단언(테이블 존재 + 모델 정합 smoke). 기존 통합 테스트 관례(vitest, `@/db`)를 따른다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/folder/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/folder/schema.test.ts` 통과 (global-setup이 0002 마이그레이션을 적용한 뒤 folder/folder_closure select가 []).
    - `drizzle/0002_*.sql`이 존재하고 `CREATE TABLE "folder"` 와 `CREATE TABLE "folder_closure"` 를 포함한다: `grep -l 'CREATE TABLE "folder_closure"' drizzle/0002_*.sql` 가 파일을 출력.
    - 생성된 마이그레이션에 folder(name) 또는 (parent_id, name)에 대한 UNIQUE 제약이 없다: `grep -iE 'unique.*(folder_name|parent_id)' drizzle/0002_*.sql` 결과 없음.
    - `pnpm exec tsc --noEmit` 통과 (self-FK AnyPgColumn 타입 정합).
  </acceptance_criteria>
  <done>folder/folder_closure 테이블이 TRD §3대로 존재하고 마이그레이션이 커밋되었으며, schema smoke 테스트가 green. 형제-유일성 제약 없음.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 마이그레이션 → DB | 스키마 DDL이 실제 DB 상태를 비가역적으로 바꾼다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01-DDL | Tampering | folder_closure FK | medium | mitigate | ancestor_id/descendant_id 모두 folder.id FK(ON DELETE CASCADE) — 고아 closure 행 방지(TRD §3). |
| T-03-01-UNIQ | Tampering | folder unique 제약 | low | accept | 형제-유일성 제약을 의도적으로 두지 않음(CONTEXT.md 잠금). 이름 검증은 route 계층 zod로 이관. |
| T-03-01-SC | Tampering | 패키지 설치 | low | accept | 이 phase는 신규 패키지 설치가 없다(RESEARCH Package Legitimacy Audit). 공급망 위험 없음. |
</threat_model>

<verification>
- `pnpm vitest run tests/folder/schema.test.ts` green.
- `pnpm vitest run` 전체가 기존 759 + schema smoke 포함 green (회귀 없음).
- `pnpm exec tsc --noEmit` clean.
</verification>

<success_criteria>
folder/folder_closure 테이블·인덱스가 TRD §3대로 존재하고, 마이그레이션 파일이 커밋되며, 정합성 smoke 테스트가 통과한다. 형제-유일성 제약이 없다.
</success_criteria>

<output>
Create `.planning/phases/03-folder-tree-closure-table/03-01-SUMMARY.md` when done
</output>
