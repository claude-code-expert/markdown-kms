---
phase: 03-folder-tree-closure-table
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/lib/closure.ts
  - src/lib/validation.ts
  - src/app/api/folders/route.ts
  - src/app/(main)/w/[wsId]/page.tsx
  - src/app/(main)/w/[wsId]/page.module.css
  - src/components/tree/FolderTree.tsx
  - src/components/tree/FolderTree.module.css
  - src/components/tree/tree-utils.ts
  - tests/folder/closure.test.ts
  - tests/folder/query-count.test.ts
  - e2e/folder-tree.spec.ts
autonomous: true
requirements: [TREE-01, TREE-02, TREE-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "createFolder(workspaceId, null, name)이 folder 1행 + folder_closure self 행 (newId,newId,0) 1개만 만든다(부모 없으면 조상 복사 0행)."
    - "createFolder(workspaceId, parentId, name)이 부모의 조상 행을 depth+1로 복사하고 self 행을 추가한다."
    - "getWorkspaceFolders(workspaceId)가 트리 깊이와 무관하게 고정 개수의 SQL statement로 활성 폴더 전체를 반환한다(N+1 없음, TREE-02)."
    - "POST /api/folders는 EDITOR 미만이면 403, parentId가 다른 워크스페이스 폴더면 400을 반환하고, workspaceId를 클라이언트 입력이 아니라 서버가 결정한다(IDOR 방지)."
    - "사이드바에 워크스페이스 루트 아래 생성한 폴더가 계층으로 표시되고, '새 폴더' 입력으로 만든 폴더가 서버 확정 후 트리에 나타난다(TREE-01)."
  artifacts:
    - src/lib/closure.ts
    - src/app/api/folders/route.ts
    - src/components/tree/FolderTree.tsx
    - src/components/tree/tree-utils.ts
    - tests/folder/closure.test.ts
    - tests/folder/query-count.test.ts
    - e2e/folder-tree.spec.ts
  key_links:
    - "w/[wsId]/page.tsx (RSC) → getWorkspaceFolders(wsId) → <FolderTree folders workspaceId>: 초기 트리 로드."
    - "FolderTree 새 폴더 입력 → POST /api/folders → 성공 후 router.refresh()/재조회 → 트리 갱신(낙관적 UI 없음)."
    - "POST /api/folders → requireRole(workspaceId, 'EDITOR'): workspaceId는 parentId의 folder 행 또는 body에서 서버가 검증한 값."
---

<objective>
TRACER — "새 폴더 생성이 closure 행으로 쓰이고 사이드바 트리에 나타난다"는 단일 경로를 모든 계층(closure lib → API route → RSC 트리 로드 → FolderTree 렌더 → 생성 입력)에 걸쳐 end-to-end로 관통시킨다. TDD로 red 먼저, 그다음 최소 슬라이스를 green으로 만든다.

Purpose: 이 phase의 아키텍처(평면 초기 로드 + closure 생성 + IDOR-안전 route + 서버 확정 후 렌더)가 실제로 성립하는지를 한 경로로 먼저 증명한다. 나머지 연산(이동/삭제)·풀 UI는 이 검증된 슬라이스에서 확장한다.
Output: `closure.ts`(createFolder, getWorkspaceFolders), `folderSchema`, `POST /api/folders`, page.tsx 사이드바 배선, 최소 `FolderTree` + `tree-utils.buildTree`, 그리고 이를 관통하는 통합/e2e 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/TRD.md
@.planning/phases/03-folder-tree-closure-table/03-RESEARCH.md
@.planning/phases/03-folder-tree-closure-table/03-UI-SPEC.md
@src/db/schema.ts
@src/lib/rbac.ts
@src/lib/validation.ts
@src/app/api/workspaces/route.ts
@src/app/(main)/w/[wsId]/page.tsx
</context>

<artifacts_produced>
## Artifacts this plan produces
- **`src/lib/closure.ts`** — `getWorkspaceFolders(workspaceId, dbOrTx?)`, `createFolder(workspaceId, parentId, name, dbOrTx?)`
- **`src/lib/validation.ts`** — `folderSchema`, `FolderInput` (신규 export)
- **route** `POST /api/folders` (body `{ name, parentId, workspaceId? }`) — 폴더 생성, EDITOR+, IDOR-안전
- **`src/components/tree/FolderTree.tsx`** (최소 렌더 + 헤더 "새 폴더" 인라인 생성) + `FolderTree.module.css`
- **`src/components/tree/tree-utils.ts`** — `buildTree(folders)`
- **수정** `w/[wsId]/page.tsx` (260px 사이드바 grid + 초기 트리 로드), `page.module.css`
- **테스트** `tests/folder/closure.test.ts`, `tests/folder/query-count.test.ts`, `e2e/folder-tree.spec.ts`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — createFolder / getWorkspaceFolders 통합 테스트 + 쿼리 수 단언 + e2e 생성 스펙</name>
  <files>tests/folder/closure.test.ts, tests/folder/query-count.test.ts, e2e/folder-tree.spec.ts</files>
  <read_first>
    - docs/TRD.md §4 (폴더 생성 = 부모 조상 행 복사 + self 행; 서브트리 단일 쿼리)
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Pattern 1·3, Pitfall 5(루트 parentId=null → 조상 복사 0행), Code Examples §"쿼리 개수 단언 — postgres.js debug 훅"
    - tests/rbac/helpers.ts (createTestUser/createTestWorkspace/addMember/mockSessionFor 재사용), tests/rbac/matrix.test.ts (vi.mock("@/auth"), afterEach cleanup 패턴)
    - e2e/workspace-create.spec.ts (signup → workspace 흐름 패턴)
    - src/db/schema.ts (folder/folderClosure — 03-01에서 추가됨)
  </read_first>
  <behavior>
    - closure.test.ts:
      - createFolder(ws, null, "A") → folder 1행(name "A", parentId null) + folder_closure에 (A,A,0) 정확히 1행, 다른 조상 행 없음.
      - createFolder(ws, A, "B") → folder_closure에 (B,B,0)과 (A,B,1) 두 행 존재(부모 A의 조상 행 복사됨).
      - createFolder(ws, B, "C") → (C,C,0),(B,C,1),(A,C,2) — 조부모까지 depth 누적.
      - getWorkspaceFolders(ws) → is_deleted=false인 A·B·C 반환, 소프트삭제 표시된 행은 제외(수동으로 is_deleted=true set한 행이 결과에 없음).
    - query-count.test.ts: depth 5+ 트리를 시드한 뒤 getWorkspaceFolders 호출 시 실행된 SQL statement 수가 depth와 무관하게 동일(고정값). postgres.js debug 콜백으로 statement를 카운트하고, 절대값은 실행 로그로 확정하되 "깊이가 늘어도 증가하지 않음"을 두 depth(예: 2 vs 6)에서 같은 수로 단언.
    - e2e/folder-tree.spec.ts: signup → 기본 워크스페이스 진입(/w/...) → 사이드바 헤더 "새 폴더" 버튼 → 인라인 입력에 이름 → Enter → 해당 이름 노드가 사이드바에 보인다.
  </behavior>
  <action>
    세 테스트 파일을 작성하고 커밋한다. closure.test.ts·query-count.test.ts는 `@/lib/closure`에서 import하므로 지금은 import 실패로 red다(closure.ts 미존재). e2e는 사이드바/생성 UI가 없어 red다. query-count는 프로덕션 `@/db`를 건드리지 않고, `DATABASE_URL_TEST`에 별도 postgres 클라이언트를 debug 콜백과 함께 연결해 카운트만 관찰한다(RESEARCH Code Examples 그대로). getWorkspaceFolders가 두 번째 인자로 db/tx 클라이언트를 받도록 설계(테스트가 debugDb를 주입) — 이 시그니처 요구를 테스트로 고정한다. 트랜잭션 계열이 아닌 단순 SELECT이므로 BEGIN/COMMIT 없이 statement 1개가 기대값이나, 절대값은 실행 로그로 확정한다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/folder/closure.test.ts tests/folder/query-count.test.ts 2>&1 | grep -qiE 'fail|error|cannot find' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/folder/closure.test.ts`, `tests/folder/query-count.test.ts`, `e2e/folder-tree.spec.ts` 세 파일이 존재하고 커밋됨.
    - closure/query-count 테스트가 현재 red(`@/lib/closure` 미해결로 실패) — RED-OK 출력.
    - query-count 테스트가 서로 다른 두 depth에서 동일 statement 수를 단언한다: `grep -c 'toBe' tests/folder/query-count.test.ts` >= 1 이고 테스트 본문에 서로 다른 depth 시드가 존재.
    - 테스트가 tests/rbac/helpers.ts의 factory와 vi.mock("@/auth")를 재사용한다: `grep -q 'rbac/helpers' tests/folder/closure.test.ts`.
  </acceptance_criteria>
  <done>세 테스트가 커밋되고 closure/query-count는 red, e2e는 생성 UI 부재로 red. TDD red 확립.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: TRACER — 생성 슬라이스 end-to-end (closure create + getWorkspaceFolders + POST route + 사이드바 트리 + 생성 입력)</name>
  <precondition>folder/folder_closure 마이그레이션이 실행 중인 개발 DB(DATABASE_URL)에 적용되어 있다(03-01 Task 2의 `pnpm drizzle-kit migrate` — Playwright e2e가 개발 서버·개발 DB를 사용하므로).</precondition>
  <reversibility rating="costly">POST /api/folders의 요청 body 계약({name, parentId, workspaceId?})은 FolderTree·이후 이동/이름변경 UI가 의존하는 공개 계약 — 바꾸면 클라이언트 호출부를 함께 고쳐야 한다.</reversibility>
  <files>src/lib/closure.ts, src/lib/validation.ts, src/app/api/folders/route.ts, src/app/(main)/w/[wsId]/page.tsx, src/app/(main)/w/[wsId]/page.module.css, src/components/tree/FolderTree.tsx, src/components/tree/FolderTree.module.css, src/components/tree/tree-utils.ts</files>
  <read_first>
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Pattern 1·3, Anti-Patterns(클라이언트 workspaceId 신뢰 금지 / 크로스 워크스페이스 parent 거부), Pitfall 2·3
    - .planning/phases/03-folder-tree-closure-table/03-PATTERNS.md §"src/lib/closure.ts"(db.transaction 패턴, db/tx 주입 A3), §"src/app/api/folders/route.ts", §"w/[wsId]/page.tsx", §"FolderTree/FolderTreeNode"
    - src/app/api/workspaces/route.ts (POST: session 가드 → JSON 파싱 가드 → zod → db.transaction → 201)
    - src/lib/rbac.ts (requireRole/ForbiddenError/forbiddenResponse — EDITOR 게이트, UUID fail-closed)
    - src/lib/validation.ts (workspaceSchema 패턴 → folderSchema)
    - src/app/(main)/w/[wsId]/page.tsx + page.module.css (requireRole VIEWER + notFound 게이트 유지)
    - src/components/layout/EditorPreviewLayout.tsx ("use client" 조립형), src/components/workspace/CreateWorkspaceModal.tsx (클라 zod 가드 + fetch + 에러)
    - .planning/phases/03-folder-tree-closure-table/03-UI-SPEC.md (260px 사이드바, 헤더 40px "새 폴더" 버튼, Tree Node 32px/들여쓰기 8+16×N, 낙관적 UI 없음)
  </read_first>
  <behavior>
    - closure.createFolder: db.transaction 안에서 folder insert().returning() → 부모 조상 행 복사 INSERT ... SELECT ancestor_id, newId, depth+1 FROM folder_closure WHERE descendant_id = parentId(파라미터 바인딩; parentId=null이면 0행) → self 행 (newId,newId,0) 무조건 insert. sql 템플릿은 값 인터폴레이션(파라미터 바인딩)만 사용.
    - closure.getWorkspaceFolders(workspaceId, dbOrTx = db): folder에서 workspaceId AND is_deleted=false 평면 단일 SELECT(closure join 아님).
    - POST /api/folders: session 가드 → JSON 파싱 가드 → folderSchema로 name 검증(+parentId/workspaceId 검증). workspaceId 결정: parentId가 있으면 그 folder 행의 workspace_id를 SELECT해 사용(클라이언트 workspaceId 불신), parentId가 null이면 body.workspaceId를 z.uuid로 검증 후 사용. requireRole(workspaceId, "EDITOR"). parentId가 있고 그 워크스페이스가 결정된 workspaceId와 다르면 400. createFolder 호출 후 201 { id, name }.
    - page.tsx: 기존 requireRole VIEWER + notFound 유지, 그 뒤 getWorkspaceFolders(wsId) 호출, .page를 grid-template-columns: 260px minmax(0,1fr)로 감싸 좌측에 <FolderTree folders workspaceId={wsId} />, 우측에 기존 타이틀 + EditorPreviewLayout(내부 불변).
    - FolderTree("use client"): folders를 buildTree로 트리화, 워크스페이스 루트 라벨 + 폴더 노드를 들여쓰기(8+16×N)로 렌더. 헤더 "새 폴더" 버튼 클릭 시 루트 대상 인라인 입력(placeholder "새 폴더") 표시, Enter 시 POST /api/folders → 성공 후 router.refresh()로 재조회(낙관적 UI 없음). 빈/공백만 입력은 제출 비활성화(클라 최소 가드).
  </behavior>
  <action>
    `src/lib/validation.ts`에 `folderSchema`(z.string().trim().min(1,"폴더 이름을 입력해 주세요.").max(255,"폴더 이름은 255자를 넘을 수 없습니다.")) + `FolderInput`을 workspaceSchema 관례대로 추가한다.
    `src/lib/closure.ts`를 신규 작성한다: `createFolder`, `getWorkspaceFolders`. 두 함수 모두 두 번째/세 번째 인자로 선택적 db/tx 클라이언트를 받아 주입 가능하게(RESEARCH A3, query-count 테스트 격리 + 이후 cascade 트랜잭션 재사용). 트랜잭션 롤백은 db.transaction 콜백 throw에 위임(수동 ROLLBACK 금지).
    `src/app/api/folders/route.ts`에 `POST`를 workspaces/route.ts 골격으로 작성하되 위 behavior의 workspaceId 서버 결정 + requireRole(EDITOR) + 크로스 워크스페이스 parent 400 + ForbiddenError→forbiddenResponse 매핑을 넣는다. `export const runtime = "nodejs"`.
    `w/[wsId]/page.tsx`와 `page.module.css`를 위 behavior대로 수정한다(EditorPreviewLayout 내부는 손대지 않음).
    `src/components/tree/tree-utils.ts`에 `buildTree(folders)`(parentId 기반 O(n) 트리 구성)를 작성한다.
    `src/components/tree/FolderTree.tsx` + `FolderTree.module.css`를 최소 렌더 + 헤더 생성 입력만 작성한다(컨텍스트 메뉴·DnD·이동·이름변경은 03-05). ui-kit 토큰·CSS Modules 사용.
  </action>
  <verify>
    <automated>pnpm vitest run tests/folder/closure.test.ts tests/folder/query-count.test.ts && pnpm exec playwright test e2e/folder-tree.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/folder/closure.test.ts tests/folder/query-count.test.ts` 전부 green (createFolder 조상 복사·루트 self-only·getWorkspaceFolders 고정 쿼리 수).
    - `pnpm exec playwright test e2e/folder-tree.spec.ts` green (생성한 폴더가 사이드바에 나타남).
    - POST /api/folders 통합 확인: EDITOR 미만/비멤버 호출은 403, 다른 워크스페이스 folder를 parentId로 준 호출은 400 — closure.test.ts 또는 route 통합 테스트에서 단언(둘 중 하나에 케이스 존재).
    - `pnpm exec tsc --noEmit` clean, `pnpm lint` 위반 없음.
    - page.module.css에 `grid-template-columns` 260px 사이드바 규칙 존재: `grep -q '260px' src/app/\(main\)/w/\[wsId\]/page.module.css`.
  </acceptance_criteria>
  <done>새 폴더 생성이 closure 행 → 서버 확정 → 사이드바 트리로 관통되고, 생성 슬라이스가 커밋됨. 나머지 연산/UI가 확장할 검증된 골격.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → POST /api/folders | 신뢰 불가한 name/parentId/workspaceId가 서버로 넘어온다. |
| route → closure/DB | closure 벌크 SQL이 트랜잭션 경계 안에서 실행된다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-IDOR | Elevation of Privilege | POST /api/folders workspaceId 결정 | high | mitigate | workspaceId를 클라이언트 입력 그대로 requireRole에 넘기지 않는다 — parentId가 있으면 그 folder 행의 workspace_id를 서버가 SELECT해 사용, 없으면 z.uuid 검증 후 requireRole(EDITOR)로 멤버십 재확인. |
| T-03-02-XWS | Tampering | 크로스 워크스페이스 parent | high | mitigate | parentId의 workspace_id가 결정된 workspaceId와 다르면 400 — 트리가 두 워크스페이스에 걸치는 오염 차단(Pitfall 2). |
| T-03-02-SQLI | Tampering | closure sql 템플릿 | high | mitigate | sql`...${value}...` 파라미터 바인딩만 사용, 사용자 입력을 원문 문자열로 SQL에 삽입하지 않는다(RESEARCH Security Domain). |
| T-03-02-MASS | Tampering | folder 필드 매스 어사인 | medium | mitigate | createFolder는 name/workspaceId/parentId만 값으로 받고 is_deleted/is_trash_root/id 등은 서버 기본값 — body에서 임의 컬럼을 주입할 수 없다. |
| T-03-02-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 없음(RESEARCH). |
</threat_model>

<verification>
- `pnpm vitest run` 전체 green (기존 + folder closure/query-count).
- `pnpm exec playwright test e2e/folder-tree.spec.ts` green.
- `pnpm exec tsc --noEmit` clean, `pnpm lint` clean.
</verification>

<success_criteria>
새 폴더를 만들면 closure self/조상 행이 정확히 쓰이고, EDITOR 미만·크로스 워크스페이스는 서버가 거부하며, 생성한 폴더가 사이드바 트리에 계층으로 나타난다. 초기 트리 로드가 깊이 무관 고정 쿼리 수(TREE-02).
</success_criteria>

<output>
Create `.planning/phases/03-folder-tree-closure-table/03-02-SUMMARY.md` when done
</output>
