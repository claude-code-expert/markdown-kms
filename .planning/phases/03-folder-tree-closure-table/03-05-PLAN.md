---
phase: 03-folder-tree-closure-table
plan: 05
type: execute
wave: 5
depends_on: [03-04]
files_modified:
  - src/components/tree/FolderTree.tsx
  - src/components/tree/FolderTreeNode.tsx
  - src/components/tree/FolderContextMenu.tsx
  - src/components/tree/MoveFolderModal.tsx
  - src/components/tree/tree-utils.ts
  - src/components/tree/FolderTreeNode.module.css
  - src/components/tree/FolderContextMenu.module.css
  - src/components/tree/MoveFolderModal.module.css
  - e2e/folder-tree.spec.ts
autonomous: true
requirements: [TREE-01, TREE-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사이드바가 워크스페이스 루트 > 폴더 > 하위 폴더 계층을 들여쓰기(8+16×N)로 표시하고, 자식 있는 노드만 체브론으로 펼침/접힘(비영속 클라이언트 상태)한다(TREE-01)."
    - "hover kebab 또는 우클릭이 같은 컨텍스트 메뉴(새 하위 폴더/이름 변경/이동…/삭제)를 연다."
    - "이름변경은 인라인 입력(Enter/blur=제출, Escape=취소·원래 이름 복구), 서버 응답 대기 후에만 트리 갱신(낙관적 UI 없음)."
    - "폴더를 다른 폴더 위로 드래그해 드롭하면 move API가 호출되고 성공 후 트리가 재조회된다(TREE-03)."
    - "자기 자손 위로의 드래그는 클라이언트가 즉시 판정해 드롭을 막고(preventDefault 미호출로 금지 커서), '이동' 폴백 모달에서도 자기 서브트리는 선택 불가로 회색 처리한다."
    - "삭제는 확인 다이얼로그(복원 가능 안내) 후 소프트삭제 API를 호출하고 성공 시 서브트리가 목록에서 사라진다."
  artifacts:
    - src/components/tree/FolderTreeNode.tsx
    - src/components/tree/FolderContextMenu.tsx
    - src/components/tree/MoveFolderModal.tsx
    - src/components/tree/tree-utils.ts
    - e2e/folder-tree.spec.ts
  key_links:
    - "FolderTreeNode dragstart(setData)/dragover(isDescendantOrSelf 판정 → 유효 시 preventDefault)/drop → POST /api/folders/[id]/move → 성공 후 router.refresh()."
    - "컨텍스트 메뉴 '삭제' → ConfirmDialog → DELETE /api/folders/[id]; '이름 변경' → 인라인 입력 → PATCH; '이동…' → MoveFolderModal → move API."
    - "isDescendantOrSelf(folders, draggedId, targetId): DnD·이동 모달의 사이클 사전 판정(클라이언트 UX 보조, 서버가 트랜잭션에서 재검증)."
---

<objective>
검증된 라우트(03-04) 위에 트리 상호작용 UI를 완성한다: 계층 렌더 + 펼침/접힘, hover kebab·우클릭 컨텍스트 메뉴, 인라인 이름변경, 네이티브 HTML5 DnD 이동(클라이언트 사이클 사전 판정), "이동" 폴백 모달, 삭제 확인. 신규 라이브러리 없이 네이티브 API + 자체 팝업으로 구현(UI-SPEC 잠금). e2e red 먼저.

Purpose: TREE-01(계층 표시)·TREE-03(생성·이름변경·이동·삭제 동작)의 사용자 표면을 완성한다. 상호작용·DnD·인라인 편집은 vitest(node)로 렌더 단위 테스트가 불가하므로 Playwright e2e로 검증한다(RESEARCH Validation Architecture).
Output: `FolderTreeNode`/`FolderContextMenu`/`MoveFolderModal`, `tree-utils.isDescendantOrSelf`, 확장된 `FolderTree`, 그리고 트리 상호작용 e2e.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-folder-tree-closure-table/03-UI-SPEC.md
@.planning/phases/03-folder-tree-closure-table/03-RESEARCH.md
@src/components/tree/FolderTree.tsx
@src/components/tree/tree-utils.ts
@src/components/workspace/WorkspaceCard.tsx
@src/components/ui/Modal.tsx
@src/components/workspace/DeleteWorkspaceDialog.tsx
@src/components/workspace/CreateWorkspaceModal.tsx
@e2e/workspace-create.spec.ts
</context>

<artifacts_produced>
## Artifacts this plan produces
- **`src/components/tree/FolderTreeNode.tsx`** (+css) — 노드 1개: 체브론 펼침/접힘, hover kebab, selected/pending, DnD 핸들러(draggable/dragstart/dragover/drop), 인라인 이름변경 입력
- **`src/components/tree/FolderContextMenu.tsx`** (+css) — 우클릭/kebab 공용 팝업 메뉴(Escape 닫힘)
- **`src/components/tree/MoveFolderModal.tsx`** (+css) — "이동" 폴백 모달(들여쓰기 리스트, 자기 서브트리 선택 불가)
- **`src/components/tree/tree-utils.ts`** — `isDescendantOrSelf(folders, rootId, candidateId)` 추가
- **수정** `src/components/tree/FolderTree.tsx` (노드·메뉴·모달·DnD·펼침 상태 배선)
- **테스트** `e2e/folder-tree.spec.ts` (컨텍스트 메뉴·인라인 이름변경·DnD·삭제 확장)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: RED — 트리 상호작용 e2e (컨텍스트 메뉴 / 인라인 이름변경 / DnD 이동 / 삭제)</name>
  <files>e2e/folder-tree.spec.ts</files>
  <read_first>
    - .planning/phases/03-folder-tree-closure-table/03-UI-SPEC.md (Tree Node Contract, Interaction Contract, Copywriting — 메뉴 항목/카피)
    - e2e/workspace-create.spec.ts (signup → workspace 흐름), e2e/folder-tree.spec.ts (03-02가 만든 생성 스펙 — 여기에 확장)
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Validation Architecture(DnD는 Playwright dragTo/dragAndDrop), Pattern 6·7
  </read_first>
  <behavior>
    - 계층 표시: 생성으로 A>B를 만든 뒤 A가 체브론으로 펼쳐지고 B가 들여쓰기되어 보인다.
    - 컨텍스트 메뉴: 폴더 노드 hover 시 kebab 노출, 클릭 시 "새 하위 폴더/이름 변경/이동…/삭제" 항목이 뜬다. 우클릭도 같은 메뉴.
    - 인라인 이름변경: "이름 변경" → 입력이 기존 이름 prefill → 새 이름 Enter → 서버 응답 후 라벨이 새 이름으로 바뀜. Escape는 원래 이름 유지.
    - DnD 이동: 폴더 B를 폴더 C 위로 드래그·드롭 → move API 호출 → B가 C 아래로 재배치.
    - 삭제: "삭제" → 확인 다이얼로그 → "삭제" → 서브트리가 목록에서 사라짐.
  </behavior>
  <action>
    `e2e/folder-tree.spec.ts`에 위 시나리오 테스트를 추가한다(03-02 생성 스펙과 같은 signup→workspace 셋업 재사용). UI 컴포넌트가 아직 없어 red다. DnD는 Playwright의 dragTo/dragAndDrop을 사용한다. 카피·aria-label은 UI-SPEC Copywriting/Interaction Contract 문언과 일치시킨다.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/folder-tree.spec.ts 2>&1 | grep -qiE 'fail|error|timeout' && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/folder-tree.spec.ts`에 컨텍스트 메뉴·인라인 이름변경·DnD·삭제 시나리오가 존재: `grep -c 'test(' e2e/folder-tree.spec.ts` >= 4.
    - 상호작용 시나리오가 현재 red(UI 부재) — RED-OK.
    - 메뉴 항목 카피가 UI-SPEC과 일치: `grep -q '이름 변경' e2e/folder-tree.spec.ts` 및 `grep -q '이동' e2e/folder-tree.spec.ts`.
  </acceptance_criteria>
  <done>트리 상호작용 e2e가 커밋되고 red. TDD red 확립.</done>
</task>

<task type="auto">
  <name>Task 2: FolderTreeNode + FolderContextMenu + 인라인 이름변경 + DnD (isDescendantOrSelf)</name>
  <precondition>folder/folder_closure 마이그레이션이 실행 중인 개발 DB(DATABASE_URL)에 적용되어 있다(03-01 Task 2 — Playwright e2e가 개발 서버·개발 DB를 사용).</precondition>
  <files>src/components/tree/tree-utils.ts, src/components/tree/FolderTreeNode.tsx, src/components/tree/FolderTreeNode.module.css, src/components/tree/FolderContextMenu.tsx, src/components/tree/FolderContextMenu.module.css</files>
  <read_first>
    - .planning/phases/03-folder-tree-closure-table/03-UI-SPEC.md (Tree Node Contract 전부: 32px 행, 들여쓰기, 체브론 rotate, hover/selected/pending/드래그 스타일, 인라인 이름변경; Interaction Contract; Color — accent 예약 목록)
    - .planning/phases/03-folder-tree-closure-table/03-PATTERNS.md §"FolderTreeNode"·"FolderContextMenu"·"InlineRenameInput", §"tree-utils.ts"
    - .planning/phases/03-folder-tree-closure-table/03-RESEARCH.md Pattern 6(isDescendantOrSelf)·7(dragstart setData)
    - src/components/workspace/WorkspaceCard.tsx (행 + 조건부 액션 버튼 + 자식 다이얼로그 상태), src/components/ui/Modal.tsx (Escape 리스너 패턴), src/components/workspace/CreateWorkspaceModal.tsx (클라 zod 가드 + fetch + 에러)
    - src/lib/validation.ts (folderSchema — 인라인 입력 클라 최소 가드)
  </read_first>
  <behavior>
    - tree-utils.isDescendantOrSelf(folders, rootId, candidateId): RESEARCH Pattern 6 그대로(순수 함수, rootId==candidateId면 true).
    - FolderTreeNode("use client"): 32px 행, 들여쓰기 8+16×N, 체브론(자식 있을 때만, 펼침 시 rotate 90deg, 트랜지션 없음), Folder 아이콘, 라벨 ellipsis. hover 시 kebab(MoreHorizontal) 노출. selected는 accent-weak 배경+accent 텍스트+weight 600. pending은 opacity 0.5 + pointer-events none + 스피너. draggable + onDragStart(dataTransfer.setData("text/plain", id), effectAllowed="move") + onDragOver(isDescendantOrSelf면 preventDefault 미호출=금지 커서, 아니면 preventDefault + 유효 스타일) + onDrop(move API 호출 → 성공 후 상위 콜백으로 재조회). 인라인 이름변경: 라벨→input, 기존 이름 prefill+전체선택, Enter/blur=제출(folderSchema 클라 가드 → PATCH), Escape=취소(원래 이름 복구), 서버 응답 후에만 트리 갱신.
    - FolderContextMenu: Modal의 Escape/외부클릭 닫힘 로직 재사용하되 backdrop 없이 고정 위치 팝업. 폴더 노드 메뉴 4항목(새 하위 폴더/이름 변경/이동…/삭제; 삭제는 destructive 색), 워크스페이스 루트 메뉴 1항목(새 폴더). ui-kit dropdown-menu 스타일을 CSS Modules로.
  </behavior>
  <action>
    `src/components/tree/tree-utils.ts`에 `isDescendantOrSelf`를 추가한다(RESEARCH Pattern 6). `src/components/tree/FolderTreeNode.tsx`(+css)와 `src/components/tree/FolderContextMenu.tsx`(+css)를 UI-SPEC 계약대로 작성한다. 인라인 이름변경은 FolderTreeNode 내부 상태로 구현(별도 파일 불필요). DnD 핸들러는 RESEARCH Pattern 6·7을 따르되, 이동 실행은 노드가 직접 fetch하지 않고 상위(FolderTree)가 넘긴 콜백을 호출해 재조회를 일원화한다. "삭제" 항목은 ConfirmDialog(재사용)로 복원 가능 안내 후 DELETE. 이동 성공/실패 카피는 UI-SPEC Copywriting Contract 재사용.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm exec playwright test e2e/folder-tree.spec.ts -g "이름 변경|삭제|컨텍스트|menu"</automated>
  </verify>
  <acceptance_criteria>
    - 컨텍스트 메뉴·인라인 이름변경·삭제 e2e 시나리오 green.
    - isDescendantOrSelf가 rootId==candidateId에서 true, 서브트리 포함 시 true, 무관 노드에서 false를 반환(e2e DnD 거부 또는 별도 순수함수 단언으로 증명).
    - `pnpm exec tsc --noEmit` clean, `pnpm lint` clean.
    - 노드 selected/hover가 accent를 UI-SPEC 예약 목록 안에서만 사용(체브론/폴더 아이콘 기본색은 중립색).
  </acceptance_criteria>
  <done>노드 렌더·펼침/접힘·컨텍스트 메뉴·인라인 이름변경·삭제·DnD 핸들러가 UI-SPEC대로 동작한다.</done>
</task>

<task type="auto">
  <name>Task 3: MoveFolderModal 폴백 + FolderTree 배선 + e2e green</name>
  <precondition>folder/folder_closure 마이그레이션이 실행 중인 개발 DB(DATABASE_URL)에 적용되어 있다(03-01 Task 2).</precondition>
  <files>src/components/tree/MoveFolderModal.tsx, src/components/tree/MoveFolderModal.module.css, src/components/tree/FolderTree.tsx</files>
  <read_first>
    - .planning/phases/03-folder-tree-closure-table/03-UI-SPEC.md ("이동" 폴백 모달: Modal 재사용, 들여쓰기 리스트, 자기 서브트리 회색 pointer-events none, 취소/이동 버튼)
    - .planning/phases/03-folder-tree-closure-table/03-PATTERNS.md §"MoveFolderModal"(DeleteWorkspaceDialog fetch/로딩/에러 골격)
    - src/components/ui/Modal.tsx, src/components/workspace/DeleteWorkspaceDialog.tsx
    - src/components/tree/FolderTree.tsx (03-02 최소 렌더 — 여기에 노드/메뉴/모달/펼침·선택 상태 배선)
  </read_first>
  <behavior>
    - MoveFolderModal: Modal로 감싼 "'{폴더명}' 이동" 타이틀 + 워크스페이스 루트부터의 들여쓰기 리스트. 이동 대상 폴더 자신과 그 서브트리는 isDescendantOrSelf로 선택 불가(회색 + pointer-events none). 항목 클릭=선택 표시, "이동" 버튼(선택 전 disabled)=POST /api/folders/[id]/move → 성공 시 재조회·닫기, 실패 시 에러 카피.
    - FolderTree: buildTree 결과를 FolderTreeNode로 렌더하고, 펼침 상태(Set<string>, 비영속)·selected·draggedId·컨텍스트 메뉴 오픈·MoveFolderModal 오픈 상태를 소유. 모든 뮤테이션 성공은 router.refresh()로 재조회(낙관적 UI 없음). 빈 상태("폴더가 없어요" + 안내)를 렌더.
  </behavior>
  <action>
    `src/components/tree/MoveFolderModal.tsx`(+css)를 DeleteWorkspaceDialog fetch/로딩/에러 골격 + Modal로 작성한다. `src/components/tree/FolderTree.tsx`를 확장해 FolderTreeNode·FolderContextMenu·MoveFolderModal을 배선하고 펼침/선택/드래그/메뉴/모달 상태를 관리한다. 재조회는 router.refresh()로 일원화. 빈 상태 카피는 UI-SPEC Copywriting Contract 사용.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/folder-tree.spec.ts && pnpm vitest run</automated>
    <human-check>폴더를 자기 자손 위로 드래그하면 커서가 '금지(not-allowed)'로, 유효한 다른 폴더 위로 드래그하면 accent 아웃라인으로 즉시 피드백되는지 실브라우저(`pnpm dev`)에서 확인(VALIDATION.md Manual-Only: 커서·아웃라인 시각 상태는 자동 스냅샷보다 사람 눈이 확실).</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test e2e/folder-tree.spec.ts` 전체 green(계층·컨텍스트 메뉴·인라인 이름변경·DnD 이동·삭제).
    - MoveFolderModal에서 이동 대상 자신·서브트리가 선택 불가(회색)로 렌더 — e2e 또는 수동으로 확인.
    - `pnpm vitest run` 전체 green(회귀 없음), `pnpm exec tsc --noEmit` clean, `pnpm lint` clean.
    - 빈 워크스페이스에서 "폴더가 없어요" 빈 상태가 보인다(UI-SPEC Copywriting).
  </acceptance_criteria>
  <done>트리 상호작용(계층·메뉴·이름변경·DnD·이동 모달·삭제)이 완성되고 e2e가 green. TREE-01/03 사용자 표면 완결.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 클라이언트 (트리 UI) | 사이클 사전 판정·펼침 상태는 클라이언트에 있으나 신뢰 경계가 아니다 — 모든 변경은 서버 라우트가 재검증한다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05-CLIENTTRUST | Tampering | 클라이언트 사이클 판정(isDescendantOrSelf) | medium | mitigate | 클라이언트 판정은 UX 보조(즉시 금지 커서)일 뿐 — 실제 이동은 03-04 move 라우트가 트랜잭션에서 사이클을 재검증(409). UI를 우회한 직접 API 호출도 서버가 막는다. |
| T-03-05-UIHIDE | Elevation of Privilege | 메뉴/버튼 노출 | medium | mitigate | UI 버튼 숨김은 보안이 아니다(CLAUDE.md) — 권한 없는 사용자가 메뉴를 봐도 서버 requireRole(EDITOR)가 403. UI는 편의만 담당. |
| T-03-05-XSS | Cross-site Scripting | 폴더 라벨 렌더 | low | mitigate | 폴더 이름은 React 텍스트 노드로 렌더(자동 이스케이프), dangerouslySetInnerHTML 미사용. |
| T-03-05-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 없음 — DnD·메뉴는 네이티브 HTML5 API + 자체 팝업(UI-SPEC Registry Safety). |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/folder-tree.spec.ts` green (기존 preview-perf 등 e2e도 회귀 없음).
- `pnpm vitest run` 전체 green.
- `pnpm exec tsc --noEmit` clean, `pnpm lint` clean.
- 수동: DnD 커서·아웃라인 시각 피드백(위 human-check).
</verification>

<success_criteria>
사이드바가 워크스페이스>폴더>하위 폴더 계층을 표시하고(TREE-01), 우클릭/kebab 메뉴·인라인 이름변경·네이티브 DnD 이동·이동 폴백 모달·삭제 확인이 서버 확정 후 동작하며(TREE-03), 자기 자손으로의 이동은 클라이언트 즉시 금지 + 서버 재검증으로 막힌다.
</success_criteria>

<output>
Create `.planning/phases/03-folder-tree-closure-table/03-05-SUMMARY.md` when done
</output>
