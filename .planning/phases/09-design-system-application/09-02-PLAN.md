---
phase: 09-design-system-application
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/lib/db-membership.ts
  - tests/membership/list-memberships.test.ts
  - src/components/workspace/WorkspaceCard.tsx
  - src/components/workspace/WorkspaceCard.module.css
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/dashboard/page.module.css
  - src/components/workspace/CreateWorkspaceButton.module.css
  - src/components/workspace/JoinWorkspaceInput.module.css
autonomous: true
requirements: [WS-01, WS-02]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "WS-01: 권한 매트릭스(Owner/Admin/Editor/Viewer) 위반 시 서버가 403을 반환한다 — 리스킨 후에도 회귀 없음(rbac vitest + e2e). (spec-less 프로브 unresolved 행의 회귀-확정 해소, behavior-frozen 가정)"
    - "WS-02: 회원이 워크스페이스를 생성하면 OWNER가 되고 삭제는 OWNER만 가능하다 — 리스킨 후에도 회귀 없음(e2e). (spec-less 프로브 unresolved 행의 회귀-확정 해소, behavior-frozen 가정)"
    - "워크스페이스 카드가 소유자 이름·생성일·문서 개수·폴더 개수를 실제 DB 조회값으로 표시한다(하드코딩 플레이스홀더 없음) (D-08)"
    - "신규 워크스페이스도 조건부 숨김 없이 항상 \"문서 0개 · 폴더 0개\"로 실측 렌더된다 (UI Consideration: populated covered)"
    - "가입 시 기본 워크스페이스 자동 배정으로 0개 카드 빈 상태는 구조적으로 발생하지 않는다 (UI Consideration: zero-one-many covered)"
    - "워크스페이스 이름은 1줄 말줄임(text-overflow: ellipsis) 동작을 유지한다 (UI Consideration: long-text covered)"
    - "워크스페이스 메인 라우트는 /dashboard 를 유지한다(rename 없음) (D-07)"
    - "OWNER 가 없는 워크스페이스(시드 기본 워크스페이스 등)는 ownerName 이 null 이고 UI에서 \"-\"로 표시된다 (D-08)"
  prohibitions:
    - "리스킨 중 서버 RBAC(requireRole) 검증·워크스페이스 소프트삭제(is_deleted) 동작을 변경하지 않는다 — WorkspaceCard 의 role===\"OWNER\" 조건부 삭제 버튼은 UX 편의일 뿐 실제 게이트는 서버(무변경)"
  artifacts:
    - "listMembershipsForUser 확장 반환 형태: { id, name, role, createdAt, ownerName: string|null, docCount: number, folderCount: number }"
    - "WorkspaceCardProps 확장: ownerName, createdAt, docCount, folderCount"
    - "WorkspaceCard 메타 라인 마크업(\"소유자 … · 생성일 … · 문서 N개 · 폴더 N개\")"
    - "tests/membership/list-memberships.test.ts (신규 집계 검증 테스트)"
  key_links:
    - "listMembershipsForUser 반환 필드 ↔ dashboard/page.tsx 전달 ↔ WorkspaceCardProps — 한 곳이라도 어긋나면 카드 메타 미표시"
    - "document/folder 집계 서브쿼리 WHERE(is_deleted=false, folder 은 is_trash_root=false) ↔ 실사용 개수 정합성"
---

<objective>
워크스페이스 메인(`/dashboard`) 화면을 `docs/images/workspace-main-wireframe.svg` 구성(폴더 트리 없는 중앙 정렬 카드 리스트 + 워크스페이스 만들기 + 참여 신청)대로 재작업하고, 카드가 **실제 DB 조회값**(소유자/생성일/문서 개수/폴더 개수)을 리스팅하도록 `listMembershipsForUser` 를 확장한다. 라우트는 그대로 `/dashboard`(D-07). RBAC·소프트삭제 등 동작은 100% 유지, 프레젠테이션과 표시용 데이터만 재작업.

**결정 근거:** D-07(/dashboard 유지), D-08(카드 데이터는 실측·listMembershipsForUser 확장, "공개" 배지 스킵). 09-01 의 전역 토큰을 소비한다.

**behavior-frozen 가정(spec-less 프로브 unresolved 6행 중 2행):** WS-01/WS-02 는 리스킨으로 바뀌지 않는다 — 회귀 e2e/vitest로 확정.

Purpose: 사용자가 처음 요청한 "구성을 따라 데이터를 조회한 뒤 리스팅"을 실측값으로 충족.
Output: 확장된 멤버십 집계 쿼리, 메타 라인이 있는 리스킨된 카드, 리스킨된 대시보드.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-application/09-CONTEXT.md
@.planning/phases/09-design-system-application/09-UI-SPEC.md
@.planning/phases/09-design-system-application/09-PATTERNS.md
@src/db/schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: listMembershipsForUser 집계 확장 (소유자/생성일/문서수/폴더수)</name>
  <files>src/lib/db-membership.ts, tests/membership/list-memberships.test.ts</files>
  <read_first>
    - src/lib/db-membership.ts (현재 workspaceMember ⋈ workspace, id/name/role 만 select, where is_deleted=false)
    - src/db/schema.ts (workspace.createdAt:29, document.workspaceId/isDeleted, folder.workspaceId/isDeleted/isTrashRoot, workspaceMember.role, user.name — 집계 대상 컬럼)
    - .planning/phases/09-design-system-application/09-PATTERNS.md (listMembershipsForUser 확장 지침 + Drizzle sql 서브쿼리 예시)
    - .planning/phases/09-design-system-application/09-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - 반환 객체에 createdAt(workspace.createdAt), ownerName(string|null), docCount(number), folderCount(number) 4필드가 추가된다.
    - docCount = document 중 workspace_id 일치 AND is_deleted=false 의 개수. 신규 워크스페이스면 0.
    - folderCount = folder 중 workspace_id 일치 AND is_deleted=false AND is_trash_root=false 의 개수. 신규면 0.
    - ownerName: 해당 워크스페이스에 role='OWNER' 인 workspaceMember 의 user.name. OWNER 행이 없으면(시드 기본 워크스페이스 = 전원 EDITOR) null.
    - is_deleted=true 워크스페이스는 계속 결과에서 제외(기존 계약 유지).
  </behavior>
  <action>
    listMembershipsForUser 의 select 절을 확장한다. createdAt 은 workspace.createdAt 을 그대로 추가. docCount/folderCount 는 drizzle-orm 의 sql<number>`(select count(*) from ...)` 상관 서브쿼리로 select 절에 인라인(WHERE 조건은 behavior 그대로, parameterized 바인딩만 사용 — 문자열 보간 금지). ownerName 은 role='OWNER' 인 workspaceMember → user 를 leftJoin 하거나 sql<string|null> 상관 서브쿼리로 취득(OWNER 없으면 null). 기존 innerJoin(workspace) + where(and(userId 일치, workspace.isDeleted=false)) 는 유지. TRD Closure Table 고정 쿼리 수 불변식과 무관(목록이 이미 워크스페이스 단위, N+1 아님). 신규 테스트 tests/membership/list-memberships.test.ts 작성: 문서/폴더가 있는 워크스페이스의 실측 카운트, 신규 워크스페이스 0/0, OWNER 없는 워크스페이스 ownerName null, soft-deleted 워크스페이스 제외.
  </action>
  <verify>
    <automated>pnpm vitest run tests/membership/list-memberships.test.ts</automated>
  </verify>
  <precondition>로컬 Postgres(.env.local DATABASE_URL 또는 DATABASE_URL_TEST)가 기동되어 있어야 DB 테스트가 실행된다.</precondition>
  <acceptance_criteria>
    - listMembershipsForUser 반환에 createdAt/ownerName/docCount/folderCount 4필드 존재(타입: ownerName string|null, docCount/folderCount number)
    - docCount 는 is_deleted=false 문서만, folderCount 는 is_deleted=false AND is_trash_root=false 폴더만 집계
    - OWNER 없는 워크스페이스 ownerName === null
    - is_deleted 워크스페이스는 반환되지 않음
    - 집계 SQL 은 parameterized(sql 템플릿 바인딩) — 사용자 입력 문자열 보간 없음
    - pnpm vitest run tests/membership/list-memberships.test.ts green
  </acceptance_criteria>
  <done>listMembershipsForUser 가 4필드 확장 반환하며 실측 카운트를 제공하고 soft-deleted 를 제외, 신규 테스트 green.</done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceCard props 확장 + 메타 라인 + 대시보드 화면 리스킨</name>
  <files>src/components/workspace/WorkspaceCard.tsx, src/components/workspace/WorkspaceCard.module.css, src/app/(main)/dashboard/page.tsx, src/app/(main)/dashboard/page.module.css, src/components/workspace/CreateWorkspaceButton.module.css, src/components/workspace/JoinWorkspaceInput.module.css</files>
  <read_first>
    - src/components/workspace/WorkspaceCard.tsx (현재 props {id,name,role}, Card 래핑, Link 이름, role==="OWNER" 조건부 삭제 버튼 — 삭제 게이트 로직 무변경)
    - src/app/(main)/dashboard/page.tsx (memberships.map → WorkspaceCard 전달부, 확장 필드 전달 추가 지점)
    - src/app/(main)/dashboard/page.module.css, WorkspaceCard.module.css (하드코딩 잔재 var() 치환 대상)
    - .planning/phases/09-design-system-application/09-UI-SPEC.md (Copywriting Contract "워크스페이스 카드 메타 라인"·"문서 버튼" 행 + Visual Hierarchy "워크스페이스 메인" 행 + UI Considerations 표)
  </read_first>
  <action>
    (1) WorkspaceCardProps 에 ownerName: string|null, createdAt: Date|string, docCount: number, folderCount: number 를 추가한다. 카드 본문에 메타 라인을 렌더한다: "소유자 {ownerName} · 생성일 {createdAt} · 문서 {docCount}개 · 폴더 {folderCount}개"(Copywriting Contract 그대로, ownerName null 이면 "-", createdAt 은 날짜 표기로 포맷). 카드 제목 .name 의 1줄 말줄임(ellipsis)은 유지. "문서" 버튼은 카드 Link 와 동일 동작(워크스페이스 진입, 별도 라우트 아님)이므로 시각 버튼만 추가하거나 생략(재량) — 새 핸들러/라우트 금지.

    (2) dashboard/page.tsx 의 map 에서 membership.createdAt/ownerName/docCount/folderCount 4필드를 WorkspaceCard 에 전달한다. auth()/redirect/listMembershipsForUser 호출 흐름·라우트(/dashboard)는 무변경.

    (3) page.module.css / WorkspaceCard.module.css / CreateWorkspaceButton.module.css / JoinWorkspaceInput.module.css: 리스트 컨테이너는 중앙 정렬 + var(--radius-lg), 카드는 var(--radius-md), 메타 라인은 Label 타이포(12px/600, muted 색). "워크스페이스 만들기"가 유일한 accent 요소(우상단, 시선 유도). 색/반경/타이포는 전역 토큰이 자동 적용되므로 하드코딩 잔재만 var(--token) 으로 치환. 삭제/생성/참여 동작(DeleteWorkspaceDialog, CreateWorkspaceButton, JoinWorkspaceInput 로직)은 무변경.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec playwright test e2e/document-workspace.spec.ts 2>/dev/null || pnpm exec playwright test --grep "workspace|워크스페이스|dashboard"</automated>
    grep 확인: `grep -F 'docCount' src/components/workspace/WorkspaceCard.tsx` 와 `grep -F 'folderCount' src/app/(main)/dashboard/page.tsx` 각 1건; 라우트 유지 — `grep -F "'/dashboard'" src/app` 관련 경로 무변경(page.tsx redirect 대상 유지).
  </verify>
  <precondition>로컬 Postgres(.env.local DATABASE_URL)가 기동되어 있어야 워크스페이스 playwright 회귀가 실행된다. playwright 는 단일 실행만.</precondition>
  <acceptance_criteria>
    - WorkspaceCard 가 ownerName/createdAt/docCount/folderCount 메타 라인을 렌더(ownerName null → "-")
    - dashboard/page.tsx 가 4개 확장 필드를 카드에 전달
    - 카드 제목 1줄 말줄임 유지, 리스트 컨테이너 중앙 정렬
    - WS-02 e2e(워크스페이스 생성=OWNER, OWNER만 삭제) + WS-01 rbac vitest 회귀 green
    - /dashboard 라우트·auth 흐름 diff 없음, DeleteWorkspaceDialog/Create/Join 로직 무변경
    - pnpm vitest run 전체 green
  </acceptance_criteria>
  <done>카드가 실측 소유자/생성일/문서수/폴더수를 새 토큰으로 표시하고 WS-01/WS-02 동작이 회귀 없이 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 워크스페이스 변경 API(생성/삭제) | 서버 requireRole 게이트가 이 경계를 지킨다. 이번 phase 무접촉 |
| listMembershipsForUser → DB | userId 스코프 조회 + 신규 집계 서브쿼리. 사용자 입력이 SQL로 흐르지 않음 |

## STRIDE Threat Register

ASVS L1, block on high. **이번 phase는 패키지 설치가 없다** → 패키지 정당성 게이트(T-*-SC) 해당 없음.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-02-RBAC | Elevation of Privilege | WorkspaceCard role==="OWNER" 조건부 삭제 버튼 | low | accept | UI 조건부 렌더는 UX 편의일 뿐, 실제 권한 경계는 서버 requireRole(DELETE 라우트) — 이번 phase 무접촉. 버튼 숨김은 보안이 아니라는 CLAUDE.md 불변식 그대로 유지 |
| T-09-02-SQLI | Tampering | listMembershipsForUser 집계 서브쿼리 | medium | mitigate | docCount/folderCount/ownerName 서브쿼리는 drizzle sql 템플릿 parameterized 바인딩만 사용, userId 는 세션 유래(사용자 자유입력 아님). 문자열 보간 금지 |
| T-09-02-IDOR | Information Disclosure | 카드 데이터 노출 | low | accept | listMembershipsForUser 는 세션 userId 스코프(본인 소속 워크스페이스만) — 스코프 로직 무변경, 신규 필드도 동일 워크스페이스 범위 내 집계 |
</threat_model>

<verification>
- pnpm vitest run 전체 green (멤버십 집계 테스트 + rbac 회귀 포함)
- 워크스페이스 playwright e2e green (WS-01/WS-02 회귀 없음)
- 카드 메타 라인이 실측값(하드코딩 아님)으로 렌더(수동/스냅샷 확인)
</verification>

<success_criteria>
- 워크스페이스 메인이 와이어프레임 구성대로 실제 워크스페이스 데이터를 조회해 리스팅한다 (ROADMAP Phase 9 성공기준 2)
- 카드 데이터가 전부 실측값이며 RBAC·소프트삭제 동작 회귀 없음
</success_criteria>

<output>
Create `.planning/phases/09-design-system-application/09-02-SUMMARY.md` when done
</output>
