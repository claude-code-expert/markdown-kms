---
phase: 09-design-system-application
plan: 04
type: execute
wave: 3
depends_on: [09-02, 09-03]
files_modified:
  - e2e/design-system-flow.spec.ts
  - CLAUDE.md
  - changelog/changelog.md
autonomous: true
requirements: [AUTH-01, AUTH-02, WS-01, WS-02, DOC-01, DOC-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "로그인 → 워크스페이스 생성/가입 → 폴더 생성 → 문서 생성/수정/삭제 전체 플로우가 새 UI에서 매 단계 회귀 없이 통과한다(playwright + vitest 전체) (ROADMAP Phase 9 성공기준 4)"
    - "AUTH-01/AUTH-02/WS-01/WS-02/DOC-01/DOC-02 6개 behavior-frozen 요구가 리스킨 후 전체 플로우 단일 e2e에서 재확인된다(spec-less 프로브 unresolved 6행의 통합 회귀 확정)"
    - "CLAUDE.md 디자인 토큰 원천 설명이 \"라이트=ui-kit.html 색상 + 신규 전역 타입/반경/모션, 다크=신규 파생 색상 + 동일 전역 토큰\"으로 갱신된다 (D-06)"
    - "폰트 교체(IBM Plex→DM Sans)·다크 팔레트 전면 교체·전역 radius/motion 토큰 도입이 changelog 에 비가역 결정으로 기록된다"
  prohibitions:
    - "전체 플로우 e2e를 다른 실행과 동시에 돌리지 않는다 — dev 스크립트의 포트 3000 kill 이 겹친 실행 서버를 죽여 스위트가 붕괴한다(단일 실행만)"
  artifacts:
    - "e2e/design-system-flow.spec.ts (전체 플로우 회귀 스펙 — 기존 스펙 재사용 시 참조 문서화)"
    - "CLAUDE.md 디자인 토큰 §갱신"
    - "changelog/changelog.md Phase 9 엔트리"
  key_links:
    - "09-01/09-02/09-03 완료 ↔ 전체 플로우 e2e — 세 화면이 모두 리스킨된 뒤에만 크로스-화면 회귀가 유효"
---

<objective>
Phase 9 마감 플랜: 세 화면(랜딩·워크스페이스 메인·에디터)이 모두 리스킨된 상태에서 로그인부터 문서 CRUD까지 **전체 플로우**를 새 UI에서 단일 e2e로 회귀 검증하고(ROADMAP 성공기준 4), 디자인 토큰 체계 변경을 문서(CLAUDE.md)와 changelog 에 반영한다.

**결정 근거:** D-06(CLAUDE.md 토큰 원천 설명 갱신 필요). 크로스-화면 회귀는 09-02·09-03 완료에 의존하므로 Wave 3.

**behavior-frozen 통합 확정:** spec-less 프로브의 6개 unresolved 요구(AUTH-01/02, WS-01/02, DOC-01/02)를 개별 플랜의 화면별 회귀에 더해 전체 플로우 단일 경로에서 최종 재확인한다.

Purpose: 화면별 회귀를 넘어 전체 사용자 여정이 새 UI에서 끊김 없이 동작함을 증명하고, 비가역 결정을 기록.
Output: 전체 플로우 e2e green, 갱신된 CLAUDE.md, changelog 엔트리.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-application/09-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전체 플로우 e2e 회귀 (로그인→워크스페이스→폴더→문서 CRUD) + vitest 전체</name>
  <files>e2e/design-system-flow.spec.ts</files>
  <read_first>
    - e2e/document-workspace.spec.ts, e2e/folder-tree.spec.ts, e2e/document-trash.spec.ts (기존 스펙 — 재사용 가능한 헬퍼/셀렉터 파악)
    - .planning/ROADMAP.md (Phase 9 성공기준 4 — 검증 대상 플로우)
    - .planning/STATE.md ("e2e 전체 스위트가 stale locator 잡음" · "playwright 동시실행 위험" 교훈 — 단일 실행·전체 스위트 필수)
  </read_first>
  <action>
    로그인 → 워크스페이스 생성(또는 기본 워크스페이스 진입)/참여 신청 → 폴더 생성 → 문서 생성 → 문서 수정(입력·자동저장) → 문서 소프트삭제 → 휴지통 확인까지 한 사용자 여정을 새 UI에서 검증하는 e2e 스펙을 작성한다(e2e/design-system-flow.spec.ts). 기존 스펙의 셀렉터/헬퍼를 최대한 재사용하고, 리스킨으로 바뀐 클래스/구조에 셀렉터를 맞춘다. 그 뒤 vitest 전체와 playwright 전체 스위트를 각각 1회(동시 실행 금지) 돌려 회귀가 없음을 확인한다.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec playwright test</automated>
  </verify>
  <precondition>로컬 Postgres(.env.local DATABASE_URL)와 dev 서버가 기동되어 있어야 한다. playwright 전체 스위트는 반드시 단일 실행(겹친 실행 시 포트 3000 kill 로 서버 붕괴).</precondition>
  <acceptance_criteria>
    - e2e/design-system-flow.spec.ts 가 로그인→워크스페이스→폴더 생성→문서 생성/수정/삭제→휴지통 전 단계를 새 UI에서 통과
    - pnpm vitest run 전체 green
    - pnpm exec playwright test 전체 스위트 green (단일 실행)
  </acceptance_criteria>
  <done>전체 플로우가 새 UI에서 회귀 없이 통과하고 vitest+playwright 전체 green (성공기준 4 충족).</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md 토큰 원천 설명 갱신(D-06) + changelog 비가역 결정 기록</name>
  <files>CLAUDE.md, changelog/changelog.md</files>
  <read_first>
    - CLAUDE.md (§문서 체계의 docs/ui-kit.html 토큰 원천 설명 + 지켜야 할 불변식 — 갱신 지점)
    - .planning/phases/09-design-system-application/09-CONTEXT.md (D-06 — 갱신 문구 지침)
    - changelog/changelog.md (append-only 포맷 확인, 없으면 스킬 관례대로 생성)
  </read_first>
  <action>
    (1) CLAUDE.md 의 디자인 토큰 원천 설명을 D-06 대로 갱신한다: 라이트 모드 = docs/ui-kit.html 색상 유지 + 신규 전역 타이포(DM Sans/DM Mono)·반경(6/12/18px)·모션(180/240/300ms) 토큰, 다크 모드 = 신규 파생 팔레트(--accent #7359f8 등) + 동일 전역 타입/반경/모션. 폰트가 IBM Plex 에서 DM Sans/Mono 자체호스팅으로 대체됐음을 반영.

    (2) changelog/changelog.md 에 Phase 9 의 비가역 결정을 append 한다: IBM Plex → DM Sans/DM Mono 폰트 교체(자체호스팅), 다크 팔레트 blue-accent → 파생 팔레트 전면 교체, 전역 --radius-*/--duration-*/--ease-* 토큰 도입, 각 근거(D-03/D-04/D-05/D-06).
  </action>
  <verify>
    <automated>grep -F 'DM Sans' CLAUDE.md && grep -F '#7359f8' changelog/changelog.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md 디자인 토큰 설명이 라이트/다크 이원 토큰 체계 + DM Sans 를 반영하도록 갱신됨
    - changelog/changelog.md 에 Phase 9 폰트/다크팔레트/전역토큰 교체가 근거와 함께 기록됨
  </acceptance_criteria>
  <done>문서와 changelog 가 새 토큰 체계(D-06)와 비가역 결정을 반영한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (해당 없음) | 이번 플랜은 e2e 스펙·문서·changelog 만 생성/수정한다. 배포 UI/서버 코드에 신규 실행 경로를 만들지 않는다 |

## STRIDE Threat Register

ASVS L1, block on high. **이번 phase는 패키지 설치가 없다** → 패키지 정당성 게이트(T-*-SC) 해당 없음. 이 플랜은 검증·문서 전용이라 신규 공격 표면이 없다.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04-NONE | (없음) | e2e 스펙 / 문서 / changelog | low | accept | 프로덕션 실행 경로·입력 표면 없음. e2e 는 테스트 전용 코드, CLAUDE.md/changelog 는 문서 — 리스크 무시 가능 |
</threat_model>

<verification>
- pnpm vitest run 전체 green
- pnpm exec playwright test 전체 스위트 green (단일 실행)
- CLAUDE.md·changelog 갱신 확인(grep)
</verification>

<success_criteria>
- 로그인부터 문서 CRUD까지 전체 플로우가 새 UI에서 매 단계 vitest+playwright 회귀 테스트로 검증된다 (ROADMAP Phase 9 성공기준 4)
- 디자인 토큰 체계 변경이 CLAUDE.md·changelog 에 반영됨 (D-06)
</success_criteria>

<output>
Create `.planning/phases/09-design-system-application/09-04-SUMMARY.md` when done
</output>
