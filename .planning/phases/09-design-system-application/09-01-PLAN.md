---
phase: 09-design-system-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/fonts/dm-sans-400.woff2
  - public/fonts/dm-sans-500.woff2
  - public/fonts/dm-sans-600.woff2
  - public/fonts/dm-sans-700.woff2
  - public/fonts/dm-mono-400.woff2
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/ui/Button.module.css
  - src/components/ui/Card.module.css
  - src/components/ui/Modal.module.css
  - src/components/ui/ConfirmDialog.module.css
  - src/components/ui/Input.module.css
  - src/components/ui/Form.module.css
  - src/app/(auth)/login/login-form.tsx
  - src/app/(auth)/login/page.module.css
  - src/app/(auth)/signup/signup-form.tsx
  - src/app/(auth)/signup/page.module.css
autonomous: true
requirements: [AUTH-01, AUTH-02]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "AUTH-01: 이메일+비밀번호로 가입하면 즉시 로그인 상태가 된다 — 리스킨 후에도 회귀 없음(playwright e2e). (spec-less 프로브 unresolved 행의 회귀-확정 해소, behavior-frozen 가정)"
    - "AUTH-02: 로그인 세션이 브라우저 새로고침 후에도 유지된다 — 리스킨 후에도 회귀 없음(playwright e2e). (spec-less 프로브 unresolved 행의 회귀-확정 해소, behavior-frozen 가정)"
    - "DM Sans(400/500/600/700)·DM Mono(400)가 public/fonts 자체호스팅으로 로드되고 폰트 404가 없다 (D-03)"
    - "라이트 :root 색상 12개(--bg #ffffff, --accent #2563eb 등)는 변경되지 않는다 (D-04/D-06)"
    - "[data-theme=\"dark\"]가 새 파생 팔레트(--accent #7359f8, --bg #0e0d11, --surface #1c1b22)로 렌더되고 기존 blue-accent 다크값 #3b82f6 은 소스에서 제거된다 (D-04)"
    - "전역(라이트/다크 공통) --radius-sm/md/lg(6/12/18px), --duration-fast/standard/slow(180/240/300ms), --ease-fluid/--ease-elastic 토큰이 :root에 정의된다 (D-05)"
    - "6개 UI 프리미티브(Button/Card/Modal/ConfirmDialog/Input/Form)가 하드코딩 대신 var(--radius-*)/var(--duration-*) 를 참조하고 컴포넌트 .tsx 는 무수정이다 (D-05)"
    - "Button :active 에 transform: scale(0.98) translateY(1px) press 피드백이 --duration-fast + --ease-elastic 으로 적용된다"
    - "Google 계속하기 placeholder 는 disabled 로 렌더되고 onClick/OAuth 배선이 없다 — loading/error 상태가 존재하지 않는다 (UI Consideration: loading/error covered)"
    - "로그인/회원가입 폼의 에러 카피(LOGIN_ERROR 등)·표시 로직은 100% 유지되고 FormError 는 시각 토큰만 재작업된다 (UI Consideration: error covered)"
  prohibitions:
    - "docs/design_system 의 브랜드 카피·마케팅 문구(\"400+ apps\" 등)·인물/몬스터 이미지를 배포 UI(src/app, public 렌더 경로)에 넣지 않는다 (D-01/D-02)"
    - "Google 계속하기 placeholder 를 클릭 가능하게 만들거나 실제 Auth.js OAuth provider(signIn 프로바이더 호출)에 배선하지 않는다"
  artifacts:
    - public/fonts/dm-sans-400.woff2, dm-sans-500.woff2, dm-sans-600.woff2, dm-sans-700.woff2, dm-mono-400.woff2
    - "@font-face 선언(next/font/local 또는 globals.css) — DM Sans / DM Mono, font-display: swap"
    - "CSS 커스텀 프로퍼티(신규): --radius-sm, --radius-md, --radius-lg, --duration-fast, --duration-standard, --duration-slow, --ease-fluid, --ease-elastic"
    - "다크 팔레트 값(전면 교체): --bg/--surface/--surface-2/--border/--border-strong/--text/--muted/--dim/--accent/--accent-strong/--accent-weak/--destructive/--code-bg (+ --heading-text 선택)"
    - "Google 계속하기 disabled placeholder 버튼 마크업 (login-form.tsx / signup-form.tsx)"
  key_links:
    - "layout.tsx 폰트 variable ↔ globals.css body font-family — 이름이 일치해야 DM Sans 가 실제 적용됨"
    - "globals.css 전역 토큰 ↔ 6개 프리미티브의 var() 참조 — 토큰 정의 누락 시 전 화면 폴백"
---

<objective>
Phase 9(순수 프레젠테이션 리스킨)의 **트레이서 슬라이스**: 디자인 토큰 파운데이션을 세우고, 그 토큰이 실제로 캐스케이드됨을 랜딩(로그인/회원가입) 화면에서 end-to-end 증명한다. 자산(폰트) → 전역 토큰(globals.css) → 폰트 배선(layout.tsx) → 공유 프리미티브(6개 UI 컴포넌트 CSS) → 한 화면(인증 폼) → 회귀 검증까지 한 수직 경로를 관통한다.

이 슬라이스가 통과하면 나머지 두 화면(워크스페이스 메인 09-02, 에디터 09-03)은 동일 토큰을 소비만 하므로 아키텍처 위험이 이 플랜에서 전부 소진된다.

**결정 근거:** D-01/D-02(토큰만 추출, 브랜드 카피/이미지 미사용), D-03(폰트 자체 호스팅), D-04(라이트 기본 유지·다크 전면 교체), D-05(타입/반경/모션 전역 교체), D-06(라이트=ui-kit.html 색상 유지).

**behavior-frozen 가정(spec-less 프로브 unresolved 6행 중 2행):** AUTH-01/AUTH-02 는 이번 phase가 기능을 바꾸지 않는다 — 리스킨 후 동작이 동일하다는 것을 회귀 e2e로 확정한다.

Purpose: 토큰 아키텍처를 최소 화면에서 증명해 후속 화면의 재작업 위험 제거.
Output: 자체호스팅 폰트, 전역 토큰 세트, Dracula 파생 다크 팔레트, 리스킨된 프리미티브, 리스킨된 인증 화면 + Google placeholder.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-application/09-CONTEXT.md
@.planning/phases/09-design-system-application/09-UI-SPEC.md
@.planning/phases/09-design-system-application/09-PATTERNS.md
</context>

<tasks>

<task type="tracer" reversibility="reversible">
  <name>Task 1: 폰트 자체호스팅 이식 + globals.css 전역 토큰 + layout.tsx 폰트 스왑</name>
  <files>public/fonts/dm-sans-400.woff2, public/fonts/dm-sans-500.woff2, public/fonts/dm-sans-600.woff2, public/fonts/dm-sans-700.woff2, public/fonts/dm-mono-400.woff2, src/app/globals.css, src/app/layout.tsx</files>
  <read_first>
    - src/app/globals.css (현재 :root 라이트 12토큰 + [data-theme="dark"] + @media 블록 + body 규칙 — 무엇을 바꾸고 무엇을 남기는지 확인)
    - src/app/layout.tsx (현재 next/font/google IBM_Plex_Sans/Mono + variable --font-ibm-plex-sans/--font-ibm-plex-mono + html className 배선)
    - docs/design_system/fonts/fonts.css (이식할 @font-face 원본 — dm-sans-400/500/600/700, dm-mono-400, font-display: swap)
    - .planning/phases/09-design-system-application/09-UI-SPEC.md (Typography / Radius Scale / Motion Tokens / Color 표 — 다크 CSS 변수 전체 매핑 표가 이 태스크의 다크 블록 원천)
  </read_first>
  <action>
    (1) docs/design_system/fonts/ 의 dm-sans-400/500/600/700.woff2 + dm-mono-400.woff2 5개를 public/fonts/ 로 복사한다. @font-face 선언은 next/font/local 로 이식하는 것을 권장(기존 next/font/google 패턴을 자연 대체) — dm-sans 4웨이트를 하나의 localFont(variable, weight 매핑) + dm-mono 400을 별도 localFont 로 선언하고 각각 variable 을 부여한다. 대안으로 globals.css 최상단 @font-face 직접 선언도 허용(재량, CLAUDE.md CSS Modules 원칙과 충돌 없음).

    (2) layout.tsx 의 IBM_Plex_Sans/IBM_Plex_Mono(next/font/google) import 와 인스턴스를 DM Sans/DM Mono localFont 로 교체한다. html className 에 새 variable 을 넣고, body font-family 참조와 이름을 일치시킨다(예: --font-sans / --font-mono, 또는 기존 --font-ibm-plex-sans 변수명을 유지하되 소스만 DM Sans 로 교체 — 재량이되 body 참조와 반드시 일치).

    (3) globals.css :root 에 전역(라이트/다크 공통) 토큰을 추가한다 — --radius-sm: 6px; --radius-md: 12px; --radius-lg: 18px; --duration-fast: 180ms; --duration-standard: 240ms; --duration-slow: 300ms; --ease-fluid: cubic-bezier(.36,.66,.6,1); --ease-elastic: cubic-bezier(.42,0,.58,1.8);. 라이트 색상 12개와 --space-* 스케일은 절대 손대지 않는다(D-04/D-06, UI-SPEC Spacing "변경 없음").

    (4) [data-theme="dark"] 블록을 UI-SPEC "다크 모드 CSS 변수 전체 매핑" 표로 전면 교체한다 — --bg: #0e0d11; --surface: #1c1b22; --surface-2: #383645; --border: #383645; --border-strong: #514f60; --text: #c2c0ce; --muted: #b2afc0; --dim: #757383; --accent: #7359f8; --accent-strong: #9580fa; --accent-weak: #241f3d; --destructive: #e5484d; --code-bg: #1c1b22;. (--heading-text: #d3d1db; 선택 추가 — 미적용 시 회귀 아님.) 기존 slate/blue 다크값은 남기지 않는다.

    (5) @media (prefers-color-scheme: dark) 의 :root:not([data-theme]) 블록도 (4)와 동일 값으로 재복제한다(현행 이중 관리 패턴 유지 — 서버가 OS 설정을 못 읽어 첫 방문 폴백용).

    (6) body 규칙: font-family 를 새 sans variable 로, font-size 를 13px → 14px 로, line-height 1.5 유지.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
    grep 확인: `grep -F -- '--radius-sm' src/app/globals.css`, `grep -F -- '--duration-fast' src/app/globals.css`, `grep -F -- '--ease-fluid' src/app/globals.css` 각 1건 이상; 다크 팔레트 유입 `grep -Fc '#7359f8' src/app/globals.css` >= 1 그리고 `grep -F '#0e0d11' src/app/globals.css`; 구 blue-accent 제거 `grep -Fc '#3b82f6' src/app/globals.css` 결과가 0. `pnpm build` 또는 dev 부팅에서 public/fonts woff2 404 없음.
  </verify>
  <precondition>docs/design_system/fonts/dm-sans-400/500/600/700.woff2 와 dm-mono-400.woff2 5개 파일이 존재한다(검증됨). 없으면 중단.</precondition>
  <acceptance_criteria>
    - public/fonts/ 에 5개 woff2 가 존재하고 @font-face 가 선언됨(next/font/local 또는 globals.css)
    - globals.css :root 에 --radius-sm/md/lg, --duration-fast/standard/slow, --ease-fluid, --ease-elastic 8개 토큰 존재
    - 라이트 :root 색상 12개(--bg #ffffff … --code-bg #0f172a)와 --space-* 는 diff 없음
    - [data-theme="dark"] 와 @media prefers-color-scheme:dark 블록이 새 팔레트(--accent #7359f8, --bg #0e0d11)로 동일하게 교체됨, #3b82f6 부재
    - body font-size 14px, font-family 가 layout.tsx 의 새 variable 을 참조
    - pnpm vitest run 전체 green
  </acceptance_criteria>
  <done>전역 radius/motion 토큰 + DM Sans/Mono 자체호스팅 폰트 + Dracula 파생 다크 팔레트가 배선되고, 라이트 색상은 무변경. vitest 전체 green.</done>
</task>

<task type="auto">
  <name>Task 2: 6개 UI 프리미티브 CSS 토큰 스왑 + press 피드백 + squircle 폴백</name>
  <files>src/components/ui/Button.module.css, src/components/ui/Card.module.css, src/components/ui/Modal.module.css, src/components/ui/ConfirmDialog.module.css, src/components/ui/Input.module.css, src/components/ui/Form.module.css</files>
  <read_first>
    - src/components/ui/Button.module.css (border-radius: 6px, transition: background-color 0.15s ease 하드코딩 — 치환 원형)
    - src/components/ui/Card.module.css (border-radius: 8px — --radius-md 12px 로 상향 + squircle 대상)
    - src/components/ui/Modal.module.css, ConfirmDialog.module.css, Input.module.css, Form.module.css (동일 하드코딩 패턴)
    - .planning/phases/09-design-system-application/09-UI-SPEC.md (Radius Scale / Motion Tokens 표 + squircle @supports 예시)
  </read_first>
  <action>
    각 module.css 의 하드코딩 값을 전역 토큰 참조로 기계적 치환한다(컴포넌트 .tsx 는 절대 수정하지 않는다 — Phase 5 무수정 리스킨 관례):
    - border-radius 하드코딩 → 역할별 토큰: 버튼/인풋/배지 = var(--radius-sm)(6px), 카드/모달/드롭다운/다이얼로그 = var(--radius-md)(12px).
    - transition 의 하드코딩 duration/easing(예: 0.15s ease) → var(--duration-fast) var(--ease-fluid). 모달/다이얼로그 열림-닫힘 전환이 있으면 var(--duration-standard).
    - Button.module.css 의 .btn 에 :active 규칙 추가: transform: scale(0.98) translateY(1px); transition 을 var(--duration-fast) var(--ease-elastic) 로. (press 피드백 = 색 플래시 아님.)
    - Card.module.css / Modal.module.css 에 progressive-enhancement 블록 추가: @supports (corner-shape: squircle) { .card { corner-shape: squircle; } } — 폴백은 항상 적용되는 var(--radius-md) 라 시각 회귀 없음.
    - font-size 13px 하드코딩 잔재가 있으면 14px(Body) 로.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
    grep 확인: 6개 module.css에서 하드코딩 반경 부재 — `grep -REc 'border-radius:[[:space:]]*(4|6|8|12)px' src/components/ui/*.module.css` 결과가 0; Button press 피드백 존재 — `grep -F 'scale(0.98)' src/components/ui/Button.module.css` 1건; 토큰 참조 유입 — `grep -Fl 'var(--radius-' src/components/ui/Button.module.css src/components/ui/Card.module.css`.
  </verify>
  <acceptance_criteria>
    - Button/Card/Modal/ConfirmDialog/Input/Form .module.css 어디에도 하드코딩 border-radius(4/6/8/12px) 리터럴이 남지 않음(전부 var(--radius-*))
    - Button.module.css .btn:active 에 transform: scale(0.98) translateY(1px) 존재
    - Card.module.css / Modal.module.css 에 @supports (corner-shape: squircle) 블록 존재
    - 6개 컴포넌트 .tsx 파일은 diff 없음
    - pnpm vitest run 전체 green
  </acceptance_criteria>
  <done>6개 프리미티브가 전역 토큰만 참조하고 press 피드백 + squircle 폴백이 적용됨, 컴포넌트 tsx 무수정, vitest green.</done>
</task>

<task type="auto">
  <name>Task 3: 랜딩(로그인/회원가입) 화면 리스킨 + Google 계속하기 placeholder + 회귀</name>
  <files>src/app/(auth)/login/login-form.tsx, src/app/(auth)/login/page.module.css, src/app/(auth)/signup/signup-form.tsx, src/app/(auth)/signup/page.module.css</files>
  <read_first>
    - src/app/(auth)/login/login-form.tsx (Form/FormField/FormLabel/FormError/FormSubmit/Input 조합 — 마크업 구조 무변경 대상, LOGIN_ERROR 카피 유지)
    - src/app/(auth)/signup/signup-form.tsx (동일 Form 프리미티브 패턴)
    - src/app/(auth)/login/page.module.css, src/app/(auth)/signup/page.module.css (하드코딩 잔재 var() 치환 대상)
    - .planning/phases/09-design-system-application/09-UI-SPEC.md (Copywriting Contract "Google 로그인 placeholder" 행 + Visual Hierarchy "랜딩" 행)
  </read_first>
  <action>
    (1) 두 폼(login-form.tsx, signup-form.tsx)의 제출 버튼 아래에 "Google로 계속하기" placeholder 를 추가한다: type="button" 인 disabled 버튼, onClick 핸들러 없음, signIn 등 인증 함수 호출 없음. 스타일은 cursor: not-allowed + 낮춘 opacity 로 클릭 불가함을 명확히 표시(가짜 어포던스 방지). 문구는 정확히 "Google로 계속하기" 하나만 — 마케팅 카피/브랜드 톤 금지.

    (2) 기존 폼 마크업 구조(Form/FormField/Input/FormError/FormSubmit)와 기존 카피(LOGIN_ERROR = "이메일 또는 비밀번호가 올바르지 않습니다." 등), handleSubmit/signIn("credentials") 흐름은 무변경 — 회귀 방지. 시각은 전역 토큰이 자동 적용되므로 page.module.css 의 하드코딩 색/반경/타이포 잔재만 var(--token) 으로 치환.

    (3) Visual Hierarchy: 제출 버튼 = accent primary(1차 액션), Google placeholder = 비활성 스타일로 낮은 시각 무게(명확히 하위). 폼 카드가 중앙 1차 앵커.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec playwright test e2e/auth.spec.ts 2>/dev/null || pnpm exec playwright test --grep "로그인|회원가입|signup|login"</automated>
    grep 확인: `grep -F 'Google로 계속하기' src/app/(auth)/login/login-form.tsx src/app/(auth)/signup/signup-form.tsx` 각 1건 + 같은 파일에 `disabled`; OAuth 미배선 — `grep -REc 'signIn\("google"' src/app` 결과 0; 마케팅 카피 부재 — `grep -REc '400\+ apps' src/app` 결과 0.
  </verify>
  <precondition>로컬 Postgres(.env.local DATABASE_URL)가 기동되어 있어야 인증 playwright 회귀가 실행된다. playwright 는 단일 실행만(동시 실행 시 dev 포트 kill 로 서버가 죽음).</precondition>
  <acceptance_criteria>
    - login-form.tsx / signup-form.tsx 에 "Google로 계속하기" disabled 버튼 존재, onClick/signIn 프로바이더 호출 없음
    - 기존 폼 마크업 구조와 에러 카피 문자열 diff 없음(FormError 렌더 로직 무변경)
    - 인증 e2e(가입→즉시 로그인 AUTH-01, 새로고침 세션 유지 AUTH-02, 로그인 실패 시 LOGIN_ERROR 표시) green
    - src/app 하위에 "400+ apps" 등 마케팅 문구 부재
    - pnpm vitest run 전체 green
  </acceptance_criteria>
  <done>인증 화면이 새 토큰으로 렌더되고 Google placeholder 가 클릭 불가 비활성이며, 기존 로그인/가입 동작이 회귀 없이 e2e green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → 인증 API(signIn credentials) | 사용자 이메일/비밀번호가 이 경계를 넘는다. 이번 phase는 폼 마크업/CSS만 바꾸므로 경계 자체는 무변경 |
| 정적 자산(public/fonts) → 브라우저 | 신규 woff2 자산 서빙. 실행 코드 아님 |

## STRIDE Threat Register

ASVS L1, block on high. **이번 phase는 npm/pip/cargo 패키지 설치가 없다**(폰트는 파일 복사, 신규 의존성 없음) — 따라서 패키지 정당성 게이트(T-*-SC)는 해당 없음.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01-SPOOF | Spoofing | Google 계속하기 placeholder | medium | mitigate | disabled 속성 + onClick/OAuth 미배선 + cursor:not-allowed·낮춘 opacity 로 가짜 인증 어포던스 위조 차단. grep 게이트로 signIn("google") 부재 검증 |
| T-09-01-XSS | Tampering(XSS) | 인증 폼 마크업 재작업 | medium | mitigate | Input value 는 controlled state, dangerouslySetInnerHTML 미도입 — 폼 마크업 변경이 새 raw-HTML 렌더 경로를 만들지 않음. FormError 는 텍스트 노드만 렌더 |
| T-09-01-SESSION | Information Disclosure | signIn("credentials") 세션/쿠키 흐름 | low | accept | handleSubmit/signIn 로직 무변경 — CSRF/세션 쿠키 처리는 Auth.js 계층 그대로. 마크업만 리스킨 |
| T-09-01-BRAND | Information Disclosure | 배포 UI 브랜드 유출 | low | mitigate | docs/design_system 의 마케팅 카피/인물·몬스터 이미지를 UI에 넣지 않음(D-01/D-02) — grep 게이트로 "400+ apps" 등 부재 검증 |
</threat_model>

<verification>
- pnpm vitest run 전체 green (토큰 회귀·프리미티브 회귀 없음)
- 인증 playwright e2e green (AUTH-01/AUTH-02 회귀 없음)
- 폰트 404 없음, 라이트 색상 무변경, 다크 팔레트 교체 확인(grep)
- Google placeholder 비활성 + OAuth 미배선(grep)
</verification>

<success_criteria>
- 랜딩(로그인/회원가입) 화면이 docs/design_system 토큰(색·타이포·반경·모션)으로 렌더되고 Google 버튼 placeholder 를 비활성으로 보인다 (ROADMAP Phase 9 성공기준 1)
- 전역 토큰 파운데이션이 서고 후속 화면(09-02/09-03)이 소비할 준비 완료
</success_criteria>

<output>
Create `.planning/phases/09-design-system-application/09-01-SUMMARY.md` when done
</output>
