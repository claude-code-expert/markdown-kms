---
phase: 09-design-system-application
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/app/(main)/w/[wsId]/layout.tsx
  - src/components/tree/FolderTree.module.css
  - src/components/tree/FolderTreeNode.module.css
  - src/components/tree/SearchBox.module.css
  - src/components/tree/FolderContextMenu.module.css
  - src/components/tree/MoveFolderModal.module.css
  - src/components/document/DocumentWorkspace.module.css
  - src/components/document/SaveStatusBar.module.css
  - src/components/document/TagBar.module.css
  - src/components/editor/Toolbar.module.css
  - src/components/editor/HeadingDropdown.module.css
  - src/components/editor/EditorHost.module.css
  - src/components/layout/LayoutModeToggle.module.css
  - src/components/preview/PreviewPane.module.css
  - src/app/(main)/w/[wsId]/d/[docId]/page.module.css
autonomous: true
requirements: [DOC-01, DOC-02]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DOC-01: 문서 생성·수정·소프트삭제가 동작하고 삭제는 즉시 휴지통에 나타난다 — 리스킨 후에도 회귀 없음(e2e). (spec-less 프로브 unresolved 행의 회귀-확정 해소, behavior-frozen 가정)"
    - "DOC-02: 휴지통 복원(cascade, 원위치 또는 루트)과 완전삭제(ADMIN 이상)가 동작한다 — 리스킨 후에도 회귀 없음(e2e). (spec-less 프로브 unresolved 행의 회귀-확정 해소, behavior-frozen 가정)"
    - "폴더 트리는 순수 스타일링만 재작업하고 스크롤·들여쓰기·DnD·컨텍스트 메뉴 동작은 무변경이다 (UI Consideration: overflow covered, D-09)"
    - "자동저장 seq-guard 동작이 100% 유지된다 — \"저장됨\"은 마지막 발신 seq 응답일 때만 표시(EDIT-07 계약 불변, DocumentWorkspace 로직 무접촉)"
    - "에디터 화면이 write-form-wireframe 구성(사이드바 폴더 트리+검색 · 상단 브레드크럼/제목/저장·삭제 · 서식 툴바 · 보기모드 4버튼 · 분할 에디터/프리뷰 · 상태바)으로 렌더된다"
    - "현재 활성/선택된 폴더·문서 트리 노드가 accent 로 표시된다 (UI-SPEC Accent reserved)"
    - "와이어프레임의 \"전체 문서\"/\"미분류 문서\" 고정 뷰·폴더열 정렬/추가 버튼은 구현하지 않는다 (D-09 스코프 밖, Deferred Idea)"
  prohibitions:
    - "리스킨 중 autosave seq-guard·soft-delete·closure-table 폴더 로직·rehype-sanitize 파이프라인·서식 플러그인 동작을 변경하지 않는다 — CSS/클래스 스타일링만"
    - "PreviewPane 렌더 경로를 바꿔 새 raw-HTML/XSS 표면을 도입하지 않는다 (sanitize 없는 렌더링 금지 불변식 유지)"
  artifacts:
    - "(신규 심볼 없음 — 순수 CSS/스타일 재작업. 09-01 의 전역 토큰을 소비만 한다.)"
  key_links:
    - "DocumentWorkspace seq-guard 상태 흐름 ↔ SaveStatusBar 표시 — 스타일 변경이 상태 로직에 닿지 않아야 함"
    - "전역 토큰(09-01) ↔ 에디터/트리/툴바/프리뷰 module.css var() 참조"
---

<objective>
문서 에디터 글쓰기 화면(`/w/[wsId]/d/[docId]`)을 `docs/images/write-form-wireframe.svg` 구성대로 재구성하고 전역 토큰을 적용한다: 사이드바(폴더 트리 + 검색), 상단 브레드크럼/제목/저장·삭제, 서식 툴바, 보기모드 4버튼, 분할 에디터/프리뷰, 상태바. **순수 스타일링만**(D-09) — 폴더 트리 로직·자동저장 seq-guard·소프트삭제·sanitize 파이프라인·서식 플러그인은 100% 유지, 신규 네비게이션 항목("전체 문서"/"미분류"/정렬·추가 버튼)은 구현하지 않는다(D-09 스코프 밖).

**결정 근거:** D-09(FolderTree 순수 스타일링, 필터·정렬 신규 기능은 Deferred). 09-01 의 전역 토큰을 소비한다.

**behavior-frozen 가정(spec-less 프로브 unresolved 6행 중 2행):** DOC-01/DOC-02 는 리스킨으로 바뀌지 않는다 — 회귀 e2e로 확정.

Purpose: 글쓰기 화면을 새 룩앤필로 재구성하되 에디터 핵심 동작을 절대 회귀시키지 않음.
Output: 리스킨된 사이드바·툴바·본문·상태바(전부 CSS-only).
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-design-system-application/09-CONTEXT.md
@.planning/phases/09-design-system-application/09-UI-SPEC.md
@.planning/phases/09-design-system-application/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 사이드바(폴더 트리 + 검색) 리스킨 — 순수 스타일링</name>
  <files>src/app/(main)/w/[wsId]/layout.tsx, src/components/tree/FolderTree.module.css, src/components/tree/FolderTreeNode.module.css, src/components/tree/SearchBox.module.css, src/components/tree/FolderContextMenu.module.css, src/components/tree/MoveFolderModal.module.css</files>
  <read_first>
    - src/components/tree/FolderTree.tsx (구조 파악 — 이 태스크는 .tsx 로직/DnD/컨텍스트 메뉴를 절대 수정하지 않는다, D-09)
    - src/components/tree/FolderTree.module.css, FolderTreeNode.module.css, SearchBox.module.css, FolderContextMenu.module.css, MoveFolderModal.module.css (하드코딩 색/반경/타이포 치환 대상)
    - src/app/(main)/w/[wsId]/layout.tsx (사이드바 셸 — 폭/래퍼 스타일)
    - .planning/phases/09-design-system-application/09-CONTEXT.md (D-09) + docs/images/write-form-wireframe.svg (사이드바 260px·검색창 위치 참고)
    - .planning/phases/09-design-system-application/09-UI-SPEC.md (Accent reserved: 활성 트리 노드 = accent)
  </read_first>
  <action>
    폴더 트리·검색·컨텍스트 메뉴·이동 모달의 CSS 를 전역 토큰으로 재작업한다(D-09 — 순수 스타일링, 로직 무변경). 하드코딩 색 → var(--surface/--surface-2/--text/--muted/--border), 하드코딩 반경 → var(--radius-sm)(노드/입력), var(--radius-md)(컨텍스트 메뉴/모달), transition → var(--duration-fast) var(--ease-fluid). 사이드바 폭 260px·검색창 상단 위치는 와이어프레임 참고. 현재 활성/선택된 폴더·문서 노드는 accent 로 강조(UI-SPEC Accent reserved). FolderTree.tsx 등 .tsx 파일은 이 태스크 file 목록에 없다 — 마크업/로직 변경 금지. "전체 문서"/"미분류"/정렬(↕)/추가(＋) 버튼은 추가하지 않는다(D-09).
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec playwright test e2e/folder-tree.spec.ts 2>/dev/null || pnpm exec playwright test --grep "folder|폴더|tree"</automated>
    grep 확인: 트리 CSS 토큰 유입 — `grep -Fl 'var(--radius-' src/components/tree/FolderTree.module.css src/components/tree/FolderTreeNode.module.css`; 로직 무변경 — `git diff --name-only` 에 src/components/tree/*.tsx 부재(스타일 파일만 변경).
  </verify>
  <precondition>로컬 Postgres(.env.local DATABASE_URL)가 기동되어 있어야 폴더 트리 playwright 회귀가 실행된다. playwright 는 단일 실행만.</precondition>
  <acceptance_criteria>
    - 트리/검색/컨텍스트메뉴/이동모달 CSS 가 하드코딩 대신 var(--token) 참조
    - src/components/tree/*.tsx 파일은 diff 없음(순수 스타일링, D-09)
    - 활성/선택 트리 노드가 accent 로 표시
    - "전체 문서"/"미분류"/정렬/추가 신규 항목 미추가
    - folder-tree e2e 회귀 green (생성/이름변경/이동/삭제/DnD)
    - pnpm vitest run 전체 green
  </acceptance_criteria>
  <done>사이드바가 새 토큰으로 렌더되고 트리/검색/컨텍스트메뉴/DnD 동작이 회귀 없이 green, .tsx 무수정.</done>
</task>

<task type="auto">
  <name>Task 2: 본문(제목행·툴바·보기모드·분할·상태바·프리뷰) 리스킨 — CSS-only</name>
  <files>src/components/document/DocumentWorkspace.module.css, src/components/document/SaveStatusBar.module.css, src/components/document/TagBar.module.css, src/components/editor/Toolbar.module.css, src/components/editor/HeadingDropdown.module.css, src/components/editor/EditorHost.module.css, src/components/layout/LayoutModeToggle.module.css, src/components/preview/PreviewPane.module.css, src/app/(main)/w/[wsId]/d/[docId]/page.module.css</files>
  <read_first>
    - src/components/document/DocumentWorkspace.tsx (구조 파악 — seq-guard/자동저장 로직 무접촉 확인, 이 태스크 file 목록에 .tsx 없음)
    - src/components/document/SaveStatusBar.module.css (저장 상태 배지 — "저장됨" accent 배경 대상)
    - DocumentWorkspace.module.css, TagBar.module.css, editor/Toolbar.module.css, HeadingDropdown.module.css, EditorHost.module.css, layout/LayoutModeToggle.module.css, preview/PreviewPane.module.css, d/[docId]/page.module.css (치환 대상)
    - .planning/phases/09-design-system-application/09-UI-SPEC.md (Visual Hierarchy "에디터 글쓰기 화면" 행 + Copywriting Contract "에디터 저장" 행 — EDIT-07 카피 불변)
    - docs/images/write-form-wireframe.svg (브레드크럼/제목/저장·삭제·툴바·보기모드 4버튼·분할·상태바 배치)
  </read_first>
  <action>
    상단 제목행·서식 툴바·보기모드 4버튼(LayoutModeToggle)·분할 에디터/프리뷰·상태바·태그바·프리뷰 프로즈의 시각을 전역 토큰으로 재작업(CSS-only, .tsx 무수정). 툴바는 무채색(var(--surface-2))으로 눌러 본문에서 시선을 뺏지 않고, 서식 아이콘은 lucide + pressed 상태 + 300ms 툴팁(기존 EDIT-10 규정, 값은 var(--duration-slow) 로 토큰명만 부여). SaveStatusBar 의 "저장됨" 배지는 accent 배경(EDIT-07 카피 "저장 중…"·"저장됨"·"저장 실패" 문자열 불변). 반경은 var(--radius-sm/md), 페이지 셸 프레임은 var(--radius-lg). transition 은 var(--duration-*) + var(--ease-*). PreviewPane 은 시각 토큰만 — sanitize/렌더 경로·--code-bg 코드 블록 배경 계약 유지. 자동저장(seq-guard)·저장 버튼·미리보기·태그 로직은 절대 건드리지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm exec playwright test e2e/document-workspace.spec.ts e2e/document-trash.spec.ts 2>/dev/null || pnpm exec playwright test --grep "document|문서|trash|휴지통"</automated>
    grep 확인: 본문 CSS 토큰 유입 — `grep -Fl 'var(--radius-' src/components/document/DocumentWorkspace.module.css src/components/editor/Toolbar.module.css`; 로직 무변경 — `git diff --name-only` 에 src/components/document/*.tsx / src/components/editor/*.tsx 부재; EDIT-07 카피 불변 — `grep -F '저장됨' src/components/document/SaveStatusBar.tsx`(문자열 소스 유지 확인, tsx 무수정이므로 자동 충족).
  </verify>
  <precondition>로컬 Postgres(.env.local DATABASE_URL)가 기동되어 있어야 문서/휴지통 playwright 회귀가 실행된다. playwright 는 단일 실행만.</precondition>
  <acceptance_criteria>
    - 제목행/툴바/보기모드/분할/상태바/태그바/프리뷰 CSS 가 var(--token) 참조로 재작업
    - src/components/document/*.tsx, src/components/editor/*.tsx, src/components/preview/*.tsx 는 diff 없음(CSS-only)
    - "저장됨" 배지 accent 배경, 툴바 무채색(surface-2)
    - EDIT-07 저장 상태 카피 문자열 불변
    - DOC-01(문서 CRUD) + DOC-02(휴지통 복원/완전삭제) + 자동저장 e2e 회귀 green
    - pnpm vitest run 전체 green
  </acceptance_criteria>
  <done>에디터 화면이 write-form 구성 + 새 토큰으로 렌더되고 서식/자동저장/저장/미리보기/휴지통 동작이 회귀 없이 green, .tsx 무수정.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 본문 → PreviewPane 렌더 | 마크다운이 rehype-sanitize 를 거쳐 렌더된다. 이번 phase는 PreviewPane CSS 만 바꾸고 렌더 경로는 무접촉 |
| 브라우저 → 자동저장/삭제 API | seq-guard/soft-delete 서버 계약. 이번 phase 무접촉 |

## STRIDE Threat Register

ASVS L1, block on high. **이번 phase는 패키지 설치가 없다** → 패키지 정당성 게이트(T-*-SC) 해당 없음.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03-XSS | Tampering(XSS) | PreviewPane 리스킨 | medium | mitigate | PreviewPane 은 module.css 만 변경(file 목록에 .tsx 없음) — rehype-sanitize 렌더 경로·dangerouslySetInnerHTML 계약 무접촉. sanitize 없는 렌더링 금지 불변식 유지. git diff 게이트로 preview/*.tsx 무변경 검증 |
| T-09-03-BEHAVIOR | Tampering | 자동저장 seq-guard / soft-delete / closure-table | high | mitigate | 전 태스크가 CSS/스타일 파일만 수정(.tsx file 목록 제외) — 상태 로직에 닿지 않음. git diff 게이트로 document/*.tsx·editor/*.tsx·tree/*.tsx 무변경을 검증하고, DOC-01/DOC-02·자동저장 e2e 회귀로 동작 불변 확인 |
| T-09-03-DOS | Denial of Service | 툴팁/모션 토큰 적용 | low | accept | 순수 CSS transition/duration 토큰 — 신규 실행 경로 없음 |
</threat_model>

<verification>
- pnpm vitest run 전체 green
- 문서 CRUD·휴지통·자동저장·폴더 트리 playwright e2e green (DOC-01/DOC-02·EDIT-07 회귀 없음)
- git diff 로 src/components/{tree,document,editor,preview}/*.tsx 무변경 확인(순수 스타일링)
</verification>

<success_criteria>
- 에디터 글쓰기 화면이 write-form-wireframe 구성대로 재구성되고 기존 에디터 기능(서식/자동저장/저장/미리보기)이 회귀 없이 동작한다 (ROADMAP Phase 9 성공기준 3)
</success_criteria>

<output>
Create `.planning/phases/09-design-system-application/09-03-SUMMARY.md` when done
</output>
