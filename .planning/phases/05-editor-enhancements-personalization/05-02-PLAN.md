---
phase: 05-editor-enhancements-personalization
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/editor/ImageDropzone.tsx
  - src/components/editor/ImageDropzone.module.css
  - src/components/editor/UploadErrorBanner.tsx
  - src/components/editor/UploadErrorBanner.module.css
  - src/components/editor/useImageUpload.ts
  - src/components/layout/EditorPreviewLayout.tsx
  - src/components/layout/EditorPreviewLayout.module.css
autonomous: true
requirements: [EDIT-09]
user_setup: []
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "파일을 에디터 페인 위로 드래그하면 드롭존 오버레이(2px dashed accent + Upload 아이콘 + 안내)가 나타나고, 드롭 시 커서 위치 삽입 업로드 플로우가 시작된다 (EDIT-09)"
    - "업로드 실패(크기/형식/네트워크) 시 placeholder가 제거되고 에러 배너가 정확한 스펙 문구로 표시된다"
    - "에러 배너는 자동 사라짐 없이 사용자가 닫거나 다음 업로드가 내용을 교체할 때까지 유지된다 (Phase 4 배너 관례)"
  artifacts:
    - src/components/editor/ImageDropzone.tsx
    - src/components/editor/UploadErrorBanner.tsx
    - src/components/editor/useImageUpload.ts (에러 상태 확장)
  key_links:
    - "ImageDropzone dragover/drop → useImageUpload(같은 훅) → 05-01의 getView dispatch 경로 재사용"
    - "useImageUpload 에러 union → UploadErrorBanner 문구 매핑(size/type/network 3종)"
---

<objective>
05-01 tracer가 증명한 업로드 오케스트레이션 위에 UX 확장을 얹는다: (1) 에디터 페인 드래그드롭 진입점(드롭존 오버레이) — 툴바 버튼과 같은 `useImageUpload` 플로우를 재사용, (2) 실패 3종(크기/형식/네트워크)에 대한 에러 배너. tracer가 이미 getView-dispatch 경로와 placeholder 삽입/치환을 확립했으므로 이 plan은 진입점 하나와 에러 표면 하나를 추가할 뿐이다.

Purpose: EDIT-09의 나머지 UX 상태(partial=드래그오버, error 3종)를 채운다.
Output: ImageDropzone + UploadErrorBanner + useImageUpload 에러 상태.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-UI-SPEC.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@src/components/editor/useImageUpload.ts
@src/components/layout/EditorPreviewLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImageDropzone 오버레이 + 드롭 → 업로드 플로우</name>
  <files>src/components/editor/ImageDropzone.tsx, src/components/editor/ImageDropzone.module.css, src/components/layout/EditorPreviewLayout.tsx, src/components/layout/EditorPreviewLayout.module.css</files>
  <read_first>
    - 05-UI-SPEC.md "Image Upload Contract"(149-164행) — 트리거 2(드래그 드롭), hover-drop 오버레이 시각 계약(배경 var(--surface), 2px dashed var(--accent), 중앙 Upload 24px + 텍스트)
    - 05-UI-SPEC.md "Layout & Composition Contract"(40행) — 드롭존은 .editorPane 내부 전체 덮는 position:absolute, 드래그 중만 조건부 렌더
    - 05-UI-SPEC.md Copywriting(229행) — "여기에 이미지를 놓아 업로드"
    - src/components/layout/EditorPreviewLayout.tsx(05-01 수정본) — .editorPane, useImageUpload 마운트 지점
    - 05-RESEARCH.md Pattern 2(리터럴 placeholder) — 드롭도 현재 캐럿 위치 삽입(드롭 좌표를 오프셋으로 변환하지 않음, UI-SPEC 156행)
  </read_first>
  <action>
    src/components/editor/ImageDropzone.tsx를 만든다: `.editorPane`을 덮는 오버레이(position:absolute, inset 0), 드래그 중일 때만 조건부 렌더(DOM에서 완전 제거 — display 토글 아님, 리플로우 비용 0). 배경 var(--surface) 불투명, 테두리 2px dashed var(--accent), 중앙에 lucide `Upload` 24px var(--accent) + "여기에 이미지를 놓아 업로드"(UI-SPEC Copywriting).
    EditorPreviewLayout.tsx: `.editorPane`에 dragenter/dragover/dragleave 핸들러를 걸어 "드래그 중" 로컬 상태를 관리하고(파일 드래그만 — dataTransfer.types에 "Files" 포함 시), 그 상태가 true일 때 ImageDropzone을 렌더한다. drop 핸들러가 `event.dataTransfer.files[0]`을 05-01의 useImageUpload가 노출하는 업로드 함수에 넘긴다(툴바 버튼과 동일 경로 — 새 fetch/placeholder 로직 만들지 않음). 드롭 시 현재 캐럿 위치에 삽입(좌표→오프셋 매핑 없음). dragleave/drop 시 "드래그 중" 상태 해제. EditorPreviewLayout.module.css에 .editorPane의 position:relative(오버레이 기준점)만 없으면 추가.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/components/editor/ImageDropzone.tsx','utf8'); if(!/여기에 이미지를 놓아 업로드/.test(c)) throw new Error('missing copy');"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 드래그오버 시 ImageDropzone이 조건부 렌더되고 드롭 시 useImageUpload의 업로드 함수(05-01)가 호출됨
    - 드롭존은 드래그 중이 아닐 때 DOM에 존재하지 않는다(조건부 렌더)
    - 드롭이 05-01의 placeholder 삽입/치환 경로를 재사용(새 fetch 로직 없음)
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>드래그드롭이 두 번째 업로드 진입점으로 동작하고 툴바 버튼과 같은 삽입 플로우를 탄다.</done>
</task>

<task type="auto">
  <name>Task 2: UploadErrorBanner + useImageUpload 에러 상태(3종)</name>
  <files>src/components/editor/UploadErrorBanner.tsx, src/components/editor/UploadErrorBanner.module.css, src/components/editor/useImageUpload.ts, src/components/layout/EditorPreviewLayout.tsx</files>
  <read_first>
    - 05-UI-SPEC.md "Image Upload Contract"(161-164행) — 에러 3종 + 에러 배너 스타일(배경 var(--surface-2), 좌측 4px var(--border-strong), padding 12px 16px, AlertCircle 16px var(--destructive) + 텍스트 + X 닫기)
    - 05-UI-SPEC.md Copywriting(230-232행) — 크기/형식/일반 실패 3문구
    - 05-UI-SPEC.md Layout(41행) — 배너는 툴바 아래 에디터 페인 상단 position:absolute(문서 높이 안 밀어냄)
    - src/components/editor/useImageUpload.ts(05-01) — 업로드 실패 시 placeholder 제거 지점
    - src/components/document/SaveStatusBar.tsx:35-37 — 재시도/닫기 버튼 관례
  </read_first>
  <action>
    useImageUpload.ts를 확장해 실패 원인별 에러 상태를 노출한다: 서버 400의 error 문구(TOO_LARGE→"이미지 크기는 5MB를 넘을 수 없어요.", BAD_TYPE→"PNG, JPEG, GIF, WEBP 형식만 업로드할 수 있어요.")를 그대로 쓰거나, 네트워크/기타 실패는 "이미지를 업로드하지 못했어요. 다시 시도해 주세요."(공통 톤, Phase 4 재사용). 실패 시 placeholder를 제거하고 errorMessage 상태를 set한다. dismiss/다음 업로드 시 null로 리셋.
    src/components/editor/UploadErrorBanner.tsx: errorMessage가 있을 때만 렌더, UI-SPEC 스타일(배경 var(--surface-2), 좌측 4px var(--border-strong), padding 12px 16px, lucide AlertCircle 16px var(--destructive) + 텍스트 var(--destructive) Body 13/400 + 우측 lucide X 16px var(--muted) 닫기 버튼). 자동 타이머 없음. EditorPreviewLayout이 툴바 아래 에디터 페인 상단에 position:absolute로 얹는다(문서 높이 안 밀어냄).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/components/editor/UploadErrorBanner.tsx','utf8'); if(!/AlertCircle/.test(c)) throw new Error('missing icon');"</automated>
  </verify>
  <acceptance_criteria>
    - 크기 초과/형식 불일치/네트워크 실패 3종이 각각 정확한 UI-SPEC 문구로 배너에 표시됨
    - 실패 시 placeholder가 에디터에서 제거됨
    - 배너에 X 닫기 버튼이 있고 자동 사라짐 타이머가 없다
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>업로드 실패 3종이 스펙 문구로 배너에 뜨고 placeholder는 깨끗이 제거된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client drag-drop → useImageUpload → POST /api/uploads | 05-01과 동일 신뢰 경계(서버 매직바이트·크기·RBAC가 진짜 게이트). 이 plan은 진입점/에러 표면만 추가 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-02-BYPASS | Elevation of Privilege | 드래그드롭 진입점 | high | mitigate | 드롭도 05-01의 POST /api/uploads(requireRole EDITOR + 매직바이트)를 그대로 탄다 — 클라 진입점이 늘어도 서버 검증은 단일. UI 진입점은 보안 경계가 아님(CLAUDE.md) |
| T-05-02-ERRLEAK | Information Disclosure | 에러 배너 문구 | low | accept | 배너 문구는 고정 스펙 문구 3종만 — 서버 내부 스택/경로를 노출하지 않음 |

*신규 패키지 설치 없음 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` clean
- grep 게이트(드롭존 카피·AlertCircle) 통과
- 수동(Manual-Only, VALIDATION): 큰 이미지 업로드 시 placeholder→실제 이미지 전환, 드래그 리사이즈와 무관하게 드롭존 표시
</verification>

<success_criteria>
드래그드롭 업로드와 실패 배너 3종이 05-01의 서버 검증 경로를 재사용해 동작하고, 에러 상태에서 placeholder가 깨끗이 회수된다.
</success_criteria>

## Artifacts this phase produces (05-02)

- `src/components/editor/ImageDropzone.tsx` + `.module.css` — 드래그드롭 오버레이
- `src/components/editor/UploadErrorBanner.tsx` + `.module.css` — 실패 3종 배너
- `src/components/editor/useImageUpload.ts` — 에러 상태 확장
- `src/components/layout/EditorPreviewLayout.tsx` + `.module.css` — 드롭존/배너 오버레이 슬롯

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-02-SUMMARY.md` when done
</output>
