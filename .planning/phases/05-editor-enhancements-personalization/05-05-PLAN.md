---
phase: 05-editor-enhancements-personalization
plan: 05
type: execute
wave: 4
depends_on: [05-01, 05-04, 05-08]
files_modified:
  - src/lib/documents.ts
  - src/app/(main)/w/[wsId]/d/[docId]/page.tsx
  - src/components/document/DraftRecoveryDialog.tsx
  - src/components/document/DraftRecoveryDialog.module.css
  - src/components/document/DocumentWorkspace.tsx
  - tests/draft/draft-comparison.test.ts
autonomous: true
requirements: [EDIT-11]
user_setup: []
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "문서 진입 시 RSC가 getDocument+getDraft를 같은 요청에서 조회해 draft.updatedAt > document.updatedAt일 때만 hasNewerDraft=true + draftContent를 prop으로 내려준다 (EDIT-11 / Pitfall 7)"
    - "hasNewerDraft면 복구 다이얼로그가 자동 open되고, 복원/폐기/나중에 3선택지를 준다 (UI-SPEC Draft Recovery Dialog)"
    - "복원 클릭 시 draft 내용이 getView().dispatch로 에디터에 적재되고, 그 dispatch가 기존 자동저장을 트리거해 정식 저장되며 성공 시 서버가 draft를 삭제한다(연쇄) (CONTEXT 잠금)"
    - "60초 draft 컨트롤러가 DocumentWorkspace 입력에 연결돼 입력이 있을 때 draft가 upsert된다"
    - "DocumentWorkspace가 CodeMirror/EditorView를 직접 import하지 않고 EditorPreviewLayout의 getView 핸들로만 접근한다 (계층 보존, Pitfall 6)"
  artifacts:
    - src/lib/documents.ts (isDraftNewer 순수 함수)
    - src/app/(main)/w/[wsId]/d/[docId]/page.tsx (getDraft + hasNewerDraft/draftContent)
    - src/components/document/DraftRecoveryDialog.tsx
    - src/components/document/DocumentWorkspace.tsx (useDraft + 복구 배선)
    - tests/draft/draft-comparison.test.ts
  key_links:
    - "RSC getDocument+getDraft(Promise.all) → isDraftNewer → hasNewerDraft/draftContent props → DraftRecoveryDialog"
    - "복원 → EditorPreviewLayoutHandle.getView().dispatch(from:0,to:len,insert:draftContent) → updateListener → scheduleSave(기존 자동저장) → autosave 성공 → 서버 deleteDraft(05-04 연쇄)"
    - "DocumentWorkspace handleContentChange → useDraft.onContentChange(60초 컨트롤러) + scheduleSave(자동저장) 둘 다 연결"
---

<objective>
EDIT-11의 마지막 조각: 복구 다이얼로그 + RSC 비교 + uncontrolled 에디터에 draft 적재를 배선한다. 문서 진입 시 RSC가 `getDocument`+`getDraft`를 같은 요청에서 병렬 조회해 `draft.updatedAt > document.updatedAt`을 서버에서 계산(Pitfall 7, 클라 시계차 노출 금지)하고, 최신 draft가 있으면 `ConfirmDialog` 확장 다이얼로그가 자동 open된다. "복원"은 05-01이 노출한 `getView()`에 `dispatch` 한 번을 쏘는 것으로 끝나고(그 dispatch가 기존 자동저장을 트리거 → 05-04의 성공-게이트 삭제 연쇄가 draft를 지움), 60초 draft 컨트롤러(05-04)를 DocumentWorkspace 입력에 연결한다.

Purpose: draft 크래시 복구를 사용자에게 실제로 노출하는 최종 배선. 05-01(getView)·05-04(useDraft/라우트)·05-08(DocumentWorkspace/d-page 형태)에 의존.
Output: isDraftNewer + RSC 비교 + DraftRecoveryDialog + DocumentWorkspace 복구 배선.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@.planning/phases/05-editor-enhancements-personalization/05-PATTERNS.md
@.planning/phases/05-editor-enhancements-personalization/05-UI-SPEC.md
@src/components/document/DocumentWorkspace.tsx
@src/components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isDraftNewer 순수 함수 + RSC 병렬 조회·비교</name>
  <files>src/lib/documents.ts, src/app/(main)/w/[wsId]/d/[docId]/page.tsx, tests/draft/draft-comparison.test.ts</files>
  <read_first>
    - src/lib/documents.ts:22-28(getDocument), 05-03의 getDraft
    - src/app/(main)/w/[wsId]/d/[docId]/page.tsx(05-08 수정본) — getDocument 후 DocumentWorkspace 렌더, 이미 cookies() 읽음
    - 05-RESEARCH.md Pitfall 7(385-389행) — 비교는 서버에서 끝내 hasNewerDraft/draftContent만 클라로(클라 시계차 금지)
    - 05-UI-SPEC.md "Draft Recovery Dialog Contract"(214행) — RSC가 draft.updated_at > document.updated_at을 이미 계산해 prop
  </read_first>
  <behavior>
    - isDraftNewer(null, doc) → false (draft 없음)
    - isDraftNewer({updatedAt: T+1}, {updatedAt: T}) → true
    - isDraftNewer({updatedAt: T}, {updatedAt: T}) → false (동일은 최신 아님)
    - isDraftNewer({updatedAt: T-1}, {updatedAt: T}) → false
  </behavior>
  <action>
    먼저 tests/draft/draft-comparison.test.ts를 RED로 커밋(TDD): `isDraftNewer(draft, doc)` 순수 함수(draft가 있고 draft.updatedAt > doc.updatedAt일 때만 true)를 위 4케이스로 검증.
    src/lib/documents.ts에 `isDraftNewer(draft: { updatedAt: Date } | null, doc: { updatedAt: Date }): boolean`를 추가한다(순수 비교, 신뢰 경계 안 두 서버 타임스탬프).
    d/[docId]/page.tsx(RSC): 기존 getDocument 호출을 `const [doc, draft] = await Promise.all([getDocument(docId, wsId), getDraft(docId)])`로 바꾸고(같은 요청 컨텍스트), `const hasNewerDraft = isDraftNewer(draft, doc)`를 계산해 DocumentWorkspace에 `hasNewerDraft`(boolean)와 `draftContent`(hasNewerDraft일 때만 draft.content, 아니면 null) prop을 추가로 내려준다. document/draft 비교를 클라로 새어나가게 하지 않는다(두 raw 타임스탬프를 각각 클라에 보내 클라가 비교하는 코드 금지).
  </action>
  <verify>
    <automated>pnpm vitest run tests/draft/draft-comparison.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - isDraftNewer가 4케이스(없음/최신/동일/구버전)를 올바르게 판정
    - RSC가 getDocument+getDraft를 Promise.all로 조회하고 hasNewerDraft/draftContent만 prop으로 내려준다
    - 클라 컴포넌트에 두 raw 타임스탬프 비교 코드가 없다
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>RSC가 서버에서 최신 draft 여부를 계산해 hasNewerDraft/draftContent만 안전하게 내려준다.</done>
</task>

<task type="auto">
  <name>Task 2: DraftRecoveryDialog (ConfirmDialog 확장 + 폐기 슬롯)</name>
  <files>src/components/document/DraftRecoveryDialog.tsx, src/components/document/DraftRecoveryDialog.module.css</files>
  <read_first>
    - src/components/ui/ConfirmDialog.tsx(전문) — open/title/cancelLabel/confirmLabel/onCancel/onConfirm/children 슬롯 + destructive prop
    - 05-UI-SPEC.md "Draft Recovery Dialog Contract"(208-220행) — 3선택지(복원/폐기/나중에), children에 폐기 인라인 버튼, confirmLabel="복원"(primary), cancelLabel="나중에"
    - 05-UI-SPEC.md Copywriting(233-236행) — 제목/본문/폐기/나중에 문구
    - 05-PATTERNS.md "DraftRecoveryDialog.tsx"(221-239행) — ConfirmDialog 직접 사용 + children 폐기 버튼 완성 스케치, destructive 미사용
    - src/components/document/SaveStatusBar.tsx:35-37 — "재시도" destructive 텍스트 버튼 스타일(폐기 버튼 이식 원본)
  </read_first>
  <action>
    src/components/document/DraftRecoveryDialog.tsx를 PATTERNS 스케치(221-239행)대로 만든다: `ConfirmDialog`를 직접 사용(새 셸 만들지 않음), props `open`(=hasNewerDraft), `onRestore`(=onConfirm), `onDismiss`(=onCancel, "나중에"), `onDiscard`(폐기). title "임시 저장된 내용이 있어요", 본문(children) "마지막 저장 이후 자동으로 임시 저장된 더 최신 내용이 있어요. 복원할까요?" + 그 아래 "폐기" 인라인 버튼(SaveStatusBar 재시도 버튼 스타일: 배경 없음, color var(--destructive), weight 600, hover underline, padding 0). confirmLabel="복원"(destructive prop 미사용 — 복원이 primary/accent), cancelLabel="나중에". 폐기 버튼 클릭은 onDiscard 호출.
    DraftRecoveryDialog.module.css에 .discard 버튼 스타일만(나머지는 ConfirmDialog 재사용).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/components/document/DraftRecoveryDialog.tsx','utf8'); if(!/ConfirmDialog/.test(c)) throw new Error('must reuse ConfirmDialog'); if(!/임시 저장된 내용이 있어요/.test(c)) throw new Error('missing title copy');"</automated>
  </verify>
  <acceptance_criteria>
    - ConfirmDialog를 재사용하고 children 슬롯에 "폐기" 버튼을 얹어 3선택지(복원/폐기/나중에)를 제공
    - 복원은 primary(accent), destructive prop 미사용
    - 문구가 UI-SPEC Copywriting과 정확히 일치
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>복구 다이얼로그가 ConfirmDialog 확장으로 3선택지를 제공한다.</done>
</task>

<task type="auto">
  <name>Task 3: DocumentWorkspace 복구 배선 (useDraft + getView 적재 + 폐기)</name>
  <files>src/components/document/DocumentWorkspace.tsx</files>
  <read_first>
    - src/components/document/DocumentWorkspace.tsx(05-08 수정본) — handleContentChange/handleTitleChange, EditorPreviewLayout 렌더(layoutMode/splitRatio props)
    - src/components/layout/EditorPreviewLayout.tsx(05-01) — EditorPreviewLayoutHandle(getView) forwardRef
    - src/components/document/useDraft.ts(05-04) — onContentChange
    - 05-RESEARCH.md Pattern 5(277-281행) — 복원=getView().dispatch(from:0,to:len,insert:draftContent) 한 번, 별도 강제저장 불필요
    - 05-RESEARCH.md Pitfall 6(379-383행) — DocumentWorkspace는 CodeMirror import 금지, EditorPreviewLayout 핸들로만 접근
    - 05-UI-SPEC.md "Draft Recovery Dialog Contract"(214-220행) — 복원/폐기/나중에 각 동작
  </read_first>
  <action>
    DocumentWorkspace.tsx에 새 props `hasNewerDraft: boolean`, `draftContent: string | null`을 추가하고, `docId`에서 wsId는 필요 없다(draft 라우트는 docId 기반). EditorPreviewLayout에 `ref`(useRef<EditorPreviewLayoutHandle>)를 붙여 getView 핸들을 얻는다(CodeMirror/EditorView를 직접 import하지 않는다 — Pitfall 6, 핸들 경유만).
    useDraft(docId)를 마운트해 `handleContentChange`가 `scheduleSave`(자동저장)와 `draft.onContentChange`(60초 컨트롤러)를 **둘 다** 호출하게 배선한다.
    hasNewerDraft가 true면 DraftRecoveryDialog를 open. onRestore: `layoutRef.current?.getView()`의 view에 `view.dispatch({ changes: { from: 0, to: view.state.doc.length, insert: draftContent } })` 한 번(Pattern 5) → 다이얼로그 닫기. 이 dispatch가 EditorHost updateListener → onChange → handleContentChange → scheduleSave를 자연 트리거하고, 그 자동저장 성공이 05-04의 라우트 게이트로 draft를 지운다(별도 삭제 호출 없음). onDiscard: `fetch(`/api/documents/${docId}/draft`, { method: "DELETE" })` → 다이얼로그 닫기(에디터는 현재 document.content 유지). onDismiss("나중에"): 다이얼로그만 닫기(draft 유지, 다음 진입 시 조건 참이면 재표시).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/components/document/DocumentWorkspace.tsx','utf8'); if(/@codemirror\/(view|state)/.test(c)) throw new Error('DocumentWorkspace must not import CodeMirror directly (Pitfall 6)'); if(!/DraftRecoveryDialog/.test(c)) throw new Error('dialog not wired');"</automated>
  </verify>
  <acceptance_criteria>
    - hasNewerDraft면 복구 다이얼로그가 마운트 시 자동 open
    - 복원은 EditorPreviewLayout 핸들의 getView().dispatch 한 번으로 draft를 적재하고 별도 강제저장/삭제 호출을 하지 않는다(자동저장 연쇄에 위임)
    - 폐기는 DELETE /api/documents/:id/draft, 나중에는 닫기만
    - handleContentChange가 자동저장과 draft 60초 컨트롤러를 둘 다 트리거
    - DocumentWorkspace가 @codemirror/view·@codemirror/state를 직접 import하지 않는다(계층 보존)
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>복구 다이얼로그의 복원 한 번이 draft를 에디터에 적재하고 기존 자동저장 연쇄로 정식 저장·draft 삭제까지 이어진다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC 서버 계산 → 클라 prop | draft/document 비교는 서버가 끝내 hasNewerDraft/draftContent만 내려줌(두 raw 타임스탬프를 클라에 노출하지 않음) |
| client 폐기 → DELETE /api/documents/:id/draft | 05-04 라우트의 IDOR/RBAC 경계가 진짜 게이트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05-CLOCK | Tampering | draft 최신 비교 | medium | mitigate | 비교를 RSC가 같은 요청의 두 서버 타임스탬프로 계산(Pitfall 7) — 클라 시계차/네트워크 지연이 복구 판정을 흔들지 못함 |
| T-05-05-LAYER | (Tampering) | DocumentWorkspace 계층 | low | mitigate | DocumentWorkspace가 EditorView를 직접 import하지 않고 getView 핸들로만 접근 — 계층 위반을 grep 게이트가 차단(Pitfall 6) |
| T-05-05-DISCARD | Elevation of Privilege | 폐기 DELETE | high | mitigate | 폐기는 05-04의 draft DELETE 라우트(requireRole EDITOR + IDOR)를 탄다 — 타 워크스페이스 draft 삭제 불가 |

*신규 패키지 설치 없음 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm vitest run tests/draft/draft-comparison.test.ts` — isDraftNewer green
- `pnpm vitest run tests/draft/ tests/upload/ tests/theme/ tests/layout/` — phase 전체 신규 스위트 green
- `pnpm exec tsc --noEmit` clean
- DocumentWorkspace CodeMirror 직접 import 부재 grep 게이트 통과
- 수동(Manual-Only): 문서 편집 → 60초 후 draft upsert → 새로고침 시 복구 다이얼로그 → 복원/폐기/나중에 각 동작
</verification>

<success_criteria>
1분 스냅샷이 저장되고, 재진입 시 임시본이 최신이면 복구 다이얼로그가 3선택지로 뜨며, 복원 한 번이 draft를 에디터에 적재해 정식 저장·draft 삭제까지 연쇄된다(EDIT-11 완결).
</success_criteria>

## Artifacts this phase produces (05-05)

- `src/lib/documents.ts` — isDraftNewer 순수 비교 함수
- `src/app/(main)/w/[wsId]/d/[docId]/page.tsx` — getDraft 병렬 조회 + hasNewerDraft/draftContent
- `src/components/document/DraftRecoveryDialog.tsx` + `.module.css` — ConfirmDialog 확장 3선택지
- `src/components/document/DocumentWorkspace.tsx` — useDraft + getView 적재 + 폐기 배선
- `tests/draft/draft-comparison.test.ts`

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-05-SUMMARY.md` when done
</output>
