---
phase: 05-editor-enhancements-personalization
plan: 04
type: execute
wave: 2
depends_on: [05-03]
files_modified:
  - src/app/api/documents/[id]/draft/route.ts
  - src/app/api/documents/[id]/route.ts
  - src/components/document/draft-controller.ts
  - src/components/document/useDraft.ts
  - tests/draft/draft-controller.test.ts
  - tests/draft/draft-autodelete.test.ts
autonomous: true
requirements: [EDIT-11]
user_setup: []
estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PUT /api/documents/:id/draft가 IDOR/RBAC(workspace 역산 + EDITOR)를 통과한 요청만 upsert하고, DELETE가 draft를 폐기한다 (EDIT-11)"
    - "draft 컨트롤러가 마지막 upsert 후 입력이 있었을 때만 60초마다 upsert하고, 입력 없는 60초 경과에는 발사하지 않는다 (TRD §7, 더티 플래그 + 주기)"
    - "자동저장이 실제로 반영됐을 때만(autosaveDocument===true) draft가 삭제되고, stale seq(0행, 200 반환)에서는 draft가 살아남는다 (Pitfall 5 — 복구 데이터 손실 방지)"
  artifacts:
    - src/app/api/documents/[id]/draft/route.ts (PUT/DELETE)
    - src/components/document/draft-controller.ts (순수 60초 컨트롤러)
    - src/components/document/useDraft.ts (얇은 훅)
    - tests/draft/draft-controller.test.ts (fake timers)
    - tests/draft/draft-autodelete.test.ts (stale seq 회귀)
  key_links:
    - "PUT /api/documents/:id → if(await autosaveDocument(...)) await deleteDraft(id) → 자동저장 성공 연쇄가 draft를 지움(복원 버튼이 별도로 지울 필요 없음, TRD §7)"
    - "draft-controller setInterval(더티 플래그) → PUT /api/documents/:id/draft upsert(순서 가드 없음, draft 전용)"
---

<objective>
draft 크래시 복구(EDIT-11)의 네트워크·타이밍 계층을 만든다: (1) `PUT/DELETE /api/documents/:id/draft` 라우트(기존 documents 라우트의 IDOR/RBAC 4단 구조 복제), (2) TRD §7 "정식 저장 성공 시 draft 삭제"를 자동저장 라우트에 `autosaveDocument`의 boolean 반환값으로 게이트(Pitfall 5 — stale seq에서 draft를 지우면 복구 데이터 손실), (3) autosave-controller를 본뜬 순수 60초 draft 컨트롤러 + 얇은 훅. 정확성 핵심(stale seq에서 draft 생존, 입력 있을 때만 60초 발사)은 실패 테스트를 먼저 커밋한다(TDD).

Purpose: 05-05 복구 UI가 얹힐 라우트·컨트롤러. 05-03(스키마·서비스)에 의존.
Output: draft 라우트 + 자동저장 삭제 게이팅 + draft-controller/useDraft.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@.planning/phases/05-editor-enhancements-personalization/05-PATTERNS.md
@src/app/api/documents/[id]/route.ts
@src/components/document/autosave-controller.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: PUT/DELETE /api/documents/:id/draft 라우트</name>
  <files>src/app/api/documents/[id]/draft/route.ts</files>
  <read_first>
    - src/app/api/documents/[id]/route.ts(전문) — uuid 검증 → resolveWorkspaceIdForDocument(IDOR) → requireRole(EDITOR) → zod safeParse → 서비스 호출 4단 구조 + runtime="nodejs"
    - 05-PATTERNS.md "draft/route.ts (PUT/DELETE)"(93-114행) — 4단 구조 복제, GET 미생성 이유
    - src/lib/documents.ts:36-46(resolveWorkspaceIdForDocument), 05-03의 upsertDraft/deleteDraft
    - src/lib/validation.ts draftBodySchema(05-03 추가)
  </read_first>
  <action>
    src/app/api/documents/[id]/draft/route.ts에 PUT/DELETE를 documents/[id]/route.ts 관례로 작성한다. `export const runtime = "nodejs";`. PUT: `await context.params`로 id → `z.uuid().safeParse(id)` 실패 시 400 → `resolveWorkspaceIdForDocument(id)`(활성 문서만, 없으면 forbiddenResponse) → `requireRole(target.workspaceId, "EDITOR")`(ForbiddenError→forbiddenResponse) → `draftBodySchema.safeParse(await req.json())` 실패 시 400(첫 메시지) → `upsertDraft(id, parsed.data.content)` → `Response.json({}, { status: 200 })`. DELETE: 같은 4단 앞부분(uuid→IDOR→RBAC) 후 body 없이 `deleteDraft(id)` → 200(또는 204). GET은 만들지 않는다(복구 비교는 RSC가 getDraft 직접 호출 — Pitfall 7, documents/[id]/route.ts "GET deliberately absent" 관례).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/app/api/documents/[id]/draft/route.ts','utf8'); if(!/runtime = .nodejs./.test(c)) throw new Error('no nodejs runtime'); if(!/requireRole/.test(c)) throw new Error('no rbac'); if(/export async function GET/.test(c)) throw new Error('GET must be absent');"</automated>
  </verify>
  <acceptance_criteria>
    - PUT/DELETE가 uuid 검증 → workspace 서버 역산 → requireRole(EDITOR) → 서비스 호출 순서를 지킨다
    - GET 핸들러가 존재하지 않는다(RSC가 getDraft 직접 호출)
    - 존재하지 않는/타 워크스페이스/삭제된 문서 id는 모두 403(존재 여부 미노출, 03-04 IDOR 관례)
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>draft PUT/DELETE 라우트가 기존 문서 라우트와 동일한 IDOR/RBAC 경계를 통과한 요청만 처리한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 자동저장 성공에 게이트한 draft 삭제 (Pitfall 5 회귀 테스트)</name>
  <files>src/app/api/documents/[id]/route.ts, tests/draft/draft-autodelete.test.ts</files>
  <read_first>
    - src/app/api/documents/[id]/route.ts:45-49(PUT 핸들러 끝 — autosaveDocument 호출 후 200 반환)
    - src/lib/documents.ts:110-123(autosaveDocument 반환값 rows.length===1 — 실제 반영 시에만 true)
    - 05-RESEARCH.md Pitfall 5(538-546행) — if(saved) deleteDraft 완성 코드 + stale seq에서 200이지만 0행인 함정
    - 05-PATTERNS.md "draft 삭제를 자동저장 성공에 게이트"(144행)
  </read_first>
  <behavior>
    - autosaveDocument가 true(실제 반영) → deleteDraft(id) 호출됨 → draft 사라짐
    - autosaveDocument가 false(stale/tied seq, 0행이지만 라우트는 여전히 200) → deleteDraft 호출 안 됨 → draft 살아남음(복구 데이터 보존)
  </behavior>
  <action>
    먼저 tests/draft/draft-autodelete.test.ts를 RED로 커밋한다(TDD). 실 DB(5433)에서 문서 + draft를 시드하고, (a) 최신 seq로 PUT 자동저장 → draft 삭제됨을 assert, (b) stale(더 낮은) seq로 PUT → autosaveDocument가 0행이라 draft가 그대로 남음을 assert(Pitfall 5 회귀 방지 — 이게 이 태스크의 핵심). 라우트 레벨 검증이 어려우면 autosaveDocument+deleteDraft를 게이트하는 얇은 헬퍼 또는 라우트 핸들러를 직접 호출하는 통합 테스트로 구성한다.
    그 다음 src/app/api/documents/[id]/route.ts PUT 핸들러 끝을 RESEARCH Pitfall 5 코드(538-546행)로 수정: `const saved = await autosaveDocument(id, content, title, seq);` → `if (saved) { await deleteDraft(id); }` → `Response.json({ seq }, { status: 200 })`. deleteDraft를 documents에서 import. **200 반환 여부가 아니라 boolean 반환값을 게이트로 삼는다**(stale seq는 200이지만 저장 안 됨).
  </action>
  <verify>
    <automated>pnpm vitest run tests/draft/draft-autodelete.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 최신 seq 자동저장은 draft를 지우고, stale seq(0행 반영) 자동저장은 draft를 지우지 않음을 테스트가 각각 assert
    - route가 autosaveDocument의 boolean 반환값을 게이트로 사용(200 응답 자체가 아니라)
    - 전체 green
  </acceptance_criteria>
  <done>자동저장이 실제 반영됐을 때만 draft가 삭제되고, stale seq에서는 복구 데이터가 보존된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: draft-controller(60초 순수 컨트롤러) + useDraft 훅 (TDD fake timers)</name>
  <files>src/components/document/draft-controller.ts, src/components/document/useDraft.ts, tests/draft/draft-controller.test.ts</files>
  <read_first>
    - src/components/document/autosave-controller.ts(전문) — React-less 순수 함수 + send 주입, dispose/clearTimeout 관례
    - tests/documents/autosave-controller.test.ts(head) — vi.useFakeTimers() + deferred() + advanceTimersByTime 패턴
    - 05-RESEARCH.md Pattern 4(244-274행) — createDraftController 완성 코드(setInterval + 더티 플래그)
    - 05-PATTERNS.md "draft-controller.ts"(163-183), "useDraft.ts"(187-217)
    - 05-RESEARCH.md Anti-Patterns(327행) — draft 타이머를 autosave와 같은 인스턴스에 합치지 않는다
  </read_first>
  <behavior>
    - onContentChange 없이 advanceTimersByTime(60000) → send 미호출(더티 아님)
    - onContentChange("x") 후 advanceTimersByTime(60000) → send("x") 정확히 1회
    - 발사 후 더티 리셋: 추가 입력 없이 다시 60000 경과 → send 재호출 안 됨
    - onContentChange 여러 번 후 60000 → 마지막 content로 1회만 send
    - dispose() 후 타이머 정지(추가 발사 없음)
  </behavior>
  <action>
    먼저 tests/draft/draft-controller.test.ts를 RED로 커밋한다(TDD, autosave-controller.test.ts와 동형 fake-timer 패턴). 위 behavior 5케이스를 assert.
    그 다음 src/components/document/draft-controller.ts를 RESEARCH Pattern 4(244-274행)대로 작성: `createDraftController({ send, intervalMs = 60_000 })` → `let dirty=false; let latestContent=""; const timer=setInterval(() => { if(!dirty) return; dirty=false; void send(latestContent); }, intervalMs);` → `{ onContentChange(content) { latestContent=content; dirty=true; }, dispose() { clearInterval(timer); } }`. seq 가드/재시도 없음(draft 전용, Anti-Pattern). intervalMs는 테스트 override용.
    src/components/document/useDraft.ts("use client")를 useAutosave.ts 관례로: `useMemo(() => createDraftController({ send: async (content) => { const res = await fetch(`/api/documents/${docId}/draft`, { method:"PUT", headers:{"content-type":"application/json"}, body: JSON.stringify({ content }) }); return { ok: res.ok }; } }), [docId])` + `useEffect(() => () => controller.dispose(), [controller])` → `{ onContentChange: controller.onContentChange }`. status state 없음(draft 저장은 시각 피드백 없음).
  </action>
  <verify>
    <automated>pnpm vitest run tests/draft/draft-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 입력 없는 60초 경과에 send 미호출, 입력 있는 60초에 마지막 content로 1회 send, 발사 후 더티 리셋을 각각 assert
    - draft-controller가 React-less 순수 함수이고 send 주입으로 fake-timer만으로 테스트됨
    - useDraft가 status state 없이 onContentChange만 노출
    - 전체 green
  </acceptance_criteria>
  <done>60초 더티-플래그 컨트롤러가 입력 있을 때만 upsert하고, autosave와 분리된 순수 함수로 검증된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PUT/DELETE /api/documents/:id/draft | 신뢰 불가능한 draft content + document id가 서버로 넘어옴 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04-IDOR | Information Disclosure | draft 라우트 | high | mitigate | resolveWorkspaceIdForDocument로 workspace_id 서버 역산(클라 미신뢰) + 활성 문서만 — 타 워크스페이스 문서 draft 쓰기/삭제 차단(03-04 IDOR 관례, 존재 미노출 403) |
| T-05-04-PRIV | Elevation of Privilege | draft PUT/DELETE | high | mitigate | requireRole(target.workspaceId, "EDITOR") — VIEWER는 draft 쓰기/삭제 불가 |
| T-05-04-DATALOSS | Tampering | 자동저장 draft 삭제 게이팅 | high | mitigate | autosaveDocument boolean 반환값을 게이트로 — stale seq(0행, 200)에서 draft를 지우지 않아 복구 데이터 손실 방지(Pitfall 5). 회귀 테스트로 증명 |

*신규 패키지 설치 없음 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm vitest run tests/draft/` — 컨트롤러 fake-timer + autodelete 회귀 green
- `pnpm exec tsc --noEmit` clean
- draft/route.ts grep 게이트(runtime nodejs·requireRole·GET 부재) 통과
</verification>

<success_criteria>
draft 라우트가 IDOR/RBAC 경계를 지키고, 60초 컨트롤러가 입력 있을 때만 upsert하며, 자동저장 성공에 게이트된 draft 삭제가 stale seq에서 복구 데이터를 보존한다.
</success_criteria>

## Artifacts this phase produces (05-04)

- `src/app/api/documents/[id]/draft/route.ts` — PUT(upsert)/DELETE(폐기), IDOR/RBAC
- `src/app/api/documents/[id]/route.ts` — 자동저장 성공(boolean)에 게이트한 deleteDraft 연쇄
- `src/components/document/draft-controller.ts` — 순수 60초 더티-플래그 컨트롤러
- `src/components/document/useDraft.ts` — 얇은 훅
- `tests/draft/draft-controller.test.ts`, `tests/draft/draft-autodelete.test.ts`

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-04-SUMMARY.md` when done
</output>
