---
phase: 05-editor-enhancements-personalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage.ts
  - tests/upload/storage.test.ts
  - src/app/api/uploads/route.ts
  - tests/upload/rbac.test.ts
  - src/components/editor/useImageUpload.ts
  - src/components/layout/EditorPreviewLayout.tsx
  - src/components/editor/Toolbar.tsx
  - public/uploads/.gitkeep
  - .gitignore
autonomous: true
requirements: [EDIT-09]
user_setup: []
estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "서버가 매직바이트로 png/jpeg/gif/webp만 허용하고, .exe를 .png로 위장하거나 content-type을 스푸핑한 파일을 400으로 거부한다 (EDIT-09 / Pitfall 1)"
    - "5MB를 초과하는 파일은 바이트를 읽기 전에 400으로 거부된다 (Pitfall 3)"
    - "저장 파일명은 100% 서버 생성 uuid이며 클라가 보낸 어떤 문자열도 파일시스템 경로에 관여하지 않는다 (경로순회 원천 차단)"
    - "POST /api/uploads는 VIEWER를 403으로 거부하고 EDITOR 이상만 저장한다 (서버 강제, requireRole)"
    - "툴바 image 버튼 클릭 시 숨겨진 파일 입력이 열리고, 업로드 완료 시 커서 위치에 ![원본파일명](/uploads/{uuid}.{ext}) 마크다운이 삽입되며 기존 자동저장이 이어받는다 (EDIT-09 성공기준)"
  artifacts:
    - src/lib/storage.ts (saveUpload 단일 export)
    - src/app/api/uploads/route.ts (POST, runtime=nodejs, requireRole EDITOR)
    - src/components/editor/useImageUpload.ts (업로드 오케스트레이션 훅)
    - tests/upload/storage.test.ts
    - tests/upload/rbac.test.ts
    - public/uploads/.gitkeep
  key_links:
    - "클라 File → POST /api/uploads → saveUpload(매직바이트 판정) → { url } → getView().dispatch(placeholder→URL 치환) → EditorHost updateListener → 기존 자동저장 (변경 없음)"
    - "EditorPreviewLayout forwardRef getView 노출 → useImageUpload가 live EditorView에 직접 dispatch (Pitfall 6 아키텍처 스파인, draft 복구도 재사용)"
---

<objective>
이 phase의 TRACER 슬라이스다. 이미지 업로드의 단일 happy-path(툴바 버튼 클릭 → 파일 선택 → 서버 검증·저장 → 커서 위치에 마크다운 삽입 → 기존 자동저장 이어받기)를 모든 계층을 관통해 end-to-end로 배선한다. 이 슬라이스가 이 phase에서 가장 위험한 두 축을 먼저 증명한다: (1) **보안 표면** — 서버가 클라 Content-Type/파일명을 신뢰하지 않고 바이트(매직넘버)를 직접 검사, (2) **아키텍처 스파인** — 마운트-1회 uncontrolled `EditorView`에 바깥(레이아웃 레벨 훅)에서 `dispatch`를 쏘는 경로. 이 두 축은 draft 복구(05-05)와 업로드 UX 확장(05-02)이 그대로 재사용한다.

Purpose: 신규 인프라(파일시스템 storage)와 신규 아키텍처(getView 상위 노출)를 에이전트의 초반 컨텍스트에서 먼저 증명해, 나중에 열 층을 다 쌓은 뒤가 아니라 한 커밋 뒤에 막다른 길을 잡는다.
Output: storage 모듈 + 업로드 라우트 + 업로드 오케스트레이션 훅 + getView 노출된 EditorPreviewLayout. 드롭존/에러 배너는 05-02(확장), draft는 05-03~05는 별도.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@.planning/phases/05-editor-enhancements-personalization/05-PATTERNS.md
@.planning/phases/05-editor-enhancements-personalization/05-UI-SPEC.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: storage.ts 매직바이트 스니핑 + saveUpload (TDD RED 먼저)</name>
  <files>src/lib/storage.ts, tests/upload/storage.test.ts, public/uploads/.gitkeep, .gitignore</files>
  <read_first>
    - 05-RESEARCH.md "Code Examples: 이미지 업로드 — storage 모듈"(414-458행) — 완성 구현이 이미 존재, 그대로 채용
    - 05-RESEARCH.md Common Pitfalls #1(File.type 미신뢰)·#3(크기 체크 먼저)
    - src/lib/documents.ts:1-9 — "한 파일이 하나의 관심사를 캡슐화"하는 lib 관례
    - 05-PATTERNS.md "src/lib/storage.ts"(60-64행) — no-analog, RESEARCH가 원천
  </read_first>
  <behavior>
    - png(89 50 4E 47 0D 0A 1A 0A) 시그니처 버퍼 → { url: "/uploads/{uuid}.png" }
    - jpeg(FF D8 FF) → .jpg, gif(47 49 46 38 [37|39] 61) → .gif, webp(RIFF....WEBP, offset 8) → .webp
    - .exe/텍스트/svg 등 시그니처 불일치 버퍼 → { error: "BAD_TYPE" }
    - file.size > 5*1024*1024 → { error: "TOO_LARGE" }, 그리고 이 경우 arrayBuffer()는 호출되지 않는다(크기 우선)
    - 파일명은 crypto.randomUUID() 기반, 클라 file.name은 저장 경로 결정에 전혀 관여하지 않는다
  </behavior>
  <action>
    먼저 tests/upload/storage.test.ts를 작성해 RED로 커밋한다(CLAUDE.md TDD 불변식, TRD §10). 각 포맷의 고정 매직바이트 버퍼 픽스처로 `new File([buf], name, { type })`를 만들되 위장 케이스(예: name=".png", type="image/png"이지만 바이트는 텍스트)가 BAD_TYPE로 거부되는지 반드시 포함한다. 5MB 초과 픽스처는 `new File([new Uint8Array(0)], "big.png")` 위에 `Object.defineProperty(file, "size", { value: 6*1024*1024 })`로 size만 조작해 크기 우선 거부를 검증한다.
    그 다음 src/lib/storage.ts를 RESEARCH Code Examples(414-458행) 구현으로 채워 GREEN. `saveUpload(file: File): Promise<{ url: string } | { error: "TOO_LARGE" | "BAD_TYPE" }>` 단일 export. 검증 순서는 반드시 (1) size cap → (2) arrayBuffer → sniffImageType → (3) mkdir(recursive) → uuid 파일명 → writeFile → `/uploads/{filename}` 반환. `sniffImageType`은 png/jpeg/gif/webp 4종 매직바이트만 비교(신규 패키지 금지 — Node 내장 crypto/fs/promises만). 저장 디렉터리는 `path.join(process.cwd(), "public", "uploads")` 고정.
    public/uploads/.gitkeep을 생성하고 .gitignore에 업로드 산출물은 무시하되 .gitkeep은 추적하는 규칙을 추가한다(public/uploads/의 런타임 파일은 커밋되지 않게).
  </action>
  <verify>
    <automated>pnpm vitest run tests/upload/storage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/upload/storage.test.ts가 png/jpeg/gif/webp 4종 통과 + 위장 content-type BAD_TYPE 거부 + 5MB 초과 TOO_LARGE 거부를 각각 assert하고 전부 green
    - saveUpload 구현이 size 비교보다 arrayBuffer()를 먼저 호출하지 않는다(코드 순서로 검증)
    - saveUpload 시그니처에 file.name/file.type에서 추출한 확장자가 파라미터로 들어가지 않는다
    - .gitignore가 public/uploads/의 런타임 파일은 무시하고 public/uploads/.gitkeep은 추적한다
  </acceptance_criteria>
  <done>4종 매직바이트 판정·크기 캡·위장 거부가 단위 테스트로 green이고, uuid 파일명이 서버 전적으로 생성된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/uploads 라우트 + RBAC 게이트 (EDITOR+)</name>
  <files>src/app/api/uploads/route.ts, tests/upload/rbac.test.ts</files>
  <read_first>
    - 05-RESEARCH.md "Code Examples: 이미지 업로드 — Route Handler"(462-497행) — 완성 코드, 그대로 채용
    - src/app/api/documents/[id]/route.ts:1-30 — requireRole/ForbiddenError/forbiddenResponse 관례 + runtime="nodejs"(6행)
    - src/lib/rbac.ts — requireRole(wsId, minRole), UUID_RE fail-closed 가드(malformed wsId 방어)
    - 05-PATTERNS.md "src/app/api/uploads/route.ts"(68-89행) — wsId는 쿼리스트링으로 받음(업로드는 문서 비종속)
    - tests/rbac/matrix.test.ts — 역할별 403/2xx 테스트 패턴
  </read_first>
  <action>
    src/app/api/uploads/route.ts에 POST 핸들러를 RESEARCH Code Examples(462-497행)대로 작성한다. `export const runtime = "nodejs";`(fs 접근 필수, Pitfall 4)를 최상단에. `req.nextUrl.searchParams.get("wsId")` → 없으면 400 → `requireRole(wsId, "EDITOR")`를 try/catch(ForbiddenError→forbiddenResponse) → `req.formData()` → `formData.get("file")` File 타입가드 실패 시 400 → `saveUpload(file)` → error union을 UI-SPEC Copywriting Contract 문구로 매핑(TOO_LARGE→"이미지 크기는 5MB를 넘을 수 없어요.", BAD_TYPE→"PNG, JPEG, GIF, WEBP 형식만 업로드할 수 있어요.") 400, 성공 시 `{ url }` 200. UUID_RE가 이미 malformed wsId를 막으므로 라우트에서 별도 uuid zod 검증은 생략(rbac.ts:15,42 재사용).
    tests/upload/rbac.test.ts에서 VIEWER 멤버가 POST를 호출하면 403, EDITOR 이상은 통과(200)함을 tests/rbac/matrix.test.ts 패턴으로 검증한다. saveUpload는 실제 fs 쓰기를 피하도록 vi.mock 처리하거나 유효 png 버퍼를 임시 디렉터리에 쓰도록 하되, 핵심 assert는 상태코드(403 vs 200)다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/upload/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - VIEWER 역할 요청이 403(forbiddenResponse)로 거부되고 EDITOR/ADMIN/OWNER는 통과함을 테스트가 assert
    - route.ts 최상단에 export const runtime = "nodejs"가 존재
    - error union이 UI-SPEC Copywriting Contract 정확한 한글 문구로 400 매핑됨
    - wsId 누락 시 400
  </acceptance_criteria>
  <done>업로드 라우트가 EDITOR+ 서버 게이트를 통과한 요청만 저장하고, 검증 실패는 스펙 문구로 400을 낸다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TRACER 배선 — getView 상위 노출 + useImageUpload(버튼 경로) + Toolbar image 가로채기</name>
  <files>src/components/layout/EditorPreviewLayout.tsx, src/components/editor/useImageUpload.ts, src/components/editor/Toolbar.tsx, e2e/image-upload.spec.ts</files>
  <read_first>
    - src/components/layout/EditorPreviewLayout.tsx(현재 전문) — hostRef만 내부 보유, Toolbar에만 getView 전달
    - src/components/editor/EditorHost.tsx:8-17,38-40 — forwardRef + useImperativeHandle(getView) 패턴 원본
    - src/components/editor/Toolbar.tsx:44-64 — 현재 onClick(view.dispatch(plugin.run)) + onMouseDown preventDefault
    - src/components/editor/plugins/image.ts(전문) + plugins/types.ts — image plugin.id="image", run(state) 순수 계약(수정 금지)
    - 05-RESEARCH.md Pattern 1(209-226)·Pattern 2(228-231)·Pitfall 2(동시 업로드 방지)·Pitfall 6(getView 상위 노출)
    - 05-PATTERNS.md "useImageUpload + Toolbar"(243-262), "EditorPreviewLayout forwardRef"(266-285)
    - src/components/document/useAutosave.ts — fetch 래핑 훅 관례
  </read_first>
  <action>
    EditorPreviewLayout을 `forwardRef<EditorPreviewLayoutHandle, EditorPreviewLayoutProps>`로 승격해 `getView: () => EditorView | null`을 상위로 노출한다(EditorHost.tsx:8-17,38-40 패턴 그대로 — 05-05 draft 복구가 이 핸들을 재사용). 업로드 오케스트레이션은 Pattern 1대로 EditorPreviewLayout 레벨에서 소유: 숨겨진 `<input type="file" accept="image/png,image/jpeg,image/gif,image/webp">`를 layout이 렌더하고, `useImageUpload(getView)` 훅을 마운트해 파일 선택 시 업로드 플로우를 돌린다. Toolbar에는 `onImageButtonClick` 콜백을 새 prop으로 넘겨 숨김 input.click()을 트리거하게 한다.
    src/components/editor/useImageUpload.ts: 파일을 받아 (1) `getView().dispatch`로 커서 위치에 리터럴 placeholder `![업로드 중...]()`를 삽입 → (2) FormData(file) → `POST /api/uploads?wsId=${wsId}` → (3) 성공 시 `view.state.doc.toString().indexOf(placeholder)`로 위치를 다시 찾아 `![${file.name}](${url})`로 dispatch 치환(Pattern 2 리터럴 검색), 실패 시 placeholder 제거. 동시 업로드는 Pitfall 2대로 "진행 중" 플래그로 차단(진행 중이면 새 업로드 무시). wsId는 클라 훅이 `useParams()`(next/navigation, 라우트 /w/[wsId]/d/[docId]에서 wsId 반환)로 읽는다 — DocumentWorkspace/d-page로 prop을 threading하지 않아(다른 wave가 그 파일들을 만지지 않게) tracer가 자체 완결된다.
    Toolbar.tsx: onClick에서 `plugin.id === "image"`면 `view.dispatch(plugin.run(...))` 대신 `onImageButtonClick()`으로 가로채고 return. 나머지 13개 플러그인 경로는 한 줄도 바꾸지 않는다. `onMouseDown preventDefault`는 image 버튼에도 유지(포커스 보존). **src/components/editor/plugins/image.ts는 절대 수정하지 않는다**(TRD §6 1기능1파일 불변식 — run(state) 계약 유지, 기존 tests/editor/image.test.ts 그대로 green).
    e2e/image-upload.spec.ts: 문서를 열고 툴바 image 버튼 클릭 → 숨김 input에 유효 png 픽스처를 setInputFiles → 에디터 내용에 `](/uploads/` 및 `![`가 나타남을 assert(placeholder→URL 치환 완료). 이것이 tracer의 진짜 end-to-end 증명이다.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/image-upload.spec.ts && pnpm exec tsc --noEmit && git diff --exit-code src/components/editor/plugins/image.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e가 image 버튼 → 파일 선택 → 에디터에 ![파일명](/uploads/{uuid}.{ext}) 삽입까지 통과
    - EditorPreviewLayout이 forwardRef로 getView를 노출하는 EditorPreviewLayoutHandle을 export
    - git diff가 src/components/editor/plugins/image.ts에 아무 변경이 없음을 확인(exit 0)
    - 동시 업로드 시도(진행 중 두 번째 클릭)가 무시되어 placeholder가 하나만 생김
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>툴바 image 버튼 한 번으로 실제 파일이 서버 검증·저장되고 커서 위치에 마크다운으로 삽입되며, 그 dispatch가 기존 자동저장 파이프라인을 자연히 트리거한다. getView가 상위로 노출돼 이후 plan들이 재사용할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/uploads | 신뢰 불가능한 파일 바이트 + 클라 Content-Type/파일명 + wsId가 서버로 넘어옴 |
| uploaded file → public/uploads 정적 서빙 | 저장된 파일이 이후 브라우저에 그대로 서빙됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01-SPOOF | Tampering | storage.saveUpload | high | mitigate | 서버 매직바이트 스니핑 — 클라 File.type/file.name 확장자는 저장 결정에 전혀 관여하지 않음(Pitfall 1). 위장 파일 거부를 테스트가 증명 |
| T-05-01-TRAVERSAL | Tampering | storage 파일명/경로 | high | mitigate | 저장 파일명 100% 서버 crypto.randomUUID(). 클라 문자열이 path.join에 들어가지 않음(sanitize가 아니라 애초에 입력 미사용) |
| T-05-01-DOS | Denial of Service | storage 크기 검증 | medium | mitigate | 5MB 캡 + 크기 우선 체크(바이트 읽기 전 거부, Pitfall 3). 워크스페이스 총량 제한은 R2 범위 밖 |
| T-05-01-PRIV | Elevation of Privilege | POST /api/uploads | high | mitigate | requireRole(wsId, "EDITOR") — 모든 변경 API의 유일한 진짜 경계(CLAUDE.md 불변식). VIEWER 403 테스트로 증명 |
| T-05-01-SVGXSS | Tampering/XSS | 허용 포맷 화이트리스트 | medium | accept | png/jpeg/gif/webp만 허용(SVG 제외, CONTEXT 잠금) — 매직바이트 화이트리스트가 SVG를 애초에 거부. 이미지 alt/URL의 XSS는 미리보기 rehype-sanitize가 하류에서 이미 차단(EDIT-08) |

*이 phase는 신규 npm 패키지를 설치하지 않는다 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm vitest run tests/upload/` — storage 매직바이트/크기 + 라우트 RBAC 전부 green
- `pnpm exec playwright test e2e/image-upload.spec.ts` — 버튼 → 삽입 end-to-end
- `pnpm exec tsc --noEmit` clean
- `git diff --exit-code src/components/editor/plugins/image.ts` — 플러그인 불변식 보존
</verification>

<success_criteria>
이미지 업로드의 단일 happy-path가 서버 검증(매직바이트·크기·RBAC)을 통과해 커서 위치 삽입까지 end-to-end로 동작하고, getView 상위 노출 아키텍처가 확립돼 05-02/05-05가 재사용 가능하다.
</success_criteria>

## Artifacts this phase produces (05-01)

- `src/lib/storage.ts` — saveUpload(file) 단일 export, 매직바이트+uuid+public/uploads write 격리(TRD §8)
- `src/app/api/uploads/route.ts` — POST, runtime=nodejs, requireRole EDITOR
- `src/components/editor/useImageUpload.ts` — 업로드 오케스트레이션(placeholder 삽입/치환, 동시 차단)
- `src/components/layout/EditorPreviewLayout.tsx` — forwardRef getView 노출 + 숨김 파일 입력 + useImageUpload 마운트
- `src/components/editor/Toolbar.tsx` — image 버튼만 가로채기(나머지 13개 불변)
- `public/uploads/.gitkeep`, `.gitignore` 업로드 산출물 무시 규칙
- `tests/upload/storage.test.ts`, `tests/upload/rbac.test.ts`, `e2e/image-upload.spec.ts`

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-01-SUMMARY.md` when done
</output>
