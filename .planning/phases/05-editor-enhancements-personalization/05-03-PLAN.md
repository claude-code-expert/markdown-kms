---
phase: 05-editor-enhancements-personalization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/0004_*.sql
  - drizzle/meta/*
  - src/lib/validation.ts
  - src/lib/documents.ts
  - tests/draft/services.test.ts
  - tests/draft/helpers.ts
autonomous: false
requirements: [EDIT-11]
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "document_draft 테이블이 마이그레이션되어 PG16(5433)에 존재하고 document_id가 PK(문서당 1행)이며 document 삭제 시 CASCADE된다 (TRD §3)"
    - "upsertDraft(documentId, content)가 onConflictDoUpdate로 문서당 정확히 1행을 유지한다 (NFR-2.1)"
    - "getDraft가 있으면 행을, 없으면 null을 반환하고, deleteDraft가 행을 제거한다"
    - "draftBodySchema가 { content: string }만 검증한다(seq 없음 — draft는 순서 가드 불필요) (TRD §7)"
  artifacts:
    - src/db/schema.ts (documentDraft 테이블)
    - drizzle/0004_*.sql (마이그레이션)
    - src/lib/documents.ts (upsertDraft/getDraft/deleteDraft)
    - src/lib/validation.ts (draftBodySchema)
    - tests/draft/services.test.ts
  key_links:
    - "document_draft.document_id → document.id (ON DELETE CASCADE — 문서 완전삭제 시 draft도 함께 사라짐)"
    - "onConflictDoUpdate target=document_draft.document_id → 문서당 1행 upsert 불변식의 DB 레벨 강제"
---

<objective>
draft 크래시 복구(EDIT-11)의 데이터·서비스 substrate를 만든다. TRD §3 DDL 그대로 `document_draft` 테이블을 스키마에 추가·마이그레이션하고(마이그레이션은 되돌릴 수 없는 one-way 변경이므로 결정 게이트를 먼저 통과), `src/lib/documents.ts`에 `upsertDraft`/`getDraft`/`deleteDraft`를 기존 `DbClient` 주입 패턴 그대로 추가한다. 문서당 1행(document_id PK) 불변식이 이 plan의 핵심이므로 upsert 동작을 실 DB 통합 테스트로 먼저 확인한다(TDD, TRD §10).

Purpose: draft의 라우트·컨트롤러·복구 UI(05-04/05-05)가 얹힐 백엔드 기반. 라우트·UI는 이 plan에 의존한다.
Output: document_draft 테이블 + upsert/get/delete 서비스 + draftBodySchema.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@.planning/phases/05-editor-enhancements-personalization/05-PATTERNS.md
@docs/TRD.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>document_draft 마이그레이션을 지금 생성·적용할지 (one-way, 되돌릴 수 없음)</decision>
  <context>새 테이블 추가는 되돌리기 어려운 스키마 변경이다. Phase 3/4의 folder/document 마이그레이션과 동일한 one-way 게이트다 — 그 두 번은 사용자가 orchestrator 경유로 사전 승인해 재프롬프트 없이 apply-now로 진행했다(STATE 03-01/04-01 결정 참조).</context>
  <options>
    <option id="apply-now">
      <name>지금 생성+적용</name>
      <pros>이후 태스크(upsert 통합 테스트)가 바로 실 DB에서 검증 가능</pros>
      <cons>스키마 변경이 즉시 확정</cons>
    </option>
    <option id="review-first">
      <name>DDL만 검토 후 적용</name>
      <pros>생성된 SQL을 먼저 사람이 확인</pros>
      <cons>왕복 1회 추가</cons>
    </option>
  </options>
  <resume-signal>Select: apply-now 또는 review-first</resume-signal>
  <reversibility rating="one-way">새 테이블 마이그레이션은 되돌릴 수 없는 스키마 변경 — Phase 3/4 게이트와 동일 성격</reversibility>
</task>

<task type="auto">
  <name>Task 2: documentDraft 스키마 + 마이그레이션 + draftBodySchema</name>
  <files>src/db/schema.ts, drizzle/0004_*.sql, drizzle/meta/*, src/lib/validation.ts</files>
  <read_first>
    - 05-RESEARCH.md "document_draft — 스키마 추가"(500-511행) — TRD §3 DDL 1:1 대응 완성 코드
    - src/db/schema.ts:1-15(imports), :94-125(document 테이블 — pgTable/uuid/text/timestamp 관례, folder_id CASCADE 없음 주석)
    - 05-PATTERNS.md "src/db/schema.ts (+documentDraft)"(37-56행) — document_id 자체가 PK, 서러게이트 id 발명 금지, 인덱스 불필요
    - src/lib/validation.ts:55-67 — documentSchema/autosaveBodySchema 관례(content는 trim 없음, NFR-5.2)
    - CLAUDE.md 명령 — pnpm drizzle-kit generate → migrate
  </read_first>
  <action>
    src/db/schema.ts에 `documentDraft`를 추가한다: `pgTable("document_draft", { documentId: uuid("document_id").primaryKey().references(() => document.id, { onDelete: "cascade" }), content: text("content").notNull(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() })`. 서러게이트 id 만들지 않는다(document_id가 PK — 문서당 1행 불변식). 인덱스 없음(PK 조회만).
    `pnpm drizzle-kit generate`로 0004_*.sql을 생성하고 `pnpm drizzle-kit migrate`로 적용(로컬 PG16 @ 5433, .env.local DATABASE_URL). 생성된 SQL을 손편집하지 않는다.
    src/lib/validation.ts에 `draftBodySchema = z.object({ content: z.string() })` + `DraftBodyInput` 타입을 autosaveBodySchema(63-67행) 바로 아래 관례로 추가한다. seq/title 없음 — draft는 순서 가드가 없고 content만 저장(TRD §7). content는 trim 없음(documentSchema 규칙 승계).
  </action>
  <verify>
    <automated>pnpm drizzle-kit generate --config drizzle.config.ts 2>&1 | grep -qiv error && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - drizzle/0004_*.sql이 document_draft 테이블(document_id PK, content NOT NULL, updated_at)을 CREATE하고 document(id) FK에 ON DELETE CASCADE를 건다
    - documentDraft가 서러게이트 id 없이 document_id를 PK로 둔다
    - draftBodySchema가 content만 검증하고 seq 필드가 없다
    - tsc --noEmit clean
  </acceptance_criteria>
  <precondition>로컬 PG16이 5433에서 accepting connections이고 .env.local DATABASE_URL이 그 인스턴스를 가리킨다 (user_setup — 로컬 dev DB)</precondition>
  <done>document_draft 테이블이 마이그레이션되어 PG에 존재하고 draftBodySchema가 content만 검증한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: upsertDraft/getDraft/deleteDraft 서비스 + 실 DB 통합 테스트 (TDD)</name>
  <files>src/lib/documents.ts, tests/draft/services.test.ts, tests/draft/helpers.ts</files>
  <read_first>
    - 05-RESEARCH.md "document_draft — upsert"(513-536행) — onConflictDoUpdate 완성 코드([VERIFIED] drizzle 타입 인용)
    - src/lib/documents.ts:1-8(DbClient 유니온 타입), :110-123(autosaveDocument 반환값 boolean 관례)
    - 05-PATTERNS.md "src/lib/documents.ts (+upsertDraft/getDraft/deleteDraft)"(118-144행)
    - tests/documents/ 기존 통합 테스트(실 DB 대상) 관례 — helpers로 문서 시드
  </read_first>
  <behavior>
    - upsertDraft(docId, "v1") 후 getDraft(docId) → { documentId: docId, content: "v1" }
    - 같은 docId로 upsertDraft(docId, "v2") 재호출 → 여전히 1행, content "v2"(onConflictDoUpdate로 갱신, 새 행 아님)
    - deleteDraft(docId) 후 getDraft(docId) → null
    - getDraft(존재하지 않는 docId) → null
  </behavior>
  <action>
    먼저 tests/draft/services.test.ts를 RED로 커밋한다(TDD). 실 DB(5433)에서 문서를 시드(tests/draft/helpers.ts에 createTestDocument 유사 헬퍼 — 기존 tests/documents 헬퍼 재사용 가능하면 재사용)한 뒤 upsert→get→재upsert(1행 유지)→delete→null 흐름을 assert한다.
    그 다음 src/lib/documents.ts에 upsertDraft/getDraft/deleteDraft를 RESEARCH 완성 코드(513-536행)대로 추가한다. `DbClient` 유니온 타입(8행)을 그대로 재사용(새 타입 정의 금지). upsert는 `onConflictDoUpdate({ target: documentDraft.documentId, set: { content, updatedAt: new Date() } })`. schema.ts에서 documentDraft를 import한다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/draft/services.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 같은 document_id로 두 번 upsert해도 행이 1개이고 content가 마지막 값으로 갱신됨을 테스트가 assert
    - deleteDraft 후 getDraft가 null
    - 세 함수 모두 client: DbClient = db 주입 파라미터를 가짐(기존 관례)
    - 전체 green
  </acceptance_criteria>
  <done>upsert/get/delete가 실 DB에서 문서당 1행 불변식과 함께 green이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (이 plan은 라우트 없음 — 서비스/스키마만) | 신뢰 경계는 05-04의 draft 라우트에서 성립(IDOR/RBAC). 이 plan은 DB 스키마·순수 서비스 함수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03-CASCADE | Tampering | document_draft FK | low | mitigate | document_id FK ON DELETE CASCADE — 문서가 완전삭제되면 고아 draft가 남지 않음(TRD §3) |
| T-05-03-MULTIROW | Tampering | upsertDraft | low | mitigate | document_id PK + onConflictDoUpdate — 동시 upsert가 문서당 2행을 만들 수 없음(DB 제약이 심판) |

*신규 패키지 설치 없음 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm vitest run tests/draft/services.test.ts` — upsert 1행/get/delete green
- `pnpm exec tsc --noEmit` clean
- 마이그레이션이 PG16(5433)에 적용됨
</verification>

<success_criteria>
document_draft 테이블이 존재하고 문서당 1행 upsert/조회/삭제 서비스가 실 DB에서 검증되며, draftBodySchema가 content만 검증한다.
</success_criteria>

## Artifacts this phase produces (05-03)

- `src/db/schema.ts` — documentDraft 테이블(document_id PK, content, updated_at, CASCADE)
- `drizzle/0004_*.sql` — 마이그레이션
- `src/lib/documents.ts` — upsertDraft/getDraft/deleteDraft
- `src/lib/validation.ts` — draftBodySchema
- `tests/draft/services.test.ts`, `tests/draft/helpers.ts`

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-03-SUMMARY.md` when done
</output>
