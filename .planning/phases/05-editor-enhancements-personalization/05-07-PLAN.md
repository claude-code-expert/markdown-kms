---
phase: 05-editor-enhancements-personalization
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/editor/Toolbar.module.css
  - src/app/layout.tsx
  - src/components/layout/ThemeToggle.tsx
  - src/components/layout/ThemeToggle.module.css
  - src/components/tree/FolderTree.tsx
  - tests/theme/rsc-cookie.test.ts
autonomous: true
requirements: [EDIT-12, EDIT-10]
user_setup: []
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "theme 쿠키가 dark면 RSC가 <html data-theme=\"dark\">를 첫 HTML에 심어 FOUC 없이 다크로 렌더된다 (EDIT-12)"
    - "쿠키가 없는 첫 방문은 @media (prefers-color-scheme: dark)가 :root:not([data-theme])를 통해 OS 설정을 따른다 (CONTEXT 잠금)"
    - "사이드바 하단 테마 토글이 document.cookie 직접 쓰기 + data-theme 즉시 갱신으로 라이트↔다크를 즉시 전환한다 (신규 API 라우트 0개)"
    - "툴바 버튼이 :active로 pressed(accent-weak), 툴팁이 hover 후 300ms 지연 표시·해제 즉시로 동작한다 (EDIT-10)"
    - "기존 트리·휴지통·모달·상태바가 12개 CSS 변수 override만으로 다크 전환된다(컴포넌트별 재작성 없음)"
  artifacts:
    - src/app/globals.css ([data-theme="dark"] 12변수 + @media 폴백)
    - src/components/editor/Toolbar.module.css (:active pressed + 300ms 툴팁 + 툴팁 배경 고정)
    - src/app/layout.tsx (async cookies() → data-theme)
    - src/components/layout/ThemeToggle.tsx
    - tests/theme/rsc-cookie.test.ts
  key_links:
    - "RSC cookies().get('theme') → <html data-theme> → globals.css [data-theme=dark] 변수 override → 모든 var(--token) 참조 컴포넌트 자동 전환"
    - "ThemeToggle document.cookie 쓰기 + document.documentElement.dataset.theme 갱신 → 다음 요청부터 RSC가 같은 값 렌더(SSR-클라 일관)"
---

<objective>
개인화의 테마 축(EDIT-12 라이트/다크)과 툴바 폴리시(EDIT-10 pressed + 300ms 툴팁)를 얹는다. 다크 팔레트는 `src/app/globals.css`의 `:root` 12개 변수를 `[data-theme="dark"]`가 override하는 방식(컴포넌트 코드는 안 건드림 — 전부 `var(--token)` 참조)이고, 첫 방문 OS 추종은 `@media (prefers-color-scheme: dark)` 폴백으로 처리한다(RESEARCH Open Q #1 → @media 이중 블록 채택). 쿠키는 RSC가 `await cookies()`로 읽어 첫 HTML에 `data-theme`를 심어 FOUC를 없애고, 토글은 클라가 `document.cookie`로 직접 쓴다(신규 라우트 0개). 툴바 pressed/300ms는 순수 CSS만(JS 상태 불필요).

Purpose: 세션 전역 개인화. 다른 plan과 파일 충돌이 없어 Wave 1에서 tracer와 병렬 실행.
Output: 다크 토큰 + 테마 토글 + RSC 쿠키 읽기 + 툴바 CSS 폴리시.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@.planning/phases/05-editor-enhancements-personalization/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 다크 토큰 override + @media 폴백 + 툴바 pressed/300ms CSS</name>
  <files>src/app/globals.css, src/components/editor/Toolbar.module.css</files>
  <read_first>
    - src/app/globals.css:1-25 — :root 12개 변수(--bg/--surface/--surface-2/--border/--border-strong/--text/--muted/--dim/--accent/--accent-strong/--accent-weak/--destructive/--code-bg)
    - 05-UI-SPEC.md "Color / 다크"(109-131행) — 12개 변수의 다크 값 전문(slate/blue 스케일, WCAG 검증됨) + 알려진 예외(툴팁 배경)
    - 05-UI-SPEC.md "Toolbar Interaction Contract"(135-145행) — :active pressed + 방향별 transition-delay 원문
    - 05-RESEARCH.md Pattern 6(283-297) @media 이중 블록 + Open Q #1(권장: @media 채택)
    - src/components/editor/Toolbar.module.css 현재 .tooltip(background: var(--text)) 규칙
  </read_first>
  <action>
    src/app/globals.css의 `:root` 블록 아래에 `[data-theme="dark"] { ... }`를 추가해 UI-SPEC Color 다크 섹션(109-131행)의 12개 변수 값을 재선언한다(--code-bg는 #020617까지 내림). 이어서 `@media (prefers-color-scheme: dark) { :root:not([data-theme]) { ... 동일 12개 값 ... } }`를 추가해 쿠키 없는 첫 방문이 OS 다크를 따르게 한다(값 중복 12개, CONTEXT "첫 방문은 prefers-color-scheme 따름").
    src/components/editor/Toolbar.module.css: (1) pressed — `.button:active { background: var(--accent-weak); color: var(--accent); }`(UI-SPEC 144행). (2) 300ms 툴팁 — 기존 즉시 전환을 "숨김 즉시 / 표시 300ms 지연"으로 분리: `.tooltip { transition: opacity 0s linear 0s; }` + `.buttonWrap:hover .tooltip { transition: opacity 0s linear 300ms; opacity: 1; }`. (3) 툴팁 배경 — `.tooltip`의 `background: var(--text)`를 고정값 `background: #0f172a;`로 교체(UI-SPEC "알려진 예외" 131행 — 다크에서 --text가 흰색이 되어 흰 배경 흰 글자가 되는 것 방지, 툴팁은 테마 비종속). 폰트 크기 11px는 건드리지 않는다.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('src/app/globals.css','utf8'); if(!/\[data-theme=.dark.\]/.test(c)) throw new Error('no dark block'); if(!/prefers-color-scheme/.test(c)) throw new Error('no media fallback'); const t=require('fs').readFileSync('src/components/editor/Toolbar.module.css','utf8'); if(!/:active/.test(t)) throw new Error('no pressed'); if(!/300ms/.test(t)) throw new Error('no 300ms delay');"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css에 [data-theme="dark"] 블록(12개 변수)과 @media (prefers-color-scheme: dark) { :root:not([data-theme]) } 폴백이 둘 다 존재
    - Toolbar.module.css에 .button:active pressed 규칙과 방향별 300ms transition-delay 분리 규칙이 존재
    - 툴팁 배경이 var(--text)가 아니라 고정 #0f172a
  </acceptance_criteria>
  <done>다크 팔레트가 변수 override로 도입되고, 첫 방문 OS 폴백과 툴바 pressed/300ms 툴팁이 순수 CSS로 동작한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RSC cookies() → data-theme (FOUC 방지)</name>
  <files>src/app/layout.tsx, tests/theme/rsc-cookie.test.ts</files>
  <read_first>
    - src/app/layout.tsx(현재 전문) — RootLayout, suppressHydrationWarning 이미 존재, 폰트 변수 className
    - 05-RESEARCH.md "테마 — RSC에서 읽기"(548-563행) — await cookies() → data-theme 완성 코드
    - 05-RESEARCH.md Pitfall 9(cookies()가 전 라우트를 동적 렌더링시킴 — 의도된 트레이드오프, requireRole로 이미 동적)
    - 05-RESEARCH.md State of the Art — Next.js 15 cookies()는 async(await 필수)
  </read_first>
  <behavior>
    - theme 쿠키 없음 → <html>에 data-theme 속성 미부여(첫 방문, @media 폴백에 위임)
    - theme=light → data-theme="light"
    - theme=dark → data-theme="dark"
  </behavior>
  <action>
    RootLayout을 `async` 함수로 바꿔 `const cookieStore = await cookies();`(next/headers) → `const theme = cookieStore.get("theme")?.value;` → `<html ... data-theme={theme}>`. theme이 undefined면 data-theme 속성이 렌더되지 않아야 하므로(React는 undefined attribute를 생략) 값이 없을 때 속성 자체가 빠진다. suppressHydrationWarning·폰트 className은 그대로. Pitfall 9의 "cookies() 도입으로 로그인 등 전 라우트가 동적 렌더링으로 전환됨"은 의도된 트레이드오프로 SUMMARY에 한 줄 기록(requireRole DB 조회로 인증 라우트는 이미 동적).
    tests/theme/rsc-cookie.test.ts: layout이 반환하는 트리에서 theme 쿠키 3케이스(없음/light/dark)에 따라 data-theme이 올바르게(없음이면 undefined) 설정되는지 검증한다. next/headers cookies()를 vi.mock으로 스텁해 순수 렌더 로직만 테스트(실 요청 불필요).
  </action>
  <verify>
    <automated>pnpm vitest run tests/theme/rsc-cookie.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 쿠키 없음/light/dark 3케이스에서 data-theme이 각각 미부여/"light"/"dark"로 렌더됨을 테스트가 assert
    - RootLayout이 async이고 await cookies()를 사용
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>RSC가 쿠키를 읽어 첫 HTML에 data-theme을 심어 FOUC 없이 초기 렌더한다.</done>
</task>

<task type="auto">
  <name>Task 3: ThemeToggle 컴포넌트 + 사이드바 배치</name>
  <files>src/components/layout/ThemeToggle.tsx, src/components/layout/ThemeToggle.module.css, src/components/tree/FolderTree.tsx</files>
  <read_first>
    - 05-UI-SPEC.md "Theme Toggle Contract"(182-191행) — 위치(휴지통 행 바로 아래), 스타일(40px, 상단 1px border, padding 8px, 아이콘 16px + gap 4px), Moon/Sun 아이콘 + 문구
    - 05-PATTERNS.md "ThemeToggle.tsx"(317-334행) — document.cookie 직접 쓰기 + dataset.theme + useState 완성 스케치
    - src/components/tree/FolderTree.tsx:339-348 — 휴지통 고정 행(trashLink, flex-shrink:0, 하단) — 이 바로 아래에 동일 패턴 신규 행
    - 05-RESEARCH.md Pitfall 8(cookies() 클라 import 금지 — 초기 테마는 prop 또는 dataset에서 읽기)
  </read_first>
  <action>
    src/components/layout/ThemeToggle.tsx("use client")를 PATTERNS 스케치(317-334행)대로 작성한다: `initialTheme: "light" | "dark"` prop(부모가 dataset/쿠키에서 읽어 전달 — Pitfall 8, cookies()를 클라에서 import 금지) → useState → toggle()이 `document.cookie = "theme=${next}; path=/; max-age=31536000; samesite=lax"` + `document.documentElement.dataset.theme = next` + setState. 라이트일 때 lucide `Moon` + "다크 모드로 전환", 다크일 때 `Sun` + "라이트 모드로 전환"(UI-SPEC Copywriting). ThemeToggle.module.css는 UI-SPEC Theme Toggle Contract 스타일(휴지통 trashLink 관례와 동일: 40px, 상단 1px solid var(--border), padding 8px, 아이콘 16px var(--muted) + gap 4px, hover var(--surface-2)).
    FolderTree.tsx: 휴지통 고정 행(339-348행) 바로 아래에 `<ThemeToggle initialTheme={...} />`를 렌더한다. FolderTree는 클라 컴포넌트이므로 초기값은 ThemeToggle이 마운트 시 `document.documentElement.dataset.theme`(RSC가 이미 심은 값)에서 1회 읽어 "light"로 안전 폴백(Pitfall 8) — initialTheme prop을 optional로 두고 미지정 시 dataset에서 읽는 방식으로 일관 구현.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/components/layout/ThemeToggle.tsx','utf8'); if(!/document\.cookie/.test(c)) throw new Error('no cookie write'); if(!/dataset\.theme/.test(c)) throw new Error('no dom update');"</automated>
  </verify>
  <acceptance_criteria>
    - ThemeToggle이 document.cookie 쓰기 + document.documentElement.dataset.theme 갱신 + useState로 라이트↔다크를 즉시 전환
    - 라이트일 때 Moon+"다크 모드로 전환", 다크일 때 Sun+"라이트 모드로 전환" 표시
    - FolderTree 휴지통 행 바로 아래에 ThemeToggle 행이 렌더됨
    - 신규 API 라우트를 추가하지 않는다(document.cookie 직접 쓰기)
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>사이드바 하단 테마 토글이 클라에서 쿠키를 직접 써 즉시 다크↔라이트를 전환하고, RSC가 다음 렌더에서 같은 값을 심는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → document.cookie (theme) | 비민감·비-httpOnly 개인화 값. 클라가 소유(서버 검증 대상 아님) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07-COOKIE | Tampering | theme 쿠키 | low | accept | theme은 비민감 UI 설정 — 사용자가 조작해도 자기 화면 색만 바뀜. httpOnly/서버검증 불필요(RESEARCH Responsibility Map). samesite=lax로 최소 방어 |
| T-05-07-XSS | (없음) | data-theme 속성 | low | accept | data-theme 값은 서버 쿠키 문자열 그대로이나 속성값으로만 쓰이고 HTML로 해석되지 않음 — 실행 경로 없음 |

*신규 패키지 설치 없음 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm vitest run tests/theme/rsc-cookie.test.ts` — 쿠키 3케이스 data-theme green
- `pnpm exec tsc --noEmit` clean
- globals.css/Toolbar.module.css grep 게이트 통과(다크 블록·@media·:active·300ms)
- 수동(Manual-Only, VALIDATION): 다크 설정 후 새로고침 시 흰 플래시 없이 즉시 다크(no-FOUC)
</verification>

<success_criteria>
라이트/다크 테마가 쿠키 영속·FOUC 없이 전환되고, 첫 방문은 OS 설정을 따르며, 툴바가 pressed·300ms 툴팁을 갖는다. 기존 컴포넌트는 변수 override만으로 자동 다크 전환된다.
</success_criteria>

## Artifacts this phase produces (05-07)

- `src/app/globals.css` — [data-theme="dark"] 12변수 override + @media prefers-color-scheme 폴백
- `src/components/editor/Toolbar.module.css` — :active pressed + 300ms 툴팁 지연 + 툴팁 배경 고정(EDIT-10)
- `src/app/layout.tsx` — async cookies() → <html data-theme>
- `src/components/layout/ThemeToggle.tsx` + `.module.css` — 사이드바 하단 테마 토글
- `src/components/tree/FolderTree.tsx` — 휴지통 행 아래 토글 배치
- `tests/theme/rsc-cookie.test.ts`

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-07-SUMMARY.md` when done
</output>
