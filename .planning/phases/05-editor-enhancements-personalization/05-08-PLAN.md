---
phase: 05-editor-enhancements-personalization
plan: 08
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - src/components/layout/EditorPreviewLayout.tsx
  - src/components/layout/EditorPreviewLayout.module.css
  - src/components/layout/LayoutModeToggle.tsx
  - src/components/layout/LayoutModeToggle.module.css
  - src/components/document/DocumentWorkspace.tsx
  - src/app/(main)/w/[wsId]/d/[docId]/page.tsx
  - tests/layout/resize-clamp.test.ts
autonomous: true
requirements: [EDIT-12]
user_setup: []
estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "제목 입력 행 우측 3버튼 세그먼트로 split/editor-only/preview-only를 전환하고, 현재 모드에 따라 EditorPreviewLayout grid가 바뀐다 (EDIT-12)"
    - "split 모드에서 에디터|미리보기 경계를 드래그하면 비율이 20~80%로 클램프되며 바뀌고, 드래그 종료 시 1회 쿠키에 저장된다"
    - "레이아웃 모드/리사이즈 비율이 쿠키에 영속돼 새로고침 후에도 유지되고, RSC가 첫 렌더에 반영한다(FOUC 없음)"
    - "editor-only/preview-only에서는 리사이즈 핸들이 DOM에 없다(패널이 하나뿐)"
  artifacts:
    - src/components/layout/EditorPreviewLayout.tsx (grid 변형 + 리사이즈 핸들)
    - src/components/layout/LayoutModeToggle.tsx (3버튼 세그먼트)
    - src/app/(main)/w/[wsId]/d/[docId]/page.tsx (layoutMode/splitRatio 쿠키 읽기)
    - tests/layout/resize-clamp.test.ts (20~80% 클램프 순수 함수)
  key_links:
    - "RSC d/[docId]/page.tsx cookies(layoutMode/splitRatio) → DocumentWorkspace initial props → EditorPreviewLayout grid-template-columns"
    - "리사이즈 mousemove → clampRatio(20~80) → mouseup 1회만 document.cookie splitRatio 저장(RESEARCH Pattern 7)"
---

<objective>
개인화의 레이아웃 축(EDIT-12 split/editor-only/preview-only + 패널 리사이즈)을 얹는다. `EditorPreviewLayout`의 고정 `1fr 1fr` grid를 레이아웃 모드/리사이즈 비율에 따른 동적 값으로 바꾸고(기존 클래스명·구조 유지, 값만 조건부), split 모드에 네이티브 드래그 리사이즈 핸들(라이브러리 없음, RESEARCH Pattern 7)을 추가한다. 레이아웃 토글은 제목 입력 행 우측 3버튼 세그먼트, 영속화는 쿠키(테마와 일관), RSC가 첫 렌더에 반영(FOUC 없음). 05-01/05-02가 확립한 EditorPreviewLayout 위에 얹으므로 이 plan은 Wave 3다.

Purpose: EDIT-12의 레이아웃/리사이즈 절반(테마 절반은 05-07).
Output: grid 변형 + 리사이즈 핸들 + LayoutModeToggle + 쿠키 읽기.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-editor-enhancements-personalization/05-CONTEXT.md
@.planning/phases/05-editor-enhancements-personalization/05-RESEARCH.md
@.planning/phases/05-editor-enhancements-personalization/05-UI-SPEC.md
@src/components/layout/EditorPreviewLayout.tsx
@src/components/document/DocumentWorkspace.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EditorPreviewLayout grid 변형 + 네이티브 리사이즈 핸들 + clampRatio</name>
  <files>src/components/layout/EditorPreviewLayout.tsx, src/components/layout/EditorPreviewLayout.module.css, tests/layout/resize-clamp.test.ts</files>
  <read_first>
    - src/components/layout/EditorPreviewLayout.tsx(05-02 수정본) — .grid/.editorPane/.previewPane 클래스, forwardRef getView
    - src/components/layout/EditorPreviewLayout.module.css — 현재 grid-template-columns: minmax(0,1fr) minmax(0,1fr), .editorPane border-right
    - 05-UI-SPEC.md "Layout & Composition Contract"(38-39행) — grid 변형(split={ratio}% minmax(0,1fr) / editor-only=1fr / preview-only=1fr), 리사이즈 핸들(6px, position:absolute right:-3px, idle는 1px 보더만)
    - 05-UI-SPEC.md "Split-Pane Resize Contract"(196-204행) — 20~80% 클램프, 기본 50/50, mouseup 1회 저장, split에서만 핸들
    - 05-RESEARCH.md Pattern 7(299-323행) — mousedown(핸들) → mousemove/mouseup(window), body cursor/userSelect 고정 완성 코드 + Pitfall 10
  </read_first>
  <behavior>
    - clampRatio(50) → 50, clampRatio(10) → 20, clampRatio(95) → 80, clampRatio(20)/clampRatio(80) → 경계값 그대로
  </behavior>
  <action>
    먼저 tests/layout/resize-clamp.test.ts를 RED로 커밋(TDD): `clampRatio(pct)` 순수 함수(20~80 클램프)를 별도 export(예: EditorPreviewLayout에서 export하거나 작은 lib 함수)로 두고 경계·초과·미만 케이스를 assert.
    EditorPreviewLayout에 props `layoutMode: "split" | "editor-only" | "preview-only"`, `initialSplitRatio: number`(기본 50)를 추가한다. `.grid`의 grid-template-columns를 레이아웃 모드에 따라 조건부로: split=`${ratio}% minmax(0,1fr)`(인라인 스타일 또는 CSS 변수), editor-only=`1fr`(previewPane·구분선 미렌더), preview-only=`1fr`(editorPane·툴바 미렌더, 미리보기만). 기존 클래스명(.grid/.editorPane/.previewPane) 유지. split 모드에서만 리사이즈 핸들(6px 투명 히트 영역, .editorPane border-right 위치에 position:absolute right:-3px)을 렌더하고, RESEARCH Pattern 7대로 mousedown → window mousemove(`clampRatio(((ev.clientX-rect.left)/rect.width)*100)`로 setRatio) → mouseup(window 리스너 제거 + body cursor/userSelect 복원 + `document.cookie = "splitRatio=${ratio}; path=/; max-age=31536000; samesite=lax"` 1회). 드래그 중 body cursor=col-resize + userSelect=none(Pitfall 10). hover/dragging 시 보더 자리에 2px var(--accent) 바(UI-SPEC).
    preview-only에서 툴바가 숨으므로 이미지 업로드 진입점(툴바 버튼)도 함께 사라지는 것은 UI-SPEC과 정합(툴바 없는 모드) — 드롭존은 editorPane이 없으니 자연히 비활성.
  </action>
  <verify>
    <automated>pnpm vitest run tests/layout/resize-clamp.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - clampRatio가 20 미만→20, 80 초과→80으로 클램프됨을 테스트가 assert
    - split에서만 리사이즈 핸들이 렌더되고 editor-only/preview-only에서는 DOM에 없다
    - editor-only는 previewPane, preview-only는 editorPane(+툴바)을 렌더하지 않는다
    - 드래그 종료 시 splitRatio 쿠키가 1회 기록됨(mousemove마다 아님)
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>3개 레이아웃 모드 grid와 20~80% 클램프 드래그 리사이즈가 동작하고, 비율은 mouseup에 쿠키로 저장된다.</done>
</task>

<task type="auto">
  <name>Task 2: LayoutModeToggle + DocumentWorkspace 제목 행 배치</name>
  <files>src/components/layout/LayoutModeToggle.tsx, src/components/layout/LayoutModeToggle.module.css, src/components/document/DocumentWorkspace.tsx</files>
  <read_first>
    - 05-UI-SPEC.md "Layout Mode Toggle Contract"(168-178행) — 3버튼 세그먼트(24×24, gap 4px), Columns2/PanelRightClose/PanelLeftClose 아이콘, 비활성/hover/활성(accent-weak+accent) 상태, aria-label, 클릭 즉시 전환+쿠키
    - 05-UI-SPEC.md "Layout & Composition Contract"(36행) — 토글은 제목 입력 행 우측(새 행 추가 금지, 좌 flex:1 제목 + 우 고정폭 세그먼트)
    - src/components/document/DocumentWorkspace.tsx(전문) — 제목 input 행(titleInput), EditorPreviewLayout 렌더 지점
    - 05-UI-SPEC.md Color "Accent reserved for"(100-104행) — 레이아웃 활성 모드는 accent 허용(툴바 서식-활성 금지와 다른 범주)
  </read_first>
  <action>
    src/components/layout/LayoutModeToggle.tsx("use client")를 만든다: `mode`, `onChange(next)` props. 3버튼 세그먼트(split=Columns2, editor-only=PanelRightClose, preview-only=PanelLeftClose, 각 24×24 gap 4px). 활성 버튼은 배경 var(--accent-weak) + 아이콘 var(--accent), 미선택은 transparent+var(--muted), hover는 var(--surface-2)+var(--text). aria-label "분할 보기"/"에디터만"/"미리보기만"(UI-SPEC Copywriting). 300ms 지연 툴팁은 툴바와 같은 CSS 패턴 재사용. 클릭 시 onChange + `document.cookie = "layoutMode=${next}; path=/; max-age=31536000; samesite=lax"`.
    DocumentWorkspace.tsx: 제목 input 행을 좌(flex:1 titleInput 그대로) + 우(LayoutModeToggle)로 나눈다(새 행 추가 금지, UI-SPEC 36행). layoutMode를 useState로 관리(초기값은 새 prop initialLayoutMode, 05-04 task 3에서 RSC가 내려줌)하고 EditorPreviewLayout에 layoutMode/initialSplitRatio prop을 전달한다. splitRatio 초기값도 새 prop initialSplitRatio로 받아 EditorPreviewLayout에 넘긴다.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/components/layout/LayoutModeToggle.tsx','utf8'); if(!/Columns2/.test(c)||!/PanelRightClose/.test(c)||!/PanelLeftClose/.test(c)) throw new Error('missing lucide icons'); if(!/layoutMode=/.test(c)) throw new Error('no cookie write');"</automated>
  </verify>
  <acceptance_criteria>
    - LayoutModeToggle이 3개 lucide 아이콘 세그먼트로 렌더되고 활성 모드가 accent-weak/accent로 표시됨
    - 클릭 시 layoutMode 쿠키를 직접 쓰고 DocumentWorkspace 상태를 전환
    - 토글이 제목 입력 행 우측에 있고 별도 새 행을 만들지 않는다
    - DocumentWorkspace가 EditorPreviewLayout에 layoutMode/initialSplitRatio를 전달
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>제목 행 우측 3버튼 세그먼트로 레이아웃 모드를 즉시 전환·쿠키 저장한다.</done>
</task>

<task type="auto">
  <name>Task 3: RSC 쿠키 읽기 → DocumentWorkspace 초기 prop</name>
  <files>src/app/(main)/w/[wsId]/d/[docId]/page.tsx</files>
  <read_first>
    - src/app/(main)/w/[wsId]/d/[docId]/page.tsx(전문) — 현재 getDocument 후 DocumentWorkspace 렌더(key/docId/initialTitle/initialContent/initialSeq props)
    - 05-RESEARCH.md Pattern 6(283-286) + "테마 RSC 읽기"(548-563) — await cookies() 관례(여기선 layoutMode/splitRatio)
    - 05-RESEARCH.md Pitfall 9 — 이 페이지는 requireRole로 이미 동적 렌더링(cookies() 추가 비용 없음)
    - 05-PATTERNS.md "w/[wsId]/layout.tsx cookies 읽기"(303-313) — 레이아웃모드/리사이즈비율은 문서 페이지에서 읽어 prop 전달
  </read_first>
  <action>
    d/[docId]/page.tsx(RSC)에서 `const cookieStore = await cookies();`(next/headers)로 `layoutMode`(split|editor-only|preview-only, 없으면 "split")와 `splitRatio`(숫자, 없으면 50)를 읽어 DocumentWorkspace에 `initialLayoutMode`/`initialSplitRatio` prop으로 내려준다. 잘못된 쿠키 값은 안전 기본값(split/50)으로 폴백. requireRole가 이미 이 라우트를 동적 렌더링시키므로 cookies() 추가 비용 없음(Pitfall 9).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && node -e "const c=require('fs').readFileSync('src/app/(main)/w/[wsId]/d/[docId]/page.tsx','utf8'); if(!/cookies\(\)/.test(c)) throw new Error('no cookies read'); if(!/initialLayoutMode/.test(c)) throw new Error('no layoutMode prop');"</automated>
  </verify>
  <acceptance_criteria>
    - RSC가 await cookies()로 layoutMode/splitRatio를 읽어 DocumentWorkspace에 prop으로 전달
    - 쿠키 없음/이상값 시 split/50으로 폴백
    - tsc --noEmit clean
  </acceptance_criteria>
  <done>RSC가 레이아웃 모드/비율 쿠키를 첫 렌더에 반영해 새로고침 후에도 FOUC 없이 유지된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → document.cookie (layoutMode/splitRatio) | 비민감 UI 설정. 클라 소유(서버 검증 대상 아님) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08-COOKIE | Tampering | layoutMode/splitRatio 쿠키 | low | mitigate | 비민감 값이나 RSC가 읽을 때 잘못된 값은 안전 기본값(split/50)으로 폴백 — 조작해도 자기 화면 레이아웃만 바뀜. clampRatio가 비율을 20~80으로 재클램프 |

*신규 패키지 설치 없음 — Package Legitimacy Gate 대상 없음(T-05-SC N/A).*
</threat_model>

<verification>
- `pnpm vitest run tests/layout/resize-clamp.test.ts` — 클램프 순수 함수 green
- `pnpm exec tsc --noEmit` clean
- grep 게이트(lucide 아이콘·쿠키 쓰기·RSC cookies 읽기) 통과
- 수동(Manual-Only, VALIDATION): split에서 경계 드래그 → 비율 변경·새로고침 유지, 모드 3종 전환
</verification>

<success_criteria>
레이아웃 모드 3종 전환과 20~80% 드래그 리사이즈가 동작하고, 모드·비율이 쿠키로 영속돼 RSC가 첫 렌더에 반영한다.
</success_criteria>

## Artifacts this phase produces (05-08)

- `src/components/layout/EditorPreviewLayout.tsx` + `.module.css` — grid 변형 + 네이티브 리사이즈 핸들 + clampRatio
- `src/components/layout/LayoutModeToggle.tsx` + `.module.css` — 3버튼 세그먼트
- `src/components/document/DocumentWorkspace.tsx` — 제목 행 토글 배치 + 레이아웃 prop 전달
- `src/app/(main)/w/[wsId]/d/[docId]/page.tsx` — layoutMode/splitRatio 쿠키 읽기
- `tests/layout/resize-clamp.test.ts`

<output>
Create `.planning/phases/05-editor-enhancements-personalization/05-08-SUMMARY.md` when done
</output>
