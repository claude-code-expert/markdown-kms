---
phase: 04-documents-autosave-3-pane-workspace
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/app/api/documents/[id]/route.ts
  - src/components/tree/FolderTree.tsx
  - src/components/tree/FolderTreeNode.tsx
  - src/components/tree/DocumentTreeLeaf.tsx
  - src/components/document/DocumentWorkspace.tsx
  - tests/documents/crud.test.ts
  - e2e/document-workspace.spec.ts
autonomous: true
requirements: [DOC-01]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "문서 노드 컨텍스트 메뉴 '삭제' → 확인 다이얼로그 → 소프트삭제되면 트리에서 즉시 사라지고 휴지통으로 이동한다"
    - "DELETE /api/documents/:id는 EDITOR+만 허용하고 workspace_id를 서버에서 재유도한다 (IDOR)"
    - "열람 중인 문서를 삭제하면 w/[wsId] index(빈 상태)로 이동한다"
  artifacts:
    - src/app/api/documents/[id]/route.ts (DELETE 소프트삭제)
    - src/components/tree/DocumentTreeLeaf.tsx (컨텍스트 메뉴 '삭제' 배선)
    - tests/documents/crud.test.ts ('소프트삭제 → 휴지통 즉시 반영' 케이스)
  key_links:
    - "DELETE route → softDeleteDocument(04-01) → is_deleted/is_trash_root=true → getWorkspaceDocuments가 더 이상 반환 안 함(트리에서 사라짐)"
    - "DocumentTreeLeaf 삭제 → 열람 중이면 router.push(/w/[wsId]) (삭제된 문서를 에디터에 못 띄움)"
---

<objective>
tracer(04-02)가 증명한 슬라이스에서 문서 소프트삭제를 확장한다. DELETE /api/documents/:id 라우트(EDITOR+, IDOR 서버 재유도)를 folders DELETE와 동형으로 추가하고, 트리 문서 노드에 1항목 컨텍스트 메뉴("삭제")를 배선해 ConfirmDialog로 소프트삭제한다. 삭제된 문서는 트리에서 즉시 사라지고(휴지통 노출은 04-05 UI) 열람 중이면 빈 상태로 이탈한다.

Purpose: DOC-01의 "삭제는 소프트 삭제로 즉시 휴지통에 나타난다"를 실제 문서 노드 경로로 완성한다. 폴더 cascade 소프트삭제(하위 문서 포함)는 04-04가 소유한다.
Output: DELETE 소프트삭제 라우트 + 트리 삭제 메뉴/확인 + 통합/e2e 검증.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md
@src/app/api/folders/[id]/route.ts
@src/components/tree/FolderTree.tsx
@src/components/tree/FolderContextMenu.tsx
@src/components/ui/ConfirmDialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DELETE /api/documents/:id 소프트삭제 라우트 (TDD)</name>
  <read_first>
    - src/app/api/folders/[id]/route.ts (43-63행 DELETE) — 소프트삭제 라우트 동형(resolveActiveWorkspaceId → requireRole(EDITOR) → softDeleteFolder → 204). documents는 resolveWorkspaceIdForDocument + softDeleteDocument로 치환.
    - src/lib/documents.ts (04-01) — resolveWorkspaceIdForDocument / softDeleteDocument.
    - src/app/api/documents/[id]/route.ts (04-02 PUT) — 같은 파일에 DELETE 핸들러 추가(PUT 무변경).
    - tests/documents/crud.test.ts (04-02) — 여기에 소프트삭제 케이스 추가.
    - tests/rbac/helpers.ts — mockSessionFor / addMember로 역할별 호출자 위조.
  </read_first>
  <behavior>
    - DELETE /api/documents/:id: resolveWorkspaceIdForDocument(id) null이면 403, requireRole(EDITOR) 실패 403(VIEWER 거부), 성공 시 softDeleteDocument → 204.
    - 소프트삭제 후: getWorkspaceDocuments(wsId)가 그 문서를 더 이상 반환하지 않고, 해당 행은 is_deleted=true·is_trash_root=true·deleted_at 세팅(휴지통 즉시 반영의 근거).
    - 이미 삭제된 문서에 재-DELETE: idempotent(softDeleteDocument WR-01 동형), 204.
    - 잘못된 uuid 경로 파라미터: 400.
  </behavior>
  <action>
    tests/documents/crud.test.ts에 위 behavior 실패 케이스를 먼저 추가한다(RED). 그다음 src/app/api/documents/[id]/route.ts에 DELETE 핸들러를 folders/[id]/route.ts DELETE 그대로 미러링해 추가한다(z.uuid() 경로 검증 → resolveWorkspaceIdForDocument → requireRole(EDITOR) → softDeleteDocument → `new Response(null, { status: 204 })`). PUT 핸들러는 건드리지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/documents/crud.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - VIEWER가 DELETE를 호출하면 403이고 EDITOR는 204임을 단언하는 케이스가 GREEN.
    - 소프트삭제 후 getWorkspaceDocuments가 그 문서를 반환하지 않고 is_trash_root=true임을 단언하는 케이스가 GREEN.
    - `pnpm vitest run` 전체 GREEN.
  </acceptance_criteria>
  <done>DELETE 소프트삭제 라우트가 RBAC/IDOR를 통과하고 삭제된 문서가 활성 목록에서 사라짐이 증명된다.</done>
</task>

<task type="auto">
  <name>Task 2: 트리 문서 노드 '삭제' 메뉴 + 확인 다이얼로그 + 열람중 삭제 이탈</name>
  <read_first>
    - src/components/tree/FolderTree.tsx (137-183행 openMenu/menuItems, 123-135행 confirmDelete, 221-237행 ConfirmDialog) — 문서 삭제도 이 패턴 재사용.
    - src/components/tree/FolderContextMenu.tsx (전체) — items 1개("삭제")만 전달하는 방식.
    - src/components/tree/DocumentTreeLeaf.tsx (04-02) — kebab/우클릭 → onOpenDocMenu 배선 추가.
    - src/components/ui/ConfirmDialog.tsx (전체) — destructive prop 재사용.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md (Tree Node Contract 컨텍스트 메뉴 행, Interaction Contract 문서 삭제, Copywriting) — 1항목 메뉴 "삭제"(Trash2), 확인 문구, 열람 중 삭제 시 index 이동.
    - src/components/document/DocumentWorkspace.tsx (04-02) — 필요 시 삭제 후 라우팅 처리 지점.
  </read_first>
  <action>
    DocumentTreeLeaf에 우클릭/kebab → 문서 전용 메뉴(FolderContextMenu에 1항목: label "삭제", icon Trash2, destructive) 트리거를 배선한다. FolderTree.tsx에 문서 삭제 상태(docDeleteTarget)와 confirmDeleteDocument(fetch DELETE /api/documents/:id → 실패 시 인라인 에러 "문서를 삭제하지 못했어요. 다시 시도해 주세요." → 성공 시 router.refresh())를 추가한다. ConfirmDialog는 UI-SPEC Copywriting 그대로: 제목 "'{문서명}' 문서를 삭제할까요?", 본문 "휴지통으로 이동합니다. 휴지통에서 복원할 수 있어요.", 확인 "삭제"(destructive), 진행 중 pending 스피너(Phase 3 관례). 문서 이름 변경은 트리 메뉴에 넣지 않는다(중복 진입점 방지 — 제목은 문서 상단 입력에서).
    열람 중 삭제 이탈: 현재 라우트 docId가 삭제 대상과 같으면 삭제 성공 후 router.push(`/w/${wsId}`)(빈 상태) — 삭제된 문서를 에디터에 띄워둘 수 없으므로(UI-SPEC). 판단은 usePathname/useParams로 현재 docId를 읽어 처리.
    e2e/document-workspace.spec.ts에 삭제 흐름을 이어붙인다: 문서 노드 우클릭/kebab → 삭제 → 확인 → 트리에서 사라짐 + (열람 중이었다면) 빈 상태로 이동 확인.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/document-workspace.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 문서 노드 메뉴가 정확히 1항목("삭제")이고 이름 변경·이동 항목이 없다.
    - 삭제 확인 후 트리에서 노드가 사라지고, 열람 중이던 문서였다면 w/[wsId] 빈 상태로 이동함을 e2e가 확인한다.
    - 삭제 실패 시 인라인 에러 문구가 UI-SPEC Copywriting과 일치한다.
    - `pnpm exec playwright test e2e/document-workspace.spec.ts` GREEN, `pnpm tsc --noEmit` 통과.
  </acceptance_criteria>
  <done>문서 노드에서 소프트삭제까지 UI 경로가 완성되고 열람 중 삭제 이탈이 e2e로 확인된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → DELETE /api/documents/:id | URL docId만 신뢰, workspace_id는 서버 재유도 후 requireRole(EDITOR) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03-IDOR | Information Disclosure | DELETE route | high | mitigate | resolveWorkspaceIdForDocument로 workspace 재유도, null이면 403(존재/미존재 비노출) |
| T-04-03-RBAC | Elevation of Privilege | DELETE route | high | mitigate | requireRole(wsId, EDITOR) — VIEWER 소프트삭제 거부, UI disabled는 보안 아님 |
| T-04-03-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 설치 없음(04-RESEARCH) |
</threat_model>

<verification>
- `pnpm vitest run tests/documents/crud.test.ts` GREEN(소프트삭제 케이스 포함).
- `pnpm exec playwright test e2e/document-workspace.spec.ts` GREEN(삭제 흐름 포함).
- `pnpm vitest run` 전체 + `pnpm tsc --noEmit` 회귀 없음.
</verification>

<success_criteria>
- 문서 소프트삭제가 EDITOR+·IDOR 안전하게 동작하고 트리에서 즉시 사라진다.
- 열람 중 문서를 삭제하면 빈 상태로 이탈한다.
</success_criteria>

## Artifacts this phase produces (04-03)

| 종류 | 이름 | 비고 |
|------|------|------|
| route | DELETE /api/documents/:id | 소프트삭제(EDITOR) 204 |
| ui | DocumentTreeLeaf 컨텍스트 메뉴 "삭제" | 1항목, Trash2 |
| ui | 문서 삭제 ConfirmDialog | destructive |
| behavior | 열람 중 삭제 → w/[wsId] 이탈 | |
| test | tests/documents/crud.test.ts (소프트삭제 케이스) | |

<output>
Create `.planning/phases/04-documents-autosave-3-pane-workspace/04-03-SUMMARY.md` when done
</output>
