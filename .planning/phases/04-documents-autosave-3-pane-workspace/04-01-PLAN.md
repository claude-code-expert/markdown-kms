---
phase: 04-documents-autosave-3-pane-workspace
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/lib/documents.ts
  - src/lib/validation.ts
  - drizzle/0003_*.sql
  - drizzle/meta/*
  - tests/documents/helpers.ts
  - tests/documents/autosave-seq-guard.test.ts
autonomous: false
requirements: [DOC-01, EDIT-07]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "document 테이블이 마이그레이션되어 PG16(5433)에 존재하고 활성 조회 부분 인덱스를 탄다"
    - "autosaveDocument(id, content, title, seq)가 saved_seq < seq일 때만 반영하고, 역순(stale) seq는 0행으로 자연 무시된다 (EDIT-07 / TRD §7)"
    - "getDocument(docId, wsId)가 workspace_id까지 스코프해 IDOR를 차단한다 (RESEARCH Pitfall 6)"
  artifacts:
    - src/db/schema.ts (document 테이블)
    - drizzle/0003_*.sql (마이그레이션)
    - src/lib/documents.ts (createDocument/getDocument/getWorkspaceDocuments/resolveWorkspaceIdForDocument/softDeleteDocument/autosaveDocument)
    - src/lib/validation.ts (documentSchema/autosaveBodySchema)
    - tests/documents/autosave-seq-guard.test.ts
    - tests/documents/helpers.ts (createTestDocument)
  key_links:
    - "document.folder_id → folder.id (ON DELETE CASCADE 없음 — 완전삭제 FK 순서의 근거, RESEARCH Pitfall 4)"
    - "autosaveDocument의 WHERE saved_seq < seq → Postgres가 자동저장 순서의 유일한 심판 (TRD §7)"
---

<objective>
Phase 4의 데이터·서비스 substrate를 만든다. TRD §3 DDL 그대로 `document` 테이블을 스키마에 추가하고 마이그레이션하며, `src/lib/documents.ts`에 문서 CRUD/소프트삭제/자동저장(seq 가드) 서비스 함수를 `src/lib/closure.ts`의 DbClient 주입 패턴 그대로 작성한다. 자동저장 seq 가드는 이 phase 정확성의 핵심이므로 실패 테스트를 먼저 커밋한 뒤(TDD RED) 구현한다.

Purpose: 뒤이은 tracer(04-02)·소프트삭제(04-03)·휴지통(04-04) 플랜이 전부 이 테이블과 서비스 함수를 import한다. 여기서 seq 가드 SQL이 정확하지 않으면 이후 모든 자동저장이 조용히 틀린다.
Output: `document` 테이블 + 마이그레이션 + `lib/documents.ts` 서비스 + zod 스키마 + 서버 seq-가드 통합 테스트(GREEN).
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md
@docs/TRD.md
@src/db/schema.ts
@src/lib/closure.ts
@src/lib/validation.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: document 마이그레이션 one-way 게이트 (비가역 결정)</name>
  <decision>`document` 테이블을 추가하는 신규 마이그레이션(drizzle/0003_*.sql)을 지금 생성·적용할지</decision>
  <context>
    03-01의 folder 마이그레이션과 동일한 비가역 게이트다. 마이그레이션 적용은 로컬 PG16(5433) 스키마를 되돌리기 어려운 방향으로 바꾼다(one-way). document 테이블은 TRD §3 DDL로 이미 확정돼 재논의 대상이 아니며, 되돌림은 down-migration이 아니라 새 마이그레이션으로만 가능하다. STATE.md 기록상 03-01의 동일 게이트는 사용자가 orchestrator 통해 사전 승인(apply-now)했다.
  </context>
  <reversibility rating="one-way">스키마 마이그레이션 적용은 down 스크립트가 없어 되돌리려면 별도 보정 마이그레이션이 필요하다.</reversibility>
  <options>
    <option id="apply-now">
      <name>지금 생성·적용</name>
      <pros>tracer(04-02)가 곧바로 실제 테이블 위에서 동작. Phase 3 게이트와 동일 관례.</pros>
      <cons>비가역 — 되돌리려면 보정 마이그레이션 필요.</cons>
    </option>
    <option id="defer">
      <name>보류</name>
      <pros>추가 검토 시간 확보.</pros>
      <cons>Phase 4 전체가 블록됨 — 모든 플랜이 이 테이블에 의존.</cons>
    </option>
  </options>
  <resume-signal>Select: apply-now 또는 defer</resume-signal>
</task>

<task type="auto">
  <name>Task 2: document 테이블 스키마 + 마이그레이션 + zod 스키마</name>
  <read_first>
    - docs/TRD.md §3 (94-110행) — `document` DDL 원문. 이 표를 컬럼·기본값·인덱스까지 문자 그대로 옮긴다.
    - src/db/schema.ts (49-91행 folder/folderClosure) — Drizzle 관례(camelCase 컬럼명, `references(..., { onDelete })`, 부분 인덱스 `.where(sql\`...\`)`) analog.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md (32-60행) — document 테이블 pattern 예시(bigint import 추가).
    - src/lib/validation.ts (38-48행 folderSchema) — zod 스키마 analog.
  </read_first>
  <action>
    src/db/schema.ts에 `document` 테이블을 추가한다. 컬럼은 TRD §3 DDL 그대로: id(uuid pk defaultRandom), workspaceId(uuid notNull references workspace.id onDelete cascade), folderId(uuid references folder.id — **onDelete 지정 없음**, RESEARCH Pitfall 4의 완전삭제 FK 순서 근거), title(text notNull default "제목 없음" — TRD §3 DDL의 `DEFAULT '제목 없음'` 그대로), content(text notNull default ""), savedSeq(bigint mode:"number" notNull default 0 — `drizzle-orm/pg-core`에서 bigint를 신규 import), isDeleted(boolean notNull default false), isTrashRoot(boolean notNull default false), deletedAt(timestamp withTimezone nullable), createdAt/updatedAt(timestamp withTimezone notNull defaultNow). 인덱스는 `document_active_idx`(workspaceId, folderId) `.where(sql\`${table.isDeleted} = false\`) 하나만 — folder_active_idx와 동일 관례.
    document_tag·document_draft 테이블과 trigram(gin) 인덱스는 이 phase에서 만들지 않는다(각각 Phase 6/5, CLAUDE.md 불변식) — TRD §3 DDL의 그 부분은 의도적으로 생략한다.
    이어서 src/lib/validation.ts에 documentSchema(title: trim().max(255).catch("") — RESEARCH A2에 따라 상한 255는 유추값이나 DB 컬럼이 text라 값 자체엔 영향 없음; content: z.string() — trim 없음, 개행/공백이 마크다운 의미를 가짐)와 autosaveBodySchema(documentSchema.extend({ seq: z.number().int().nonnegative() }))를 추가한다. 제목 필수(min) 검증은 넣지 않는다 — UI-SPEC이 빈 제목을 placeholder로 허용.
    마지막으로 `pnpm drizzle-kit generate`로 drizzle/0003_*.sql을 생성하고 `pnpm drizzle-kit migrate`로 적용한다(로컬 PG16 5433, .env.local의 DATABASE_URL — worktree 아닌 main tree 실행).
  </action>
  <verify>
    <automated>pnpm drizzle-kit generate 2>&1 | tail -5 && ls drizzle/0003_*.sql && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - drizzle/0003_*.sql이 존재하고 `CREATE TABLE "document"`와 `document_active_idx` 부분 인덱스를 포함한다.
    - 생성된 SQL에 document_tag / document_draft / gin(trgm) 인덱스가 **없다**.
    - `pnpm tsc --noEmit`가 에러 없이 통과한다(schema.ts의 document export와 documentSchema/autosaveBodySchema 타입 포함).
    - document.folder_id 컬럼의 FK에 ON DELETE CASCADE가 지정돼 있지 않다.
  </acceptance_criteria>
  <done>document 테이블이 마이그레이션돼 PG16에 존재하고, documentSchema/autosaveBodySchema가 validation.ts에 있으며 tsc가 통과한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/documents.ts 서비스 + 서버 seq-가드 통합 테스트 (TDD)</name>
  <read_first>
    - src/lib/closure.ts (1-41행) — DbClient 타입 정의·resolveActiveWorkspaceId·import 관례. documents.ts는 이 패턴을 그대로 복제한다.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md (609-708행) — "문서 CRUD" 골격 + "완전삭제/seq 가드" 코드 예시 + Vitest seq-가드 테스트 예시.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md (64-126행) — documents.ts 함수별 pattern.
    - tests/rbac/helpers.ts (전체) — createTestWorkspace/createTestUser/addMember 팩토리 재사용 근거.
    - tests/folder/closure.test.ts (구조 참고) — DB 통합 테스트 파일 구조·import 관례.
    - vitest.config.ts — environment node, DATABASE_URL_TEST, fileParallelism:false.
  </read_first>
  <behavior>
    - autosaveDocument: saved_seq=5인 문서에 seq=3(역순)으로 호출 → returning 배열 길이 0(false 반환), content 미변경(옛 내용이 최신을 덮지 않음). seq=6(신규)으로 호출 → 길이 1(true), content/title/savedSeq 반영.
    - getDocument(docId, wrongWsId): 다른 워크스페이스 id로 조회 시 null 반환(IDOR 차단). 올바른 wsId면 행 반환.
    - softDeleteDocument: is_deleted=true, deleted_at 세팅, is_trash_root=true. 이미 is_deleted=true면 idempotent(WR-01 동형 — 추가 변경 없음).
    - resolveWorkspaceIdForDocument: 활성 문서만 workspaceId 반환, 소프트삭제된 문서는 null.
    - getWorkspaceDocuments(wsId): 활성 문서만 flat 목록(id/folderId/title) 반환.
  </behavior>
  <action>
    먼저 tests/documents/helpers.ts에 createTestDocument(workspaceId, folderId, overrides?) 팩토리를 작성한다(tests/rbac/helpers.ts의 createTestWorkspace 패턴 그대로, db.insert(document).returning()). 이어서 tests/documents/autosave-seq-guard.test.ts에 위 behavior의 실패 테스트를 먼저 작성(RED — 아직 autosaveDocument/document 스키마 없음/불완전). 파일 상단에 `vi.mock("@/auth", ...)`는 이 테스트가 requireRole을 안 타므로 불필요.
    그다음 src/lib/documents.ts를 구현한다: closure.ts 12-17행의 `type DbClient` 정의를 복제하고, RESEARCH 골격 그대로 getWorkspaceDocuments / getDocument(workspaceId 스코프) / resolveWorkspaceIdForDocument(활성만) / createDocument / softDeleteDocument(WR-01 idempotency: `AND is_deleted=false` 조건) / autosaveDocument(`.update(...).where(and(eq(id), lt(savedSeq, seq))).returning({id})` 후 `rows.length === 1` 반환)을 작성한다. restoreDocument/permanentlyDeleteDocument는 이 플랜에서 만들지 않는다(04-04).
    autosaveDocument는 raw content를 절대 trim/변형하지 않는다(NFR-5.2·마크다운 의미 보존). 클라이언트가 보낸 값 외 다른 필드(mass-assignment)를 set하지 않는다 — content/title/savedSeq/updatedAt만.
  </action>
  <verify>
    <automated>pnpm vitest run tests/documents/autosave-seq-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/documents/autosave-seq-guard.test.ts의 모든 케이스가 GREEN.
    - 역순 seq(saved_seq보다 작은 값) 호출이 returning 0행이고 문서 content가 최신값 그대로임을 단언하는 케이스가 존재한다.
    - getDocument가 잘못된 workspaceId에 null을 반환함을 단언하는 케이스가 존재한다.
    - `pnpm vitest run` 전체가 기존 811 테스트 포함 GREEN(회귀 없음).
  </acceptance_criteria>
  <done>lib/documents.ts 서비스 6함수가 존재하고 seq-가드 통합 테스트가 GREEN이며 기존 스위트가 회귀 없이 통과한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → 자동저장 API(다음 플랜) | content/title/seq가 이 서비스 계층으로 흐른다 — 여기서 seq 가드가 최종 심판 |
| document.id/workspace_id 경계 | 다른 워크스페이스의 문서 id로 조회 시도(IDOR)를 getDocument의 workspace_id 스코프가 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01-SEQ | Tampering | autosaveDocument | high | mitigate | `WHERE saved_seq < :seq`가 순서 심판 — 역순 seq는 0행. seq-가드 테스트로 회귀 방지 |
| T-04-01-IDOR | Information Disclosure | getDocument | high | mitigate | `WHERE id=:id AND workspace_id=:wsId AND is_deleted=false` 스코프, 불일치 null. IDOR 테스트로 회귀 방지 |
| T-04-01-MASS | Tampering | autosaveDocument/createDocument | medium | mitigate | 명시 컬럼만 set(content/title/savedSeq/updatedAt) — 클라 임의 필드 반영 금지 |
| T-04-01-SC | Tampering | 패키지 설치 | low | accept | 이 phase는 신규 npm 패키지를 설치하지 않는다(04-RESEARCH Package Legitimacy Audit) — 공급망 노출 없음 |
</threat_model>

<verification>
- `pnpm drizzle-kit generate`가 document 테이블 마이그레이션을 생성하고 `migrate`가 적용됨.
- `pnpm vitest run tests/documents/autosave-seq-guard.test.ts` GREEN.
- `pnpm vitest run` 전체 GREEN(회귀 없음), `pnpm tsc --noEmit` 통과.
</verification>

<success_criteria>
- document 테이블이 TRD §3 DDL 그대로(문서 tag/draft/trgm 제외) 마이그레이션됨.
- lib/documents.ts의 6개 서비스 함수가 closure.ts DbClient 패턴으로 존재.
- 서버 seq 가드가 역순 요청을 0행으로 무시함이 테스트로 증명됨.
</success_criteria>

## Artifacts this phase produces (04-01)

| 종류 | 이름 | 비고 |
|------|------|------|
| table | `document` | TRD §3 DDL: id/workspace_id/folder_id/title/content/saved_seq/is_deleted/is_trash_root/deleted_at/created_at/updated_at |
| index | `document_active_idx` | (workspace_id, folder_id) WHERE is_deleted=false |
| migration | drizzle/0003_*.sql | one-way |
| symbol | `document` (schema export) | src/db/schema.ts |
| symbol | `getWorkspaceDocuments/getDocument/resolveWorkspaceIdForDocument/createDocument/softDeleteDocument/autosaveDocument` | src/lib/documents.ts |
| symbol | `documentSchema`, `autosaveBodySchema` | src/lib/validation.ts |
| symbol | `createTestDocument` | tests/documents/helpers.ts |
| test | tests/documents/autosave-seq-guard.test.ts | 서버 seq 가드 |

<output>
Create `.planning/phases/04-documents-autosave-3-pane-workspace/04-01-SUMMARY.md` when done
</output>
