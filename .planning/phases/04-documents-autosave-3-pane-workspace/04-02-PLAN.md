---
phase: 04-documents-autosave-3-pane-workspace
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/api/documents/route.ts
  - src/app/api/documents/[id]/route.ts
  - src/app/(main)/w/[wsId]/layout.tsx
  - src/app/(main)/w/[wsId]/layout.module.css
  - src/app/(main)/w/[wsId]/page.tsx
  - src/app/(main)/w/[wsId]/page.module.css
  - src/app/(main)/w/[wsId]/d/[docId]/page.tsx
  - src/components/document/DocumentWorkspace.tsx
  - src/components/document/DocumentWorkspace.module.css
  - src/components/document/autosave-controller.ts
  - src/components/document/useAutosave.ts
  - src/components/document/SaveStatusBar.tsx
  - src/components/document/SaveStatusBar.module.css
  - src/components/document/EmptyState.tsx
  - src/components/document/EmptyState.module.css
  - src/components/tree/FolderTree.tsx
  - src/components/tree/FolderTreeNode.tsx
  - src/components/tree/DocumentTreeLeaf.tsx
  - src/components/tree/tree-utils.ts
  - tests/documents/crud.test.ts
  - tests/documents/idor.test.ts
  - tests/documents/autosave-controller.test.ts
  - e2e/document-workspace.spec.ts
autonomous: true
requirements: [DOC-01, EDIT-07]
estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "트리에서 '새 문서'로 문서를 만들면 곧바로 w/[wsId]/d/[docId]로 이동해 3분할(사이드바+에디터+미리보기+상태바)에 열린다"
    - "본문/제목을 입력하고 1초 멈추면 PUT /api/documents/:id가 발신되고 상태바가 저장 중→저장됨으로 전이한다"
    - "역순(stale) 응답이 도착해도 상태바가 옛 상태로 되돌아가지 않는다 (autosave-controller가 최신 발신 seq 응답만 반영)"
    - "새로고침 후 다시 열면 마지막 자동저장 내용이 에디터에 그대로 적재된다 (딥링크 복원)"
  artifacts:
    - src/app/api/documents/route.ts (POST 생성)
    - src/app/api/documents/[id]/route.ts (PUT 자동저장)
    - src/app/(main)/w/[wsId]/layout.tsx (사이드바 공유)
    - src/app/(main)/w/[wsId]/d/[docId]/page.tsx (문서 RSC)
    - src/components/document/DocumentWorkspace.tsx
    - src/components/document/autosave-controller.ts
    - src/components/document/useAutosave.ts
    - src/components/document/SaveStatusBar.tsx
    - src/components/tree/DocumentTreeLeaf.tsx
    - e2e/document-workspace.spec.ts
  key_links:
    - "EditorHost.onChange / 제목 input.onChange → useAutosave.scheduleSave → autosave-controller → PUT /api/documents/:id"
    - "layout.tsx가 requireRole(VIEWER)로 사이드바 게이트 + d/[docId]/page.tsx가 getDocument(docId, wsId)로 문서 IDOR 게이트"
    - "DocumentTreeLeaf 클릭 → next/link → w/[wsId]/d/[docId] (딥링크 복원의 진입점)"
---

<objective>
이 플랜은 Phase 4의 TRACER다 — 하나의 end-to-end 경로만 완성한다: **트리 '새 문서' → 3분할 화면에 문서 열림 → 본문/제목 입력 → 1초 후 자동저장 반영(seq 가드) → 새로고침 후 저장 내용 복원.** 이 한 경로가 문서 라이프사이클의 모든 계층(POST/PUT 라우트 · 레이아웃 리팩터 · 자동저장 클라 훅+상태바 · 트리 문서 노드)을 관통해 아키텍처를 증명한다. 소프트삭제·휴지통·전체 상태바 케이스는 04-03/04-04/04-05에서 이 검증된 슬라이스로부터 확장한다.

Purpose: 자동저장 seq 가드가 실제 3분할 화면 위에서 서버·클라 왕복까지 포함해 동작함을 최소 경로로 먼저 증명한다. 여기서 아키텍처가 막히면 열 계층을 다 만든 뒤가 아니라 한 커밋 뒤에 안다.
Output: 생성·열기·자동저장이 관통하는 tracer + 서버 seq 가드 PUT 라우트 + 순수 autosave-controller(신규 의존성 0) + e2e.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md
@src/app/(main)/w/[wsId]/page.tsx
@src/app/api/folders/route.ts
@src/components/layout/EditorPreviewLayout.tsx
@src/components/tree/FolderTree.tsx
@src/components/tree/FolderTreeNode.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 문서 API — POST 생성 + PUT 자동저장(seq 가드) 라우트 (TDD)</name>
  <read_first>
    - src/app/api/folders/route.ts (전체) — POST 생성 + workspaceId 서버 재유도 IDOR 패턴. documents POST가 이것과 동형.
    - src/app/api/folders/[id]/route.ts (전체) — [id] 라우트의 params await + resolveActiveWorkspaceId + requireRole + zod safeParse 관례.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md (283-340행) — Pattern 2 PUT 자동저장 완성 예시(resolveWorkspaceIdForDocument → requireRole(EDITOR) → autosaveBodySchema → autosaveDocument → 200{seq}).
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md (184-215행) — documents route pattern.
    - src/lib/documents.ts (04-01 산출) — createDocument/resolveWorkspaceIdForDocument/autosaveDocument.
    - tests/documents/helpers.ts, tests/rbac/helpers.ts — 테스트 팩토리 + `vi.mock("@/auth")` 관례.
  </read_first>
  <behavior>
    - POST /api/documents { title, folderId, workspaceId? }: folderId 있으면 resolveActiveWorkspaceId(folderId)로 wsId 재유도, 클라 workspaceId 불일치 시 400. folderId=null이면 body.workspaceId를 uuid 검증해 사용. requireRole(wsId, EDITOR) 통과 후 createDocument → 201 { id }. 비멤버 403.
    - PUT /api/documents/:id { content, title, seq }: resolveWorkspaceIdForDocument(id) null이면 403(forbiddenResponse), requireRole(EDITOR) 실패 403, body 검증 실패 400. autosaveDocument 호출 → 항상 200 { seq }(0행 갱신이어도 에러 아님).
    - PUT IDOR: 다른 워크스페이스 문서 id로 EDITOR 아닌 사용자가 저장 시도 → 403.
    - 역순 seq PUT: 200 반환하되 서버 content는 최신값 유지(04-01 seq 가드 재확인).
  </behavior>
  <action>
    tests/documents/crud.test.ts와 tests/documents/idor.test.ts에 위 behavior의 실패 테스트를 먼저 작성한다(RED). 파일 상단 `vi.mock("@/auth", () => ({ auth: vi.fn() }))` + mockSessionFor로 호출자 세션을 위조(tests/rbac/helpers.ts 관례). 라우트 함수를 직접 import해 Request 객체로 호출한다(HTTP 서버 미기동 — 기존 folder 라우트 테스트와 동형).
    그다음 src/app/api/documents/route.ts(POST)를 folders/route.ts 그대로 미러링해 작성(createDocumentBodySchema = documentSchema.pick({title:true}).extend({ folderId: z.uuid().nullable(), workspaceId: z.uuid().optional() })). 그리고 src/app/api/documents/[id]/route.ts에 PUT을 RESEARCH Pattern 2 그대로 작성(runtime="nodejs", z.uuid() 경로 검증, resolveWorkspaceIdForDocument, requireRole(EDITOR), autosaveBodySchema.safeParse, autosaveDocument, Response.json({seq},{status:200})). **GET/DELETE는 이 플랜에서 만들지 않는다** — GET은 TRD §8 표에 없고 RSC가 getDocument를 직접 호출하며(fetch 금지), DELETE(소프트삭제)는 04-03 소관.
  </action>
  <verify>
    <automated>pnpm vitest run tests/documents/crud.test.ts tests/documents/idor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - POST가 201 { id }를 반환하고 folderId 불일치 workspaceId를 400으로 거부하는 케이스가 GREEN.
    - PUT이 비멤버/비-EDITOR를 403으로 거부하고, 다른 워크스페이스 문서 저장 시도를 403으로 막는 케이스가 GREEN.
    - PUT이 역순 seq에도 200을 반환하되 서버 content가 최신값을 유지하는 케이스가 GREEN.
    - src/app/api/documents/[id]/route.ts에 GET 핸들러가 없다(fetch 금지 관례 준수).
  </acceptance_criteria>
  <done>POST 생성·PUT 자동저장 라우트가 IDOR/RBAC/seq 가드를 통과해 GREEN이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3분할 라우트 리팩터 + DocumentWorkspace + 자동저장 훅/상태바 (TDD 컨트롤러)</name>
  <read_first>
    - src/app/(main)/w/[wsId]/page.tsx + page.module.css (전체) — 이관 대상. requireRole(VIEWER)+notFound+getWorkspaceFolders 로직을 layout.tsx로 옮긴다.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md (209-281행 Pattern 1 useAutosave, 342-426행 Pattern 3 layout/d-page) — 완성 코드.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md (Layout & Composition Contract, Document Workspace Contract, Color/Typography) — 3단 세로 배치·상태바 상태별 아이콘/색·제목 입력 스타일.
    - src/components/layout/EditorPreviewLayout.tsx (전체) — DocumentWorkspace가 감쌀 대상(내부 무변경).
    - src/lib/documents.ts (getWorkspaceDocuments/getDocument) + src/lib/closure.ts (getWorkspaceFolders).
    - src/components/tree/FolderTreeNode.module.css (.spinner/.pending) — 상태바 스피너 재사용 근거.
  </read_first>
  <behavior>
    - autosave-controller(순수, React 없음): createAutosaveController({ docId, initialSeq, send, onStatus })가 { scheduleSave(content,title), retry(), reset(initialSeq), dispose() } 반환. send는 (content,title,seq)=>Promise<{ok:boolean}> 주입형(테스트에서 제어).
    - 1초 디바운스: scheduleSave 연속 호출 시 마지막 것만 1000ms 뒤 1회 send. onStatus("saving") 후 응답 시 onStatus("saved"|"error").
    - stale 무시: send A(seq=n) 발신 후 send B(seq=n+1) 발신, A의 Promise가 B보다 늦게 resolve → A 응답은 onStatus를 호출하지 않는다(latestSentSeq !== n). 상태바가 옛 상태로 되돌아가지 않음.
    - reset(문서 전환): 대기 타이머 clear + seq/latestSentSeq를 새 initialSeq로 리셋(옛 문서 저장이 새 문서에 발사되지 않음, RESEARCH Pitfall 2).
    - retry: 현재 pending 내용으로 새 seq 발급해 즉시 send.
  </behavior>
  <action>
    먼저 순수 로직을 src/components/document/autosave-controller.ts로 분리한다(신규 npm 의존성 0 — setTimeout+plain 변수, AbortController 절대 금지 NFR-1.2). React hook(useState/useEffect)은 vitest node 환경에서 renderHook 없이 테스트 불가하고 @testing-library/react·jsdom 미설치이므로, 정확성 핵심인 디바운스+seq 비교를 이 순수 컨트롤러에 담아 tests/documents/autosave-controller.test.ts로 검증한다(RED 먼저 — `vi.useFakeTimers()` + 주입 send가 반환하는 Promise를 수동 resolve해 역순 도착 재현). 그다음 컨트롤러를 GREEN으로 구현.
    이어서 src/components/document/useAutosave.ts를 얇은 래퍼로 작성한다: status를 useState로 들고, useMemo로 컨트롤러 생성(send는 fetch(`/api/documents/${docId}`, PUT ...) 기반, onStatus는 setStatus), useEffect([docId, initialSeq])에서 controller.reset + setStatus("saved"), 언마운트 시 controller.dispose. { status, scheduleSave, retry } 반환.
    src/components/document/SaveStatusBar.tsx는 UI-SPEC Document Workspace Contract "저장 상태 바" 그대로: saving=12px 스피너(FolderTreeNode.module.css .spinner 관례 재사용)+"저장 중…"(muted), saved=lucide Check 14px+"저장됨"(muted, accent/녹색 없음), error=lucide AlertCircle 14px(destructive)+"저장 실패"+인라인 "재시도" 버튼(전용 인라인 스타일, Button 컴포넌트 아님). 초기 상태는 saved 렌더.
    src/components/document/DocumentWorkspace.tsx("use client")는 UI-SPEC Layout Contract 3단 세로: ① 제목 input(Display 20/600, 하단 border, focus 시 border accent, placeholder "제목 없음") ② EditorPreviewLayout(내부 무변경 — initialContent 주입 위해 EditorPreviewLayout이 initialContent/onChange를 받도록 최소 확장하거나 DocumentWorkspace가 EditorHost/Toolbar/PreviewPane를 직접 조립; EditorHost는 uncontrolled라 mount 시 initialContent를 EditorState.doc로 1회 주입) ③ SaveStatusBar. 제목·본문 onChange 둘 다 useAutosave.scheduleSave(content, title) 호출.
    라우트 리팩터: src/app/(main)/w/[wsId]/layout.tsx 신설(page.tsx의 requireRole/notFound/getWorkspaceFolders를 이관 + getWorkspaceDocuments 추가, FolderTree에 folders+documents 전달, {children} 렌더), page.module.css의 .page 규칙을 layout.module.css로 이관. page.tsx는 EmptyState(문서 미열람, Copywriting "문서를 선택해 주세요")만 렌더로 축소. src/app/(main)/w/[wsId]/d/[docId]/page.tsx 신설(RESEARCH Pattern 3: requireRole(VIEWER)→notFound catch, getDocument(docId, wsId) null이면 notFound, DocumentWorkspace에 docId/initialTitle/initialContent/initialSeq 전달). src/components/document/EmptyState.tsx 신설(UI-SPEC Copywriting, 재사용 가능하게 heading/body prop). **RSC는 자기 API 라우트를 fetch하지 않는다** — lib/documents 함수 직접 호출(RESEARCH Anti-pattern).
    EditorPreviewLayout 내부 그리드는 건드리지 않는다(D-P2-03). initialContent 주입을 위해 EditorHost가 초기 doc를 받도록 하는 최소 변경만 허용.
  </action>
  <verify>
    <automated>pnpm vitest run tests/documents/autosave-controller.test.ts && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - autosave-controller.test.ts가 GREEN이고, "A 발신 후 B 발신, A가 늦게 resolve → A 응답이 상태를 바꾸지 않음"을 단언하는 케이스가 존재한다.
    - "1초 전에는 send가 호출되지 않고 1000ms 경과 후 1회만 호출됨"을 단언하는 디바운스 케이스가 존재한다.
    - autosave-controller.ts / useAutosave.ts 어디에도 `AbortController` / fetch `signal` 사용이 없다.
    - w/[wsId]/layout.tsx가 requireRole+getWorkspaceFolders+getWorkspaceDocuments를 소유하고, page.tsx는 EmptyState만 렌더하며, d/[docId]/page.tsx가 getDocument를 직접 호출(fetch 아님)한다.
    - `pnpm tsc --noEmit` 통과.
  </acceptance_criteria>
  <done>3분할 라우트가 layout+d-page로 분리되고 DocumentWorkspace가 자동저장 훅/상태바를 배선하며, 순수 컨트롤러의 stale-무시가 테스트로 증명된다.</done>
</task>

<task type="auto">
  <name>Task 3: 트리 문서 노드 + '새 문서' 생성·이동 + tracer e2e</name>
  <read_first>
    - src/components/tree/FolderTree.tsx (전체) — headerButton(189-196행), submitCreate(64-83행), ctx 구성(141-165행), CreateRootInput(251-286행). "새 문서" 버튼·인라인 입력이 이 패턴을 복제.
    - src/components/tree/FolderTreeNode.tsx (전체) — row 렌더(93-149행), CreateInlineRow(222-254행). DocumentTreeLeaf가 여기서 draggable/DnD/chevron 분기를 제거한 형태.
    - src/components/tree/tree-utils.ts — buildTree/FolderRow 타입(제네릭화 금지 — documentsByFolderId Map 별도 전달, RESEARCH Pitfall 7).
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md (Tree Node Contract 확장, Interaction Contract 새 문서 생성) — 문서 아이콘 FileText, 폴더 먼저·문서 나중, 클릭=Link 이동, "새 문서" 헤더 버튼 FilePlus2, 생성 후 즉시 d/[docId] 이동.
    - src/components/tree/FolderContextMenu.tsx — 문서 1항목 메뉴("삭제")는 04-03. 이 플랜은 생성/열기만.
    - e2e/folder-tree.spec.ts (구조 참고) — Playwright 로그인+워크스페이스 진입 패턴.
  </read_first>
  <action>
    FolderTree.tsx가 documents prop(DocumentRow[])을 받도록 확장하고, `documentsByFolderId: Map<string|null, DocumentRow[]>`를 구성해 FolderTreeNodeCtx에 추가한다(buildTree/tree-utils는 폴더 전용 그대로 — 제네릭화 금지). FolderTreeNode는 자기 node.id(루트 노드 렌더 지점은 folderId=null)에 해당하는 문서 배열을 폴더 자식 렌더 말미에 DocumentTreeLeaf로 붙인다(폴더 먼저·문서 나중).
    src/components/tree/DocumentTreeLeaf.tsx 신설: FolderTreeNode row(93-149행)에서 draggable/DnD 핸들러 전부 제거, 체브론은 항상 chevronSpace, lucide FileText 아이콘, 클릭은 onSelect 토글이 아니라 next/link로 `/w/${wsId}/d/${doc.id}` 이동, 현재 라우트 docId와 일치하면 accent 배경/텍스트(UI-SPEC "현재 열람 중"). pending(삭제 진행)은 Phase 3 스피너 관례 — 실제 삭제 wiring은 04-03.
    "새 문서" 생성: FolderTree headerButton 옆에 FilePlus2 버튼 추가(aria-label "새 문서", 기존 headerButton 스타일 재사용, gap xs, 순서는 새 폴더 → 새 문서). creatingDocumentRoot state로 인라인 입력 행(placeholder "새 문서", FileText 아이콘 — CreateRootInput 복제, documentSchema.pick({title:true})로 검증). Enter 제출 → POST /api/documents { title, folderId, workspaceId } → 성공 시 **router.push(`/w/${wsId}/d/${created.id}`)**(문서만 유일하게 생성 후 즉시 이동, UI-SPEC). 실패 시 인라인 에러("문서를 만들지 못했어요. 다시 시도해 주세요."). 폴더 컨텍스트 메뉴 "새 문서" 항목·루트 메뉴 항목은 04-03에서 메뉴 확장과 함께(또는 여기서 헤더 버튼만; 최소 tracer 경로는 헤더 버튼으로 충분).
    e2e/document-workspace.spec.ts 신설(Playwright): 로그인 → 워크스페이스 진입 → 헤더 "새 문서" → 제목 입력 → Enter → d/[docId] URL 진입 확인 → 에디터에 본문 입력 → 상태바 "저장 중"→"저장됨" 관찰 → 페이지 새로고침 → 본문/제목이 그대로 복원됨 확인. (자동저장 1초 디바운스 대기 포함.)
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/document-workspace.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e가 GREEN: 새 문서 생성 → d/[docId] 이동 → 입력 → 상태바 저장됨 → 새로고침 후 내용 복원까지 관통.
    - 트리에 폴더가 먼저, 문서가 나중으로 렌더되고 문서 노드 클릭이 d/[docId]로 이동한다.
    - DocumentTreeLeaf에 draggable 속성이 없다(문서 드래그 범위 밖, YAGNI).
    - tree-utils.ts의 buildTree 시그니처가 변경되지 않았다(문서는 Map으로 별도 전달).
    - `pnpm vitest run` 전체 GREEN, `pnpm tsc --noEmit` 통과.
  </acceptance_criteria>
  <done>트리에서 새 문서를 만들면 3분할 문서 화면으로 이동해 입력·자동저장·새로고침 복원이 e2e로 관통 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → POST/PUT /api/documents | title/content/seq/folderId/workspaceId 입력이 서버로 — workspaceId는 인가에 절대 사용 안 함(서버 재유도) |
| URL docId → d/[docId]/page.tsx | 다른 워크스페이스 docId 주입(IDOR)을 getDocument(docId, wsId)가 차단 |
| 네트워크 응답 도착 순서 → 상태바 | 역순 응답이 상태를 되돌리지 못하게 autosave-controller가 seq로 심판 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02-IDOR-DOC | Information Disclosure | d/[docId]/page.tsx, PUT route | high | mitigate | getDocument(docId, wsId) + resolveWorkspaceIdForDocument로 workspace 재유도, 불일치 notFound()/403 |
| T-04-02-RBAC | Elevation of Privilege | POST/PUT route | high | mitigate | requireRole(wsId, EDITOR) 서버 전용, 클라 workspaceId 불신 |
| T-04-02-SEQ | Tampering | autosave-controller + PUT | high | mitigate | 클라 seq 비교 + 서버 WHERE saved_seq<seq. AbortController 금지(NFR-1.2). controller 테스트로 회귀 방지 |
| T-04-02-MASS | Tampering | createDocumentBodySchema/autosaveBodySchema | medium | mitigate | zod pick/extend로 허용 필드만 파싱 — is_deleted/saved_seq 조작 등 차단 |
| T-04-02-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 설치 없음(04-RESEARCH) |
</threat_model>

<verification>
- `pnpm vitest run tests/documents/crud.test.ts tests/documents/idor.test.ts tests/documents/autosave-controller.test.ts` GREEN.
- `pnpm exec playwright test e2e/document-workspace.spec.ts` GREEN.
- `pnpm vitest run` 전체 + `pnpm tsc --noEmit` 회귀 없음.
</verification>

<success_criteria>
- 생성→열기→입력→자동저장→새로고침 복원의 단일 경로가 모든 계층을 관통해 동작.
- stale 응답이 상태바를 되돌리지 않음이 순수 컨트롤러 테스트로 증명됨.
- 3분할 라우트가 layout/d-page/empty로 분리됨.
</success_criteria>

## Artifacts this phase produces (04-02)

| 종류 | 이름 | 비고 |
|------|------|------|
| route | POST /api/documents | 문서 생성(EDITOR) |
| route | PUT /api/documents/:id | 자동저장 seq 가드(EDITOR), 200 {seq} |
| route(RSC) | w/[wsId]/layout.tsx | 사이드바 공유 + 멤버십 게이트 |
| route(RSC) | w/[wsId]/d/[docId]/page.tsx | 문서 로드(IDOR 가드) |
| route(RSC) | w/[wsId]/page.tsx | 문서 미열람 빈 상태로 축소 |
| symbol | `DocumentWorkspace` | src/components/document/DocumentWorkspace.tsx |
| symbol | `createAutosaveController` | src/components/document/autosave-controller.ts (순수, 신규 의존성 0) |
| symbol | `useAutosave` | src/components/document/useAutosave.ts |
| symbol | `SaveStatusBar` | 저장 중/저장됨/저장 실패+재시도 |
| symbol | `EmptyState` | 재사용(문서 미열람/휴지통) |
| symbol | `DocumentTreeLeaf` | 문서 리프 노드(draggable 없음) |
| test | tests/documents/crud.test.ts, idor.test.ts, autosave-controller.test.ts | |
| e2e | e2e/document-workspace.spec.ts | tracer 관통 |

<output>
Create `.planning/phases/04-documents-autosave-3-pane-workspace/04-02-SUMMARY.md` when done
</output>
