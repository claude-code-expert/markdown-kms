---
phase: 04-documents-autosave-3-pane-workspace
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/lib/closure.ts
  - src/lib/documents.ts
  - src/app/api/trash/[type]/[id]/restore/route.ts
  - src/app/api/trash/[type]/[id]/route.ts
  - tests/trash/restore.test.ts
  - tests/trash/permanent-delete.test.ts
  - tests/trash/rbac.test.ts
  - tests/folder/closure.test.ts
autonomous: true
requirements: [DOC-01, DOC-02]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "폴더 소프트삭제가 같은 트랜잭션에서 하위 문서까지 소프트삭제한다 (이미 독립 트래시된 문서의 is_trash_root은 건드리지 않음)"
    - "복원은 trash_root 서브트리를 활성화하되, 폴더 삭제 전에 독립적으로 트래시됐던 하위 항목은 되살리지 않는다 (Open Q #2)"
    - "원 폴더가 삭제 상태면 복원 대상은 워크스페이스 루트로 재배치된다 (moveFolder 재사용)"
    - "완전삭제는 document 행을 folder 행보다 먼저 DELETE해 FK 위반을 피하고, ADMIN+만 허용한다"
  artifacts:
    - src/lib/closure.ts (softDeleteFolder document cascade + restoreFolder + permanentlyDeleteFolder + resolveWorkspaceIdForTrashItem + getTrashItems)
    - src/lib/documents.ts (restoreDocument + permanentlyDeleteDocument)
    - src/app/api/trash/[type]/[id]/restore/route.ts (EDITOR+)
    - src/app/api/trash/[type]/[id]/route.ts (DELETE, ADMIN+)
    - tests/trash/restore.test.ts, tests/trash/permanent-delete.test.ts, tests/trash/rbac.test.ts
  key_links:
    - "restoreFolder → 부모 삭제 시 moveFolder(folderId, null, tx) 재사용 (closure 재작성 중복 금지)"
    - "permanentlyDeleteFolder → document DELETE 먼저 → folder DELETE (folder_closure는 ON DELETE CASCADE 자동정리)"
    - "trash 라우트 → resolveWorkspaceIdForTrashItem(type, id) (is_deleted 필터 없는 비활성-허용 헬퍼) → requireRole(restore:EDITOR / permanent:ADMIN)"
---

<objective>
tracer(04-02)로 증명된 스키마·서비스 위에서 휴지통 백엔드를 확장한다. Phase 3 softDeleteFolder에 문서 cascade를 추가하고, 휴지통 전용(비활성 행 허용) 헬퍼로 복원(cascade + 루트 재배치 + 독립-트래시 항목 보존)과 완전삭제(FK 순서 + ADMIN)를 구현하며, TRD §8 통합 경로 `/api/trash/:type/:id`로 노출한다. 이 플랜은 UI가 없다 — 04-05가 이 백엔드를 소비한다.

Purpose: DOC-02(복원 cascade + 완전삭제 ADMIN)와 DOC-01의 폴더 cascade 소프트삭제를 서버에서 정확히 소유한다. RESEARCH가 [VERIFIED]로 확인한 두 함정(활성-전용 헬퍼 오용, document.folder_id FK 순서)과 [ASSUMED/A3]로 남긴 독립-트래시 배제 규칙(Open Q #2)을 명시적 태스크+통합 테스트로 확정한다.
Output: closure.ts 휴지통 확장 + documents.ts 복원/완전삭제 + 통합 trash 라우트 + 시나리오 통합 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md
@docs/TRD.md
@docs/PRD.md
@src/lib/closure.ts
@src/lib/documents.ts
@src/app/api/folders/[id]/move/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: softDeleteFolder 문서 cascade + 복원 백엔드 (TDD, Open Q #2 포함)</name>
  <read_first>
    - src/lib/closure.ts (133-155행 softDeleteFolder, 85-131행 moveFolder, 69-83행 getSubtree, 30-41행 resolveActiveWorkspaceId) — 확장 대상 + 재사용 대상. moveFolder(id, null, tx)는 루트 재배치에 그대로 재사용.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md (428-507행 Pattern 4/5, 531-604행 Common Pitfalls #3/#5) — cascade + restore 완성 예시 + 독립-트래시 배제 근거.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md (130-159행) — closure.ts 확장 pattern.
    - docs/TRD.md §4 (163-164행) — 폴더 삭제 cascade + 복원 규칙 원문. docs/PRD.md §2-2/§2-3 (27-28행) — 하위 일괄 삭제·복원, 원폴더 삭제 시 루트 복원.
    - tests/folder/closure.test.ts (구조) + tests/documents/helpers.ts (createTestDocument).
  </read_first>
  <behavior>
    - softDeleteFolder cascade: 폴더 A(하위 폴더 B, A직속 문서 X, B직속 문서 Y) 삭제 → A/B is_deleted=true, X/Y is_deleted=true·deleted_at 세팅, is_trash_root은 A에만. **이미 is_deleted=true였던 하위 문서(독립 트래시)의 is_trash_root은 유지**(현재 활성 문서만 cascade 대상: `AND is_deleted=false`).
    - restoreFolder(A): trash_root=true인 A만 복원 가능(아니면 null). closure 직접 조인으로 is_deleted=true 서브트리 수집(getSubtree 재사용 불가 — is_deleted=false 필터 박힘). 서브트리에서 **자기 자신 아니면서 is_trash_root=true인 폴더는 제외**(독립 트래시 보존). restorableIds의 폴더 is_deleted=false·deleted_at=null, A의 is_trash_root=false. 문서는 `is_trash_root=false`인(cascade로 딸려왔던) 것만 복원.
    - Open Q #2 시나리오: 폴더 A 아래 문서 X를 먼저 독립 소프트삭제(X.is_trash_root=true) → A 전체 삭제 → A 복원 → **X는 여전히 휴지통에 남아야 함**(is_deleted=true 유지).
    - 루트 재배치: A.parent_id가 있고 그 부모가 삭제 상태면 moveFolder(A, null, tx) 재사용, 반환 { relocatedToRoot: true }. 부모가 활성이면 { relocatedToRoot: false }.
    - restoreDocument(id): 단일 문서(trash_root) 복원 — is_deleted=false·deleted_at=null·is_trash_root=false.
    - resolveWorkspaceIdForTrashItem(type, id): is_deleted 필터 없이 folder/document에서 workspaceId 재유도(비활성 허용). getTrashItems(wsId): folder ∪ document에서 is_trash_root=true인 항목(type/id/name·title/deleted_at) 반환.
  </behavior>
  <action>
    tests/folder/closure.test.ts에 "document cascade" 케이스(활성/독립-트래시 문서 구분)를 추가하고, tests/trash/restore.test.ts를 신설해 위 behavior(특히 Open Q #2 독립-트래시 보존 + 루트 재배치)의 실패 테스트를 먼저 작성한다(RED).
    그다음 src/lib/closure.ts를 확장한다: (1) softDeleteFolder 트랜잭션 내부, folder UPDATE 다음에 `document WHERE folderId IN (ids) AND is_deleted=false` cascade UPDATE 추가(RESEARCH Pattern 4). (2) resolveWorkspaceIdForTrashItem(type: "folder"|"document", id) 신설 — is_deleted 필터 없음. (3) restoreFolder 신설 — RESEARCH Pattern 5 그대로(closure 직접 조인 서브트리, 독립-트래시 배제 restorableIds 필터, moveFolder(id,null,tx) 루트 재배치). (4) getTrashItems(wsId) 신설(folder/document의 is_trash_root=true UNION 또는 두 쿼리 병합, deleted_at 포함). src/lib/documents.ts에 restoreDocument(id) 추가.
    독립-트래시 배제 규칙은 TRD/PRD 원문에 없는 유추(A3)이므로 반드시 위 Open Q #2 통합 테스트로 고정한다 — 추론을 코드에만 두지 않는다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/trash/restore.test.ts tests/folder/closure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "X 독립 삭제 → A 삭제 → A 복원 → X는 여전히 is_deleted=true"를 단언하는 Open Q #2 케이스가 GREEN.
    - "원 부모 삭제 상태에서 복원 → parent_id=NULL(루트) + relocatedToRoot:true"를 단언하는 케이스가 GREEN.
    - softDeleteFolder cascade가 활성 하위 문서만 대상으로 하고 독립-트래시 문서의 is_trash_root을 보존함을 단언하는 케이스가 GREEN.
    - restoreFolder가 getSubtree를 호출하지 않고 closure를 직접 조인한다(is_deleted=false 필터 회피).
    - `pnpm vitest run` 전체 GREEN.
  </acceptance_criteria>
  <done>폴더 cascade 소프트삭제와 복원(독립-트래시 보존·루트 재배치)이 통합 테스트로 증명된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 완전삭제 백엔드 — FK 순서(document 먼저) (TDD)</name>
  <read_first>
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-RESEARCH.md (571-585행 Pitfall 4, 661-687행 완전삭제 코드 예시) — document DELETE 먼저 → folder DELETE, folder_closure는 cascade 자동정리.
    - src/db/schema.ts (75-91행 folder_closure) — ancestor/descendant 둘 다 ON DELETE CASCADE (수동 삭제 불필요 근거).
    - docs/TRD.md §3 (94-106행) — document.folder_id에 ON DELETE CASCADE 없음.
    - src/lib/closure.ts (getSubtree, DbClient) — 완전삭제 서브트리 수집은 is_deleted 필터 없는 closure 직접 조인.
  </read_first>
  <behavior>
    - permanentlyDeleteFolder(A): closure 직접 조인(필터 없음)으로 서브트리 id 수집 → document(folderId IN ids) DELETE 먼저 → folder(id IN ids) DELETE. folder_closure는 cascade 자동정리(수동 DELETE 없음). 한 트랜잭션 — 부분 삭제 불가.
    - FK 순서 검증: 폴더에 문서가 있는 상태에서 folder를 먼저 지우면 FK 위반이 나야 정상 — 구현이 document를 먼저 지우므로 위반 없이 완료됨을 단언.
    - permanentlyDeleteDocument(id): 단일 행 DELETE(자식 없음).
    - 완전삭제 후: 해당 document/folder 행이 DB에 존재하지 않고 folder_closure 관련 행도 사라짐.
  </behavior>
  <action>
    tests/trash/permanent-delete.test.ts를 신설해 위 behavior(특히 "문서 있는 폴더 완전삭제가 FK 위반 없이 완료" + "완전삭제 후 행 부재")의 실패 테스트를 먼저 작성한다(RED). 그다음 src/lib/closure.ts에 permanentlyDeleteFolder를 RESEARCH 예시 그대로 추가(트랜잭션: closure 직접 조인 서브트리 → document DELETE 먼저 → folder DELETE)하고, src/lib/documents.ts에 permanentlyDeleteDocument를 추가한다.
  </action>
  <verify>
    <automated>pnpm vitest run tests/trash/permanent-delete.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - "문서를 포함한 폴더 완전삭제가 FK 위반 없이 성공"을 단언하는 케이스가 GREEN.
    - "완전삭제 후 document/folder/folder_closure 행이 모두 없음"을 단언하는 케이스가 GREEN.
    - permanentlyDeleteFolder가 document DELETE를 folder DELETE보다 먼저 실행한다.
    - `pnpm vitest run` 전체 GREEN.
  </acceptance_criteria>
  <done>완전삭제가 FK 순서를 지켜 문서·폴더·closure를 한 트랜잭션에서 물리 삭제한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 통합 trash 라우트 (restore EDITOR / permanent ADMIN) + RBAC 매트릭스 (TDD)</name>
  <read_first>
    - src/app/api/folders/[id]/move/route.ts (전체) — IDOR 재유도 + 에러 매핑 구조 analog. trash 라우트가 이것과 동형(resolveWorkspaceIdForTrashItem으로 치환).
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md (219-224행) — trash 라우트 골격.
    - docs/TRD.md §8 (240-241행) — POST /api/trash/:type/:id/restore (EDITOR) / DELETE /api/trash/:type/:id (ADMIN) 통합 경로. docs/PRD.md §3 (46-47행) — 복원 EDITOR+, 완전삭제 ADMIN+.
    - src/lib/rbac.ts (requireRole/ROLE_RANK/forbiddenResponse) + tests/rbac/helpers.ts.
    - Task 1/2 산출 (restoreFolder/restoreDocument/permanentlyDeleteFolder/permanentlyDeleteDocument/resolveWorkspaceIdForTrashItem).
  </read_first>
  <behavior>
    - POST /api/trash/:type/:id/restore: type∈{folder,document}, z.uuid(id). resolveWorkspaceIdForTrashItem(type,id) null이면 403. requireRole(EDITOR) 실패 403. type 분기로 restoreFolder/restoreDocument 호출 → 200 { relocatedToRoot } (folder) 또는 204/200 (document). VIEWER 복원 거부(403).
    - DELETE /api/trash/:type/:id: requireRole(ADMIN) — EDITOR/VIEWER 완전삭제 거부(403). type 분기로 permanentlyDeleteFolder/permanentlyDeleteDocument → 204.
    - 잘못된 type(예: "widget") → 400. 잘못된 uuid → 400.
    - RBAC 매트릭스: VIEWER=복원×/완전삭제×, EDITOR=복원○/완전삭제×, ADMIN=복원○/완전삭제○, OWNER=둘 다○ (PRD §3 46-47행).
  </behavior>
  <action>
    tests/trash/rbac.test.ts를 신설해 위 RBAC 매트릭스(4역할 × 2액션)와 type/uuid 검증의 실패 테스트를 먼저 작성한다(RED, `vi.mock("@/auth")` + mockSessionFor/addMember). 그다음 src/app/api/trash/[type]/[id]/restore/route.ts(POST, EDITOR)와 src/app/api/trash/[type]/[id]/route.ts(DELETE, ADMIN)를 move/route.ts 구조 그대로 작성한다(runtime="nodejs", type 화이트리스트 검증, z.uuid(id), resolveWorkspaceIdForTrashItem, requireRole, type 분기 서비스 호출, 에러 매핑). 클라이언트가 보낸 값은 인가에 쓰지 않는다 — id에서 서버가 재유도한 workspaceId만 신뢰.
  </action>
  <verify>
    <automated>pnpm vitest run tests/trash/rbac.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - EDITOR가 완전삭제(DELETE)를 호출하면 403, ADMIN은 204임을 단언하는 케이스가 GREEN.
    - VIEWER가 복원을 호출하면 403, EDITOR는 성공임을 단언하는 케이스가 GREEN.
    - 알 수 없는 type("widget")이 400으로 거부됨을 단언하는 케이스가 GREEN.
    - `pnpm vitest run` 전체 GREEN, `pnpm tsc --noEmit` 통과.
  </acceptance_criteria>
  <done>통합 trash 라우트가 복원(EDITOR)·완전삭제(ADMIN) 권한 매트릭스를 서버에서 강제한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → /api/trash/:type/:id(/restore) | type/id 입력, workspace_id는 resolveWorkspaceIdForTrashItem으로 서버 재유도 |
| 완전삭제 권한 경계 | ADMIN+ 전용 비가역 물리 삭제 — EDITOR 이하 차단 |
| 복원 cascade 경계 | 독립적으로 트래시된 하위 항목을 무단 부활시키지 않음(데이터 노출 방지) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-04-PRIV | Elevation of Privilege | DELETE /api/trash/:type/:id | high | mitigate | requireRole(wsId, ADMIN) — EDITOR/VIEWER 완전삭제 403. RBAC 매트릭스 테스트로 회귀 방지 |
| T-04-04-IDOR-TRASH | Information Disclosure | trash 라우트 | high | mitigate | resolveWorkspaceIdForTrashItem(비활성 허용) 재유도 후 requireRole, 크로스-워크스페이스 차단 |
| T-04-04-RESTORE | Information Disclosure | restoreFolder | medium | mitigate | 독립-트래시 항목 배제(사용자가 지운 문서의 무단 부활 방지), Open Q #2 테스트로 고정 |
| T-04-04-FK | Denial of Service | permanentlyDeleteFolder | medium | mitigate | document→folder DELETE 순서를 단일 트랜잭션에서 강제(부분 삭제 불가), FK 순서 테스트 |
| T-04-04-INJ | Tampering | closure/trash 신규 SQL | medium | mitigate | Drizzle 파라미터 바인딩만 — 문자열 결합 금지(기존 closure.ts 관례) |
| T-04-04-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 설치 없음(04-RESEARCH) |
</threat_model>

<verification>
- `pnpm vitest run tests/trash/restore.test.ts tests/trash/permanent-delete.test.ts tests/trash/rbac.test.ts tests/folder/closure.test.ts` GREEN.
- `pnpm vitest run` 전체 + `pnpm tsc --noEmit` 회귀 없음.
</verification>

<success_criteria>
- 폴더 cascade 소프트삭제가 하위 문서까지 포함하되 독립-트래시 항목을 보존.
- 복원이 cascade + 루트 재배치 + 독립-트래시 배제로 정확히 동작.
- 완전삭제가 FK 순서를 지키고 ADMIN+만 허용.
</success_criteria>

## Artifacts this phase produces (04-04)

| 종류 | 이름 | 비고 |
|------|------|------|
| symbol | `softDeleteFolder` (확장) | 문서 cascade 추가 |
| symbol | `restoreFolder` | 독립-트래시 배제 + moveFolder 루트 재배치 |
| symbol | `permanentlyDeleteFolder` | document 먼저 → folder DELETE |
| symbol | `resolveWorkspaceIdForTrashItem` | 비활성 허용 IDOR 재유도 |
| symbol | `getTrashItems` | folder ∪ document is_trash_root=true |
| symbol | `restoreDocument`, `permanentlyDeleteDocument` | src/lib/documents.ts |
| route | POST /api/trash/:type/:id/restore | EDITOR+ |
| route | DELETE /api/trash/:type/:id | ADMIN+ |
| test | tests/trash/{restore,permanent-delete,rbac}.test.ts | |

<output>
Create `.planning/phases/04-documents-autosave-3-pane-workspace/04-04-SUMMARY.md` when done
</output>
