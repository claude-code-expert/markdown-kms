---
phase: 04-documents-autosave-3-pane-workspace
plan: 05
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - src/app/(main)/w/[wsId]/trash/page.tsx
  - src/components/trash/TrashList.tsx
  - src/components/trash/TrashList.module.css
  - src/components/trash/RestoreRootBanner.tsx
  - src/components/trash/RestoreRootBanner.module.css
  - src/components/tree/FolderTree.tsx
  - src/components/tree/FolderTree.module.css
  - e2e/document-trash.spec.ts
autonomous: true
requirements: [DOC-02]
estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "사이드바 하단 '휴지통' 링크로 w/[wsId]/trash에 들어가면 is_trash_root=true인 폴더·문서가 혼합 목록으로 보인다"
    - "복원 클릭 시 확인 없이 즉시 복원되고 목록에서 사라진다(트리에 다시 나타남); 원 폴더가 삭제 상태였으면 루트-복원 안내 배너가 뜬다"
    - "완전 삭제는 ConfirmDialog 확인 후 비가역 물리 삭제되며, VIEWER/EDITOR에겐 버튼이 비활성+안내로 노출된다(숨김 아님)"
  artifacts:
    - src/app/(main)/w/[wsId]/trash/page.tsx (RSC)
    - src/components/trash/TrashList.tsx
    - src/components/trash/RestoreRootBanner.tsx
    - e2e/document-trash.spec.ts
  key_links:
    - "trash/page.tsx → requireRole(VIEWER) role + getTrashItems(wsId) → TrashList (role로 버튼 게이팅)"
    - "복원/완전삭제 버튼 → POST /api/trash/:type/:id/restore · DELETE /api/trash/:type/:id → router.refresh()"
    - "사이드바 '휴지통' 링크(FolderTree 하단) → w/[wsId]/trash"
---

<objective>
04-04 휴지통 백엔드를 소비하는 UI를 완성한다. 사이드바 하단 "휴지통" 링크, `w/[wsId]/trash` 목록 RSC, 복원(확인 없음)·완전삭제(ADMIN 확인) 액션, 권한 게이팅(비활성+안내), 루트-복원 안내 배너를 UI-SPEC Trash Contract 그대로 조립한다. 기존 컴포넌트(ConfirmDialog/Button/EmptyState/FolderContextMenu)를 확장·재사용하며 새 시각 언어를 발명하지 않는다.

Purpose: DOC-02의 사용자 대면 절반(복원·완전삭제 UI + 권한 투명성)을 완성해 Phase 4 성공 기준 4번을 충족한다.
Output: 휴지통 뷰 + 복원/완전삭제 UI + 루트 배너 + 사이드바 링크 + 휴지통 왕복 e2e.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md
@.planning/phases/04-documents-autosave-3-pane-workspace/04-PATTERNS.md
@src/components/tree/FolderTree.tsx
@src/components/tree/FolderTree.module.css
@src/components/ui/ConfirmDialog.tsx
@src/components/ui/Button.tsx
@src/lib/rbac.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 휴지통 RSC + TrashList + 권한 게이팅</name>
  <read_first>
    - src/app/(main)/w/[wsId]/layout.tsx (04-02) — requireRole(VIEWER) 패턴. trash/page.tsx도 동형 게이트 + role 확보.
    - src/lib/rbac.ts (33-58행 requireRole 반환 { role }, ROLE_RANK) — 게이팅용 role 소스.
    - src/lib/closure.ts (getTrashItems, 04-04) — 목록 데이터.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md (Trash Contract 표: 페이지 헤더/목록 행/권한 게이팅/빈 목록, Typography, Color, Copywriting) — 목록 행 구조·게이팅 문구.
    - src/components/document/EmptyState.tsx (04-02) — 휴지통 빈 상태 재사용(heading "휴지통이 비어있어요").
    - src/app/(main)/w/[wsId]/page.module.css / layout.module.css — .main padding·.title 관례.
    - src/components/tree/FolderTreeNode.module.css (.pending/.spinner) — 삭제 진행 스피너 관례.
  </read_first>
  <action>
    src/app/(main)/w/[wsId]/trash/page.tsx(RSC) 신설: requireRole(wsId, "VIEWER")로 게이트(ForbiddenError→notFound)하고 그 반환 role을 확보, getTrashItems(wsId) 호출, `<TrashList items role wsId />` 렌더. RSC는 자기 API를 fetch하지 않는다(직접 lib 호출).
    src/components/trash/TrashList.tsx("use client") 신설: UI-SPEC Trash Contract 그대로 — 페이지 헤더 "휴지통"(Display 20/600, page.module.css .title 관례), 목록 행(타입별 Folder/FileText 16px + 이름 ellipsis + 삭제 시각 Body 13/400 muted + 우측 액션 2버튼), 빈 목록은 EmptyState 재사용(좌측 정렬 안내). **권한 게이팅**: "복원" 버튼은 ROLE_RANK[role] < EDITOR면 disabled + 옆 안내 "편집자 이상만 복원할 수 있어요"; "완전 삭제" 버튼은 role < ADMIN이면 disabled + "관리자만 완전 삭제할 수 있어요"(UI-SPEC — 숨김 아니라 비활성+설명). Button 컴포넌트 재사용(복원=secondary, 완전삭제=danger). 실제 액션 wiring/배너/ConfirmDialog는 Task 2.
    TrashList.module.css는 기존 트리 hover/행 관례(--surface-2 hover, md padding, 40px 행)를 재사용해 작성 — 신규 팔레트·토큰 없음.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/document-trash.spec.ts -g "휴지통 목록" || pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - trash/page.tsx가 requireRole(VIEWER)로 비멤버를 notFound 처리하고 getTrashItems를 직접 호출(fetch 아님)한다.
    - TrashList가 폴더/문서 혼합 항목을 타입별 아이콘으로 렌더하고 빈 목록은 EmptyState("휴지통이 비어있어요")를 보인다.
    - VIEWER에게 복원·완전삭제 버튼이 disabled+안내로 보이고, EDITOR에겐 복원만 활성, ADMIN에겐 둘 다 활성이다(버튼이 숨겨지지 않음).
    - `pnpm tsc --noEmit` 통과.
  </acceptance_criteria>
  <done>휴지통 목록 RSC와 권한-투명 버튼 게이팅이 UI-SPEC대로 렌더된다.</done>
</task>

<task type="auto">
  <name>Task 2: 복원/완전삭제 액션 wiring + 루트 배너 + 사이드바 휴지통 링크</name>
  <read_first>
    - src/components/tree/FolderTree.tsx (107-135행 moveFolderTo/confirmDelete → router.refresh 패턴, 221-237행 ConfirmDialog) — 복원/완전삭제 액션이 이 뮤테이션 관례 재사용.
    - src/components/ui/ConfirmDialog.tsx (전체, destructive prop) — 완전삭제 확인 재사용(신규 다이얼로그 금지).
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-UI-SPEC.md (Trash Contract 복원 클릭/복원-루트 안내 배너/완전 삭제 클릭 행, Copywriting Contract 전체) — 문구·배너 구조.
    - src/components/tree/FolderTree.module.css (.header/.sidebar flex 구조) — 휴지통 링크를 flex-shrink:0 하단 행으로.
    - src/app/api/trash/[type]/[id]/... (04-04) — 복원 POST / 완전삭제 DELETE 계약.
  </read_first>
  <action>
    TrashList에 액션 wiring 추가: 복원 클릭 → **확인 없이** fetch POST /api/trash/${type}/${id}/restore → 실패 시 인라인 에러("복원하지 못했어요. 다시 시도해 주세요.") → 성공 시 router.refresh(); 응답의 relocatedToRoot가 true면 RestoreRootBanner를 그 항목 이름으로 표시. 완전삭제 클릭 → ConfirmDialog(destructive, 제목 "'{이름}' 완전 삭제할까요?", 본문 "이 작업은 되돌릴 수 없어요. 폴더/문서와 하위 항목이 영구 삭제됩니다.", 확인 "완전 삭제") → 확인 시 fetch DELETE /api/trash/${type}/${id} → 실패 시 "완전히 삭제하지 못했어요..." → 성공 시 router.refresh(). 진행 중 pending 스피너(Phase 3 관례).
    src/components/trash/RestoreRootBanner.tsx 신설: UI-SPEC 배너 그대로 — 헤더 아래 폭 전체, 배경 --surface-2, 좌측 4px --border-strong 보더(accent 예약 목록 안 늘림), lucide Info 16px + 문구 "'{이름}'의 원래 폴더가 삭제되어 워크스페이스 루트로 복원했어요." + 우측 닫기 X 버튼. 자동 타이머 없음(사용자가 닫거나 다음 복원이 교체).
    사이드바 "휴지통" 링크: FolderTree.tsx 하단(tree flex:1 아래 flex-shrink:0 행)에 next/link 추가 — lucide Trash2 16px + "휴지통", href `/w/${wsId}/trash`, 현재 라우트가 /trash면 accent 배경/텍스트(UI-SPEC). FolderTree.module.css에 해당 하단 행 스타일(상단 border, 40px, sm padding) 추가.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/document-trash.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - 복원 클릭이 ConfirmDialog 없이 즉시 POST를 발신하고 성공 시 목록에서 사라진다.
    - 원 폴더 삭제 상태에서 복원 시 RestoreRootBanner가 UI-SPEC 문구로 표시된다.
    - 완전삭제는 ConfirmDialog 확인을 거치며 확인 문구가 Copywriting Contract와 일치한다.
    - 사이드바 하단 "휴지통" 링크가 w/[wsId]/trash로 이동하고 현재 위치를 accent로 표시한다.
    - `pnpm tsc --noEmit` 통과.
  </acceptance_criteria>
  <done>복원·완전삭제 액션과 루트 배너·사이드바 링크가 백엔드와 배선돼 동작한다.</done>
</task>

<task type="auto">
  <name>Task 3: 휴지통 왕복 e2e</name>
  <read_first>
    - e2e/folder-tree.spec.ts / e2e/document-workspace.spec.ts (04-02/04-03) — 로그인·워크스페이스 진입·문서 생성/삭제 헬퍼 패턴.
    - .planning/phases/04-documents-autosave-3-pane-workspace/04-VALIDATION.md (Manual-Only Verifications) — 자동 커버 범위 확인.
    - src/components/trash/TrashList.tsx (Task 1/2 산출) — 셀렉터.
  </read_first>
  <action>
    e2e/document-trash.spec.ts 신설(Playwright): (1) 문서 생성 → 트리에서 소프트삭제(04-03 경로) → 사이드바 "휴지통" 진입 → 그 문서가 목록에 나타남 확인. (2) 복원 클릭 → 목록에서 사라지고 트리에 다시 나타남 확인. (3) 다시 삭제 → 휴지통에서 "완전 삭제" → ConfirmDialog 확인 → 목록에서 사라짐(로그인 사용자가 기본 워크스페이스 EDITOR이므로 완전삭제 ADMIN 게이팅을 검증하려면 별도 role 시드가 필요 — 최소 e2e는 복원 왕복 + 완전삭제 버튼 비활성(EDITOR) 노출 확인으로 하고, ADMIN 완전삭제의 실제 물리삭제는 04-04 통합 테스트가 이미 커버). 상단 describe 주석에 이 분담을 명시.
    폴더 cascade 삭제 → 휴지통에 폴더 1건만 노출(하위 문서는 숨김) → 복원 시 하위 문서 함께 복귀도 가능하면 한 케이스 추가(PRD §2-2 인수 조건, 시간 허용 시).
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/document-trash.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - "삭제 → 휴지통에 표시 → 복원 → 트리 복귀"의 왕복이 e2e로 GREEN.
    - EDITOR 세션에서 "완전 삭제" 버튼이 비활성+안내로 노출됨을 e2e가 확인한다.
    - `pnpm exec playwright test` 전체 GREEN, `pnpm vitest run` 전체 + `pnpm tsc --noEmit` 회귀 없음.
  </acceptance_criteria>
  <done>휴지통 삭제→표시→복원 왕복과 권한 게이팅 노출이 e2e로 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI 버튼 게이팅 → 서버 검증 | disabled+안내는 투명성일 뿐 — 서버 requireRole(04-04)이 유일한 진짜 경계(NFR-3.2) |
| 클라이언트 → 복원/완전삭제 fetch | 04-04 라우트가 workspace 재유도 + RBAC로 최종 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-UIBYPASS | Elevation of Privilege | TrashList 버튼 게이팅 | medium | mitigate | UI disabled는 편의일 뿐, 실제 인가는 04-04 서버 requireRole(EDITOR/ADMIN) — UI 우회해도 서버가 403 |
| T-04-05-CSRF-LIKE | Tampering | 복원/완전삭제 fetch | low | accept | same-origin fetch + Auth.js 세션 쿠키, 기존 folder 뮤테이션과 동일 위협모델(추가 완화 없음) |
| T-04-05-SC | Tampering | 패키지 설치 | low | accept | 신규 패키지 설치 없음(04-RESEARCH) |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/document-trash.spec.ts` GREEN.
- `pnpm exec playwright test` 전체 + `pnpm vitest run` 전체 + `pnpm tsc --noEmit` 회귀 없음.
</verification>

<success_criteria>
- 휴지통 뷰가 is_trash_root 항목을 혼합 표시하고 복원·완전삭제·루트 배너·권한 게이팅이 UI-SPEC대로 동작.
- 삭제→휴지통→복원 왕복이 e2e로 증명됨.
</success_criteria>

## Artifacts this phase produces (04-05)

| 종류 | 이름 | 비고 |
|------|------|------|
| route(RSC) | w/[wsId]/trash/page.tsx | getTrashItems + role 게이팅 |
| symbol | `TrashList` | 목록 + 복원/완전삭제 + 게이팅 |
| symbol | `RestoreRootBanner` | 루트-복원 안내 |
| ui | FolderTree 하단 "휴지통" 링크 | current-route accent |
| e2e | e2e/document-trash.spec.ts | 삭제→휴지통→복원 왕복 |

<output>
Create `.planning/phases/04-documents-autosave-3-pane-workspace/04-05-SUMMARY.md` when done
</output>
