---
phase: 07-workspace-collaboration-join-invite
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/lib/member-search.ts
  - src/lib/members.ts
  - src/app/api/workspaces/[id]/members/search/route.ts
  - tests/invitations/member-search.test.ts
  - tests/members/list.test.ts
autonomous: true
requirements: [WS-05]
user_setup: []
estimate:
  tokens: 42000
  raw_tokens: 23000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "ADMIN이 GET /api/workspaces/:id/members/search?q=로 이메일/이름 부분일치(ILIKE)로 기존 회원을 검색하고, 각 결과에 이 워크스페이스 멤버 여부(isMember)가 표시된다 (WS-05)"
    - "비-ADMIN의 회원 검색 요청은 403이고, ILIKE의 %/_ 메타문자는 리터럴로 이스케이프되어 광범위 매치를 유발하지 않는다 (NFR-3.2, Pitfall 8)"
    - "getWorkspaceMembers가 workspace_member+user 조인으로 멤버 목록(userId·name·email·role)을, getPendingJoinRequests가 PENDING 신청 목록을 반환한다 (멤버 페이지 RSC용)"
    - "검색 쿼리가 drizzle sql 템플릿 바인딩으로만 들어가고 문자열 연결이 없다 (T-06-SQLI 관례 재사용)"
  artifacts:
    - "src/lib/member-search.ts (searchUsersForInvite — ILIKE + isMember 플래그)"
    - "src/lib/members.ts (getWorkspaceMembers, getPendingJoinRequests)"
    - "src/app/api/workspaces/[id]/members/search/route.ts (GET — requireRole ADMIN)"
    - "tests/invitations/member-search.test.ts, tests/members/list.test.ts"
  key_links:
    - "member-search route → searchUsersForInvite → 07-05 InviteSearch UI가 결과+isMember로 초대 버튼/배지 렌더"
    - "getWorkspaceMembers/getPendingJoinRequests → 07-05 members/page.tsx RSC가 직접 await"
    - "search.ts의 .replace(/[\\%_]/g, '\\$&') + ESCAPE '\\' 이스케이프를 member-search가 그대로 재사용"
---

<objective>
회원 초대의 데이터 계층(WS-05 나머지)을 완성한다: ADMIN이 기존 계정을 이메일/이름 부분일치로 검색(각 결과에 isMember 플래그)하는 member-search, 그리고 멤버 페이지 RSC가 소비할 멤버 목록·PENDING 신청 목록 조회 함수.

Purpose: 07-05 멤버 페이지 UI가 클라이언트 fetch 없이(멤버/PENDING은 RSC 직접 조회) 혹은 debounce fetch로(회원 검색) 소비할 데이터 함수를 먼저 검증된 상태로 놓는다. 전부 기존 search.ts·db-membership.ts의 직접 재사용이라 신규 알고리즘이 없다.
Output: src/lib/member-search.ts + src/lib/members.ts + members/search 라우트 + 통합 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-collaboration-join-invite/07-CONTEXT.md
@.planning/phases/07-workspace-collaboration-join-invite/07-RESEARCH.md
@.planning/phases/07-workspace-collaboration-join-invite/07-PATTERNS.md
@src/lib/search.ts
@src/lib/db-membership.ts
@src/app/api/workspaces/[id]/search/route.ts
</context>

## Artifacts this phase produces

| Artifact | 역할 | 소비처 |
|----------|------|--------|
| `src/lib/member-search.ts` | 이메일/이름 ILIKE + isMember | members/search 라우트 → 07-05 InviteSearch |
| `src/lib/members.ts` | 멤버 목록 + PENDING 목록 조회 | 07-05 members/page.tsx RSC |
| `.../members/search/route.ts` (GET) | 회원 검색 라우트(ADMIN) | 07-05 InviteSearch |
| `tests/invitations/member-search.test.ts`, `tests/members/list.test.ts` | ILIKE/isMember/RBAC + 목록 조회 단언 | 회귀 게이트 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: member-search.ts + members/search 라우트 (ILIKE·isMember·ADMIN, TDD)</name>
  <files>src/lib/member-search.ts, src/app/api/workspaces/[id]/members/search/route.ts, tests/invitations/member-search.test.ts</files>
  <read_first>
    - `src/lib/search.ts` 전체 — `.replace(/[\\%_]/g, "\\$&")` + `%${escaped}%` + `ESCAPE '\\'` + drizzle `sql` 템플릿 바인딩(43~61행), `SearchResult`/`SearchRow` 타입 관례(15~27행), `DbClient` 유니언(13행), 빈 q→빈 배열(39행).
    - 07-PATTERNS.md "src/lib/member-search.ts"(129~139행) — search.ts를 user.email/user.name ILIKE + EXISTS(workspace_member)로 변형, `MemberSearchResult`(email/name/isMember) 미러링.
    - `src/app/api/workspaces/[id]/search/route.ts` 전체 — `z.uuid().safeParse(wsId)` + requireRole + `q?.trim()` + 빈 쿼리→빈 배열. minRole만 VIEWER→ADMIN으로 바꿈.
    - 07-RESEARCH.md 플로우 A(122~142행, ILIKE + EXISTS(workspace_member)→isMember 플래그), Pitfall 8(이스케이프 누락 금지), Open Q1(이미 멤버는 검색엔 나오되 UI가 배지 처리).
  </read_first>
  <behavior>
    tests/invitations/member-search.test.ts (통합, 실 DB — RED 먼저):
    - 이메일 부분일치: q가 어떤 회원 이메일의 일부 → 그 회원이 결과에 포함, `{ userId, email, name, isMember }` 형태.
    - 이름 부분일치: q가 이름 일부 → 매치.
    - isMember 정확성: 그 워크스페이스 멤버인 회원은 isMember=true, 아닌 회원은 false.
    - 이스케이프: q에 `%`/`_` 포함 시 리터럴로 매치(광범위 매치 아님) — 예: 이름에 `_`가 없는 회원은 q="a_b"에 매치되지 않음.
    - 빈 q → 빈 배열.
    - RBAC(라우트): VIEWER/EDITOR/비멤버/비로그인 → 403; ADMIN → 200 + results. 잘못된 wsId → 400.
    - SQLi: q=`%' OR '1'='1`가 리터럴로 바인딩되어 전체 회원을 반환하지 않는다.
  </behavior>
  <action>
    `src/lib/member-search.ts`: search.ts 골격 복사. `MemberSearchResult { userId, email, name, isMember }`. `searchUsersForInvite(workspaceId, q, client=db)` — 빈 q면 `[]`. `escaped = q.replace(/[\\%_]/g, "\\$&")`, `pattern = %escaped%`. `sql` 템플릿: `SELECT u.id AS "userId", u.email, u.name, EXISTS(SELECT 1 FROM workspace_member wm WHERE wm.workspace_id = ${workspaceId} AND wm.user_id = u.id) AS "isMember" FROM "user" u WHERE u.email ILIKE ${pattern} ESCAPE '\\' OR u.name ILIKE ${pattern} ESCAPE '\\' ORDER BY u.name` — 모든 변수는 sql 템플릿 placeholder(문자열 연결 금지). isMember 불리언 매핑.

    `members/search/route.ts` (`runtime="nodejs"`): search/route.ts와 동일 구조 — wsId uuid 가드 → `requireRole(wsId,"ADMIN")` try/catch→forbiddenResponse → `rawQ = new URL(req.url).searchParams.get("q")?.trim() ?? ""` → `results = rawQ ? await searchUsersForInvite(wsId, rawQ) : []` → `Response.json({ results })`. (검색어 NFC 재정규화는 이 phase 범위 밖 — 이메일/이름은 단순 ILIKE, search 라우트의 normalizeNFC 단계는 생략해도 무방하나, 일관성을 원하면 동일 적용 가능 — 실행자 재량, 단 함수 내부는 재정규화하지 않음.) 테스트 RED 후 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/invitations/member-search.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - member-search.test.ts 전 케이스 GREEN.
    - 이메일·이름 ILIKE가 부분일치하고 각 결과에 정확한 isMember 불리언이 붙는다.
    - `%`/`_`/`%' OR '1'='1` 입력이 리터럴로 바인딩되어 광범위/주입 매치를 유발하지 않는다 (grep: member-search.ts에 문자열 `+` 연결로 만든 쿼리 부재, `sql` 템플릿만).
    - 라우트가 비-ADMIN에 403, ADMIN에 200을 반환하고 잘못된 wsId에 400.
    - 빈 q가 빈 배열을 반환한다.
  </acceptance_criteria>
  <done>ADMIN 회원 검색이 이스케이프된 ILIKE + isMember 플래그로 동작하고 RBAC·SQLi 방어가 증명된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: members.ts (getWorkspaceMembers + getPendingJoinRequests, TDD)</name>
  <files>src/lib/members.ts, tests/members/list.test.ts</files>
  <read_first>
    - `src/lib/db-membership.ts` 전체(21행) — `listMembershipsForUser`의 innerJoin(workspace_member ⨝ workspace) 구조. 여기선 대상을 반전(workspace_member ⨝ user, workspaceId로 필터).
    - 07-PATTERNS.md "src/lib/members.ts"(143~155행) — getWorkspaceMembers 코드(select userId/name/email/role, innerJoin user, where workspaceId), getPendingJoinRequests는 workspaceJoinRequest ⨝ user + `eq(status,'PENDING')`.
    - `src/db/schema.ts` — user·workspaceMember·workspaceJoinRequest 컬럼.
    - `src/lib/documents.ts` — `DbClient` 유니언 관례.
  </read_first>
  <behavior>
    tests/members/list.test.ts (통합, 실 DB — RED 먼저):
    - getWorkspaceMembers: 여러 역할의 멤버가 있는 워크스페이스 → 각 멤버의 { userId, name, email, role } 반환. 다른 워크스페이스 멤버는 포함되지 않음.
    - getPendingJoinRequests: PENDING 신청 2건 + APPROVED 1건 → PENDING 2건만, 신청자 정보(userId/name/email + reqId + createdAt) 반환. 다른 워크스페이스의 PENDING은 제외.
  </behavior>
  <action>
    `src/lib/members.ts`: `DbClient` 유니언 복사. `getWorkspaceMembers(workspaceId, client=db)` — `client.select({ userId: user.id, name: user.name, email: user.email, role: workspaceMember.role }).from(workspaceMember).innerJoin(user, eq(workspaceMember.userId, user.id)).where(eq(workspaceMember.workspaceId, workspaceId))`. `getPendingJoinRequests(workspaceId, client=db)` — 동일 조인 형태를 `workspaceJoinRequest` 대상으로, `where(and(eq(workspaceJoinRequest.workspaceId, workspaceId), eq(workspaceJoinRequest.status, 'PENDING')))`, 반환에 reqId(workspaceJoinRequest.id)·userId·name·email·createdAt 포함(PendingRequestRow가 요청일·이메일을 렌더). 테스트 RED 후 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/members/list.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - list.test.ts 전 케이스 GREEN.
    - getWorkspaceMembers가 workspaceId로 스코프된 멤버만 { userId, name, email, role }로 반환한다 (다른 워크스페이스 멤버 미포함).
    - getPendingJoinRequests가 status='PENDING'인 신청만 반환하고 reqId·userId·name·email·createdAt을 포함한다.
    - 두 함수 모두 `client: DbClient = db` 마지막 인자를 받는다.
  </acceptance_criteria>
  <done>멤버 페이지 RSC가 소비할 멤버 목록·PENDING 목록 조회가 워크스페이스 스코프로 정확히 동작함이 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저(ADMIN) → members/search | q·wsId는 신뢰 불가. requireRole ADMIN + zod uuid + ILIKE 이스케이프 + sql 바인딩. |
| members.ts ↔ DB | workspaceId 스코프 필터로 타 워크스페이스 멤버/신청 노출 차단. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04-ENUM | Information Disclosure | members/search | medium | mitigate | ADMIN 전용(requireRole) + 최소 필드(userId/email/name/isMember)만 반환 — 회원 열거를 ADMIN으로 제한. member-search.test.ts RBAC 케이스가 증명. |
| T-07-04-SQLI | Tampering | searchUsersForInvite | high | mitigate | drizzle `sql` 템플릿 바인딩 + `%`/`_` 이스케이프 + `ESCAPE '\\'`(search.ts T-06-SQLI 검증 패턴). 주입 케이스 테스트가 증명. |
| T-07-04-IDOR | Elevation of Privilege | members/search·members.ts | high | mitigate | wsId를 URL에서 받아 requireRole로 검증 후 그 wsId로만 스코프 — 타 워크스페이스 데이터 접근 차단. |
| T-07-04-SC | Tampering | npm/pip/cargo installs | high | accept | 신규 패키지 설치 없음(RESEARCH Audit: 0 installs). |
</threat_model>

<verification>
- `pnpm vitest run tests/invitations/member-search.test.ts tests/members/list.test.ts` GREEN.
- `pnpm vitest run` 전체 스위트 회귀 없음.
- `pnpm exec tsc --noEmit` 통과.
</verification>

<success_criteria>
- ADMIN 회원 검색(isMember 포함) + 멤버/PENDING 목록 조회가 완성되고, RBAC·이스케이프·워크스페이스 스코프가 증명됨.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-collaboration-join-invite/07-04-SUMMARY.md` when done
</output>
