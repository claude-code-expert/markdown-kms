---
phase: 07-workspace-collaboration-join-invite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/
  - src/lib/invitation-token.ts
  - tests/invitations/token.test.ts
autonomous: false
requirements: [WS-03, WS-04, WS-05]
user_setup:
  - service: env
    why: "HMAC 서명 시크릿으로 기존 AUTH_SECRET을 재사용한다 (신규 시크릿 미도입)"
    env_vars:
      - name: AUTH_SECRET
        source: ".env.local — 이미 Auth.js v5 로그인이 사용 중인 값 (신규 추가 아님, 존재 확인만)"
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "invitation 테이블이 (id, workspace_id, invitee_id, created_by, expires_at, used_at, created_at)로 존재하고 workspace/user 삭제 시 cascade 된다 (WS-05, TRD §3)"
    - "workspace_join_request 테이블이 (id, workspace_id, user_id, status, decided_by, created_at, decided_at)로 존재하고 status CHECK 제약이 PENDING/APPROVED/REJECTED만 허용한다 (WS-03/WS-04, TRD §3)"
    - "DB에 토큰 원문 컬럼이 없다 — invitation 행은 id·expires_at만 보관하고 서명은 검증 시점에 재계산된다 (WS-05, NFR-3.3)"
    - "encodeInvitationToken이 base64url(invitation_id + '.' + HMAC-SHA256(secret, invitation_id + expires_at-epoch-ms))를 정확히 생성한다 (WS-05, TRD §9)"
    - "verifyMac이 위조/변조된 mac을 timingSafeEqual 상수시간 비교로 거부하고, 발급 시와 검증 시 expires_at을 .getTime() epoch-ms로 동일하게 직렬화한다 (WS-05, RESEARCH Pitfall 3)"
  artifacts:
    - "src/db/schema.ts (invitation, workspaceJoinRequest 테이블 선언)"
    - "drizzle/ 신규 마이그레이션 1건 (invitation + workspace_join_request generate)"
    - "src/lib/invitation-token.ts (encodeInvitationToken / parseInvitationToken / verifyMac 순수 헬퍼)"
    - "tests/invitations/token.test.ts"
  key_links:
    - "invitation.expiresAt (timestamptz) → verifyMac의 .getTime() epoch-ms 직렬화가 발급·검증 양쪽에서 소비 (Pitfall 3)"
    - "workspaceJoinRequest.status CHECK → join-requests.ts의 WHERE status='PENDING' 가드-업데이트가 소비 (07-03)"
    - "invitation-token.ts는 AUTH_SECRET을 인자로만 받는다 → 호출부(invitations.ts·라우트)만 process.env.AUTH_SECRET 참조 (순수성 유지)"
---

<objective>
Phase 7 협업 기능의 기반(substrate)을 놓는다: invitation·workspace_join_request 두 테이블(TRD §3 DDL)과 그 마이그레이션, 그리고 초대 토큰의 크립토 핵심인 HMAC encode/verify 순수 헬퍼를 만든다.

Purpose: 이후 모든 wave가 이 두 테이블과 토큰 헬퍼를 소비한다. 토큰 서명(Pitfall 3의 epoch-ms 직렬화)은 이 phase 유일의 신규 알고리즘이므로, DB 없는 순수 유닛 테스트로 가장 먼저 RED→GREEN을 돌려 가장 큰 불확실성을 초기에 제거한다.
Output: schema.ts 테이블 2개 + drizzle 마이그레이션 + src/lib/invitation-token.ts + 순수 유닛 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-collaboration-join-invite/07-CONTEXT.md
@.planning/phases/07-workspace-collaboration-join-invite/07-RESEARCH.md
@.planning/phases/07-workspace-collaboration-join-invite/07-PATTERNS.md
@src/db/schema.ts
@docs/TRD.md
</context>

## Artifacts this phase produces

| Artifact | 역할 | 소비처 |
|----------|------|--------|
| `src/db/schema.ts` (+invitation, +workspaceJoinRequest) | 데이터 모델 | 07-02(invitations.ts)·07-03(join-requests.ts)·07-04(members.ts) |
| `drizzle/NNNN_*.sql` (신규 마이그레이션) | DB 스키마 반영 | 로컬 PG16@5433 |
| `src/lib/invitation-token.ts` | HMAC 서명 순수 헬퍼 | 07-02(invitations.ts·invitations 라우트) |
| `tests/invitations/token.test.ts` | 토큰 위조/만료/재사용 거부 단언 | 회귀 게이트 |

<tasks>

<task type="auto">
  <name>Task 1: schema.ts에 invitation · workspaceJoinRequest 테이블 추가</name>
  <files>src/db/schema.ts</files>
  <read_first>
    - `src/db/schema.ts` 전체 — 특히 `workspaceMember`(role CHECK 제약 관례, 46행), `document`(FK `onDelete: "cascade"`, nullable timestamp 컬럼 관례), `documentDraft`(단일 컬럼 FK 관례). import는 `check`, `sql`, `timestamp`, `uuid`, `text`가 이미 존재.
    - `docs/TRD.md` §3 — `CREATE TABLE workspace_join_request`(129행), `CREATE TABLE invitation`(139행)의 리터럴 DDL.
    - 07-PATTERNS.md "src/db/schema.ts" 섹션 — usedAt은 nullable timestamp(가드 컬럼, 별도 unique/partial index 없음), status CHECK 패턴 복제.
  </read_first>
  <action>
    schema.ts 끝에 두 테이블을 TRD §3 DDL 그대로 선언한다. `workspaceMember`의 `check(... sql\`${table.role} IN (...)\`)` 관례와 `document`의 FK `.references(() => X.id, { onDelete: "cascade" })` 관례를 그대로 따른다.

    `invitation` (TRD §3, camelCase ↔ snake_case 매핑은 기존 관례대로):
    - `id`: uuid PK defaultRandom
    - `workspaceId`: uuid notNull, references workspace.id, onDelete cascade
    - `inviteeId`: uuid notNull, references user.id, onDelete cascade
    - `createdBy`: uuid notNull, references user.id (onDelete 지정 없음 — TRD DDL이 created_by에 CASCADE를 주지 않음, folder.parentId 선례처럼 리터럴 준수)
    - `expiresAt`: timestamp withTimezone notNull
    - `usedAt`: timestamp withTimezone (nullable — 미사용 상태를 NULL로 표현, document.savedSeq 가드 컬럼과 동형)
    - `createdAt`: timestamp withTimezone notNull defaultNow

    `workspaceJoinRequest` (TRD §3):
    - `id`: uuid PK defaultRandom
    - `workspaceId`: uuid notNull, references workspace.id, onDelete cascade
    - `userId`: uuid notNull, references user.id, onDelete cascade
    - `status`: text notNull default 'PENDING'
    - `decidedBy`: uuid (nullable), references user.id (onDelete 지정 없음 — TRD DDL 준수)
    - `createdAt`: timestamp withTimezone notNull defaultNow
    - `decidedAt`: timestamp withTimezone (nullable)
    - 테이블 콜백에 status CHECK 제약: workspaceMember role CHECK와 동일 형태로 status가 PENDING/APPROVED/REJECTED 중 하나임을 강제.

    partial unique index나 별도 index는 추가하지 않는다 — 중복 PENDING 방지·재사용 방지는 애플리케이션 레벨 WHERE 가드로 처리한다(RESEARCH Alternatives·Open Q2 확정, TRD DDL 무변경).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema.ts`에 `export const invitation`, `export const workspaceJoinRequest` 두 pgTable이 선언된다.
    - invitation은 workspaceId·inviteeId FK가 onDelete cascade, createdBy FK는 cascade 없음, usedAt은 nullable.
    - workspaceJoinRequest는 status text notNull default 'PENDING' + CHECK 제약(PENDING/APPROVED/REJECTED), decidedBy·decidedAt nullable.
    - `pnpm exec tsc --noEmit`가 통과한다.
    - partial unique index 선언이 없다 (grep으로 확인: 두 테이블 콜백에 `unique(` 부재).
  </acceptance_criteria>
  <done>두 테이블이 TRD §3 DDL과 컬럼·제약·FK cascade 정책이 일치하고 타입체크가 통과한다.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 일방향(one-way) invitation/workspace_join_request 마이그레이션 게이트 → generate + apply</name>
  <reversibility rating="one-way">DB 스키마에 신규 테이블 2개를 추가하는 마이그레이션은 되돌리려면 별도 down 마이그레이션과 데이터 파기가 필요하다 — 03-01/04-01/06-01의 스키마 게이트와 동일 성격.</reversibility>
  <decision>invitation · workspace_join_request 두 테이블을 로컬 DB(PG16@5433)에 적용하는 일방향 마이그레이션을 지금 생성·실행할지</decision>
  <read_first>
    - `drizzle.config.ts` — generate/migrate 대상 스키마·마이그레이션 디렉터리 설정.
    - `drizzle/` 기존 마이그레이션들 — Phase 3~6 스키마 게이트의 SQL 형태(선례).
    - STATE.md Decisions — 03-01/04-01/06-01 "one-way 마이그레이션 게이트는 오케스트레이터가 apply-now로 사전 승인" 선례.
    - MEMORY: local-dev-postgres — localhost:5433 무비번, 워크트리엔 .env.local 없음(메인 트리에서 DATABASE_URL 설정 상태로 실행).
  </read_first>
  <context>
    이 마이그레이션은 신규 테이블 2개를 추가한다. Phase 3~6의 스키마 마이그레이션과 동일하게 오케스트레이터가 apply-now로 사전 승인하는 관례를 따른다(STATE.md: 03-01/04-01/06-01 선례 — 재프롬프트 없이 진행). 승인 후 실행할 명령: `pnpm drizzle-kit generate` → `pnpm drizzle-kit migrate`. 워크트리에는 `.env.local`이 없으므로, 마이그레이션은 메인 트리에서 `DATABASE_URL` 설정 상태로 실행한다(MEMORY: local-dev-postgres — localhost:5433 무비번).
  </context>
  <options>
    <option id="apply-now">
      <name>지금 generate + migrate 적용</name>
      <pros>Wave 2 이후 모든 통합 테스트가 실제 테이블 대상으로 즉시 실행 가능. Phase 3~6과 동일 흐름.</pros>
      <cons>일방향 — 롤백하려면 수동 DROP TABLE 필요.</cons>
    </option>
    <option id="defer">
      <name>마이그레이션 SQL만 생성하고 apply는 보류</name>
      <pros>DB 변경 지연.</pros>
      <cons>Wave 2 통합 테스트가 실 테이블 없이 실패 — phase 진행 불가. 권장하지 않음.</cons>
    </option>
  </options>
  <resume-signal>Select: apply-now 또는 defer</resume-signal>
  <action>
    승인(apply-now) 후: 메인 트리에서 `pnpm drizzle-kit generate`로 신규 마이그레이션 SQL을 생성하고(드리즐이 schema.ts의 두 신규 테이블을 감지), `pnpm drizzle-kit migrate`로 로컬 DB에 적용한다. 생성된 SQL이 `CREATE TABLE invitation`·`CREATE TABLE workspace_join_request`와 CHECK/FK를 포함하는지 육안 확인한다. 기존 테이블에 대한 예기치 않은 DROP/ALTER가 SQL에 없어야 한다.
  </action>
  <verify>
    <automated>psql "postgresql://localhost:5433/markdown_kms" -c "\d invitation" -c "\d workspace_join_request"</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/` 아래 신규 마이그레이션 파일 1건이 생성되고 `CREATE TABLE invitation` + `CREATE TABLE workspace_join_request`를 포함한다.
    - 생성 SQL에 기존 테이블(user/workspace/document 등)에 대한 DROP 또는 파괴적 ALTER가 없다.
    - `\d invitation`이 invitee_id·created_by·expires_at·used_at 컬럼을, `\d workspace_join_request`가 status CHECK 제약을 실제 DB에서 보여준다.
  </acceptance_criteria>
  <done>두 테이블이 로컬 DB에 존재하고 통합 테스트가 실제 스키마 대상으로 실행 가능하다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: invitation-token.ts 순수 HMAC 헬퍼 (TDD)</name>
  <files>src/lib/invitation-token.ts, tests/invitations/token.test.ts</files>
  <read_first>
    - 07-RESEARCH.md "Pattern 1: 순수 크립토 헬퍼" 코드 블록(237~286행) — `computeMac`/`encodeInvitationToken`/`parseInvitationToken`/`verifyMac`의 정확한 구현이 리터럴로 제공됨. 이 코드가 구현의 원천이다.
    - 07-RESEARCH.md Pitfall 2(무구분자 연결의 안전성 근거), Pitfall 3(expires_at을 .getTime() epoch-ms로 직렬화하는 이유), Anti-Patterns("timingSafeEqual 앞 길이 체크 생략 금지", "=== 비교 금지").
    - `src/lib/storage.ts` — 순수 판정 함수(sniffImageType)가 DB/세션 없이 입출력만 갖는 격리 관례(정신적 analog).
    - `docs/TRD.md` §9 — `token = base64url(invitation_id + "." + HMAC-SHA256(secret, invitation_id + expires_at))` 리터럴 공식.
  </read_first>
  <behavior>
    tests/invitations/token.test.ts (DB 없는 순수 유닛 — RED 먼저 커밋):
    - encode→parse 왕복: `encodeInvitationToken(id, expiresAt, secret)`의 출력을 `parseInvitationToken`하면 `{ invitationId: id, mac }`이 나온다.
    - 정상 검증: `verifyMac(id, expiresAt, secret, parsed.mac)`이 true.
    - 변조 거부: mac 한 글자를 바꾸면 verifyMac이 false (timingSafeEqual, 예외 던지지 않음).
    - 다른 시크릿 거부: 다른 secret으로 verifyMac하면 false.
    - 다른 expiresAt 거부: 발급 때와 다른 expiresAt(예: +1ms)으로 verifyMac하면 false — epoch-ms 직렬화가 서명에 실제로 반영됨을 증명.
    - epoch-ms 일관성: 같은 순간을 가리키는 두 Date 객체(하나는 new Date(ms), 하나는 DB 왕복 모사)의 .getTime()이 동일해 서명이 일치한다.
    - parse 견고성: base64url이 아닌 쓰레기 문자열, `.` 없는 payload, 빈 invitationId/mac은 모두 null.
  </behavior>
  <action>
    RESEARCH Pattern 1 코드를 `src/lib/invitation-token.ts`로 구현한다. import는 `node:crypto`의 `createHmac`, `timingSafeEqual`만. `computeMac`은 `createHmac("sha256", secret).update(\`${invitationId}${expiresAtMs}\`).digest("base64url")` — expiresAtMs는 `expiresAt.getTime()`(정수). `encodeInvitationToken`은 `Buffer.from(\`${invitationId}.${mac}\`, "utf8").toString("base64url")`. `parseInvitationToken`은 base64url 디코드 실패·구분자 부재·빈 파트를 전부 null로. `verifyMac`은 길이 비교 후 `timingSafeEqual` — 길이가 다르면 예외 없이 false. secret은 인자로만 받는다(process.env 직접 참조 금지 — 07-PATTERNS 격리 원칙). 테스트를 먼저 작성·RED 확인 후 구현으로 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/invitations/token.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/invitations/token.test.ts`가 위 behavior 7개 케이스를 모두 단언하고 GREEN.
    - `src/lib/invitation-token.ts`는 `node:crypto`의 createHmac·timingSafeEqual만 import하고 DB/세션/process.env 참조가 없다 (grep: `process.env` 부재).
    - 변조 mac·다른 secret·다른 expiresAt 케이스가 모두 false를 반환하고, verifyMac이 길이 불일치에서 예외를 던지지 않는다.
    - `===`나 커스텀 바이트 루프로 mac을 비교하지 않는다 (timingSafeEqual만 사용).
  </acceptance_criteria>
  <done>토큰 encode/parse/verify가 TRD §9 공식대로 동작하고, 위조·변조·잘못된 시크릿·잘못된 만료가 상수시간 비교로 거부됨이 순수 유닛 테스트로 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 초대 링크(공개) → 서버 | 토큰은 이메일(dev: 콘솔)로 나가 공개 채널을 건넌다. 서버는 토큰을 신뢰하지 않고 HMAC으로 무결성을 재검증한다. |
| 애플리케이션 ↔ DB | invitation 행은 서명 재계산에 필요한 expires_at을 보관하되, 토큰 원문·mac은 저장하지 않는다(NFR-3.3). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01-FORGE | Tampering, Elevation of Privilege | invitation-token.ts verifyMac | high | mitigate | HMAC-SHA256 재계산 + `timingSafeEqual` 상수시간 비교. `===`·커스텀 루프 금지. Task 3 변조/다른-시크릿 테스트가 증명. |
| T-07-01-SER | Tampering | expires_at 직렬화(발급 vs 검증) | high | mitigate | `.getTime()` epoch-ms 정수로 발급·검증 양쪽 통일(Pitfall 3). 다른-expiresAt 거부 테스트가 증명. |
| T-07-01-STORE | Information Disclosure | invitation 테이블 | medium | mitigate | DB에 토큰 원문·mac 미저장(NFR-3.3). id·expires_at으로 검증 시 재계산. |
| T-07-01-TIMING | Information Disclosure | verifyMac | medium | mitigate | `timingSafeEqual`(상수시간)으로 서명 바이트 추정 방지. |
| T-07-01-SC | Tampering | npm/pip/cargo installs | high | accept | 이 phase는 신규 패키지를 설치하지 않는다(RESEARCH Package Legitimacy Audit: 0 installs, HMAC은 node:crypto 내장). 공급망 표면 없음. |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` 통과 (schema 타입).
- `pnpm vitest run tests/invitations/token.test.ts` GREEN (토큰 순수 유닛).
- `pnpm vitest run` 전체 스위트가 기존 그린 유지(회귀 없음).
- 로컬 DB에 invitation·workspace_join_request 테이블 존재(`\d`).
</verification>

<success_criteria>
- 두 테이블이 TRD §3 DDL과 일치하고 로컬 DB에 적용됨.
- invitation-token.ts가 TRD §9 공식을 정확히 구현하고 위조/변조/만료 거부가 유닛 테스트로 증명됨.
- DB에 토큰 원문이 저장되지 않음(NFR-3.3).
</success_criteria>

<output>
Create `.planning/phases/07-workspace-collaboration-join-invite/07-01-SUMMARY.md` when done
</output>
