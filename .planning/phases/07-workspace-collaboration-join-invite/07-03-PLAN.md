---
phase: 07-workspace-collaboration-join-invite
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/lib/join-requests.ts
  - src/app/api/workspaces/[id]/join-requests/route.ts
  - src/app/api/workspaces/[id]/join-requests/[reqId]/route.ts
  - tests/join-requests/create.test.ts
  - tests/join-requests/decide.test.ts
autonomous: true
requirements: [WS-03, WS-04]
user_setup: []
estimate:
  tokens: 52000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "로그인한 회원(아직 그 워크스페이스 멤버가 아님)이 POST /api/workspaces/:id/join-requests로 가입 신청을 만들 수 있다 (WS-03)"
    - "이미 멤버이거나 이미 PENDING 신청이 있으면 400으로 거부된다 (WS-03, RESEARCH C 플로우)"
    - "ADMIN이 PATCH .../join-requests/:reqId로 승인하면 신청자가 EDITOR로 편입되고 status='APPROVED'가, 거절하면 status='REJECTED'가 된다 (WS-04)"
    - "비-ADMIN의 승인/거절 요청은 403이고, 이미 결정된(PENDING 아님) 신청에 대한 재결정은 409다 (WS-04, NFR-3.2, Pitfall 4)"
    - "승인의 status 갱신 + 멤버십 INSERT가 단일 트랜잭션으로 원자적이고, WHERE status='PENDING' 가드로 이중 결정을 막는다 (Pitfall 4)"
  artifacts:
    - "src/lib/join-requests.ts (createJoinRequest, decideJoinRequest, AlreadyDecidedError)"
    - "src/app/api/workspaces/[id]/join-requests/route.ts (POST — auth 세션만)"
    - "src/app/api/workspaces/[id]/join-requests/[reqId]/route.ts (PATCH — requireRole ADMIN)"
    - "tests/join-requests/create.test.ts, decide.test.ts"
  key_links:
    - "decideJoinRequest 트랜잭션: UPDATE ... WHERE status='PENDING' 가드-업데이트 + (APPROVED이면) INSERT workspaceMember EDITOR onConflictDoNothing"
    - "POST join-requests는 requireRole이 아니라 auth() 세션만 확인 — 신청자는 아직 비멤버(Anti-pattern 준수)"
    - "AlreadyDecidedError → PATCH 라우트가 409로 매핑 (TagLimitError→400 관례와 동형)"
---

<objective>
가입 신청/승인 백엔드(WS-03/WS-04)를 07-02가 증명한 아키텍처(lib DbClient 주입 + 가드-업데이트 트랜잭션 + requireRole 라우트)로 확장한다: 회원이 워크스페이스에 참여 신청 → ADMIN이 PENDING을 승인(EDITOR 편입)/거절.

Purpose: WS-03/WS-04의 서버 로직을 완성한다. 승인 편입은 07-02 acceptInvitation과 동일한 "가드-업데이트 + onConflictDoNothing" 관용구를 재사용하므로 신규 아키텍처가 없다.
Output: src/lib/join-requests.ts + POST/PATCH 라우트 2개 + 통합 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-collaboration-join-invite/07-CONTEXT.md
@.planning/phases/07-workspace-collaboration-join-invite/07-RESEARCH.md
@.planning/phases/07-workspace-collaboration-join-invite/07-PATTERNS.md
@src/lib/documents.ts
@src/app/api/workspaces/route.ts
@src/app/api/workspaces/[id]/route.ts
</context>

## Artifacts this phase produces

| Artifact | 역할 | 소비처 |
|----------|------|--------|
| `src/lib/join-requests.ts` | 신청 생성 + 승인/거절 서비스 | join-requests 라우트·(간접) 07-05 UI |
| `.../join-requests/route.ts` (POST) | 신청 접수(회원) | 07-05 JoinWorkspaceInput |
| `.../join-requests/[reqId]/route.ts` (PATCH) | 승인/거절(ADMIN) | 07-05 PendingRequestRow |
| `tests/join-requests/*.test.ts` | 신청/결정 + RBAC 단언 | 회귀 게이트 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: join-requests.ts (createJoinRequest + decideJoinRequest, TDD)</name>
  <files>src/lib/join-requests.ts, tests/join-requests/decide.test.ts</files>
  <read_first>
    - 07-RESEARCH.md "Pattern 2: 가드-업데이트로 낙관적 잠금"(289~305행), 플로우 C(180~205행)의 decideJoinRequest 트랜잭션 의사코드.
    - 07-PATTERNS.md "src/lib/join-requests.ts" 섹션(111~125행) — 가드-업데이트 코드 + `AlreadyDecidedError`(TagLimitError 동형 커스텀 에러) + APPROVED 시 같은 트랜잭션 INSERT onConflictDoNothing.
    - `src/lib/documents.ts` — `DbClient` 유니언(1~8행), `softDeleteDocument`의 `AND` 가드 no-op 관례(60~65행), `replaceTags`의 트랜잭션 throw-to-rollback + `TagLimitError` 클래스(165행, 172~204행).
    - `src/lib/invitations.ts`(07-02) — acceptInvitation의 트랜잭션 편입 관용구(동일 패턴 재사용).
    - `tests/rbac/helpers.ts` — 팩토리 재사용.
  </read_first>
  <behavior>
    tests/join-requests/decide.test.ts (통합, 실 DB — RED 먼저):
    - approve: PENDING 신청에 decideJoinRequest(reqId, 'APPROVED', adminId) → status='APPROVED', decidedBy=adminId, decidedAt set, workspace_member에 신청자 EDITOR 행 생성.
    - reject: decideJoinRequest(reqId, 'REJECTED', adminId) → status='REJECTED', 멤버십 INSERT 없음.
    - already-decided: 이미 APPROVED/REJECTED인 신청을 다시 결정 → `AlreadyDecidedError` throw(0행 매칭), 상태 불변.
    - 멱등 편입: 신청자가 이미 멤버인 상태에서 approve → onConflictDoNothing으로 중복 멤버 행 없이 status만 APPROVED.
    - 동시 이중 승인 방어: 같은 reqId를 두 번 approve해도 두 번째는 AlreadyDecidedError(WHERE status='PENDING' 가드가 트랜잭션 내 UPDATE에 걸림).
  </behavior>
  <action>
    `src/lib/join-requests.ts`: `DbClient` 유니언 복사. `export class AlreadyDecidedError extends Error {}`. `createJoinRequest(workspaceId, userId, client=db)` — 트랜잭션 불필요, 단순 `insert(workspaceJoinRequest).values({ workspaceId, userId }).returning()`(status는 default 'PENDING'). 중복/이미-멤버 사전 체크는 라우트가 담당(Task 2). `decideJoinRequest(reqId, decision: 'APPROVED'|'REJECTED', adminUserId, client=db)` — `client.transaction`: `update(workspaceJoinRequest).set({ status: decision, decidedBy: adminUserId, decidedAt: new Date() }).where(and(eq(id, reqId), eq(status, 'PENDING'))).returning({ userId, workspaceId })`; 0행이면 `throw new AlreadyDecidedError()`(트랜잭션 롤백); decision==='APPROVED'이면 같은 tx에서 `insert(workspaceMember).values({ workspaceId, userId, role:'EDITOR' }).onConflictDoNothing()`. 반환은 `{ userId, workspaceId, decision }` 정도. 테스트 RED 후 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/join-requests/decide.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - decide.test.ts 전 케이스 GREEN.
    - approve가 status='APPROVED' + EDITOR 멤버 행을 단일 트랜잭션으로 만든다 (grep: `.transaction(`).
    - reject가 멤버십 INSERT 없이 status='REJECTED'만 만든다.
    - 이미 결정된 신청 재결정이 `AlreadyDecidedError`를 던지고 상태를 바꾸지 않는다.
    - WHERE 가드가 `eq(status, 'PENDING')`을 트랜잭션 내부 UPDATE에 건다 (사전 SELECT로 판단하지 않음).
  </acceptance_criteria>
  <done>createJoinRequest/decideJoinRequest가 가드-업데이트 트랜잭션으로 신청 생성·승인(EDITOR 편입)·거절·이중결정 방어를 수행함이 증명된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST join-requests(회원 신청) + PATCH join-requests/:reqId(ADMIN 결정) 라우트</name>
  <files>src/app/api/workspaces/[id]/join-requests/route.ts, src/app/api/workspaces/[id]/join-requests/[reqId]/route.ts, tests/join-requests/create.test.ts</files>
  <read_first>
    - 07-PATTERNS.md "join-requests/route.ts (POST)"(184~192행, auth()만·requireRole 아님) + "join-requests/[reqId]/route.ts (PATCH)"(196~198행, requireRole ADMIN + 이중 uuid 파라미터 + AlreadyDecidedError→409).
    - 07-RESEARCH.md Anti-Patterns "가입 신청 라우트에서 requireRole 호출 금지"(325행) — 신청자는 비멤버라 requireRole은 항상 403.
    - `src/app/api/workspaces/route.ts` — `auth()` 세션 체크 + malformed JSON 가드 관례.
    - `src/app/api/workspaces/[id]/route.ts` — DELETE의 `z.uuid().safeParse(id)` 가드 + requireRole + ForbiddenError→forbiddenResponse catch.
    - `src/app/api/documents/[id]/tags/route.ts` — 커스텀 에러 catch→상태코드 매핑 관례(`if (err instanceof TagLimitError) return 400`), 여기선 AlreadyDecidedError→409.
    - `src/lib/join-requests.ts`(Task 1), `src/db/schema.ts`(workspaceMember, workspaceJoinRequest), `src/auth.ts`(auth).
  </read_first>
  <behavior>
    tests/join-requests/create.test.ts (통합 — RED 먼저): POST 라우트
    - 비멤버 신청 성공: 로그인했으나 그 워크스페이스 멤버가 아닌 사용자 → 201, PENDING 행 생성.
    - 이미 멤버: 요청자가 이미 멤버 → 400, 행 미생성.
    - 중복 PENDING: 이미 PENDING 신청이 있는 사용자 재신청 → 400, 중복 행 미생성.
    - 비로그인: 세션 없음 → 403(forbiddenResponse).
    - 잘못된 wsId(비-uuid) → 400.
    PATCH 라우트 RBAC/결정은 decide.test.ts(Task 1)와 라우트 레벨을 함께 커버:
    - VIEWER/EDITOR/비멤버가 PATCH → 403.
    - ADMIN이 잘못된 decision 값(enum 밖) → 400. 존재하지 않는/이미 결정된 reqId → 409. 잘못된 reqId uuid → 400.
  </behavior>
  <action>
    POST `join-requests/route.ts` (`runtime="nodejs"`): wsId uuid 가드 → `session = await auth()`; `if (!session?.user?.id) return forbiddenResponse()` (requireRole 아님 — Anti-pattern 준수). 이미 멤버? `select workspaceMember where (workspaceId, userId)` → 있으면 400. 이미 PENDING? `select workspaceJoinRequest where (workspaceId, userId, status='PENDING')` → 있으면 400(문구는 UI-SPEC "이미 멤버이거나 이미 신청한 워크스페이스예요."와 정합). → `createJoinRequest(wsId, session.user.id)` → 201.

    PATCH `join-requests/[reqId]/route.ts` (`runtime="nodejs"`): wsId·reqId 둘 다 `z.uuid().safeParse` 가드 → requireRole(wsId,"ADMIN") try/catch→forbiddenResponse → malformed JSON 가드 → `z.object({ decision: z.enum(["APPROVED","REJECTED"]) }).safeParse` → `decideJoinRequest(reqId, decision, session.userId)` try/catch: `if (err instanceof AlreadyDecidedError) return Response.json({error:"이미 처리된 신청이에요."}, {status:409})` → 성공 시 200(또는 204). requireRole의 반환 `{ userId }`를 adminUserId로 전달. 테스트 RED 후 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/join-requests</automated>
  </verify>
  <acceptance_criteria>
    - create.test.ts + decide.test.ts 전체 GREEN.
    - POST 라우트가 `requireRole`을 호출하지 않고 `auth()` 세션만 확인한다 (grep: POST 파일에 `requireRole` 부재).
    - 이미 멤버/중복 PENDING 신청이 400으로 거부되고 중복 행을 만들지 않는다.
    - PATCH가 비-ADMIN에 403, 이미 결정된 신청에 409, 잘못된 decision/uuid에 400을 반환한다.
    - PATCH가 wsId·reqId 두 파라미터를 모두 uuid 검증한다.
  </acceptance_criteria>
  <done>회원 신청 접수(비멤버 세션)와 ADMIN 결정(승인 편입/거절/이중결정 409/RBAC 403)이 라우트 레벨에서 증명된다.</done>
</task>

<task type="auto">
  <name>Task 3: 전체 스위트 회귀 확인 + join-requests RBAC 매트릭스 잔여 케이스 보강</name>
  <files>tests/join-requests/decide.test.ts</files>
  <read_first>
    - `tests/rbac/matrix.test.ts`(1~60행) — 4역할 × 라우트 매트릭스의 `it.each(ROLES)` 관례. 이 phase는 shared matrix.test.ts를 건드리지 않고(파일 소유권 경합 회피), 각 라우트의 RBAC를 자기 feature 테스트 파일에서 4역할로 커버한다.
    - Task 1/2의 decide.test.ts·create.test.ts 현재 케이스.
  </read_first>
  <action>
    decide.test.ts에 PATCH 라우트의 4역할(OWNER/ADMIN이 성공, EDITOR/VIEWER가 403) + 비멤버/비로그인 403을 `it.each` 형태로 보강해, join-requests 결정 라우트의 role-bypass 위협(T-07-03-ADMIN)이 4역할 전부에 대해 명시적으로 단언되게 한다. matrix.test.ts는 수정하지 않는다(각 feature 테스트가 자기 RBAC를 소유 — 07-02 create.test.ts, 07-04 member-search.test.ts와 동일 관례). 그 후 전체 스위트를 돌려 07-01~07-03 산출물이 기존 테스트를 깨지 않았는지 확인한다.
  </action>
  <verify>
    <automated>pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - decide.test.ts가 PATCH에 대해 OWNER·ADMIN 성공 / EDITOR·VIEWER·비멤버·비로그인 403을 4역할+2케이스로 단언한다.
    - `tests/rbac/matrix.test.ts`가 이 plan에서 수정되지 않는다 (git diff에 부재).
    - `pnpm vitest run` 전체 스위트 GREEN(회귀 없음).
  </acceptance_criteria>
  <done>join-requests 결정 라우트의 4역할 RBAC가 자기 feature 테스트에서 완전히 단언되고 전체 스위트가 그린이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저(신청자) → POST join-requests | 신청자는 비멤버. wsId·세션만 신뢰 검증하고, userId는 세션에서 취함(클라이언트 지정 불가). |
| 브라우저(ADMIN) → PATCH join-requests/:reqId | reqId·decision은 신뢰 불가. requireRole ADMIN + zod enum + uuid 가드. |
| PATCH ↔ DB(트랜잭션) | status 갱신 + 멤버십 INSERT가 한 트랜잭션 — 이중 결정/이중 편입 방지. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03-SPOOF | Spoofing | POST join-requests 바디 | high | mitigate | userId를 세션(`session.user.id`)에서만 취함 — 타인 대신 신청 불가. 바디는 wsId 파라미터 외 신뢰 필드 없음. |
| T-07-03-ADMIN | Elevation of Privilege | PATCH join-requests | high | mitigate | `requireRole(wsId,"ADMIN")` 서버 게이트. decide.test.ts 4역할 케이스가 증명. |
| T-07-03-TOCTOU | Repudiation | decideJoinRequest 트랜잭션 | high | mitigate | `WHERE status='PENDING'` 가드-업데이트를 트랜잭션 내 UPDATE에 건다(Pitfall 4). 이중 결정→409. |
| T-07-03-MASS | Tampering, Elevation of Privilege | 승인 편입 role | high | mitigate | 편입 role은 서버 하드코딩('EDITOR'). decision은 zod enum(APPROVED/REJECTED)만. |
| T-07-03-DUP | Tampering | 중복 신청 | low | mitigate | 이미-멤버/중복-PENDING 앱 레벨 체크(Open Q2: partial unique index 미승격, 이중 PENDING이 생겨도 각각 독립 idempotent). |
| T-07-03-SC | Tampering | npm/pip/cargo installs | high | accept | 신규 패키지 설치 없음(RESEARCH Audit: 0 installs). |
</threat_model>

<verification>
- `pnpm vitest run tests/join-requests` GREEN.
- `pnpm vitest run` 전체 스위트 회귀 없음.
- `pnpm exec tsc --noEmit` 통과.
</verification>

<success_criteria>
- 회원 가입 신청(WS-03)과 ADMIN 승인/거절(WS-04) 백엔드가 완성되고 RBAC·이중결정·편입 원자성이 증명됨.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-collaboration-join-invite/07-03-SUMMARY.md` when done
</output>
