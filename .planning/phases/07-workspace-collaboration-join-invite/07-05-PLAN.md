---
phase: 07-workspace-collaboration-join-invite
plan: 05
type: execute
wave: 4
depends_on: ["07-02", "07-03", "07-04"]
files_modified:
  - src/app/(main)/w/[wsId]/members/page.tsx
  - src/components/members/MembersView.tsx
  - src/components/members/MembersView.module.css
  - src/components/members/MemberRow.tsx
  - src/components/members/PendingRequestRow.tsx
  - src/components/members/InviteSearch.tsx
  - src/components/workspace/JoinWorkspaceInput.tsx
  - src/components/workspace/JoinWorkspaceInput.module.css
  - src/app/(main)/dashboard/page.tsx
  - src/app/(main)/dashboard/page.module.css
  - src/components/tree/FolderTree.tsx
  - src/components/tree/FolderTree.module.css
autonomous: true
requirements: [WS-03, WS-04, WS-05]
user_setup: []
estimate:
  tokens: 56000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "w/[wsId]/members 페이지가 멤버 목록(VIEWER+)을 보여주고, ADMIN에게만 '승인 대기 중'·'회원 초대' 섹션을 렌더한다 (WS-04, UI-SPEC — canManage 서버 판정)"
    - "ADMIN이 PENDING 요청을 ConfirmDialog로 승인/거절하면 PATCH 라우트를 호출하고 성공 시 router.refresh()로 목록에서 제거된다 (WS-04)"
    - "ADMIN이 회원 검색(debounce·레이스 가드)으로 기존 계정을 찾아 초대를 보내고, 이미 멤버는 배지로, 발송 성공은 버튼 상태 전환으로 표시된다 (WS-05)"
    - "대시보드에서 워크스페이스 ID 입력으로 참여 신청을 보내고, 성공/이미멤버·중복/일반실패가 인라인 문구로 표시된다 (WS-03)"
    - "FolderTree 하단 nav에 '멤버' 링크가 휴지통 링크 아래에 추가된다"
  artifacts:
    - "src/app/(main)/w/[wsId]/members/page.tsx (RSC — requireRole VIEWER, canManage 판정, lib 직접 호출)"
    - "src/components/members/{MembersView,MemberRow,PendingRequestRow,InviteSearch}.tsx"
    - "src/components/workspace/JoinWorkspaceInput.tsx (대시보드 섹션)"
    - "수정: dashboard/page.tsx, FolderTree.tsx(+membersLink)"
  key_links:
    - "members/page.tsx → getWorkspaceMembers/getPendingJoinRequests(07-04) 직접 await + canManage boolean만 MembersView로 전달(RBAC 서버 전용)"
    - "PendingRequestRow → PATCH .../join-requests/:reqId(07-03), InviteSearch → GET members/search(07-04) + POST invitations(07-02)"
    - "JoinWorkspaceInput → POST .../join-requests(07-03)"
    - "FolderTree membersLink → /w/{wsId}/members (trashLink 블록 복제)"
---

<objective>
백엔드(07-02/03/04)가 증명된 협업 API 위에 UI를 조립해 WS-03/WS-04/WS-05를 사용자에게 노출한다: 멤버 페이지(멤버 목록 + ADMIN 전용 승인 대기·회원 초대), 대시보드 참여 신청 입력, 사이드바 '멤버' 진입점. 전부 TrashList·SearchBox·CreateWorkspaceModal·ConfirmDialog 기존 컴포넌트/패턴의 확장이며 신규 시각값이 없다(UI-SPEC).

Purpose: phase의 사용자향 표면을 완성한다. RBAC는 서버 전용이 원칙이므로(CLAUDE.md), UI는 canManage boolean만 받아 섹션을 렌더/숨김하고 실제 방어는 라우트의 requireRole이 이미 담당한다.
Output: 멤버 페이지 RSC + members 컴포넌트 4종 + 대시보드 참여 신청 + FolderTree 멤버 링크.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-collaboration-join-invite/07-CONTEXT.md
@.planning/phases/07-workspace-collaboration-join-invite/07-PATTERNS.md
@.planning/phases/07-workspace-collaboration-join-invite/07-UI-SPEC.md
@src/app/(main)/w/[wsId]/trash/page.tsx
@src/components/trash/TrashList.tsx
@src/components/tree/SearchBox.tsx
@src/components/workspace/CreateWorkspaceModal.tsx
</context>

## Artifacts this phase produces

| Artifact | 역할 | 소비처 |
|----------|------|--------|
| `w/[wsId]/members/page.tsx` | 멤버 페이지 RSC(canManage 판정) | 사이드바 멤버 링크 진입 |
| `components/members/*` | 멤버 목록·승인/거절·초대 검색 UI | members/page.tsx |
| `components/workspace/JoinWorkspaceInput.tsx` | 대시보드 참여 신청 | dashboard/page.tsx |
| FolderTree membersLink | 멤버 페이지 진입점 | 전 워크스페이스 화면 |

<tasks>

<task type="auto">
  <name>Task 1: 멤버 페이지 RSC + MembersView + MemberRow + PendingRequestRow(승인/거절)</name>
  <files>src/app/(main)/w/[wsId]/members/page.tsx, src/components/members/MembersView.tsx, src/components/members/MembersView.module.css, src/components/members/MemberRow.tsx, src/components/members/PendingRequestRow.tsx</files>
  <read_first>
    - `src/app/(main)/w/[wsId]/trash/page.tsx` 전체 — requireRole(VIEWER)로 role 취득 → notFound catch → `ROLE_RANK[role] >= ROLE_RANK.ADMIN`로 boolean 계산 → 클라이언트엔 boolean만 전달(rbac 모듈이 클라 번들에 안 섞이게).
    - 07-PATTERNS.md "members/page.tsx"(222~234행, canManage 계산 + getWorkspaceMembers/getPendingJoinRequests 직접 await), "components/members/*"(244~269행, TrashList mutation 패턴·ConfirmDialog·per-row 상태).
    - `src/components/trash/TrashList.tsx` 전체 — `"use client"`, useRouter, per-row 상태(restoringId/restoreError), fetch→실패시 인라인 에러→성공시 router.refresh(), ConfirmDialog 사용(137~153행), `Intl.RelativeTimeFormat` "n일 전"(40~49행), `TrashList.module.css`의 `.wrap`/`.row`/`.title`/`.empty`.
    - `src/components/ui/ConfirmDialog.tsx` — destructive binary prop, confirmDisabled, children 인라인 에러.
    - 07-UI-SPEC.md "Members Page Contract — 승인 대기"(113~128행)·"멤버 목록"(132~145행)·Copywriting Contract(211~232행) — 섹션 순서(승인 대기→멤버 목록→회원 초대), 역할 라벨 매핑(OWNER→소유자 등), 배지 스타일, ConfirmDialog 카피.
    - `src/lib/members.ts`(07-04), `src/lib/rbac.ts`(requireRole/ForbiddenError/ROLE_RANK).
  </read_first>
  <action>
    `members/page.tsx` (RSC): trash/page.tsx 골격 복제 — `requireRole(wsId,"VIEWER")` try/catch(ForbiddenError→notFound) → `canManage = ROLE_RANK[role] >= ROLE_RANK.ADMIN` → `members = await getWorkspaceMembers(wsId)` → `pending = canManage ? await getPendingJoinRequests(wsId) : []` → `<MembersView members={members} pending={pending} canManage={canManage} wsId={wsId} />`. 클라이언트엔 boolean·데이터만 전달(rbac import 금지).

    `MembersView.tsx` (`"use client"`): 페이지 셸(`TrashList.module.css` `.wrap` 리듬을 MembersView.module.css로 이식) + 제목 "멤버"(Display) + 3섹션을 UI-SPEC 순서로: (1) canManage일 때만 "승인 대기 중" — pending.map(<PendingRequestRow>), 0건이면 "대기 중인 요청이 없어요"(좌측 정렬 `.empty`); (2) "멤버 목록" — members.map(<MemberRow>); (3) canManage일 때만 "회원 초대" — <InviteSearch wsId={wsId} />(Task 2가 구현, 여기선 import·배치만). 비-ADMIN은 (1)(3) 섹션 자체를 렌더하지 않음(UI-SPEC — disabled+hint 아님).

    `MemberRow.tsx`: 읽기 전용 행 — [이름(Body 13/600) + 이메일(Body 13/400 muted) 세로 스택][역할 배지 우측] — 역할 라벨 매핑(OWNER→"소유자"/ADMIN→"관리자"/EDITOR→"편집자"/VIEWER→"뷰어"), 배지는 pill(radius 999px, surface-2 배경, border, muted 텍스트 — Phase 6 chip 예외 재사용). 긴 이름/이메일 한 줄 말줄임.

    `PendingRequestRow.tsx` (`"use client"`): TrashList mutation 패턴 — per-row submitting 상태, [Mail 16px][이름+이메일][요청일 "n일 전"(Intl.RelativeTimeFormat 재사용)][승인 Button primary][거절 Button secondary]. 승인/거절 클릭 → ConfirmDialog(승인 destructive={false}, 거절 destructive={true}, UI-SPEC 카피) 확인 → `fetch(\`/api/workspaces/${wsId}/join-requests/${reqId}\`, { method:"PATCH", headers, body: JSON.stringify({ decision }) })` → 실패 시 다이얼로그 내 인라인 destructive 문구(닫지 않음) → 성공 시 다이얼로그 닫고 router.refresh(). 카피는 UI-SPEC 그대로.

    module.css는 전부 `var(--token)`만 참조(신규 색상값 없음). 카피 문자열은 UI-SPEC Copywriting Contract와 정확히 일치.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - members/page.tsx가 requireRole(VIEWER)로 role을 얻어 canManage boolean만 클라이언트로 전달하고, `@/lib/rbac`를 클라이언트 컴포넌트가 import하지 않는다 (grep: MembersView/MemberRow/PendingRequestRow에 `@/lib/rbac` 부재).
    - 비-ADMIN(canManage=false)에서 '승인 대기 중'·'회원 초대' 섹션이 렌더 트리에 없다 (조건부 렌더, disabled 아님).
    - PendingRequestRow의 승인/거절이 PATCH `/api/workspaces/{wsId}/join-requests/{reqId}`를 `{ decision }` 바디로 호출하고, 실패 시 다이얼로그를 닫지 않으며 성공 시 router.refresh()한다.
    - 역할 배지 라벨이 OWNER→소유자/ADMIN→관리자/EDITOR→편집자/VIEWER→뷰어로 매핑된다.
    - module.css에 하드코딩 hex가 없다; `pnpm exec tsc --noEmit` + `pnpm vitest run` 통과.
  </acceptance_criteria>
  <done>멤버 페이지가 역할별로 섹션을 노출하고, 멤버 목록(읽기전용)과 ADMIN 승인/거절(ConfirmDialog+PATCH)이 배선된다. (시각·클릭 왕복은 phase 말 수동 검증.)</done>
</task>

<task type="auto">
  <name>Task 2: InviteSearch — 회원 검색(debounce·레이스 가드) + per-row 초대 발송</name>
  <files>src/components/members/InviteSearch.tsx</files>
  <read_first>
    - `src/components/tree/SearchBox.tsx` 전체 — `useSearchResults`의 seqRef 레이스 가드 + 300ms debounce + `idle/loading/results/no-results/error` 상태 머신(38~89행), SearchBox 입력행·spinner, SearchResultsList 렌더 분기. 대상 엔드포인트만 `/api/workspaces/${wsId}/members/search?q=`로 교체.
    - 07-UI-SPEC.md "Members Page Contract — 회원 초대"(149~168행) — 검색 placeholder "이메일 또는 이름으로 검색", idle/typing/loading/results/no-results/error 상태, already-member 배지, 초대 버튼 상태(초대→보내는 중…→초대 보냄 / 실패시 복귀+인라인 에러), Copywriting Contract 초대 문구.
    - `src/components/trash/TrashList.tsx` — per-row 상태(restoringId 패턴)를 sendingId로 복제, Button variant primary/secondary.
    - 07-PATTERNS.md "components/members/*"(265~267행) — InviteSearch가 useSearchResults를 재사용, per-row sendingId.
    - member-search 라우트(07-04) 응답 형태 `{ results: { userId, email, name, isMember }[] }`, invitations POST(07-02) `{ inviteeId }`→201.
  </read_first>
  <action>
    `InviteSearch.tsx` (`"use client"`, props: `{ wsId }`): SearchBox의 `useSearchResults` 구조를 이식하되 엔드포인트를 members/search로, 결과 타입을 `MemberSearchResult`로 교체(placeholder "이메일 또는 이름으로 검색"). 결과 행: [이름(Body 13/600) + 이메일(muted)] + 우측: isMember이면 배지 "이미 멤버"(pill, 버튼 없음) / 아니면 `Button variant="primary"` "초대". 초대 클릭 → per-row `sendingId` set + `fetch(\`/api/workspaces/${wsId}/invitations\`, { method:"POST", headers:{"content-type":"application/json"}, body: JSON.stringify({ inviteeId: result.userId }) })` → 발송 중 그 행 버튼만 "보내는 중…" disabled → 성공(201) 시 그 행 버튼을 `variant="secondary" disabled` "초대 보냄"(Check 14px)로 전환(per-row sent 상태) → 실패 시 버튼 "초대"로 복귀 + 그 행 아래 인라인 destructive 문구 "초대를 보내지 못했어요. 다시 시도해 주세요." 다른 행은 영향 없음(TrashList restoringId per-row 관례). no-results/error 카피는 UI-SPEC. 신규 색상값 없음.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - InviteSearch가 300ms debounce + seqRef 레이스 가드(최신 요청 응답만 반영)를 SearchBox에서 이식해 사용한다.
    - isMember=true 결과 행은 "이미 멤버" 배지만(초대 버튼 없음), false는 "초대" 버튼을 렌더한다.
    - 초대 발송이 POST `/api/workspaces/{wsId}/invitations`를 `{ inviteeId }` 바디로 호출하고, 발송 성공 시 그 행 버튼만 "초대 보냄"으로 전환, 실패 시 그 행에만 인라인 에러를 표시한다(다른 행 불변).
    - `pnpm exec tsc --noEmit` + `pnpm vitest run` 통과.
  </acceptance_criteria>
  <done>ADMIN이 회원을 검색해 초대를 보낼 수 있고, 이미 멤버/발송 성공/실패 상태가 per-row로 표시된다. (실제 검색·발송 왕복은 phase 말 수동 검증.)</done>
</task>

<task type="auto">
  <name>Task 3: 대시보드 참여 신청(JoinWorkspaceInput) + dashboard 섹션 + FolderTree 멤버 링크</name>
  <files>src/components/workspace/JoinWorkspaceInput.tsx, src/components/workspace/JoinWorkspaceInput.module.css, src/app/(main)/dashboard/page.tsx, src/app/(main)/dashboard/page.module.css, src/components/tree/FolderTree.tsx, src/components/tree/FolderTree.module.css</files>
  <read_first>
    - `src/components/workspace/CreateWorkspaceModal.tsx` 전체 — zod client-validate + fetch + 인라인 에러/제출 상태 머신(34~67행). 여기선 Modal이 아닌 인라인 입력행.
    - 07-PATTERNS.md "JoinWorkspaceInput.tsx"(273~296행, handleSubmit 골격 + onChange에서 setFeedback(null)로 다음 keystroke에 문구 소거) + "FolderTree.tsx(수정: membersLink)"(300~315행, trashLink 블록 복제 + Users 아이콘).
    - 07-UI-SPEC.md "Dashboard Join-Request Contract"(172~187행) — 위치(.grid 아래 구분선), placeholder "워크스페이스 ID", 상태(typing/submitting/success-pending/error), Copywriting 문구.
    - `src/app/(main)/dashboard/page.tsx` 전체 + `page.module.css` — `.grid` 아래에 섹션 삽입, RSC 데이터 로직 불변.
    - `src/components/tree/FolderTree.tsx` trashLink 블록(418~427행, Read로 라인 확인) + `FolderTree.module.css`의 `trashLink`/`trashLinkActive` 선언. lucide `Users` 아이콘.
  </read_first>
  <action>
    `JoinWorkspaceInput.tsx` (`"use client"`): CreateWorkspaceModal handleSubmit 골격을 인라인 입력행으로 — `[Input placeholder="워크스페이스 ID" flex:1][Button variant="secondary" "신청"]`(가로 flex gap sm). `z.uuid()` client-validate. submit → `fetch(\`/api/workspaces/${wsIdInput}/join-requests\`, { method:"POST" })` → 제출 중 "신청하는 중…" disabled → 성공 시 입력 비움 + 인라인 중립색(var(--text)) 문구 "참여 신청을 보냈어요. 관리자 승인을 기다려 주세요." → 실패 시 `body.error ?? "이미 멤버이거나 이미 신청한 워크스페이스예요."`(중복/이미멤버) 또는 일반 실패 기본값 "일시적인 오류가 발생했어요. 잠시 후 다시 시도해 주세요."를 인라인 destructive로. onChange에서 `setFeedback(null)`(다음 keystroke에 문구 소거, 타이머 없음). module.css는 var(--token)만.

    `dashboard/page.tsx`: 기존 RSC 데이터 로직(listMembershipsForUser) 불변, `.grid` 아래에 상단 1px `var(--border)` 구분선 + `margin-top: var(--space-xl)` `padding-top: var(--space-lg)`로 분리된 섹션에 라벨 "워크스페이스 참여 신청"(Label 12/600 muted) + `<JoinWorkspaceInput />` 삽입. page.module.css에 그 섹션 클래스만 추가.

    `FolderTree.tsx`: trashLink `<Link>` 블록(418~426행) 바로 아래, `<ThemeToggle />` 위에 동일 구조로 membersLink 삽입 — `href={\`/w/${workspaceId}/members\`}`, `pathname === \`/w/${workspaceId}/members\``로 active 클래스, `<Users size={16} /><span>멤버</span>`. FolderTree.module.css에 `membersLink`/`membersLinkActive`를 `trashLink`/`trashLinkActive` 선언 복제로 추가(hover --surface-2, active --accent-weak/--accent — 신규 값 없음).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - JoinWorkspaceInput이 POST `/api/workspaces/{id}/join-requests`를 호출하고, 성공은 중립색 문구·실패는 destructive 문구를 인라인으로 표시하며, 다음 keystroke에 문구가 사라진다(타이머 없음).
    - dashboard/page.tsx의 기존 memberships 로딩 로직이 변경되지 않고, `.grid` 아래에 참여 신청 섹션이 구분선과 함께 추가된다.
    - FolderTree에 `/w/{wsId}/members`로 가는 membersLink가 trashLink 아래 ThemeToggle 위에 추가되고, membersLink/membersLinkActive CSS가 trashLink 선언 복제다(신규 색상값 없음).
    - `pnpm exec tsc --noEmit` + `pnpm vitest run` 통과.
  </acceptance_criteria>
  <done>대시보드에서 참여 신청을 보낼 수 있고 사이드바에서 멤버 페이지로 진입할 수 있다. (시각·왕복은 phase 말 수동 검증.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 UI ↔ 서버 라우트 | UI의 canManage 게이팅/버튼 숨김은 편의일 뿐 — 실제 방어는 07-02/03/04 라우트의 requireRole. UI는 신뢰 경계가 아니다(CLAUDE.md). |
| members/page.tsx(RSC) → 클라이언트 컴포넌트 | role 원본이 아니라 canManage boolean만 전달 — rbac 모듈이 클라 번들에 유입되지 않음(TrashList 선례). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05-UIGATE | Elevation of Privilege | canManage 섹션 게이팅 | medium | mitigate | UI 숨김은 UX일 뿐, 승인/거절/초대의 실제 권한은 서버 requireRole ADMIN(07-02/03/04)이 방어 — UI 우회로 라우트를 직접 쳐도 403. |
| T-07-05-BUNDLE | Information Disclosure | 클라이언트 번들 | low | mitigate | RSC가 canManage boolean만 넘기고 `@/lib/rbac`를 클라 컴포넌트가 import하지 않음(bcrypt/auth 서버 전용 유지). |
| T-07-05-XSS | Tampering | 멤버 이름/이메일 렌더 | low | mitigate | React 텍스트 노드로만 출력(dangerouslySetInnerHTML 미사용) — escaping은 React 기본(CLAUDE.md). |
| T-07-05-SC | Tampering | npm/pip/cargo installs | high | accept | 신규 패키지 설치 없음(RESEARCH Audit: 0 installs). |
</threat_model>

<verification>
- `pnpm exec tsc --noEmit` 통과.
- `pnpm vitest run` 전체 스위트 GREEN(회귀 없음).
- `/check`(lint+typecheck+test) 통과.
- 수동(phase 말 배치, 07-VALIDATION Manual-Only): ADMIN vs VIEWER 멤버 페이지 섹션 노출/숨김; 대시보드 참여 신청 왕복; ADMIN 회원 검색→초대 발송(콘솔 링크)→수락→EDITOR 편입 전체 흐름.
</verification>

<success_criteria>
- WS-03(대시보드 신청 + 승인 대기 노출)·WS-04(승인/거절 UI)·WS-05(회원 검색+초대 UI)의 사용자 표면이 완성되고, RBAC가 서버 전용으로 유지됨.
- 신규 시각값 없이 기존 토큰/컴포넌트만 확장(UI-SPEC 준수).
</success_criteria>

<output>
Create `.planning/phases/07-workspace-collaboration-join-invite/07-05-SUMMARY.md` when done
</output>
