---
phase: 07-workspace-collaboration-join-invite
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/mailer.ts
  - src/lib/invitations.ts
  - src/app/api/workspaces/[id]/invitations/route.ts
  - src/app/(auth)/invitations/accept/page.tsx
  - src/app/(auth)/invitations/accept/page.module.css
  - tests/invitations/helpers.ts
  - tests/invitations/accept.test.ts
  - tests/invitations/create.test.ts
autonomous: true
requirements: [WS-05]
user_setup: []
estimate:
  tokens: 62000
  raw_tokens: 33000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "ADMIN이 POST /api/workspaces/:id/invitations로 초대를 발급하면 invitation 행이 생기고 수락 링크가 mailer로 전달된다 (WS-05); 비-ADMIN은 403 (NFR-3.2)"
    - "이미 그 워크스페이스 멤버인 대상을 초대하면 400으로 거부된다 (RESEARCH Open Q1)"
    - "수락 링크의 토큰이 유효하면 초대받은 본인(session.user.id === invitee_id)이 EDITOR로 편입되고 used_at이 기록되어 재사용이 불가해진다 (WS-05)"
    - "acceptInvitation이 서명불일치/만료/이미사용/wrong-user 5-상태를 정확히 판정하고, 편입+used_at 갱신이 단일 트랜잭션으로 원자적이다 (WS-05, Pitfall 4)"
    - "raw 토큰은 mailer가 emit하는 링크에만 등장하고 DB에는 남지 않는다 (NFR-3.3)"
  artifacts:
    - "src/lib/mailer.ts (sendInvitationEmail — 콘솔 로그, storage.ts류 1-함수 격리)"
    - "src/lib/invitations.ts (createInvitation, acceptInvitation — DbClient 주입 + 트랜잭션)"
    - "src/app/api/workspaces/[id]/invitations/route.ts (POST)"
    - "src/app/(auth)/invitations/accept/page.tsx (RSC 5-상태 결과 페이지)"
    - "tests/invitations/helpers.ts, accept.test.ts, create.test.ts"
  key_links:
    - "invitations 라우트 → encodeInvitationToken(07-01) → mailer.sendInvitationEmail: 토큰이 링크로 나가는 유일 지점"
    - "accept RSC page → acceptInvitation() 직접 호출(GET 라우트 파일 없음, Pitfall 6) → workspace_member INSERT EDITOR"
    - "acceptInvitation 트랜잭션의 INSERT ... onConflictDoNothing + UPDATE ... WHERE used_at IS NULL: 이중 편입·재사용 방지 가드"
---

<objective>
TRACER — 초대 보안 스파인을 end-to-end로 관통한다: ADMIN이 초대를 발급(invitation INSERT + 토큰 인코드) → mailer가 수락 링크를 콘솔에 emit → 초대받은 회원이 수락 페이지를 열면 토큰이 검증(5-상태)되고 유효하면 단일 트랜잭션으로 EDITOR 편입 + used_at 기록. 한 경로를 모든 계층(라우트·lib·크립토·DB 트랜잭션·RSC 페이지)에 걸쳐 실제로 배선하고 통합 테스트로 증명한다.

Purpose: 이것이 phase에서 가장 위험한 조각(서명 검증 + 트랜잭션 편입 + wrong-user IDOR 방어)이다. 아키텍처(requireRole→lib→라우트, RSC가 lib 직접 호출, 가드-업데이트 트랜잭션)를 초기 컨텍스트에서 한 번 증명해 두면 07-03/07-04/07-05가 같은 골격을 안전하게 확장한다.
Output: mailer + invitations.ts + invitations POST 라우트 + accept RSC 페이지 + 통합 테스트.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-workspace-collaboration-join-invite/07-CONTEXT.md
@.planning/phases/07-workspace-collaboration-join-invite/07-RESEARCH.md
@.planning/phases/07-workspace-collaboration-join-invite/07-PATTERNS.md
@.planning/phases/07-workspace-collaboration-join-invite/07-UI-SPEC.md
@src/lib/documents.ts
@src/lib/rbac.ts
@src/app/api/workspaces/route.ts
</context>

## Artifacts this phase produces

| Artifact | 역할 | 소비처 |
|----------|------|--------|
| `src/lib/mailer.ts` | 수락 링크 발송(dev 콘솔) 격리 모듈 | invitations 라우트 |
| `src/lib/invitations.ts` | createInvitation·acceptInvitation 서비스 | invitations 라우트·accept 페이지 |
| `src/app/api/workspaces/[id]/invitations/route.ts` | 초대 발급 라우트(ADMIN) | 07-05 InviteSearch UI |
| `src/app/(auth)/invitations/accept/page.tsx` | 수락 결과 RSC 페이지(5-상태) | 콘솔 링크 클릭 진입 |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 초대 발급→수락→EDITOR 편입 스파인 (mailer + invitations.ts, TDD)</name>
  <files>src/lib/mailer.ts, src/lib/invitations.ts, tests/invitations/helpers.ts, tests/invitations/accept.test.ts</files>
  <read_first>
    - 07-RESEARCH.md "acceptInvitation — Pitfall 1/4의 실제 구현"(466~519행) 코드 — 검증 순서(파싱→행 조회→verifyMac→만료→used_at→invitee 일치)와 트랜잭션(onConflictDoNothing INSERT + UPDATE WHERE used_at IS NULL)이 리터럴로 제공됨.
    - 07-RESEARCH.md "mailer.ts — storage.ts류 격리"(454~463행), Pitfall 1(검증 순서 = 거부 사유 우선순위, 실행 순서는 행 조회가 먼저), Pitfall 5(not-found를 invalid-signature로 접음).
    - `src/lib/documents.ts` — `DbClient` 유니언 타입(1~8행), `replaceTags`의 `client.transaction` + throw-to-rollback(172~204행), `upsertDraft`의 `.onConflictDoUpdate`(128~136행, 자매 API `.onConflictDoNothing`의 형태 참고).
    - `src/lib/invitation-token.ts`(07-01 산출) — parseInvitationToken/verifyMac 시그니처.
    - `src/lib/storage.ts` — 1-함수 모듈 격리 관례(mailer analog).
    - `tests/rbac/helpers.ts` — `createTestUser`/`createTestWorkspace`/`addMember`/`mockSessionFor` 재사용(tests/invitations/helpers.ts가 이들을 래핑).
    - `vitest.config.ts` — `DATABASE_URL_TEST`로 실 DB 통합, `fileParallelism: false`.
  </read_first>
  <behavior>
    tests/invitations/helpers.ts: `createTestInvitation({ workspaceId, inviteeId, createdBy, expiresAt?, usedAt? })` — 만료/사용됨 등 다양한 상태의 invitation 행을 씨딩하고 `{ row }`를 반환. tests/rbac/helpers의 팩토리를 재사용.

    tests/invitations/accept.test.ts (통합, 실 DB — RED 먼저):
    - success: 유효 토큰 + 초대받은 본인 → `{ status: "success", workspaceId, workspaceName }`, workspace_member에 role='EDITOR' 행이 생기고, invitation.used_at이 non-null.
    - invalid-signature(변조): 토큰 mac 변조 → `{ status: "invalid-signature" }`, 편입 없음.
    - invalid-signature(not-found): 존재하지 않는 invitationId 토큰 → `{ status: "invalid-signature" }` (별도 not-found 상태 없음, Pitfall 5).
    - expired: expiresAt이 과거 → `{ status: "expired" }`, 편입 없음.
    - already-used: used_at이 이미 set → `{ status: "already-used" }`.
    - wrong-user: session.user.id ≠ invitee_id → `{ status: "wrong-user" }`, 편입 없음(IDOR 방어).
    - 원자성/멱등: 이미 멤버인 상태에서 success 경로를 타도 onConflictDoNothing으로 중복 행 없이 used_at만 갱신. 같은 토큰 2회 수락 시 2번째는 already-used.
  </behavior>
  <action>
    `src/lib/mailer.ts`: `sendInvitationEmail(to: string, acceptLink: string): Promise<void>` 하나만 export — `console.log(\`[mailer] invitation email → ${to}: ${acceptLink}\`)`. 신규 dep·SMTP 없음(storage.ts 격리 원칙). 이것이 토큰이 등장하는 유일 지점.

    `src/lib/invitations.ts`: `DbClient` 유니언 타입을 documents.ts에서 복사. `AcceptResult` 유니언(success/expired/already-used/invalid-signature/wrong-user)을 export. `createInvitation(workspaceId, inviteeId, createdBy, expiresAt, client=db)` — invitation INSERT 후 `.returning({ id, expiresAt })`. `acceptInvitation(token, currentUserId, client=db)` — RESEARCH 코드대로: parseInvitationToken(null→invalid-signature) → invitation 행 조회(없음→invalid-signature) → `process.env.AUTH_SECRET`(없으면 throw) → verifyMac(불일치→invalid-signature) → expiresAt.getTime()<Date.now()(→expired) → usedAt!==null(→already-used) → inviteeId!==currentUserId(→wrong-user) → workspace name 조회 → `client.transaction`: `insert(workspaceMember).values({workspaceId, userId: currentUserId, role:'EDITOR'}).onConflictDoNothing()` + `update(invitation).set({usedAt: new Date()}).where(and(eq(invitation.id,row.id), isNull(invitation.usedAt)))` → success 반환. 테스트 RED 확인 후 구현으로 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/invitations/accept.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - accept.test.ts의 5-상태 + 원자성/멱등 케이스가 전부 GREEN.
    - success 경로가 workspace_member에 정확히 1개의 EDITOR 행을 만들고 invitation.used_at을 set한다.
    - wrong-user·expired·already-used·invalid-signature 경로 어디에서도 workspace_member INSERT가 발생하지 않는다.
    - acceptInvitation의 편입+used_at 갱신이 단일 `client.transaction` 안에서 일어난다 (grep: `.transaction(` 존재, INSERT/UPDATE가 그 콜백 내부).
    - mailer.ts는 sendInvitationEmail 하나만 export하고 신규 import(nodemailer 등)가 없다.
  </acceptance_criteria>
  <done>초대 발급용 createInvitation과 수락용 acceptInvitation(5-상태 + 원자 트랜잭션 편입)이 실 DB 통합 테스트로 증명되고, mailer가 격리된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST /api/workspaces/:id/invitations 라우트 (ADMIN·회원검증·토큰·메일)</name>
  <files>src/app/api/workspaces/[id]/invitations/route.ts, tests/invitations/create.test.ts</files>
  <read_first>
    - 07-RESEARCH.md "초대 발급(라우트)" 코드(391~451행) — requireRole ADMIN → zod parse → invitee 존재/기존멤버 체크 → createInvitation → encodeInvitationToken → sendInvitationEmail → 201의 완성 구조가 리터럴로 제공됨.
    - `src/app/api/workspaces/[id]/search/route.ts` 전체 — `z.uuid().safeParse(wsId)` 파라미터 가드 + requireRole + `ForbiddenError`→`forbiddenResponse()` catch 관례.
    - `src/app/api/workspaces/route.ts` — malformed JSON try/catch→400 가드(19~24행), `export const runtime = "nodejs"`.
    - `src/lib/invitation-token.ts`(encodeInvitationToken), `src/lib/mailer.ts`, `src/lib/invitations.ts`(createInvitation), `src/lib/rbac.ts`(requireRole/ForbiddenError/forbiddenResponse).
    - Pitfall 7(AUTH_SECRET 부재 시 throw), A3(수락 링크 origin은 `new URL(req.url).origin` — 신규 env var 없음).
  </read_first>
  <behavior>
    tests/invitations/create.test.ts (통합, mailer는 vi.mock — RED 먼저):
    - ADMIN 성공: ADMIN 세션 + 유효 inviteeId → 201, invitation 행 생성, sendInvitationEmail mock이 `(invitee.email, `.../invitations/accept?token=...`)`로 정확히 1회 호출됨. 토큰을 parseInvitationToken하면 생성된 invitation.id가 나온다.
    - RBAC: VIEWER/EDITOR/비-멤버/비로그인 세션 → 403(FORBIDDEN copy), invitation 행 미생성.
    - 이미 멤버: inviteeId가 이미 그 워크스페이스 멤버 → 400 "이미 멤버예요.", 행 미생성·메일 미발송.
    - 존재하지 않는 회원: inviteeId가 user에 없음 → 400.
    - 잘못된 wsId(비-uuid) → 400. 잘못된 바디(inviteeId 누락/비-uuid) → 400.
  </behavior>
  <action>
    RESEARCH "초대 발급(라우트)" 코드대로 구현한다. `export const runtime = "nodejs"`. `bodySchema = z.object({ inviteeId: z.uuid() })`, `TTL_MS = 7*24*60*60*1000`. wsId uuid 가드 → requireRole(wsId,"ADMIN") try/catch → malformed JSON 가드 → bodySchema.safeParse → invitee 존재 SELECT(없으면 400) → 기존 멤버 SELECT(있으면 400) → `expiresAt = new Date(Date.now()+TTL_MS)` → createInvitation(...) → `secret = process.env.AUTH_SECRET`(없으면 throw, Pitfall 7) → encodeInvitationToken → `origin = new URL(req.url).origin` → sendInvitationEmail(invitee.email, `${origin}/invitations/accept?token=${token}`) → `Response.json({ id }, { status: 201 })`. role은 절대 클라이언트가 지정 못하게 — 편입 role은 acceptInvitation이 하드코딩('EDITOR'). 테스트 RED 후 GREEN.
  </action>
  <verify>
    <automated>pnpm vitest run tests/invitations/create.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - create.test.ts 전 케이스 GREEN.
    - 비-ADMIN(VIEWER/EDITOR/비멤버/비로그인) 요청이 전부 403이고 invitation 행을 만들지 않는다.
    - 성공 시 sendInvitationEmail이 초대받는 회원의 email과 `/invitations/accept?token=`를 포함한 링크로 호출되고, 그 토큰이 생성된 invitation.id로 parse된다.
    - 이미 멤버·존재하지 않는 회원·잘못된 uuid·잘못된 바디가 각각 400.
    - 라우트 바디 스키마가 `inviteeId`만 받는다 (role 등 mass-assignment 필드 부재).
  </acceptance_criteria>
  <done>ADMIN만 초대를 발급할 수 있고, 발급 시 정확한 수락 링크가 mailer로 전달되며, 이미-멤버/미존재/권한위반이 서버에서 거부됨이 증명된다.</done>
</task>

<task type="auto">
  <name>Task 3: /invitations/accept RSC 결과 페이지 (5-상태, GET 라우트 없음)</name>
  <files>src/app/(auth)/invitations/accept/page.tsx, src/app/(auth)/invitations/accept/page.module.css</files>
  <read_first>
    - 07-UI-SPEC.md "Invitation-Accept Result Contract"(191~205행) — 5개 상태별 아이콘/제목/본문/액션 버튼과 셸(로그인 페이지 `.page` + `Card` max-width:360px). "로딩 상태 없음"(RSC가 결과) 명시.
    - 07-RESEARCH.md "/invitations/accept RSC 페이지 골격"(522~545행) + Pitfall 6(GET 라우트 파일 만들지 않음, acceptInvitation 직접 await) + 미인증 redirect 처리(536~538행).
    - `src/app/(auth)/login/page.tsx` + `login/page.module.css`의 `.page` 셸, `src/components/ui/Card.tsx` — 중앙 카드 셸.
    - `src/components/ui/Button.tsx` — variant primary/secondary. lucide `CheckCircle2`/`AlertCircle`는 lucide-react 기존 설치.
    - `src/lib/invitations.ts`(acceptInvitation), `src/auth.ts`(auth()).
  </read_first>
  <action>
    `src/app/(auth)/invitations/accept/page.tsx` (async RSC): `searchParams: Promise<{ token?: string }>` → token 추출 → `session = await auth()` → 세션 없으면 `redirect(\`/login?callbackUrl=${encodeURIComponent(\`/invitations/accept?token=${token ?? ""}\`)}\`)` → `result = await acceptInvitation(token ?? "", session.user.id)` (GET API 라우트 파일 없음 — RSC가 lib 직접 호출, Pitfall 6). result.status에 따라 UI-SPEC 표의 5개 분기를 렌더: 로그인 페이지와 동일 `.page` + `Card` 셸 안에 [아이콘 24px] → 제목(Display) → 본문(Body muted) → 액션 Button/Link 1개. success는 `CheckCircle2` var(--accent) + "워크스페이스로 이동"→`/w/${result.workspaceId}`; expired/already-used/invalid-signature는 `AlertCircle` var(--destructive) + "대시보드로 이동"→`/dashboard`; wrong-user는 `AlertCircle` + "로그인"→`/login`. 카피는 UI-SPEC Copywriting Contract 문구 그대로. page.module.css는 login/page.module.css의 중앙정렬 셸 + 아이콘/제목/본문 간격만 토큰(var(--space-*))으로 정의 — 신규 색상값 없음(전부 var(--token)). 자동 리다이렉트 없음(사용자가 직접 클릭).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run tests/invitations</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/invitations/accept/route.ts` 파일이 존재하지 않는다 (Pitfall 6 — GET 라우트 없음, `ls src/app/api/invitations` 부재).
    - page.tsx가 acceptInvitation을 직접 await하고, 미인증 시 `/login?callbackUrl=`로 redirect한다.
    - 5개 result.status 각각에 대해 UI-SPEC 표의 제목·본문·버튼 target(/w/{wsId}, /dashboard, /login)이 렌더 분기로 존재한다.
    - page.module.css에 하드코딩 hex 색상이 없다 (전부 var(--token)); `pnpm exec tsc --noEmit` 통과.
    - 자동 리다이렉트(success 시 즉시 이동)가 없다 — 사용자 클릭 버튼만.
  </acceptance_criteria>
  <done>수락 페이지가 서버에서 토큰을 검증해 5-상태 결과를 렌더하고, GET API 라우트 없이 RSC가 acceptInvitation을 직접 호출한다. (실제 콘솔-링크 클릭 왕복은 phase 말 수동 검증.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저(ADMIN) → invitations 라우트 | inviteeId·wsId는 신뢰 불가 입력. requireRole ADMIN + zod uuid + invitee/멤버 재조회로 방어. |
| 공개 수락 링크 → accept RSC | 토큰은 공개 채널을 건넌다. 서버가 HMAC·만료·일회성·invitee 일치를 렌더 전에 검증. |
| accept RSC ↔ DB(트랜잭션) | 편입 INSERT + used_at UPDATE가 한 트랜잭션 — TOCTOU/이중 편입 방지. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02-ADMIN | Elevation of Privilege | invitations POST 라우트 | high | mitigate | `requireRole(wsId,"ADMIN")` 서버 게이트(UI 숨김은 보안 아님). create.test.ts RBAC 케이스가 증명. |
| T-07-02-IDOR | Elevation of Privilege | acceptInvitation | critical | mitigate | `invitation.inviteeId === session.user.id`(wrong-user 상태) — 링크를 아는 제3자의 편입 차단. accept.test.ts wrong-user 케이스가 증명. |
| T-07-02-REPLAY | Repudiation | acceptInvitation 트랜잭션 | high | mitigate | `UPDATE ... WHERE used_at IS NULL` 가드 + INSERT onConflictDoNothing, 단일 트랜잭션(Pitfall 4). 재사용→already-used. |
| T-07-02-MASS | Tampering, Elevation of Privilege | invitations 바디 | high | mitigate | zod가 `inviteeId`만 허용, 편입 role은 서버가 하드코딩('EDITOR'). 클라이언트가 role 지정 불가. |
| T-07-02-ENUM | Information Disclosure | acceptInvitation not-found | medium | mitigate | invitation 미존재를 invalid-signature로 접음(Pitfall 5) — id 열거 오라클 차단. |
| T-07-02-LINKINJ | Tampering | 수락 링크 구성 | low | accept | 링크는 서버가 `new URL(req.url).origin` + 서버 발급 토큰으로 구성 — 클라이언트가 링크 본문을 주입할 표면 없음(로컬 개발 전제, A3). |
| T-07-02-SC | Tampering | npm/pip/cargo installs | high | accept | 신규 패키지 설치 없음(RESEARCH Audit: 0 installs). |
</threat_model>

<verification>
- `pnpm vitest run tests/invitations` GREEN (token + accept + create).
- `pnpm exec tsc --noEmit` 통과.
- `pnpm vitest run` 전체 스위트 회귀 없음.
- 수동(phase 말 배치): `pnpm dev` 기동 → ADMIN이 초대 발급 → 서버 콘솔의 `[mailer]` 링크 복사 → 초대받은 계정으로 열기 → EDITOR 편입 + success 렌더 확인(07-VALIDATION Manual-Only).
</verification>

<success_criteria>
- 초대 발급→콘솔 링크→수락→EDITOR 편입의 한 경로가 통합 테스트로 관통됨.
- 5-상태 판정 + wrong-user IDOR 방어 + 일회성 재사용 방어가 증명됨.
- 토큰 원문이 DB에 남지 않고 mailer 링크에만 등장.
</success_criteria>

<output>
Create `.planning/phases/07-workspace-collaboration-join-invite/07-02-SUMMARY.md` when done
</output>
