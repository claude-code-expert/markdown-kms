---
phase: 02-markdown-rendering-editor-formatting
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/components/editor/plugins/hr.ts
  - src/components/editor/plugins/table.ts
  - src/components/editor/plugins/code-block.ts
  - src/components/editor/plugins/heading.ts
  - src/components/preview/PreviewPane.tsx
  - tests/markdown/plugin-render.test.ts
  - tests/editor/hr.test.ts
  - tests/editor/table.test.ts
  - tests/editor/code-block.test.ts
  - tests/editor/heading.test.ts
autonomous: true
requirements: [EDIT-01, EDIT-04, EDIT-05]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An integration test feeds each affected plugin's actual run(state) output through markdownProcessor.process and asserts the rendered HTML is correct — the end-to-end gate that no unit test performed and that let the mis-pinned fixtures ship (GAP root cause)."
    - "Clicking hr after a non-empty text line renders a thematic break (hr element), never a Setext heading — the plugin inserts a blank line before --- when the preceding on-line content is non-empty (GAP-1, EDIT-04)."
    - "hr on a non-empty selection preserves the selected text instead of destroying it — the rule is inserted after the selection, matching link/image preserve behavior (GAP-5, EDIT-04)."
    - "Inserting a table when content follows on the same line splits that trailing content onto its own block (trailing blank line), so it survives GFM render instead of gluing onto the last table row and being dropped (GAP-2, EDIT-05)."
    - "Wrapping a selection that has trailing same-line content in a code block lands the closing fence on its own line, so the fence closes and the rest of the document is not swallowed into the code block (GAP-3, EDIT-04)."
    - "Applying a heading to a line already carrying a list/blockquote prefix strips that prefix first, so the marker is replaced not nested — consistent with the sibling list/blockquote plugins (GAP-4, EDIT-01)."
    - "A render exception in PreviewPane is logged to the console for dev diagnosis while the user still sees the fixed generic fallback message; the caught exception detail is never rendered into the DOM (GAP-6)."
  prohibitions:
    - statement: "A gap MUST NOT be 'closed' by re-pinning a unit fixture to match the broken plugin output — every corrected/added fixture must match what the real markdownProcessor pipeline renders."
      status: enforced
      verification: "Each affected plugin has an assertion in tests/markdown/plugin-render.test.ts that runs its output through markdownProcessor and asserts correct HTML; unit fixtures in tests/editor/*.test.ts are edited to the doc string that produces that correct render."
    - statement: "The rehype-sanitize allow-list MUST NOT be weakened — src/lib/markdown/schema.ts stays the unmodified defaultSchema re-export."
      status: enforced
      verification: "git diff shows no change to src/lib/markdown/schema.ts; plugin-render integration test runs all output through the full sanitizing markdownProcessor."
    - statement: "No editor plugin may import another plugin or access EditorView/DOM (TRD §6 / CLAUDE.md 1-feature-1-file invariant)."
      status: enforced
      verification: "grep across src/components/editor/plugins/*.ts finds no import of another plugin file and no EditorView import (only doc-comment mentions)."
    - statement: "Executed plans 02-01..02-05 and their SUMMARY files MUST NOT be modified, re-planned, or discarded — they are immutable history."
      status: enforced
      verification: "git diff touches only the 10 files in files_modified; no 02-0[1-5]-PLAN.md or 02-0[1-5]-SUMMARY.md is changed."
    - statement: "No preview memoization / block caching may be added — the 60ms p95 budget is not exceeded (measured p95=13ms)."
      status: enforced
      verification: "git diff introduces no memoization in PreviewPane.tsx or pipeline.ts; only the catch-block logging changes in PreviewPane."
  artifacts:
    - path: tests/markdown/plugin-render.test.ts
      provides: "New integration gate — applies each affected plugin's run() output and feeds the resulting doc through markdownProcessor, asserting correct rendered HTML (hr, table, code-block, heading)."
    - path: src/components/editor/plugins/hr.ts
      provides: "Context-aware thematic-break insert (blank line before ---) that preserves a non-empty selection."
    - path: src/components/editor/plugins/table.ts
      provides: "Table skeleton insert that splits both leading and trailing same-line content onto their own blocks."
    - path: src/components/editor/plugins/code-block.ts
      provides: "Fence-wrap that keeps opening and closing fences on their own lines when content shares the line."
    - path: src/components/editor/plugins/heading.ts
      provides: "Heading line-prefix that strips a conflicting list/blockquote prefix before applying the marker."
    - path: src/components/preview/PreviewPane.tsx
      provides: "Render-exception fallback that also logs the error to the console (dev) without leaking it into the DOM."
---

<objective>
Close the 6 functional gaps found by code review (02-REVIEW.md / 02-VERIFICATION.md Gaps Summary) where the executed plugins faithfully implement WRONG contracts: their unit fixtures were pinned to output that has never been rendered through the real markdown pipeline. Three block-insert plugins (hr, table, code-block) splice divider/table/fence markup without accounting for content on the same line; heading does not strip a conflicting list prefix; PreviewPane swallows render exceptions silently.

The root cause is structural: no test fed plugin output through `markdownProcessor`, so broken fixtures went GREEN. This plan LEADS with that missing gate (a plugin-output → pipeline → HTML integration test) proven RED against the hr Setext defect, then expands to the remaining fixes — each RED-first, source-fixed, then GREEN with the mis-pinned unit fixture corrected to the pipeline-accurate string.

Purpose: make the whole cycle self-verifying and regression-proof — a fixture can no longer claim "correct" without the real pipeline agreeing.
Output: one new integration test file, four corrected/expanded unit test files, four plugin fixes, and one PreviewPane logging fix.
</objective>

<artifacts_produced>
This plan creates ONE new file: `tests/markdown/plugin-render.test.ts` — the plugin-output → `markdownProcessor` → HTML integration gate covering hr, table, code-block, and heading. It exports NO new test helpers: it reuses the existing `apply()` from `tests/editor/test-utils.ts` (apply a plugin's `run()` and read the resulting doc string) and the existing `markdownProcessor` export from `src/lib/markdown/pipeline.ts` (`String(await markdownProcessor.process(doc))`). It also edits four existing unit test files (`tests/editor/{hr,table,code-block,heading}.test.ts`) and five source files.
</artifacts_produced>

<planning_notes>
- **Spec-less probe fallback intentionally NOT re-run for this delta** (recorded choice): the executed plans 02-01..02-05 already carry Phase 2's requirement-derived edge coverage (18 truths + 4 backstop in their must_haves), and 02-VERIFICATION.md supplies a MORE authoritative, concrete edge contract — 6 real defects found by running plugin output through the real pipeline. This plan's `must_haves` are authored from that concrete gap contract, not from a re-derived generic probe.
- **Plan-pre gate outcomes (resolved by orchestrator, recorded — not re-run):** API-coverage (ai-integration) NOT APPLICABLE (no external API/SDK, detected=false); assumption-delta NOT APPLICABLE (no singular→plural / required→optional / derived→chosen transition, detected=false); schema-gate NOT APPLICABLE (no src/db/schema.ts or migration in scope); package-legitimacy NOT APPLICABLE (no npm/pip/cargo install — bug fixes + one test file). Security ACTIVE (ASVS L1, block_on=high) → `<threat_model>` below.
- **Reversibility:** all changes are localized bug fixes; every decision is `reversible`. No `one-way` decisions, so no `checkpoint:decision` tasks.
- **Out of scope (do NOT plan here):** the 5 human-verification / UAT items (Korean IME safety, toolbar visual walkthrough, preview overflow, non-persistent contract, heading-inside-code-fence informational edge) route to `/gsd-verify-work` AFTER these code gaps close. The ROADMAP Success-Criteria-4 wording amendment (sanitize schema intentionally unmodified) is a non-blocking doc note, deferred to a later doc pass — not a code task here. IN-01 (extract shared `ANY_LIST_PREFIX_RE`) and IN-02/IN-03 cleanups are optional and deliberately skipped to keep the diff minimal and avoid churning the four working list plugins; GAP-4 inlines the list-prefix regex in heading.ts, consistent with the existing 4-copy pattern.
</planning_notes>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-VERIFICATION.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-REVIEW.md
@CLAUDE.md
@src/lib/markdown/pipeline.ts
@tests/editor/test-utils.ts
@tests/markdown/sanitize.test.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Integration-test gate (tracer) + hr thematic-break fix (GAP-1, GAP-5)</name>
  <precondition>hr.ts + tests/editor/hr.test.ts exist (02-04) and are currently GREEN against the mis-pinned fixture; markdownProcessor is exported from src/lib/markdown/pipeline.ts (02-03).</precondition>
  <files>tests/markdown/plugin-render.test.ts, src/components/editor/plugins/hr.ts, tests/editor/hr.test.ts</files>
  <read_first>
    - src/components/editor/plugins/hr.ts (the file being fixed — current INSERT = "\n---\n", replaces the range)
    - tests/editor/test-utils.ts (the `apply(run, doc, selection)` helper returning `{doc, selection}`)
    - src/lib/markdown/pipeline.ts (the `markdownProcessor` export; `String(await markdownProcessor.process(doc))` yields sanitized HTML)
    - tests/markdown/sanitize.test.ts (the established `String(await markdownProcessor.process(md))` async assertion pattern to mirror)
    - tests/editor/hr.test.ts (the mis-pinned fixture to correct)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-REVIEW.md CR-02 + WR-03 (the hr defect analysis and fix shape)
  </read_first>
  <action>
    Stand up the missing end-to-end gate as a NEW file `tests/markdown/plugin-render.test.ts`. It imports `apply` from `../editor/test-utils`, `markdownProcessor` from `@/lib/markdown/pipeline`, and the plugins under test. Each case builds a starting doc + selection, calls `apply((state) => plugin.run(state), doc, selection)` to get the resulting doc string, then computes `String(await markdownProcessor.process(result.doc))` and asserts on the rendered HTML. Write the hr case FIRST: starting doc a single non-empty text line with the cursor at its end — assert the rendered HTML contains a thematic-break element and does NOT contain a Setext-heading element. Run it and confirm it is RED against the current hr.ts (the current output renders the preceding line as a Setext heading, not a rule).

    Then fix `hr.ts` per CR-02 + WR-03 with a single pure `run(state)`, no EditorView, no cross-import, keeping the D-P2-04 doc-comment: insert the rule after any selection (at `to`) so a non-empty selection is preserved rather than replaced; compute whether the on-line content before the insertion point is non-empty and, when it is, prepend a blank line before `---` (two leading newlines) so CommonMark reads a thematic break; when the insertion point is on empty/blank on-line content, a single leading newline is enough. Place the caret after the inserted rule. Re-run the hr integration case to confirm GREEN.

    Finally correct the mis-pinned unit fixture in `tests/editor/hr.test.ts` to the doc string the fixed plugin now produces (blank line before the rule) and its resulting selection offset — the fixture must equal the pipeline-accurate output, not the old broken string.
  </action>
  <verify>
    <automated>pnpm vitest run tests/markdown/plugin-render.test.ts tests/editor/hr.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/markdown/plugin-render.test.ts tests/editor/hr.test.ts` exits 0.
    - The hr integration assertion runs `hr.run` output through `markdownProcessor` and asserts the HTML `toContain("<hr")` and `.not.toContain("<h2")`.
    - hr.ts remains a single pure `run(state)`: `grep -L "EditorView" src/components/editor/plugins/hr.ts` still lists the file (no EditorView import) and it imports no other plugin file.
    - tests/editor/hr.test.ts asserts a doc string that contains a blank line before the `---` rule; the non-empty-selection path preserves the selected text (a hr case on a non-empty selection keeps that text in the resulting doc).
    - src/lib/markdown/schema.ts is unchanged (`git diff --quiet src/lib/markdown/schema.ts`).
  </acceptance_criteria>
  <reversibility rating="reversible">Localized single-plugin text-splicing fix plus a new test file; trivially revertible.</reversibility>
  <done>The plugin-output → markdownProcessor → HTML gate exists and is GREEN for hr; clicking hr after text renders a rule, not a heading; a non-empty selection survives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: table trailing-content split (GAP-2) + code-block closing-fence-on-own-line (GAP-3)</name>
  <precondition>Task 1 merged: plugin-render.test.ts exists with the hr case GREEN.</precondition>
  <files>tests/markdown/plugin-render.test.ts, src/components/editor/plugins/table.ts, src/components/editor/plugins/code-block.ts, tests/editor/table.test.ts, tests/editor/code-block.test.ts</files>
  <read_first>
    - src/components/editor/plugins/table.ts (current: only a leading "\n\n" separator when from !== 0, nothing after the skeleton)
    - src/components/editor/plugins/code-block.ts (current wrap branch: `${fence}\n${selected}\n${fence}` with no on-line-end guard)
    - tests/editor/test-utils.ts (the `apply` helper)
    - src/lib/markdown/pipeline.ts (`markdownProcessor` for the integration assertions)
    - tests/editor/table.test.ts, tests/editor/code-block.test.ts (existing fixtures — the two/three existing cases stay valid; add new ones)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-REVIEW.md CR-01 + WR-01 (the defect analyses and fix shapes)
  </read_first>
  <action>
    First extend `tests/markdown/plugin-render.test.ts` with two new RED cases (run and confirm they fail against the current source):
    - table mid-line insert: starting doc a single non-empty text line with the cursor at line start (content follows the cursor on the same line); after `apply(table.run)` → `markdownProcessor`, assert the HTML contains a table element AND that the trailing line text is present in the output (currently the trailing text glues onto the last row and GFM drops it, so it is absent — RED).
    - code-block wrap with trailing same-line content: starting doc a line where a leading token is selected and text follows on the same line; after `apply(codeBlock.run)` → `markdownProcessor`, assert the code block closes (a closing `</pre>` is present) and the trailing text renders OUTSIDE the code block (its position in the HTML is after `</pre>`), not swallowed inside it.

    Then fix `table.ts` per CR-01, keeping it a single pure `run(state)` with its D-P2-09 doc-comment: derive whether the insertion is at line-start and at line-end; prepend a blank-line separator only when NOT at line-start and append a blank-line separator when NOT at line-end, so both preceding and following same-line content become their own blocks. The existing "empty doc" and "insert twice" fixtures must still hold (at-line-start + at-line-end both true → no separators; end-of-doc second insert → leading separator only).

    Fix `code-block.ts` per WR-01, keeping it a single pure `run(state)` with its D-P2-10 doc-comment and the existing fence-escalation logic: when wrapping a selection, guard both ends — keep the opening fence on its own line (add a leading newline when the selection start is not at line-start) and keep the closing fence on its own line (add a trailing newline when the selection end is not at line-end). The empty-selection branch and the existing wrap/escalation fixtures must still hold.

    Finally add the corresponding pipeline-accurate cases to `tests/editor/table.test.ts` (a mid-line / trailing-content insert case) and `tests/editor/code-block.test.ts` (a trailing-same-line-content wrap case), asserting the resulting doc strings the fixed plugins now produce. Do not weaken or re-pin the existing passing fixtures to broken output.
  </action>
  <verify>
    <automated>pnpm vitest run tests/markdown/plugin-render.test.ts tests/editor/table.test.ts tests/editor/code-block.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/markdown/plugin-render.test.ts tests/editor/table.test.ts tests/editor/code-block.test.ts` exits 0.
    - The table integration assertion asserts the rendered HTML `toContain("<table")` and that the trailing line text appears in the output.
    - The code-block integration assertion asserts `toContain("<pre>")`, that the code block closes (`</pre>` present), and that the trailing text's index in the HTML is greater than the index of `</pre>` (rendered outside the code block).
    - The pre-existing table fixtures (empty-doc skeleton, insert-twice → two independent skeletons) and code-block fixtures (empty-language fence, wrap, fence-escalation) still pass unchanged.
    - Both plugins remain single pure `run(state)` with no EditorView import and no cross-plugin import; src/lib/markdown/schema.ts unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">Two localized text-splicing fixes plus additive test cases.</reversibility>
  <done>Inserting a table or code block with content on the same line no longer loses or swallows that content; the pipeline gate proves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: heading list-prefix strip (GAP-4) + PreviewPane error logging (GAP-6)</name>
  <precondition>Tasks 1-2 merged: plugin-render.test.ts has hr/table/code-block cases GREEN.</precondition>
  <files>tests/markdown/plugin-render.test.ts, src/components/editor/plugins/heading.ts, tests/editor/heading.test.ts, src/components/preview/PreviewPane.tsx</files>
  <read_first>
    - src/components/editor/plugins/heading.ts (current: ATX_RE strip only; no list/blockquote prefix strip)
    - src/components/editor/plugins/bullet-list.ts (the `ANY_LIST_PREFIX_RE` shape — `/^(?:- \[ \] |- |\d+\. |> )/` — to mirror inline, NOT import)
    - src/components/preview/PreviewPane.tsx (the current bare `catch {` with no binding and no logging)
    - tests/editor/test-utils.ts (the `apply` helper) and src/lib/markdown/pipeline.ts (`markdownProcessor`)
    - tests/editor/heading.test.ts (existing fixtures — all stay valid; add one new case)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-REVIEW.md WR-02 + WR-04 (the defect analyses and fix shapes)
  </read_first>
  <action>
    First extend `tests/markdown/plugin-render.test.ts` with a heading RED case: starting doc a line already carrying a list prefix (a bullet item), apply `heading(1).run`, feed through `markdownProcessor`, and assert the rendered HTML is a level-1 heading whose text is the item content only, with no leftover list marker inside the heading. Confirm it is RED against the current heading.ts (which prepends the marker in front of the surviving list prefix).

    Then fix `heading.ts` per WR-02, keeping it the pure `run(state)` factory with its D-P2-06/07/08 doc-comment and no cross-import: define a local list/blockquote prefix regex mirroring the list plugins' `ANY_LIST_PREFIX_RE` (inline it in this file — do NOT import another plugin) and, when a selected line is not already an ATX heading, compute the strip length from that list-prefix match so the conflicting prefix is replaced rather than nested. The existing heading fixtures (ATX replace, same-level toggle-off, paragraph, multi-line) must still hold.

    Add the pipeline-accurate case to `tests/editor/heading.test.ts` (heading over a list-prefixed line → the doc string with the list marker stripped and the heading marker applied).

    Fix `PreviewPane.tsx` per WR-04: bind the caught exception and log it via `console.error` (dev diagnosis) inside the catch, BEFORE returning the existing fixed fallback element. Do not change the user-facing fallback copy or structure, and do NOT interpolate the caught exception, the offending markdown, or any unsanitized content into the returned JSX — the DOM output stays the fixed generic message. Keep the component free of `dangerouslySetInnerHTML`.
  </action>
  <verify>
    <automated>pnpm vitest run tests/markdown/plugin-render.test.ts tests/editor/heading.test.ts && grep -q "console.error" src/components/preview/PreviewPane.tsx && pnpm vitest run tests/editor tests/markdown && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run tests/markdown/plugin-render.test.ts tests/editor/heading.test.ts` exits 0.
    - The heading integration assertion asserts the HTML `toContain("<h1>item</h1>")` (item content only, no stray list marker inside the heading element).
    - `grep -q "console.error" src/components/preview/PreviewPane.tsx` succeeds; the catch block binds the error and logs it; the returned JSX still renders the fixed fallback message and does NOT interpolate the caught error or content (`grep -c "dangerouslySetInnerHTML" src/components/preview/PreviewPane.tsx` returns 0).
    - Full regression: `pnpm vitest run tests/editor tests/markdown` exits 0 (all plugin + pipeline suites GREEN, ~735 prior tests plus the new integration file) and `pnpm exec tsc --noEmit` exits 0.
    - heading.ts imports no other plugin file and no EditorView; src/lib/markdown/schema.ts unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">One plugin regex-strip addition and one one-line logging change.</reversibility>
  <done>Heading over a list line replaces the marker instead of nesting it; a preview render exception is logged for developers while the user sees only the fixed fallback; the whole phase-2 suite plus the new integration gate is GREEN and typechecks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored markdown (plugin-inserted skeletons + typed URLs) → preview DOM | Editor plugins splice markdown text; the pipeline's rehype-sanitize stage neutralizes dangerous content at render. These gap fixes change only text-splicing offsets — they do not touch the sanitize stage or the boundary. |
| caught render exception → PreviewPane DOM | GAP-6 adds logging of a caught exception; the exception must reach the console (dev) only and never the DOM. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | PreviewPane catch-block logging (GAP-6) | low | mitigate | The caught error is logged via `console.error` (dev diagnostics) only; the returned fallback JSX renders a fixed generic message and never interpolates the caught exception, the offending markdown, or unsanitized content into the DOM. Verified by the Task 3 grep asserting no `dangerouslySetInnerHTML` and the fallback copy unchanged. |
| T-02-06-02 | Tampering / Elevation (XSS) | markdown → preview render path (all gap fixes) | medium | mitigate | Gap fixes alter only editor-plugin text splicing and one catch-block log. None touch `src/lib/markdown/schema.ts` (unmodified `defaultSchema` re-export) or the rehype-sanitize stage; all plugin output still flows through the full sanitizing `markdownProcessor`. The new `plugin-render.test.ts` runs each plugin's output through that full pipeline, so any regression that let unsanitized output through would fail the gate. |
| T-02-06-03 | Tampering | new integration test importing plugin output into the pipeline | low | accept | Test-only path (`tests/markdown/plugin-render.test.ts`); asserts sanitized HTML, never runs in production, ships no new dependency. |

No high-severity threats in scope (block_on=high → no block). No package-manager installs this cycle, so no supply-chain (T-*-SC) row applies.
</threat_model>

<verification>
- `pnpm vitest run tests/editor tests/markdown` — all 14 plugin suites + pipeline suites + the new `plugin-render.test.ts` integration gate GREEN.
- `pnpm exec tsc --noEmit` — clean.
- `git diff --quiet src/lib/markdown/schema.ts` — sanitize schema unmodified.
- Grep: no plugin imports another plugin or EditorView; `dangerouslySetInnerHTML` count is 0 in PreviewPane; `console.error` present in PreviewPane catch.
- `git status` touches only the 10 files in `files_modified` — no 02-01..02-05 PLAN/SUMMARY files changed.
</verification>

<success_criteria>
All 6 gaps closed with pipeline-accurate fixtures: hr renders a rule (not a heading) and preserves selections; table and code-block preserve same-line content; heading strips conflicting list/blockquote prefixes; PreviewPane logs render exceptions to the console while keeping the fixed user fallback. A new `tests/markdown/plugin-render.test.ts` gate runs each affected plugin's output through `markdownProcessor` and asserts correct HTML, making the cycle self-verifying. The full phase-2 suite plus the new gate is GREEN, `tsc` is clean, the sanitize schema is untouched, and no executed plan or its SUMMARY is modified.
</success_criteria>

<output>
Create `.planning/phases/02-markdown-rendering-editor-formatting/02-06-SUMMARY.md` when done. Record the corrected fixture strings, the final hr/table/code-block/heading algorithms, and the new integration test's assertions.
</output>
