---
phase: 02-markdown-rendering-editor-formatting
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/components/editor/plugins/heading.ts
  - src/components/editor/plugins/italic.ts
  - src/components/editor/plugins/strikethrough.ts
  - src/components/editor/plugins/inline-code.ts
  - src/components/editor/plugins/bullet-list.ts
  - src/components/editor/plugins/ordered-list.ts
  - src/components/editor/plugins/task-list.ts
  - src/components/editor/plugins/blockquote.ts
  - src/components/editor/plugins/code-block.ts
  - src/components/editor/plugins/hr.ts
  - src/components/editor/plugins/link.ts
  - src/components/editor/plugins/image.ts
  - src/components/editor/plugins/table.ts
  - src/components/editor/plugins/index.ts
  - src/components/editor/Toolbar.tsx
  - src/components/editor/Toolbar.module.css
  - src/components/editor/HeadingDropdown.tsx
  - src/components/editor/HeadingDropdown.module.css
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05]
user_setup: []

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All 14 formatting plugins exist as pure run(state):TransactionSpec functions in components/editor/plugins/, one file each, no cross-plugin imports, no EditorView/DOM access (TRD §6 / CLAUDE.md invariant) — the 13 plugin test files added in 02-02 are GREEN."
    - "heading applies H1–H4 as ATX line-prefixes and P removes the prefix; applying the same level toggles back to paragraph; applying a different level REPLACES it (no nesting); multi-line selection re-prefixes each line (EDIT-01, D-P2-07)."
    - "italic/strikethrough/inline-code toggle-wrap on a selection and insert an empty marker pair with caret between on an empty selection (EDIT-02, D-P2-06/07); italic toggle-off does not misfire on a bold '**' pair."
    - "bullet/ordered/task lists insert per line, renumber ordered lists 1..N across contiguous lines, and toggle back to plain paragraph on re-application (EDIT-03)."
    - "blockquote/code-block/hr insert their block markers; code-block inserts a fence with an EMPTY language slot and the language info string passes through as a class only — no syntax highlighting library (EDIT-04, D-P2-10)."
    - "link/image/table insert placeholder skeletons with the editable region pre-selected and no dialog: link '[text](url)', image '![alt](url)', table = the 2-col GFM skeleton; inserting a table twice yields two independent tables (EDIT-05, D-P2-09)."
    - "The registry index.ts lists all 14 plugins in the UI-SPEC toolbar order (heading dropdown → inline 4 → list 3 → block 3 → insert 3) with each plugin's lucide icon + tooltip metadata."
    - "The Toolbar renders all groups with 1px group dividers and a heading dropdown (5 items: 제목1–4 + 본문); each button dispatches its plugin's run through the live EditorView; still exactly default/hover states, immediate tooltip (D-P2-04/05)."
    - "A link/image inserted with a javascript: URL typed by the user is neutralized at render by the pipeline sanitize stage — no plugin bypasses sanitize (EDIT-08 continuity)."
  prohibitions:
    - "No plugin may import another plugin or EditorView (TRD §6 / CLAUDE.md invariant)."
    - "code-block MUST NOT introduce highlight.js/prism or any syntax-highlighting dependency (D-P2-10 / Deferred Ideas)."
    - "No pressed-click animation and no 300ms tooltip delay — those are Phase 5 (D-P2-05)."
    - "Insert plugins MUST NOT open a dialog/modal — skeleton insert only (D-P2-09)."
  artifacts:
    - path: src/components/editor/plugins/*.ts
      provides: "13 remaining pure run(state) formatting plugins"
      exports: ["heading", "italic", "strikethrough", "inlineCode", "bulletList", "orderedList", "taskList", "blockquote", "codeBlock", "hr", "link", "image", "table"]
    - path: src/components/editor/plugins/index.ts
      provides: "Full 14-plugin registry in UI-SPEC toolbar order"
    - path: src/components/editor/HeadingDropdown.tsx
      provides: "ui-kit dropdown for H1–H4/P heading selection"
      exports: ["HeadingDropdown"]
---

<objective>
Expand from the proven tracer slice to the full formatting surface: implement the remaining 13 plugins (heading + italic/strikethrough/inline-code + bullet/ordered/task + blockquote/code-block/hr + link/image/table), register all 14 in `index.ts` in the UI-SPEC toolbar order, and grow the Toolbar to render every group plus the heading dropdown. After this plan a user can apply every EDIT-01..05 format via the toolbar or by typing markdown.

Purpose: the tracer (02-03) proved the plugin contract + registry + dispatch path on bold; each new plugin is a horizontal expansion against that exact shape, with its behavior already pinned by the RED test written in 02-02. Every task here makes real plugin tests GREEN and adds a capability a user can immediately use.

Output: 13 plugin files, the full registry, the expanded Toolbar, and the HeadingDropdown component.
</objective>

<artifacts_produced>
This plan creates: `heading.ts`, `italic.ts`, `strikethrough.ts`, `inline-code.ts`, `bullet-list.ts`, `ordered-list.ts`, `task-list.ts`, `blockquote.ts`, `code-block.ts`, `hr.ts`, `link.ts`, `image.ts`, `table.ts` (13 pure `run()` exports); the updated `plugins/index.ts` registry; `HeadingDropdown.tsx` (+ CSS); the expanded `Toolbar.tsx` (+ CSS).
</artifacts_produced>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-CONTEXT.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-UI-SPEC.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-PATTERNS.md
@CLAUDE.md
@docs/TRD.md
@src/components/editor/plugins/bold.ts
@src/components/editor/plugins/index.ts
@src/components/editor/Toolbar.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Heading + inline plugins (heading, italic, strikethrough, inline-code)</name>
  <precondition>bold.ts + types.ts exist (02-03) and tests/editor/{heading,italic,strikethrough,inline-code}.test.ts are RED (02-02).</precondition>
  <files>src/components/editor/plugins/heading.ts, src/components/editor/plugins/italic.ts, src/components/editor/plugins/strikethrough.ts, src/components/editor/plugins/inline-code.ts</files>
  <read_first>
    - src/components/editor/plugins/bold.ts (the established pure run(state) shape to mirror)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md Pattern 1 + Common Pitfalls #5 (line-prefix near code fences)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-CONTEXT.md D-P2-06/07 (toggle / heading-replace / multi-line)
    - tests/editor/heading.test.ts, italic.test.ts, strikethrough.test.ts, inline-code.test.ts (exact fixtures to satisfy)
  </read_first>
  <action>
    Implement `heading.ts` as a line-prefix REPLACE plugin (a factory/param for level 1–4, and P=0): for each line in the selection, strip any existing leading ATX prefix and apply the target level's `#`..`####` prefix; applying the same level toggles to paragraph; P removes the prefix; multi-line re-prefixes each line. Guard against corrupting lines inside an open code fence (Pitfall #5). Implement `italic.ts` (`*` marker, with the toggle-off guard that ignores a surrounding `**` bold pair), `strikethrough.ts` (`~~`), and `inline-code.ts` (`` ` ``) as toggle-wrap plugins mirroring bold.ts (empty→marker-pair caret-between, wrap, toggle-off). Each is a single pure run(state), no EditorView, no cross-import; add the D-P2 decision-ID doc-comment. Give each plugin its EditorPlugin metadata (id, lucide icon, tooltip from UI-SPEC Toolbar Tooltip Copy).
  </action>
  <verify>
    <automated>pnpm vitest run tests/editor/heading.test.ts tests/editor/italic.test.ts tests/editor/strikethrough.test.ts tests/editor/inline-code.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The four test files exit 0 (GREEN) with their pinned fixtures (heading replace + same-level toggle + multi-line; italic/strike/code wrap + empty + toggle-off; italic bold-pair guard).
    - No plugin imports EditorView or another plugin.
    - Each plugin exports EditorPlugin metadata with a lucide icon + the UI-SPEC tooltip copy.
  </acceptance_criteria>
  <done>Heading (replace semantics) and the three remaining inline plugins are GREEN and match the D-P2-08 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: List + block plugins (bullet, ordered, task, blockquote, code-block, hr)</name>
  <precondition>tests/editor/{bullet-list,ordered-list,task-list,blockquote,code-block,hr}.test.ts are RED (02-02).</precondition>
  <files>src/components/editor/plugins/bullet-list.ts, src/components/editor/plugins/ordered-list.ts, src/components/editor/plugins/task-list.ts, src/components/editor/plugins/blockquote.ts, src/components/editor/plugins/code-block.ts, src/components/editor/plugins/hr.ts</files>
  <read_first>
    - src/components/editor/plugins/bold.ts + heading.ts (line-prefix vs wrap references)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md Pattern 1 + Open Question #1 (multi-line) + Common Pitfalls #5 (fence)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-CONTEXT.md D-P2-10 (empty-language fence, no syntax highlighting)
    - tests/editor/{bullet-list,ordered-list,task-list,blockquote,code-block,hr}.test.ts (exact fixtures)
  </read_first>
  <action>
    Implement `bullet-list.ts` (`- ` line-prefix, toggle-off, per-line, replaces a differing list prefix), `ordered-list.ts` (`N. ` prefix numbering 1..N across contiguous selected lines, toggle-off), `task-list.ts` (`- [ ] ` prefix, toggle-off) as line-prefix plugins. Implement `blockquote.ts` (`> ` line-prefix, toggle-off, per-line). Implement `code-block.ts` per D-P2-10: insert a fence with an EMPTY language slot (```` ``` ```` + newline) placing the caret on the language line; when wrapping a selection, fence it, escalating the fence length if the selection contains a triple-backtick run so inner backticks cannot close the block; the language info string is passed through only as a class downstream — introduce NO highlighting library. Implement `hr.ts` (insert a `---` thematic-break line). Each plugin: single pure run(state), EditorPlugin metadata (lucide icon + UI-SPEC tooltip), decision-ID doc-comment, no cross-import.
  </action>
  <verify>
    <automated>pnpm vitest run tests/editor/bullet-list.test.ts tests/editor/ordered-list.test.ts tests/editor/task-list.test.ts tests/editor/blockquote.test.ts tests/editor/code-block.test.ts tests/editor/hr.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The six test files exit 0 (GREEN), including ordered-list 1..N numbering, list/blockquote toggle-off, code-block empty-language fence + fence escalation, and hr thematic break.
    - code-block adds no highlighting dependency (package.json unchanged for highlighting libs).
    - No plugin imports EditorView or another plugin.
  </acceptance_criteria>
  <done>All six list/block plugins are GREEN, honoring the empty-language-fence and no-syntax-highlighting decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Insert plugins (link, image, table) + full registry + expanded Toolbar</name>
  <precondition>tests/editor/{link,image,table}.test.ts are RED (02-02); EditorHost exposes the live EditorView to the Toolbar (02-03).</precondition>
  <files>src/components/editor/plugins/link.ts, src/components/editor/plugins/image.ts, src/components/editor/plugins/table.ts, src/components/editor/plugins/index.ts, src/components/editor/Toolbar.tsx, src/components/editor/Toolbar.module.css, src/components/editor/HeadingDropdown.tsx, src/components/editor/HeadingDropdown.module.css</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-UI-SPEC.md §Insert UX Contract + §Toolbar Contract (group order, dividers, heading dropdown, 32px buttons, immediate tooltip) + §Toolbar Tooltip Copy
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md Open Question #2 (table skeleton row/col)
    - src/components/editor/plugins/index.ts + src/components/editor/Toolbar.tsx (the tracer registry + toolbar to extend)
    - tests/editor/link.test.ts, image.test.ts, table.test.ts (exact fixtures)
  </read_first>
  <action>
    Implement `link.ts` (empty→'[텍스트](url)' with '텍스트' selected; selection 'x'→'[x](url)' with 'url' selected), `image.ts` (empty→'![alt](url)' with 'alt' selected; selection 'x'→'![x](url)' with 'url' selected), and `table.ts` (insert the 2-column GFM skeleton '| 제목1 | 제목2 |\n| --- | --- |\n| 내용 | 내용 |'; a second insert produces an independent skeleton, never merging with an adjacent table) — all skeleton inserts, NO dialog (D-P2-09). Update `plugins/index.ts` to register all 14 plugins in the UI-SPEC toolbar order (heading dropdown → bold/italic/strikethrough/inline-code → bullet/ordered/task → blockquote/code-block/hr → link/image/table). Create `HeadingDropdown.tsx` (+ CSS) porting the ui-kit dropdown-trigger/menu pattern with 5 items (제목1–4 + 본문) that invoke the heading plugin at levels 1–4/0. Expand `Toolbar.tsx` to render every group with 1px `var(--border)` dividers and the heading dropdown at the front; keep exactly default/hover states and immediate tooltips (no pressed, no 300ms). Each toolbar control dispatches its plugin's run through the live EditorView.
  </action>
  <verify>
    <automated>pnpm vitest run tests/editor && pnpm build && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tests/editor/link.test.ts, image.test.ts, table.test.ts exit 0 (GREEN) with pinned fixtures incl. table twice→two-independent-tables.
    - `pnpm vitest run tests/editor` — all 14 plugin tests GREEN; `pnpm build` + `pnpm exec tsc --noEmit` clean.
    - index.ts registers all 14 plugins in the UI-SPEC order; the Toolbar renders all 5 groups with dividers + the heading dropdown; no pressed animation / 300ms delay in the code.
    - Insert plugins open no dialog/modal.
  </acceptance_criteria>
  <done>Every EDIT-01..05 format is available via the toolbar (14 plugin tests GREEN); the full functional lucide toolbar + heading dropdown is wired to the live editor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored markdown (incl. link/image URLs) → preview DOM | Link/image plugins let a user type an arbitrary URL; the pipeline sanitize stage (02-03) neutralizes javascript:/dangerous URLs at render — plugins never bypass it. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | markdown → preview XSS via inserted link/image URL (EDIT-08) | high | mitigate | Insert plugins emit only markdown skeleton text; a javascript: URL a user types is stripped at render by rehype-sanitize's protocol allow-list (proven in tests/markdown/sanitize.test.ts). No plugin renders HTML directly. |
| T-02-04 | Tampering | plugin emitting raw HTML/script into the doc | low | accept | Plugins wrap/prefix user text with markdown markers only; all output flows through the sanitizing pipeline. |
</threat_model>

<verification>
- `pnpm vitest run tests/editor` — all 14 plugin tests GREEN.
- `pnpm build` + `pnpm exec tsc --noEmit` clean.
- Grep: no plugin imports another plugin or EditorView; no highlighting library added; no pressed/300ms code in Toolbar.
- Manual toolbar walkthrough folded into the end-of-phase human-check (02-05).
</verification>

<success_criteria>
All 14 formatting plugins pass their contract tests; the full functional lucide toolbar (5 groups + heading dropdown) is wired to the live uncontrolled editor; every EDIT-01..05 capability is usable via toolbar or syntax; no scope-crept highlighting, dialog, or Phase-5 polish.
</success_criteria>

<output>
Create `.planning/phases/02-markdown-rendering-editor-formatting/02-04-SUMMARY.md` when done. Record the final registry order and any plugin-fixture adjustments made against the 02-02 tests.
</output>
