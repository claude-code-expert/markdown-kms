---
phase: 02-markdown-rendering-editor-formatting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tests/spec/commonmark.test.ts
  - tests/spec/gfm.test.ts
  - tests/markdown/sanitize.test.ts
autonomous: true
requirements: [EDIT-08]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All Phase 2 pipeline/editor packages are installed via pnpm at the exact versions in RESEARCH §Standard Stack (unified 11.0.5, remark-parse 11.0.0, remark-rehype 11.1.2, rehype-raw 7.0.0, rehype-sanitize 6.0.0, rehype-react 8.0.0, @codemirror/state 6.7.1, @codemirror/view 6.43.7, @codemirror/commands 6.10.4, the 3 micromark-extension-gfm-* + 3 mdast-util-gfm-* granular packages, commonmark-spec dev-only)."
    - "tests/spec/commonmark.test.ts exists, iterates all 652 commonmark-spec fixtures, compares against PRE-sanitize pipeline output (TRD §10), and is RED (imports a pipeline module that does not exist yet)."
    - "tests/spec/gfm.test.ts exists and asserts BOTH that strikethrough/table/task-list parse AND that footnote (`[^1]`) and autolink-literal syntax render as LITERAL text (do NOT parse) — the exactly-3-GFM-extensions guard (research conflict #2)."
    - "tests/markdown/sanitize.test.ts exists and asserts, against the UNMODIFIED rehype-sanitize defaultSchema (research conflict #1): a script element is stripped, event-handler (on*) attributes are removed, a javascript: URL is dropped, AND a GFM task checkbox (input[type=checkbox][disabled]), del, and table render intact."
    - "Empty/null markdown input renders empty and never throws (spec-less edge EDIT-08/empty)."
    - "Running the sanitize pipeline twice on the same input yields byte-identical output — sanitization is idempotent (spec-less edge EDIT-08/ordering)."
    - "A script element nested INSIDE an allowed element (e.g. inside a table cell) is still stripped (spec-less edge EDIT-08/adjacency)."
    - { statement: "Success Criteria 4's premise that the sanitize schema must be 'explicitly extended for input/del/table' is superseded — RESEARCH verified the defaultSchema ALREADY permits these; this plan records the amendment flag for TRD/ROADMAP rather than fabricating a schema merge.", verification: backstop }
  prohibitions:
    - "The bundled all-5-GFM remark plugin (RESEARCH Pitfall 2) MUST NOT be installed — package.json must contain the three granular micromark-extension-gfm-* and three mdast-util-gfm-* packages instead."
    - "No sanitize-schema merge/override may be added unless a specific sanitize.test.ts assertion fails against the unmodified defaultSchema first (RESEARCH Pitfall 1)."
    - "npm/yarn MUST NOT be used — pnpm only (CLAUDE.md invariant)."
  artifacts:
    - path: package.json
      provides: "Phase 2 pipeline + CodeMirror dependency set at pinned versions"
    - path: tests/spec/commonmark.test.ts
      provides: "CommonMark 0.31.2 conformance runner (652 fixtures, pre-sanitize)"
    - path: tests/spec/gfm.test.ts
      provides: "GFM-3-only conformance incl. footnote/autolink negative assertions"
    - path: tests/markdown/sanitize.test.ts
      provides: "XSS + task-checkbox + del + table sanitize assertions against defaultSchema"
---

<objective>
Install the Phase 2 markdown-pipeline + CodeMirror dependency set and lay down the pipeline/GFM/sanitize test suites FIRST (TDD red), ahead of any implementation. These tests encode the CommonMark 0.31.2 conformance contract, the exactly-3-GFM-extensions invariant, and the XSS/sanitize safety contract for EDIT-08.

Purpose: TRD §10 + CLAUDE.md require test files to be committed BEFORE their implementation. This plan is the Wave-0 test scaffold for the pipeline half of the phase. It also resolves the two research conflicts as executable assertions rather than silently rewriting the spec.

Output: pinned dependencies in package.json; three RED test files (`tests/spec/commonmark.test.ts`, `tests/spec/gfm.test.ts`, `tests/markdown/sanitize.test.ts`) that will go GREEN once `lib/markdown/pipeline.ts` is built in the tracer plan (02-03).

**Research conflict resolutions encoded here (do NOT silently rewrite the spec):**
- Conflict #1 — the sanitize tests are written against the UNMODIFIED `defaultSchema`. RESEARCH verified (by reading `hast-util-sanitize@5.0.2/lib/schema.js`) that `del`, `input[type=checkbox][disabled]`, `table`+subelements, `align`, and `code[className=/^language-./]` are ALREADY permitted. ROADMAP Success Criteria 4's wording ("sanitize schema explicitly extended … not left at the sanitizer's stripped default") is factually superseded — this is recorded as an amendment flag (must_haves backstop truth), and NO schema merge is written unless a test proves a gap.
- Conflict #2 — `gfm.test.ts` includes a negative test that footnote/autolink-literal render literally, because the pipeline will bypass the bundled all-5-GFM plugin (which cannot disable footnote/autolink) and compose the 3 granular extensions instead (RESEARCH Pattern 2).
</objective>

<artifacts_produced>
This plan creates: the pinned dependency set in `package.json`; `tests/spec/commonmark.test.ts`; `tests/spec/gfm.test.ts`; `tests/markdown/sanitize.test.ts`. No production source symbols are created here (they arrive in 02-03).
</artifacts_produced>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-VALIDATION.md
@CLAUDE.md
@docs/TRD.md
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 pipeline + CodeMirror packages (pnpm, pinned versions)</name>
  <precondition>PostgreSQL test DB at DATABASE_URL_TEST (localhost:5433) accepts connections — Vitest globalSetup migrates+seeds it before EVERY test file, including pure-function pipeline tests (RESEARCH §Environment Availability).</precondition>
  <files>package.json</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md §Standard Stack + §Package Legitimacy Audit (exact versions + the two SUS-but-approved @codemirror packages)
    - package.json (current dependency set — add to it, do not replace)
    - CLAUDE.md ("패키지 매니저는 pnpm 고정" invariant)
  </read_first>
  <action>
    Run a single `pnpm add` for the runtime deps and a single `pnpm add -D` for the dev dep, using the EXACT versions from RESEARCH §Standard Stack: runtime = unified@11.0.5, remark-parse@11.0.0, remark-rehype@11.1.2, rehype-raw@7.0.0, rehype-sanitize@6.0.0, rehype-react@8.0.0, @codemirror/state@6.7.1, @codemirror/view@6.43.7, @codemirror/commands@6.10.4, micromark-extension-gfm-strikethrough@2.1.0, micromark-extension-gfm-table@2.1.1, micromark-extension-gfm-task-list-item@2.1.0, mdast-util-gfm-strikethrough@2.0.0, mdast-util-gfm-table@2.0.0, mdast-util-gfm-task-list-item@2.0.0; dev = commonmark-spec@0.31.2. Do NOT install the bundled all-5-GFM remark plugin named in RESEARCH Pitfall 2 — the granular extensions above replace it to honor the "GFM 3종만" invariant. The two @codemirror packages flagged SUS by the legitimacy heuristic are dispositioned APPROVED in RESEARCH (11M+ weekly downloads, verified maintainer repo) — no human-verify gate is needed. lucide-react is already installed; do not re-add it.
  </action>
  <verify>
    <automated>pnpm ls unified rehype-sanitize @codemirror/state mdast-util-gfm-table commonmark-spec 2>&1 | grep -E "unified 11|rehype-sanitize 6|@codemirror/state 6|mdast-util-gfm-table 2|commonmark-spec 0.31"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm ls` shows unified@11.x, rehype-sanitize@6.x, @codemirror/state@6.x, all three mdast-util-gfm-* and all three micromark-extension-gfm-* granular packages, and commonmark-spec@0.31.2 (dev).
    - `node -e "console.log(require('commonmark-spec').tests.length)"` prints 652.
    - package.json does NOT list the bundled all-5-GFM plugin (RESEARCH Pitfall 2 warning sign).
    - Only pnpm was used (pnpm-lock.yaml updated; no package-lock.json/yarn.lock created).
  </acceptance_criteria>
  <done>Every RESEARCH §Standard Stack package resolves at its pinned version via pnpm; commonmark-spec exposes 652 fixtures; the bundled GFM plugin is absent.</done>
</task>

<task type="auto">
  <name>Task 2: CommonMark + GFM-3-only conformance tests (RED)</name>
  <files>tests/spec/commonmark.test.ts, tests/spec/gfm.test.ts</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md §Code Examples ("CommonMark spec-fixture test runner skeleton") and Pattern 2 (pipeline export points)
    - docs/TRD.md §10 (compare against PRE-sanitize output — "스펙은 raw HTML 보존을 기대하므로")
    - .planning/phases/02-markdown-rendering-editor-formatting/02-VALIDATION.md §Per-Task Verification Map (EDIT-08 rows)
  </read_first>
  <action>
    Create `tests/spec/commonmark.test.ts`: import `tests` from `commonmark-spec`, import a `markdownProcessorPreSanitize` (or equivalently named pre-sanitize processor) from `@/lib/markdown/pipeline` (this module does NOT exist yet — the test is RED by design), iterate all 652 fixtures, and assert `String(await processor.process(example.markdown)) === example.html`. Follow the RESEARCH skeleton verbatim for structure. Create `tests/spec/gfm.test.ts`: assert the three permitted GFM extensions parse (`~~x~~` → a `del` element, a pipe table → a `table`, `- [ ] x` → a disabled checkbox `input`) AND add the guard assertions that footnote syntax and autolink-literal syntax render LITERALLY — i.e. a footnote reference produces plain text, not a footnote link, and a bare URL is NOT wrapped in an anchor. Both files import from the not-yet-existing pipeline module so they fail at collection (acceptable RED). Name the exact pre-sanitize and full-pipeline export points the pipeline plan (02-03) must provide, in a header comment, so 02-03 knows the contract.
  </action>
  <verify>
    <automated>pnpm vitest run tests/spec/commonmark.test.ts tests/spec/gfm.test.ts 2>&1 | grep -E "fail|error|Cannot find module|No test|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/spec/commonmark.test.ts` iterates exactly `require('commonmark-spec').tests` (652 cases) and compares against pre-sanitize output.
    - `tests/spec/gfm.test.ts` contains at least: one strikethrough→del assertion, one table assertion, one task-list checkbox assertion, one footnote-renders-literally negative assertion, one autolink-literal-renders-literally negative assertion.
    - Both files are RED now (fail because `@/lib/markdown/pipeline` does not resolve) — this is the expected TDD starting state and is documented in the SUMMARY.
    - A header comment in each file names the exact pipeline export identifiers 02-03 must provide.
  </acceptance_criteria>
  <done>Two conformance test files exist and fail cleanly on the missing pipeline import; the footnote/autolink negative guards are present (conflict #2 encoded).</done>
</task>

<task type="auto">
  <name>Task 3: Sanitize/XSS + task-checkbox tests against the UNMODIFIED defaultSchema (RED)</name>
  <files>tests/markdown/sanitize.test.ts</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md §Common Pitfalls #1, §Security Domain (defaultSchema verified behaviors), §Don't Hand-Roll (sanitize row)
    - docs/TRD.md §5 (NFR-3.1 sanitize allow-list) — this plan's threat model below
  </read_first>
  <action>
    Create `tests/markdown/sanitize.test.ts` importing the FULL (post-sanitize) pipeline processor from `@/lib/markdown/pipeline` (not yet existing → RED). Author, at minimum, these assertions using the UNMODIFIED `defaultSchema` (do NOT write or import a merged schema): (a) markdown containing a script element renders with no script element and no script text in output; (b) an image/link carrying an on-error / on* event-handler attribute renders with that attribute removed; (c) a link whose URL uses the javascript: protocol renders with the href dropped/neutralized; (d) `- [ ] todo` renders a disabled checkbox input that survives sanitize; (e) `~~x~~` renders a del element and a pipe table renders a table element (proving defaultSchema already permits GFM output — conflict #1); (f) a script element nested inside a table cell is still stripped (adjacency edge); (g) empty string and whitespace-only input render empty output and never throw (empty edge); (h) processing the same input twice yields identical output (idempotence/ordering edge). Put the XSS fixture strings inside the test file only (they are inputs under test, never copied into any action/comment elsewhere). Add a header comment recording that per RESEARCH conflict #1 the schema is intentionally the untouched defaultSchema and a merge is added ONLY if one of these assertions later fails.
  </action>
  <verify>
    <automated>pnpm vitest run tests/markdown/sanitize.test.ts 2>&1 | grep -E "fail|error|Cannot find module|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/markdown/sanitize.test.ts` covers all of: script strip, on* attribute strip, javascript: URL neutralization, task-checkbox survives, del + table survive, nested-script strip, empty/null renders empty without throwing, double-process idempotence.
    - The test imports only the unmodified pipeline (no schema merge is referenced).
    - The file is RED now (missing pipeline import) and this is documented in the SUMMARY.
  </acceptance_criteria>
  <done>The sanitize/XSS suite exists, encodes the EDIT-08 safety contract and the conflict-#1 unmodified-schema decision, and fails only on the not-yet-built pipeline import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored markdown content → preview render (client) | Attacker-controlled markdown (script tags, on* attrs, javascript: URLs) crosses into the DOM the preview renders. This is the phase's primary trust boundary. |
| npm registry → local build | Third-party packages enter the build (supply-chain surface). |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | preview render of markdown (EDIT-08) | high | mitigate | `tests/markdown/sanitize.test.ts` proves script strip + on* removal + javascript: neutralization against rehype-sanitize defaultSchema BEFORE the pipeline is built; the pipeline (02-03) must pass it. |
| T-02-02 | Tampering | DOM clobbering via attacker id/name | medium | accept | hast-util-sanitize applies `clobberPrefix: user-content-` by default (RESEARCH §Security Domain) — covered by the unmodified schema; no extra control this plan. |
| T-02-03 | Denial of Service | ReDoS via nested-emphasis markdown | low | accept | micromark tokenizer is linear-time (RESEARCH) — risk exists only if a custom regex parser were hand-rolled, which is prohibited. |
| T-02-SC | Tampering | pnpm installs (supply chain) | high | mitigate | RESEARCH §Package Legitimacy Audit checked all 15 packages (`OK`, or SUS-but-approved false positives with 11M+ downloads/verified repos); no SLOP. Audit table present → gate satisfied; no blocking human checkpoint required. |
</threat_model>

<verification>
- `pnpm ls` confirms every RESEARCH §Standard Stack package at its pinned version; commonmark-spec exposes 652 fixtures.
- `pnpm vitest run tests/spec tests/markdown` runs all three new files; they are RED (missing `@/lib/markdown/pipeline`) — the expected TDD state. No other suite regresses.
- package.json does not contain the bundled all-5-GFM plugin.
</verification>

<success_criteria>
Pinned dependency set installed via pnpm; three RED pipeline/sanitize test files committed ahead of implementation; both research conflicts encoded as executable assertions + recorded amendment flag; no schema merge and no bundled GFM plugin present.
</success_criteria>

<output>
Create `.planning/phases/02-markdown-rendering-editor-formatting/02-01-SUMMARY.md` when done. Record: the RED state of all three test files, the exact pipeline export identifiers 02-03 must provide, and the Success-Criteria-4 amendment flag (conflict #1) for the eventual TRD/ROADMAP update.
</output>
