---
phase: 02-markdown-rendering-editor-formatting
plan: 05
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/lib/markdown/pipeline.ts
  - src/components/preview/PreviewPane.tsx
autonomous: true
requirements: [EDIT-06]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Playwright perf harness runs against the real editor+preview at /w/[wsId] and its self-sanity control passes (near-zero latency on a static page) BEFORE its real numbers are trusted (RESEARCH Open Question #4)."
    - "Keystroke → preview DOM update p95 ≤ 60ms for a 10,000-code-unit document (EDIT-06 / NFR-1.1 / TRD §5 budget); e2e/preview-perf.spec.ts is GREEN."
    - "Measure-first is honored: block/element memoization is added ONLY if the measured p95 exceeds 60ms, and any optimization is accompanied by a before/after p95 number (TRD §5 / CLAUDE.md — no premature optimization)."
    - "The 10,000-char measurement unit is JS String length (UTF-16 code units), documented in the spec (spec-less edge EDIT-06/encoding)."
    - "The p95 computation is guarded against an empty/single-keystroke sample (no divide-by-zero) (spec-less edge EDIT-06/empty)."
    - { statement: "Korean IME composition produces no dropped/corrupted characters during typing and formatting (Success Criteria 5); confirmed by manual human-check at end-of-phase because no authoritative headless-E2E recipe exists (RESEARCH A1/A2, VALIDATION Manual-Only).", verification: backstop }
    - { statement: "Full toolbar visual walkthrough — all 14 controls render lucide icons + immediate tooltips + exactly default/hover states; preview overflow states (long URL wrap, wide table/code horizontal scroll) behave per UI-SPEC — confirmed by manual human-check at end-of-phase (UI-SPEC UI Considerations: overflow/long-text/populated).", verification: backstop }
  prohibitions:
    - "No memoization or other optimization may be committed without a measured p95 that exceeds 60ms motivating it (TRD §5 / CLAUDE.md invariant)."
    - "The perf harness's self-sanity control MUST NOT be removed — a harness trusted without it can silently pass/fail EDIT-06 (RESEARCH A2)."
  artifacts:
    - path: e2e/preview-perf.spec.ts
      provides: "GREEN 60ms p95 measurement (authored in 02-02, made green here)"
    - path: src/lib/markdown/pipeline.ts
      provides: "(conditionally) block-memoized render path only if measurement demands it"
---

<objective>
Prove EDIT-06: keystroke → preview DOM update p95 ≤ 60ms for a 10,000-character document, by running the perf harness (authored RED in 02-02) against the real editor+preview built in the tracer, validating the harness's own self-sanity control first, and optimizing ONLY if the measured p95 exceeds budget. Then consolidate the phase's manual verifications (Korean IME safety + full toolbar/overflow visual walkthrough) into one end-of-phase human-check.

Purpose: EDIT-06 is a measurement requirement, and both TRD §5 and CLAUDE.md forbid optimizing before measuring. This plan measures first; block-level memoization (top-level hast node memo + node-hash key, per TRD §5) is added only as a measured response, never preemptively. It depends only on the tracer (02-03) — a 10,000-char perf run types text into the working editor and needs no toolbar breadth, so it runs in parallel with the plugin expansion (02-04).

Output: a GREEN `e2e/preview-perf.spec.ts` with a recorded p95; conditionally, a measured optimization to `pipeline.ts`/`PreviewPane.tsx`; and the consolidated end-of-phase human-check.
</objective>

<artifacts_produced>
This plan makes `e2e/preview-perf.spec.ts` GREEN and, only if the measurement demands it, adds a block-memoization path to `pipeline.ts`/`PreviewPane.tsx`. No new source files.
</artifacts_produced>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-VALIDATION.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-UI-SPEC.md
@CLAUDE.md
@docs/TRD.md
@e2e/preview-perf.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure 60ms p95; optimize only if the measurement demands it</name>
  <precondition>The editor+preview render at /w/[wsId] (02-03 complete) and e2e/preview-perf.spec.ts exists with its self-sanity control (02-02).</precondition>
  <files>e2e/preview-perf.spec.ts (run), src/lib/markdown/pipeline.ts (conditional), src/components/preview/PreviewPane.tsx (conditional)</files>
  <read_first>
    - e2e/preview-perf.spec.ts (the harness authored in 02-02 — its MutationObserver timing, self-sanity control, 10,000-code-unit seed, p95 computation)
    - docs/TRD.md §5 (60ms budget table + "측정 없는 최적화는 하지 않는다" + the specific block-memoization approach if needed)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md §Common Pitfalls #4 + §Open Question #4 (harness validity)
    - src/lib/markdown/pipeline.ts + src/components/preview/PreviewPane.tsx (the render path being measured)
  </read_first>
  <action>
    Run `pnpm exec playwright test e2e/preview-perf.spec.ts` against the real host route. First confirm the harness self-sanity control passes (near-zero latency on the static control) — if it does not, fix the harness (it is measuring the wrong thing) before trusting any editor number. Record the measured p95 for the 10,000-code-unit document. If p95 ≤ 60ms, commit NO optimization (measure-first — the render path stays full-document re-parse + default React reconcile per TRD §5). ONLY if p95 > 60ms, add the TRD §5 block memoization (top-level hast-node memo keyed by node hash) to the render path and re-measure, recording before/after p95 in the SUMMARY. Keep the pipeline a single shared module either way.
  </action>
  <verify>
    <automated>pnpm exec playwright test e2e/preview-perf.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - e2e/preview-perf.spec.ts exits 0: the self-sanity control passes AND p95 ≤ 60ms for the 10,000-code-unit doc.
    - The measured p95 (and before/after, if optimized) is recorded in the SUMMARY.
    - No optimization is present unless a recorded p95 > 60ms motivated it (git diff shows either no perf-motivated change, or a change paired with a before/after number).
  </acceptance_criteria>
  <done>EDIT-06 is proven: keystroke→preview p95 ≤ 60ms on a 10,000-char doc, with a validated harness and measure-first discipline intact.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidated end-of-phase verification (IME + toolbar/overflow visual)</name>
  <precondition>Plugin expansion (02-04) and the perf measurement (Task 1) are complete so the full editor + toolbar + preview are live.</precondition>
  <files>.planning/phases/02-markdown-rendering-editor-formatting/02-05-SUMMARY.md (records the manual verification checklist)</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-VALIDATION.md §Manual-Only Verifications (Korean IME)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-UI-SPEC.md §UI Considerations (overflow / long-text / populated states) + §Toolbar Contract
    - ROADMAP Phase 2 Success Criteria (all five)
  </read_first>
  <action>
    Assemble the manual verification script for the end-of-phase human-check that gsd-verify-work / end-of-phase review will run. It must cover: (a) Korean IME — type "한글 조합 테스트" via a real IME and apply Bold mid-text, confirming no dropped/duplicated/reordered syllables and doc == input after composition (Success Criteria 5); (b) each of the 14 toolbar controls renders a lucide icon with an immediate hover tooltip (UI-SPEC copy) and exactly default/hover states — no pressed animation, no 300ms delay; (c) overflow states: a long unbroken URL wraps (overflow-wrap), a wide table and a long code line scroll horizontally, a long heading/paragraph wraps naturally (no ellipsis) per UI-SPEC UI Considerations; (d) the non-persistent contract — refresh loses content with no save UI and no warning (D-P2-02). Record this as the phase's manual checklist. This task authors/records the checklist; the actual sign-off happens at end-of-phase human verification.
  </action>
  <verify>
    <human-check>Execute the assembled manual checklist against `pnpm dev` at /w/[wsId]: Korean IME composition is corruption-free (Success Criteria 5); all 14 toolbar controls show lucide icons + immediate tooltips + default/hover only; long URL wraps, wide table/code scroll horizontally, long text wraps without ellipsis; refresh loses content with no save UI. This runs at end-of-phase verification.</human-check>
  </verify>
  <acceptance_criteria>
    - The manual checklist covers all four areas (IME, toolbar visual, overflow/long-text, non-persistence) and maps each to its Success Criteria / UI-SPEC source.
    - The checklist is recorded in the SUMMARY for the end-of-phase human-check.
    - `pnpm build` + `pnpm exec tsc --noEmit` remain clean and the full `pnpm vitest run` suite is GREEN.
  </acceptance_criteria>
  <done>The phase's manual verifications are consolidated into one runnable end-of-phase human-check covering IME safety, toolbar polish scope, overflow states, and the non-persistent contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored markdown → preview DOM | Unchanged from 02-03: sanitize is the terminal pipeline stage. Any memoization added here must preserve the sanitize stage (memoize the sanitized tree, never bypass sanitize for speed). |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | markdown → preview XSS (EDIT-08) under memoization | high | mitigate | If block memoization is added, it caches the POST-sanitize hast/React nodes only — sanitize still runs on every input; no cache key bypasses the sanitize stage. Proven by re-running tests/markdown/sanitize.test.ts after any optimization. |
| T-02-07 | Denial of Service | pathological 10,000-char input blowing the 60ms budget | low | accept | micromark is linear-time; the measured p95 gate is the control. If exceeded, the measured optimization (not a preemptive one) closes it. |
</threat_model>

<verification>
- `pnpm exec playwright test e2e/preview-perf.spec.ts` GREEN: self-sanity control passes + p95 ≤ 60ms on 10,000 code units.
- If any optimization was committed, `pnpm vitest run tests/markdown/sanitize.test.ts` still GREEN (sanitize not bypassed) and a before/after p95 is recorded.
- `pnpm build` + `pnpm exec tsc --noEmit` clean; full `pnpm vitest run` GREEN.
- End-of-phase human-check covers IME, toolbar visual, overflow, and non-persistence.
</verification>

<success_criteria>
EDIT-06 measured green (p95 ≤ 60ms, validated harness) with measure-first discipline intact; the phase's manual verifications consolidated into one end-of-phase human-check; sanitize safety preserved through any optimization.
</success_criteria>

<output>
Create `.planning/phases/02-markdown-rendering-editor-formatting/02-05-SUMMARY.md` when done. Record the measured p95 (before/after if optimized), whether any optimization was needed, and the consolidated manual checklist.
</output>
