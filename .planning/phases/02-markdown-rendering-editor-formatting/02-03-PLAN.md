---
phase: 02-markdown-rendering-editor-formatting
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/lib/markdown/remark-gfm-subset.ts
  - src/lib/markdown/pipeline.ts
  - src/lib/markdown/schema.ts
  - src/components/editor/plugins/types.ts
  - src/components/editor/plugins/bold.ts
  - src/components/editor/plugins/index.ts
  - src/components/editor/EditorHost.tsx
  - src/components/editor/EditorHost.module.css
  - src/components/editor/Toolbar.tsx
  - src/components/editor/Toolbar.module.css
  - src/components/preview/PreviewPane.tsx
  - src/components/preview/PreviewPane.module.css
  - src/components/layout/EditorPreviewLayout.tsx
  - src/components/layout/EditorPreviewLayout.module.css
  - src/app/(main)/w/[wsId]/page.tsx
autonomous: true
requirements: [EDIT-02, EDIT-06, EDIT-08]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The single shared markdown pipeline in lib/markdown/ parses CommonMark 0.31.2 + EXACTLY 3 GFM extensions (strikethrough/table/task-list) by composing the granular micromark/mdast packages via unified().data() — NOT the bundled all-5-GFM plugin — so footnote and autolink-literal render as literal text (research conflict #2 resolved; tests/spec/gfm.test.ts + commonmark.test.ts GREEN)."
    - "The pipeline exposes both a PRE-sanitize processor (for the CommonMark conformance test, TRD §10) and a FULL post-sanitize processor that feeds rehype-react for the preview — one module, one source (TRD §5), shared later by presentation (Phase 8)."
    - "rehype-sanitize runs with the UNMODIFIED defaultSchema (schema.ts re-exports it and documents why no merge is needed); script/on*/javascript: are stripped and task-checkbox/del/table render (tests/markdown/sanitize.test.ts GREEN) (research conflict #1 resolved)."
    - "bold.run(state) is a pure EditorState→TransactionSpec function with no EditorView/DOM access; tests/editor/bold.test.ts is GREEN (empty→'****' caret-between, 'x'→'**x**', toggle-off '**x**'→'x', Korean fixture)."
    - "EditorHost mounts CodeMirror 6 exactly once (useRef + useEffect with empty deps) and is uncontrolled — external content is never pushed back into the view mid-session, so Korean IME composition is safe (RESEARCH Pattern 3 / Pitfall 3)."
    - "PreviewPane renders the FULL pipeline output through rehype-react; empty editor shows the empty-state copy; a pipeline exception falls back to the error-state copy (UI-SPEC Copywriting) without surfacing sanitize-stripped content as an error."
    - "EditorPreviewLayout is an assemblable 2-pane CSS Grid (grid-template-columns: minmax(0,1fr) minmax(0,1fr)) using height:100% only (never 100vh inside the component), so Phase 4 can wrap it with a sidebar + status bar without touching its internals (D-P2-01, D-P2-03 assemblable-pane)."
    - "The Toolbar renders lucide icons from plugin metadata with an immediate (no-delay) hover tooltip and exactly two visual states (default/hover) — no pressed animation, no 300ms delay (D-P2-04/05); clicking the Bold button dispatches bold.run(view.state) through the index.ts registry."
    - "app/(main)/w/[wsId]/page.tsx keeps the Phase 1 requireRole(wsId,'VIEWER') + notFound() guard and the workspace-name fetch UNCHANGED, and renders EditorPreviewLayout in place of the old placeholder paragraph; the screen is non-persistent (no save UI) (D-P2-02)."
    - "END-TO-END: on the host route, typing text, selecting it, and clicking Bold produces a strong element in the sanitized preview — proven by the tracer e2e check."
    - "Empty/null editor content renders an empty preview and never throws (spec-less edge EDIT-08/empty)."
  prohibitions:
    - "The bundled all-5-GFM remark plugin (RESEARCH Pitfall 2) MUST NOT be imported anywhere in lib/markdown/ — the granular composition is the only GFM source."
    - "No block/element memoization may be added to the pipeline or PreviewPane in this plan — measure first (TRD §5 / CLAUDE.md); optimization is 02-05's conditional job."
    - "EditorHost MUST NOT accept a controlled `value`/`content` prop that is dispatched into the view on change — that reintroduces the IME corruption of RESEARCH Pitfall 3."
    - "bold.ts and every plugin MUST NOT import EditorView or another plugin (TRD §6 / CLAUDE.md invariant)."
    - "No highlight.js/prism or in-editor syntax coloring — out of scope (D-P2-10)."
  artifacts:
    - path: src/lib/markdown/pipeline.ts
      provides: "Single shared unified processor — pre-sanitize + full post-sanitize exports"
      exports: ["markdownProcessorPreSanitize", "renderMarkdown"]
    - path: src/lib/markdown/remark-gfm-subset.ts
      provides: "Granular 3-GFM unified plugin (bypasses the bundled GFM plugin)"
      exports: ["remarkGfmSubset"]
    - path: src/lib/markdown/schema.ts
      provides: "Re-export of the unmodified defaultSchema + rationale"
    - path: src/components/editor/plugins/types.ts
      provides: "EditorPlugin interface (TRD §6)"
      exports: ["EditorPlugin"]
    - path: src/components/editor/plugins/bold.ts
      provides: "bold plugin run(state) (tracer plugin)"
      exports: ["bold"]
    - path: src/components/editor/plugins/index.ts
      provides: "Plugin registry: toolbar order + keymap → dispatch(plugin.run(view.state))"
      exports: ["plugins", "editorKeymap"]
    - path: src/components/editor/EditorHost.tsx
      provides: "Uncontrolled CM6 mount"
      exports: ["EditorHost"]
    - path: src/components/preview/PreviewPane.tsx
      provides: "Renders full pipeline output via rehype-react"
      exports: ["PreviewPane"]
    - path: src/components/layout/EditorPreviewLayout.tsx
      provides: "Assemblable 2-pane grid wrapping EditorHost + Toolbar + PreviewPane"
      exports: ["EditorPreviewLayout"]
    - path: src/components/editor/Toolbar.tsx
      provides: "lucide toolbar driven by the plugin registry"
      exports: ["Toolbar"]
---

<objective>
Build the thinnest END-TO-END vertical slice that proves the entire Phase 2 architecture on one real path: type text → click Bold → CodeMirror transaction → shared markdown pipeline → sanitized preview render, wired into the real `/w/[wsId]` host route. Everything on this path is production-quality (not a prototype): the pipeline is the full granular-GFM + unmodified-defaultSchema composition, and EditorHost is the real uncontrolled mount. Only the plugin/toolbar breadth is deferred — the 13 remaining plugins expand out from this proven slice in 02-04.

Purpose (tracer-first): the phase's real architectural risks — the granular-GFM bypass (conflict #2), the defaultSchema-already-permits-GFM finding (conflict #1), and the uncontrolled CM6 IME mount (Pitfall 3) — are all on this one path. Proving them end-to-end now, on the executor's best early-context tokens, catches an architectural dead-end after one commit instead of after ten. Building the full pipeline here also turns the 02-01 conformance/sanitize suites GREEN, so the pipeline half is complete after this plan.

Output: the `lib/markdown/` pipeline (pre-sanitize + full exports), `bold` plugin + `types.ts` + `index.ts` registry, `EditorHost`, `PreviewPane`, `EditorPreviewLayout`, `Toolbar` (bold only), and the modified host route — verified end-to-end.

**Research conflict resolutions (do NOT silently rewrite the spec):**
- #2 — `lib/markdown/` composes `micromark-extension-gfm-{strikethrough,table,task-list-item}` + `mdast-util-gfm-{strikethrough,table,task-list-item}` via `unified().data()` (RESEARCH Pattern 2) and does NOT import the bundled all-5-GFM plugin. CLAUDE.md's stack line names the bundled plugin, but the STRONGER invariant "GFM 3종만" governs — record this deviation in the SUMMARY for a CLAUDE.md/TRD note.
- #1 — `schema.ts` re-exports `defaultSchema` unmodified with a rationale comment; the sanitize suite (02-01) proves it already permits del/task-checkbox/table. Record the Success-Criteria-4 amendment (its "explicitly extended" premise is superseded) in the SUMMARY.
</objective>

<artifacts_produced>
## Artifacts This Phase Produces (full phase list — this plan creates the spine; 02-04 adds the remaining plugins)

- Pipeline: `lib/markdown/pipeline.ts` (`markdownProcessorPreSanitize`, `renderMarkdown`), `lib/markdown/remark-gfm-subset.ts` (`remarkGfmSubset`), `lib/markdown/schema.ts`.
- Plugin contract: `components/editor/plugins/types.ts` (`EditorPlugin`).
- Plugins (this plan: `bold`; 02-04: `heading`, `italic`, `strikethrough`, `inline-code`, `bullet-list`, `ordered-list`, `task-list`, `blockquote`, `code-block`, `hr`, `link`, `image`, `table`) — each a pure `run(state):TransactionSpec` export.
- Registry: `components/editor/plugins/index.ts` (`plugins`, `editorKeymap`).
- React components: `EditorHost`, `PreviewPane`, `EditorPreviewLayout`, `Toolbar` (+ their `.module.css`).
- Host route: modified `app/(main)/w/[wsId]/page.tsx`.
- Test files (from 02-01/02-02): `tests/spec/*`, `tests/markdown/sanitize.test.ts`, `tests/editor/*` (14 + test-utils), `e2e/preview-perf.spec.ts`.
</artifacts_produced>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-CONTEXT.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-UI-SPEC.md
@.planning/phases/02-markdown-rendering-editor-formatting/02-PATTERNS.md
@CLAUDE.md
@docs/TRD.md
@src/app/(main)/w/[wsId]/page.tsx
@src/components/workspace/WorkspaceCard.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the shared markdown pipeline (granular GFM + unmodified sanitize)</name>
  <reversibility rating="costly">The single shared pipeline is consumed by the preview now and by presentation in Phase 8; changing GFM scope or the sanitize schema later means updating both consumers plus the conformance/sanitize test suites. The GFM-3-only decision is already locked by the CLAUDE.md invariant, so no checkpoint — the rating is recorded for the reader.</reversibility>
  <precondition>tests/spec/commonmark.test.ts, tests/spec/gfm.test.ts, tests/markdown/sanitize.test.ts exist and are RED (from 02-01).</precondition>
  <files>src/lib/markdown/remark-gfm-subset.ts, src/lib/markdown/pipeline.ts, src/lib/markdown/schema.ts</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md §Architecture Patterns → Pattern 2 (remark-gfm-subset.ts + pipeline.ts verbatim reference) + §Common Pitfalls #1 and #2 + §Don't Hand-Roll
    - docs/TRD.md §5 (pipeline order, 60ms budget, single source) — CLAUDE.md pipeline invariants
    - tests/spec/commonmark.test.ts + tests/spec/gfm.test.ts + tests/markdown/sanitize.test.ts (the contract these files must satisfy — note the exact pre-sanitize export name they import)
  </read_first>
  <action>
    Create `lib/markdown/remark-gfm-subset.ts` as the unified plugin that pushes ONLY the three granular strikethrough/table/task-list micromark + mdast extensions onto this.data() (RESEARCH Pattern 2, verbatim shape). Create `lib/markdown/schema.ts` re-exporting the unmodified `defaultSchema` from rehype-sanitize with a one-line rationale citing conflict #1 (default already permits del/input-checkbox/table/align/language-class; no merge until a test proves a gap). Create `lib/markdown/pipeline.ts` exporting TWO processors from one composition: `markdownProcessorPreSanitize` (remark-parse → remarkGfmSubset → remark-rehype{allowDangerousHtml} → rehype-raw, stopping before sanitize, stringified for the CommonMark conformance test per TRD §10) and `renderMarkdown` (the FULL processor adding rehype-sanitize{defaultSchema} → rehype-react, returning a React element tree for the preview). Do not import the bundled all-5-GFM plugin. Keep the pipeline a single module (TRD §5). No memoization.
  </action>
  <verify>
    <automated>pnpm vitest run tests/spec/commonmark.test.ts tests/spec/gfm.test.ts tests/markdown/sanitize.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tests/spec/commonmark.test.ts (652 fixtures), tests/spec/gfm.test.ts (incl. footnote/autolink literal negatives), and tests/markdown/sanitize.test.ts (XSS + task-checkbox + del + table + nested-script + empty + idempotence) all exit 0 (GREEN).
    - lib/markdown/ imports the three granular gfm packages and NOT the bundled all-5-GFM plugin.
    - schema.ts re-exports defaultSchema unmodified (no merge/override).
    - pipeline.ts exports both the pre-sanitize and full processors from one composition.
  </acceptance_criteria>
  <done>The single shared pipeline satisfies CommonMark conformance, the 3-GFM-only guard, and the sanitize/XSS contract with the unmodified defaultSchema; both research conflicts are resolved in code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditorPlugin contract + bold plugin + registry + uncontrolled EditorHost</name>
  <precondition>tests/editor/bold.test.ts + tests/editor/test-utils.ts exist and are RED (from 02-02).</precondition>
  <files>src/components/editor/plugins/types.ts, src/components/editor/plugins/bold.ts, src/components/editor/plugins/index.ts, src/components/editor/EditorHost.tsx, src/components/editor/EditorHost.module.css</files>
  <read_first>
    - docs/TRD.md §6 (EditorPlugin interface verbatim: id, icon: LucideIcon, tooltip, keymap?, run(state):TransactionSpec)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md Pattern 1 (bold.ts changeByRange toggle/wrap/empty) + Pattern 3 (EditorHost uncontrolled mount) + Pitfall 3 (IME)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-PATTERNS.md (decision-ID doc-comment convention; import ordering from WorkspaceCard.tsx)
    - tests/editor/bold.test.ts (exact fixtures to satisfy)
  </read_first>
  <action>
    Create `components/editor/plugins/types.ts` exporting ONLY the `EditorPlugin` interface (TRD §6). Create `components/editor/plugins/bold.ts` implementing the RESEARCH Pattern 1 bold plugin (`**` marker; empty→'****' caret-between, wrap 'x'→'**x**' re-selected, toggle-off '**x**'→'x') as a pure run(state) using state.changeByRange — no EditorView, no other-plugin import. Add a leading doc-comment citing D-P2-06/07/08. Create `components/editor/plugins/index.ts` as the registry: export an ordered `plugins` array (bold only for now) and an `editorKeymap` derived from each plugin's keymap, where dispatching is `view.dispatch(plugin.run(view.state))`. Create `components/editor/EditorHost.tsx` as the uncontrolled CM6 mount (RESEARCH Pattern 3: useRef + useEffect with empty deps, mount once, read content out via updateListener, never push content in; wire history() + defaultKeymap + historyKeymap + the editorKeymap; expose the live EditorView to the Toolbar via a ref/callback so toolbar clicks can dispatch). Create EditorHost.module.css consuming globals.css tokens (Mono font, accent caret/selection per UI-SPEC Editor Pane Contract). Follow the WorkspaceCard import ordering and "use client".
  </action>
  <verify>
    <automated>pnpm vitest run tests/editor/bold.test.ts && grep -Eq "useEffect\(\(\) => \{" src/components/editor/EditorHost.tsx && echo "editor-core-ok"</automated>
  </verify>
  <acceptance_criteria>
    - tests/editor/bold.test.ts exits 0 (GREEN): empty→'****' caret-between, 'x'→'**x**', toggle-off→'x', Korean fixture passes.
    - types.ts exports only the EditorPlugin interface; bold.ts imports neither EditorView nor another plugin.
    - EditorHost.tsx mounts CM6 in a useEffect with an empty dependency array (mount-once) and reads content out via updateListener — no controlled content prop dispatched into the view.
    - index.ts exposes an ordered plugins array + keymap and routes toolbar/keymap through view.dispatch(plugin.run(view.state)).
  </acceptance_criteria>
  <done>The plugin contract, the bold plugin (GREEN), the registry, and the uncontrolled IME-safe EditorHost exist and match TRD §6 + RESEARCH Patterns 1/3.</done>
</task>

<task type="auto">
  <name>Task 3: PreviewPane + assemblable Layout + Toolbar(bold) + host route wire — end-to-end</name>
  <reversibility rating="costly">The 2-pane host layout at /w/[wsId] is the seat Phase 4 wraps into a 3-pane (sidebar + status bar). Hardcoding 100vh or the grid internals here forces Phase 4 to reopen these components (D-P2-01/03). Decision already locked in CONTEXT — recorded, no checkpoint.</reversibility>
  <files>src/components/preview/PreviewPane.tsx, src/components/preview/PreviewPane.module.css, src/components/layout/EditorPreviewLayout.tsx, src/components/layout/EditorPreviewLayout.module.css, src/components/editor/Toolbar.tsx, src/components/editor/Toolbar.module.css, src/app/(main)/w/[wsId]/page.tsx</files>
  <read_first>
    - .planning/phases/02-markdown-rendering-editor-formatting/02-UI-SPEC.md (Layout & Composition Contract, Toolbar Contract, Preview Pane Prose Contract, Editor Pane Contract, Copywriting Contract incl. empty/error state copy)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-PATTERNS.md (page.tsx MODIFY-IN-PLACE rules — keep requireRole guard + workspace fetch verbatim; dashboard grid analog for layout)
    - src/app/(main)/w/[wsId]/page.tsx (current placeholder — the exact lines to preserve vs replace)
    - .planning/phases/02-markdown-rendering-editor-formatting/02-RESEARCH.md Architecture Diagram (content flows editor→pipeline→preview)
  </read_first>
  <action>
    Create `components/preview/PreviewPane.tsx` ("use client"): take the editor content string, run `renderMarkdown`, render the resulting React tree; show the UI-SPEC empty-state copy when content is empty and the error-state copy in a try/catch fallback (do NOT surface sanitize-stripped content as an error). Create PreviewPane.module.css implementing the Preview Pane Prose Contract (heading scale, task-list list-style:none + accent-color checkbox, code-block --code-bg, table borders, link accent+underline, overflow-wrap/overflow-x). Create `components/layout/EditorPreviewLayout.tsx` ("use client"): a CSS Grid `minmax(0,1fr) minmax(0,1fr)` two-pane component using height:100% only (NEVER 100vh), owning the editor content state locally (D-P2-02 non-persistent) and passing it EditorHost→PreviewPane; render the Toolbar sticky atop the editor pane only. Create `components/editor/Toolbar.tsx` driven by the `plugins` registry: render each plugin's lucide icon button (32px, 16px icon) with an immediate hover tooltip and exactly default/hover states (no pressed, no 300ms delay); a click dispatches the plugin's run through the live EditorView. For the tracer, the registry has only bold, so the toolbar shows one Bold button. Modify `app/(main)/w/[wsId]/page.tsx`: keep the requireRole(wsId,'VIEWER')+notFound() guard and the workspace-name fetch EXACTLY as-is, declare the 100vh container at this route level only, and replace the placeholder `<p>` with `<EditorPreviewLayout />`. Add no save/status UI.
  </action>
  <verify>
    <human-check>Run `pnpm dev`, sign in, open a workspace at /w/[wsId]. Type "hello", select it, click the Bold toolbar button; confirm the editor shows `**hello**` and the RIGHT preview pane renders "hello" bold (a strong element). Clear the editor; confirm the empty-state copy shows. Type "한글 조합 테스트" via a real Korean IME; confirm no dropped/duplicated syllables and the editor text matches input (Success Criteria 5 / VALIDATION Manual-Only). This human-check runs at end-of-phase verification.</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm build` succeeds and `pnpm exec tsc --noEmit` is clean.
    - page.tsx retains the Phase 1 requireRole+notFound guard and workspace-name fetch unchanged (diff touches only the imports + the render body below the h1).
    - EditorPreviewLayout uses grid `minmax(0,1fr) minmax(0,1fr)` and contains no 100vh/100dvh inside the component (100vh only in page.tsx).
    - Toolbar renders lucide icon buttons with immediate tooltips and exactly two visual states (no pressed animation, no 300ms delay).
    - END-TO-END (human-check at end-of-phase): typing + Bold click yields a strong element in the sanitized preview; empty editor shows the empty-state copy; Korean IME composes without corruption.
  </acceptance_criteria>
  <done>The 2-pane host renders a working editor + toolbar(bold) + live sanitized preview at /w/[wsId], assemblable for Phase 4, with the full path proven end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-authored markdown → preview DOM (client) | The pipeline's sanitize stage is the ONLY place unsafe content is dropped; everything upstream is intentionally allowed to be dangerous (allowDangerousHtml) because sanitize makes it safe (TRD §2 / NFR-3.1). |
| host route entry → render | app/(main)/w/[wsId] is gated by the Phase 1 requireRole membership check before anything renders. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Elevation | markdown → preview XSS (EDIT-08) | high | mitigate | Task 1 wires rehype-sanitize(defaultSchema) as the terminal pipeline stage; proven by tests/markdown/sanitize.test.ts (script strip, on* removal, javascript: neutralization, nested-script strip). PreviewPane renders only the sanitized React tree — no dangerouslySetInnerHTML. |
| T-02-05 | Information Disclosure | error fallback leaking stripped content | low | mitigate | PreviewPane's catch renders a fixed error-state copy; sanitize-stripped content is dropped silently, never echoed to the user (UI-SPEC Copywriting). |
| T-02-06 | Elevation of Privilege | rendering the host route without membership | high | mitigate | page.tsx keeps requireRole(wsId,'VIEWER')+notFound() unchanged (reused Phase 1 gate) — no new access-control surface. |
| T-02-03 | Denial of Service | ReDoS via crafted markdown | low | accept | micromark linear-time tokenizer; no custom regex parser (RESEARCH). |
</threat_model>

<verification>
- `pnpm vitest run` — full unit suite GREEN: 652 CommonMark fixtures, GFM-3-only (footnote/autolink literal), sanitize/XSS, and bold.test.ts.
- `pnpm build` + `pnpm exec tsc --noEmit` clean.
- End-to-end human-check (end-of-phase): type + Bold → strong in sanitized preview; empty-state copy; Korean IME safe.
- Grep confirms lib/markdown/ does not import the bundled GFM plugin and EditorHost has no controlled content prop.
</verification>

<success_criteria>
The full architecture is proven on one path end-to-end: the shared pipeline (granular GFM + unmodified sanitize) passes all conformance/sanitize suites, bold works, the uncontrolled editor is IME-safe, and the assemblable 2-pane host renders a live sanitized preview at /w/[wsId]. Both research conflicts resolved in code with SUMMARY amendment flags.
</success_criteria>

<output>
Create `.planning/phases/02-markdown-rendering-editor-formatting/02-03-SUMMARY.md` when done. Record: the pipeline export names for downstream consumers, the CLAUDE.md/TRD deviation note (granular GFM vs the bundled plugin), the Success-Criteria-4 amendment (unmodified defaultSchema), and the live EditorView access contract the Toolbar/02-04 expansion relies on.
</output>
