---
phase: 06-tags-search-export
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/lib/search.ts
  - src/app/api/workspaces/[id]/search/route.ts
  - src/components/tree/SearchBox.tsx
  - src/components/tree/SearchBox.module.css
  - src/components/tree/FolderTree.tsx
  - tests/search/helpers.ts
  - tests/search/nfc-normalize.test.ts
  - tests/search/idor.test.ts
autonomous: true
requirements: [DOC-04]
user_setup: []
estimate:
  tokens: 60000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "VIEWER 이상이 사이드바 상단 검색창에 입력하면 현 워크스페이스의 제목·본문·태그를 pg_trgm ILIKE 부분일치로 검색해 결과가 나온다 (DOC-04)"
    - "NFD 로 조합된 한국어 질의가 NFC 로 저장된 문서를 매칭한다(그 역도) — 질의 시 q 를 NFC 로 정규화 (성공기준 2)"
    - "검색은 현 워크스페이스로 스코프되고 타 워크스페이스 문서를 절대 노출하지 않는다 (IDOR)"
    - "q 는 파라미터 바인딩으로 쿼리에 들어가 SQL 인젝션이 불가능하다"
    - "검색 결과 리스트가 .tree 슬롯을 대체하고 클릭 시 /w/:wsId/d/:docId 로 이동, 지우면 .tree 로 복귀"
  artifacts:
    - "src/lib/search.ts (searchWorkspace)"
    - "src/app/api/workspaces/[id]/search/route.ts (GET, VIEWER)"
    - "src/components/tree/SearchBox.tsx (+ .module.css)"
    - "tests/search/nfc-normalize.test.ts, tests/search/idor.test.ts, tests/search/helpers.ts"
  key_links:
    - "SearchBox debounce → GET /search?q= → normalizeNFC(q) → searchWorkspace ILIKE → gin_trgm_ops 인덱스(06-01)"
    - "SearchBox 마운트 → FolderTree .header 위, searchActive 시 .tree ↔ .searchResults 교체"
---

<objective>
현 워크스페이스 내 제목·본문·태그를 한국어 정확하게 부분검색한다. 06-02 tracer 가 증명한 라우트/서비스/마운트 패턴을 검색 축으로 확장한다.

Purpose: DOC-04 의 핵심은 한국어 NFC/NFD 코드포인트 불일치를 없애는 것 — 06-01 이 저장 컬럼을 NFC 로 확정했으니, 이 plan 은 질의 시점 NFC 정규화를 얹어 양쪽을 맞춘다. pg_trgm GIN 인덱스(06-01)를 ILIKE 로 태우고, q 는 반드시 파라미터 바인딩한다(문자열 조합 금지 — SQL 인젝션 차단).
Output: 동작하는 워크스페이스 검색(백엔드 + 사이드바 UI).
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tags-search-export/06-UI-SPEC.md
@.planning/phases/06-tags-search-export/06-PATTERNS.md
@.planning/phases/06-tags-search-export/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: searchWorkspace 서비스 + GET 검색 라우트 (NFC·pg_trgm·파라미터 바인딩·VIEWER)</name>
  <files>src/lib/search.ts, src/app/api/workspaces/[id]/search/route.ts, tests/search/helpers.ts, tests/search/nfc-normalize.test.ts, tests/search/idor.test.ts</files>
  <read_first>
    - src/lib/closure.ts (DbClient 별칭 라인 18, getWorkspaceFolders 라인 24-29 = flat workspace_id 필터 조회 관례, sql 템플릿 execute 라인 57-62)
    - src/lib/documents.ts (DbClient 별칭 라인 8) + src/lib/validation.ts (normalizeNFC — 06-01 에서 추가됨, 질의 시 재사용)
    - src/app/api/workspaces/[id]/route.ts (라인 13-28 = z.uuid(wsId) 검증 + requireRole + ForbiddenError→403; URL 에 wsId 직접 있으므로 resolve 단계 없음)
    - 06-PATTERNS.md 라인 274-292 (search/route.ts 패턴) + 06-RESEARCH.md Pattern 2 SQL 발췌(파라미터 바인딩 ILIKE) + Architecture Diagram [검색] 섹션
    - 06-RESEARCH.md Open Question 2 (스니펫은 LEFT(content,200) 단순 앞부분 절단으로 충분 — 매치 위치 스니펫은 Deferred)
    - tests/documents/idor.test.ts + tests/rbac/helpers.ts + tests/tags/helpers.ts (통합 테스트 setup)
  </read_first>
  <behavior>
    - searchWorkspace(wsId, "한글") 이 title/content/tag 중 하나라도 부분일치하는 활성 문서만 반환한다 (is_deleted=false)
    - NFD 자모분리형으로 정규화된 질의가 NFC 완성형으로 저장된 한국어 문서를 매칭한다 (질의를 normalizeNFC 후 비교)
    - 검색은 workspace_id 로 스코프된다 — 다른 워크스페이스의 동일 제목 문서는 결과에 없다
    - q 에 작은따옴표/퍼센트 등 SQL·LIKE 메타문자가 있어도 에러 없이 리터럴 파라미터로 처리된다 (문자열 조합이 아니라 파라미터 바인딩)
    - GET /api/workspaces/:id/search 가 VIEWER 미만은 403, 비-uuid wsId 는 400
    - 결과 원소는 { id, title, snippet, tags } 형태 (snippet = 본문 앞부분 절단)
  </behavior>
  <action>먼저 tests/search/nfc-normalize.test.ts + tests/search/idor.test.ts 를 RED 로 작성한다(위 behavior; NFD/NFC 혼재 매칭은 '각'류 자모분리 vs 완성형으로, IDOR 는 idor.test.ts 형태로). src/lib/search.ts 에 searchWorkspace(workspaceId, normalizedQ, client=db) 를 추가한다: Drizzle sql 템플릿 리터럴 파라미터 바인딩으로 title/content/tag 중 하나라도 ILIKE %q% 인 활성 문서를 조회(document_tag 조인/집계로 태그 포함, LEFT(content,200) 스니펫). 절대 q 를 문자열로 이어붙이지 않는다. src/app/api/workspaces/[id]/search/route.ts 에 GET 을 신설한다: z.uuid(wsId) 검증 → requireRole(wsId,"VIEWER") → q = normalizeNFC(new URL(req.url).searchParams.get("q")?.trim() ?? "") → 빈 q 는 빈 결과 → searchWorkspace → Response.json({ results }). runtime nodejs.</action>
  <verify>
    <automated>pnpm vitest run tests/search && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tests/search/nfc-normalize.test.ts green: NFD 정규화 질의가 NFC 저장 한국어를 매칭하고 그 역도 성립한다
    - tests/search/idor.test.ts green: 결과가 워크스페이스 경계를 넘지 않고 VIEWER 미만은 403, 비-uuid 400
    - q 에 SQL·LIKE 메타문자를 넣은 케이스가 에러 없이 리터럴로 처리된다 (파라미터 바인딩 증명)
    - searchWorkspace 반환 원소가 { id, title, snippet, tags } 계약을 만족한다
  </acceptance_criteria>
  <done>워크스페이스 스코프 pg_trgm 검색이 한국어 NFC 정확성과 SQL 인젝션 방어를 갖춘 채 동작한다.</done>
</task>

<task type="auto">
  <name>Task 2: SearchBox UI + FolderTree 마운트 (debounce·레이스 가드·결과 리스트·상태)</name>
  <files>src/components/tree/SearchBox.tsx, src/components/tree/SearchBox.module.css, src/components/tree/FolderTree.tsx</files>
  <read_first>
    - src/components/tree/FolderTree.tsx (라인 282-339 = .sidebar/.header/.tree 구조 = 마운트 지점, submitCreate 라인 96-115 = fetch→상태 갱신 관례)
    - 06-UI-SPEC.md "Search Contract" 전 행 (검색 입력 40px, Search 아이콘, placeholder "문서 검색", 지우기 X, idle/typing/loading/results/no-results/error/result-hover, 검색 결과 행 구조 제목/스니펫/태그, 요청 경합 가드) + "Copywriting Contract"
    - 06-PATTERNS.md 라인 319-332 (SearchBox 마운트: .header 위, searchActive ? .searchResults : .tree; 레이스 가드 = 최신 요청 응답만 반영)
    - src/components/document/useAutosave.ts (seq 가드 원리 — 검색 레이스 가드의 참고, 최신 요청만 반영) 및 src/components/tree/FolderTreeNode.module.css (.error/.empty/.spinner 재사용)
  </read_first>
  <action>src/components/tree/SearchBox.tsx 를 신설한다: workspaceId prop + onResults 콜백(또는 내부 결과 상태). 입력을 300ms debounce 후 GET /api/workspaces/:id/search?q= 호출, AbortController(또는 요청 시퀀스 번호)로 이전 요청의 늦은 응답을 무시(가장 최근 발사 요청의 응답만 반영 — 자동저장 seq 가드의 검색판). UI-SPEC 상태를 전부 구현: idle(Search 아이콘, .tree 유지), typing(디바운스 대기 중 시각 변화 없음), loading(좌측 아이콘 12px 스피너, 리스트는 이전 내용 유지 — 빈 화면 초기화 금지), results(.searchResults: 각 행 Link /w/:wsId/d/:docId, 제목 13/600 ellipsis + 스니펫 13/400 --muted line-clamp:2 + 읽기전용 태그 pill 18px/11px, 태그 0개면 행 미렌더), no-results("'{query}'에 대한 검색 결과가 없어요"), error(.empty 스타일 --destructive "검색하지 못했어요." + 인라인 "다시 시도" 버튼), 우측 지우기 X(값 있을 때만, 클릭 시 값 비우고 .tree 복귀). SearchBox.module.css 는 전부 var(--token) 참조. FolderTree.tsx 의 .header 바로 위에 SearchBox 를 마운트하고, 검색 활성(디바운스 후 쿼리 비어있지 않음) 시 .tree 대신 .searchResults 를 렌더하도록 분기(동시 렌더 없음, 쿼리 지우면 즉시 .tree 복귀).</action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm vitest run tests/search</automated>
    <human-check>사이드바 검색창에 IME 로 '한글' 입력 → 결과 리스트, 결과 클릭 → 문서 이동, 지우기 X → 트리 복귀, 없는 단어 → no-results 문구, 로딩 중 스피너 (다크/라이트 모두). NFD 붙여넣기 질의도 같은 문서 매칭 (수동 검증 항목, VALIDATION.md)</human-check>
  </verify>
  <acceptance_criteria>
    - SearchBox 가 FolderTree .header 위에 렌더되고 검색 활성 시 .tree 가 .searchResults 로 교체된다(동시 렌더 없음)
    - 입력이 300ms debounce 되고, 연속 입력 시 이전 요청의 늦은 응답이 최신 결과를 덮지 않는다
    - idle/loading/results/no-results/error 5개 상태가 UI-SPEC 대로 렌더되고 카피가 Copywriting Contract 와 일치한다
    - SearchBox.module.css 에 하드코딩 색상 hex 가 없다 (다크 자동)
    - tsc --noEmit 클린, tests/search 여전히 green
  </acceptance_criteria>
  <done>사이드바 검색이 debounce·레이스 가드·전 상태 커버리지로 동작하고 결과 클릭이 문서로 이동한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → GET /search?q= | 사용자 질의 문자열이 쿼리로 흐름 |
| wsId(URL) → 검색 범위 | VIEWER 권한 + 워크스페이스 스코프로 제한 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SQLI | Tampering | 검색 q 를 ILIKE 에 삽입 | high | mitigate | Drizzle sql 템플릿 파라미터 바인딩으로만 q 전달(문자열 조합 금지). tests/search 의 메타문자 케이스가 리터럴 처리를 증명 |
| T-06-SEARCH-IDOR | Information Disclosure | 타 워크스페이스 문서 검색 노출 | high | mitigate | z.uuid(wsId) → requireRole(wsId,"VIEWER") → searchWorkspace 가 workspace_id=wsId AND is_deleted=false 로 스코프. tests/search/idor.test.ts 가 증명 |
| T-06-REDOS | Denial of Service | 질의에 정규식 특수문자 대량 | low | accept | ILIKE 는 정규식이 아니라 LIKE 패턴이라 ReDoS 벡터 없음. %/_ 와일드카드 광범위 매칭은 워크스페이스 내부 신뢰 사용자 대상이라 우선순위 낮음(RESEARCH) |
</threat_model>

<verification>
- `pnpm vitest run tests/search` 전체 green (NFC 매칭 + IDOR + 파라미터 바인딩)
- `pnpm exec tsc --noEmit` 클린
- 사이드바 검색 UI 5개 상태 렌더
</verification>

<success_criteria>
한국어 NFC 정확성과 SQL 인젝션 방어를 갖춘 워크스페이스 검색이 백엔드와 사이드바 UI 로 동작하고, 결과 클릭이 문서로 이동한다.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-03)
- `src/lib/search.ts` — searchWorkspace (pg_trgm ILIKE, 워크스페이스 스코프, 파라미터 바인딩)
- `src/app/api/workspaces/[id]/search/route.ts` — GET (VIEWER, 질의 NFC 정규화)
- `src/components/tree/SearchBox.tsx` + `SearchBox.module.css`
- `src/components/tree/FolderTree.tsx` — SearchBox 마운트 + searchActive 분기
- `tests/search/helpers.ts`, `tests/search/nfc-normalize.test.ts`, `tests/search/idor.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-tags-search-export/06-03-SUMMARY.md` when done
</output>
