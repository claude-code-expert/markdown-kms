---
phase: 06-tags-search-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/
  - src/lib/validation.ts
  - package.json
  - pnpm-lock.yaml
  - tests/validation/nfc-transform.test.ts
autonomous: false
requirements: [DOC-03, DOC-04]
user_setup: []
estimate:
  tokens: 55000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "document_tag 테이블이 (document_id, tag) 복합 PK로 존재하고 document 삭제 시 cascade 된다"
    - "document.title / document.content 에 gin_trgm_ops GIN 인덱스가 is_deleted=false 부분 조건으로 2개 붙는다 (DOC-04)"
    - "기존 document 행의 title/content 가 NFC 로 백필된다"
    - "documentSchema 가 저장 시 title/content 를 NFC 로 정규화하고, content 는 여전히 trim 하지 않는다 (DOC-04, NFR-5.2)"
    - "archiver + @types/archiver 가 설치되어 06-04 export 가 소비할 수 있다"
  artifacts:
    - "src/db/schema.ts (documentTag 테이블 선언)"
    - "drizzle/ 신규 마이그레이션 2건 (document_tag 테이블 generate + pg_trgm custom SQL)"
    - "src/lib/validation.ts (normalizeNFC, tagsBodySchema, documentSchema NFC transform)"
    - "tests/validation/nfc-transform.test.ts"
  key_links:
    - "documentSchema.transform → documents/[id]/route.ts PUT(autosave) + documents POST(생성) 에 파일 수정 없이 자동 전파"
    - "gin_trgm_ops GIN 인덱스 → 06-03 searchWorkspace 의 ILIKE 가 소비"
    - "document_tag FK ON DELETE CASCADE → document 소프트/완전삭제 시 태그 정리"
---

<objective>
Phase 6 의 데이터·검증 기반을 놓는다: document_tag 테이블, pg_trgm 확장 + GIN 인덱스(제목·본문), 기존 행 NFC 백필, 저장 경로 NFC 정규화, 태그 body 스키마, archiver 설치. 태그(06-02)·검색(06-03)·export(06-04) 세 축이 전부 이 plan 의 산출물 위에서 동작한다.

Purpose: pg_trgm GIN 인덱스는 plain 컬럼 위 인덱스(함수형 아님, TRD §3 리터럴)라 한국어 NFC 정확성(성공기준 2)을 얻는 유일한 경로가 "저장 시 컬럼을 항상 NFC 로 채우고 + 기존 행을 백필하는 것"이다. 이 plan 이 그 절반(쓰기 NFC + 백필 + 인덱스)을 확정하고, 나머지 절반(질의 NFC)은 06-03 이 얹는다. 마이그레이션은 되돌릴 수 없으므로 one-way 게이트로 승인받는다.
Output: document_tag 스키마 + 마이그레이션 2건 + NFC 정규화 검증 스키마 + archiver 의존성.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tags-search-export/06-RESEARCH.md
@.planning/phases/06-tags-search-export/06-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: one-way 마이그레이션 게이트 (document_tag + pg_trgm + NFC 백필)</name>
  <decision>document_tag 테이블 생성 + pg_trgm 확장/GIN 인덱스 + 기존 행 NFC 백필 UPDATE 를 로컬 dev DB(PG16@5433)에 지금 적용할지</decision>
  <context>Phase 3(folder)·Phase 4(document)·Phase 5(draft) 의 스키마 게이트와 동일한 절차다. 마이그레이션은 되돌릴 수 없고(REVERSIBILITY: one-way), 백필 UPDATE 는 기존 문서의 title/content 를 제자리에서 NFC 로 재작성한다. 적용 후에는 롤백 대신 새 마이그레이션으로만 전진한다.</context>
  <options>
    <option id="apply-now">
      <name>지금 적용 (기존 phase 3/4/5 관례)</name>
      <pros>후속 태그·검색·export plan 이 바로 실 DB 에서 검증 가능. Phase 3/4/5 와 동일 흐름</pros>
      <cons>되돌릴 수 없음 — 백필이 기존 행을 재작성</cons>
    </option>
    <option id="defer">
      <name>보류</name>
      <pros>없음 (후속 모든 plan 이 블록됨)</pros>
      <cons>Phase 6 전체 진행 불가</cons>
    </option>
  </options>
  <resume-signal>Select: apply-now 또는 defer (orchestrator 가 사전 승인했다면 apply-now 로 기록하고 재프롬프트 없이 진행)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: document_tag 스키마 + pg_trgm custom SQL 마이그레이션 + NFC 백필</name>
  <reversibility rating="one-way">DDL + 백필 UPDATE 는 적용 후 되돌릴 수 없다 — Task 1 게이트 승인 후에만 실행</reversibility>
  <files>src/db/schema.ts, drizzle/</files>
  <precondition>.env.local 의 DATABASE_URL 이 로컬 PG16@5433 을 가리키고, 그 역할이 CREATE EXTENSION 권한을 가진다 (CONTEXT: worktree 미사용 순차 실행, main tree)</precondition>
  <read_first>
    - src/db/schema.ts (documentDraft 라인 124-130 = FK+cascade 관례, workspaceMember 라인 32-48 = 복합 PK 관례, document 라인 94-96 = "document_tag 인덱스는 Phase 6" 주석을 이 파일이 해소)
    - docs/TRD.md 라인 113-121 (pg_trgm 확장 + doc_title_trgm/doc_content_trgm GIN 인덱스 + document_tag DDL 원천 SQL)
    - 06-PATTERNS.md 라인 52-82 (document_tag drizzle 선언형 + custom SQL 마이그레이션 문체: statement-breakpoint, 큰따옴표 identifier)
    - 06-RESEARCH.md Pitfall 1·2 + Anti-Patterns (drizzle-kit 이 gin_trgm_ops 누락하는 버그 → custom SQL 로 손작성, 백필 UPDATE 는 normalize(text,NFC))
    - drizzle/0003_petite_susan_delgado.sql, drizzle/0004_high_roulette.sql (기존 마이그레이션 파일 문체)
  </read_first>
  <action>src/db/schema.ts 에 documentTag 테이블을 추가한다: documentId(uuid, notNull, references document.id onDelete cascade) + tag(text, notNull), 복합 PK primaryKey({ columns: [table.documentId, table.tag] }). pg_trgm 확장/GIN 인덱스는 drizzle DSL 에 넣지 않는다 — 다음 generate 시 drizzle 가 "인덱스 없음"으로 오인해 DROP 시도하는 것을 막기 위함(RESEARCH Pitfall 2). `pnpm drizzle-kit generate` 로 document_tag CREATE TABLE 마이그레이션을 뽑는다. 그다음 `pnpm drizzle-kit generate --custom` 으로 빈 SQL 파일을 만들어 손으로 작성: (1) CREATE EXTENSION IF NOT EXISTS pg_trgm, (2) document 의 title 에 USING gin (title gin_trgm_ops) WHERE is_deleted=false GIN 인덱스, (3) content 에 동일 형태 GIN 인덱스, (4) 기존 행 NFC 백필 UPDATE(title/content 가 normalize(...,NFC) 와 다른 행만 갱신). TRD §3 / PATTERNS 의 SQL 을 그대로 옮기되 emit 된 SQL 파일을 반드시 눈으로 열어 gin_trgm_ops 연산자 클래스가 실제로 포함됐는지 확인한다. `pnpm drizzle-kit migrate` 로 두 마이그레이션을 적용한다.</action>
  <verify>
    <automated>pnpm drizzle-kit migrate && psql "$DATABASE_URL" -tAc "SELECT count(*) FROM pg_indexes WHERE tablename='document' AND indexdef LIKE '%gin_trgm_ops%'" | grep -qx 2</automated>
    <human-check>psql 로 \d document 를 실행해 두 GIN 인덱스 정의에 gin_trgm_ops 가 박혀 있고(USING gin (title) 만 있는 게 아니라) 부분 조건 WHERE is_deleted=false 가 붙었는지 육안 확인</human-check>
  </verify>
  <acceptance_criteria>
    - document_tag 테이블이 (document_id, tag) 복합 PK 로 존재하고 document.id FK 가 ON DELETE CASCADE 이다
    - pg_indexes 에 document 대상 gin_trgm_ops 포함 인덱스가 정확히 2개(title, content) 존재한다
    - 백필 UPDATE 실행 후 SELECT count(*) FROM document WHERE title IS DISTINCT FROM normalize(title,NFC) OR content IS DISTINCT FROM normalize(content,NFC) 가 0 을 반환한다
    - schema.ts 에는 document_tag 테이블만 선언되고 pg_trgm 인덱스는 DSL 에 없다 (다음 generate 가 DROP 을 시도하지 않는다)
  </acceptance_criteria>
  <done>document_tag 테이블 + 2개 GIN 인덱스 + NFC 백필이 dev DB 에 적용되고 마이그레이션 파일이 커밋됐다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NFC 저장 정규화 + tagsBodySchema + archiver 설치</name>
  <files>src/lib/validation.ts, tests/validation/nfc-transform.test.ts, package.json, pnpm-lock.yaml</files>
  <read_first>
    - src/lib/validation.ts (documentSchema 라인 55-58, autosaveBodySchema 라인 63-65, normalizeEmail 라인 7-9 = trim+정규화 순수 헬퍼 관례)
    - 06-PATTERNS.md 라인 136-167 (normalizeNFC, documentSchema NFC transform, tagsBodySchema)
    - 06-RESEARCH.md Pattern 2 + Anti-Patterns (content 에는 trim 을 붙이지 않는다 — NFR-5.2 앞뒤 공백 보존; title 만 trim)
    - 06-RESEARCH.md Standard Stack / Package Legitimacy Audit (archiver 8.0.0 / @types/archiver 8.0.0, verdict OK/Approved)
  </read_first>
  <behavior>
    - normalizeNFC("각"의 NFD 자모분리형) === "각"의 NFC 완성형
    - documentSchema.parse 가 NFD 로 들어온 title/content 를 NFC 로 변환해 반환한다
    - documentSchema 는 content 의 앞뒤 개행/공백을 보존한다 (trim 하지 않음, NFR-5.2)
    - documentSchema 는 title 을 여전히 trim 하고 255자 초과 시 검증 에러를 낸다 (기존 계약 유지)
    - tagsBodySchema 는 tags:string[] 를 받고 각 원소를 trim/min(1) 하며 4개 이상이면 검증 에러를 낸다 (클라 계약; 서버 최종 권위는 06-02 COUNT 검증)
  </behavior>
  <action>src/lib/validation.ts 에 normalizeNFC(text)=text.normalize("NFC") 순수 헬퍼를 추가하고, documentSchema 의 title 에 .transform(normalizeNFC) 를(기존 trim/max 뒤에), content 에 .transform(normalizeNFC) 를 붙인다. content 에는 절대 .trim() 을 추가하지 않는다. tagsBodySchema = z.object({ tags: z.array(z.string().trim().min(1)).max(3, ...) }) 와 TagsBodyInput 타입을 export 한다. documentSchema 변경은 autosaveBodySchema(extend) 와 documents 라우트 2곳에 자동 전파되므로 라우트 파일은 건드리지 않는다 — 단, 전파로 기존 autosave/생성 테스트가 깨지지 않는지 전체 스위트로 확인한다. archiver 는 `pnpm add archiver` + `pnpm add -D @types/archiver` 로 설치한다(RESEARCH legitimacy 게이트 통과 [VERIFIED], 설치 스크립트 없음 — 공급망 리스크 낮음).</action>
  <verify>
    <automated>pnpm add archiver && pnpm add -D @types/archiver && pnpm vitest run tests/validation/nfc-transform.test.ts && pnpm vitest run tests/documents tests/draft</automated>
  </verify>
  <acceptance_criteria>
    - tests/validation/nfc-transform.test.ts 의 모든 케이스가 green (NFD→NFC 변환, content 공백 보존, title trim 유지)
    - 기존 tests/documents · tests/draft 스위트가 documentSchema transform 추가 후에도 전부 green (전파 회귀 없음)
    - package.json 에 archiver 와 @types/archiver 가 명시되고 pnpm-lock.yaml 이 갱신된다
    - validation.ts 의 content 필드에 .trim() 이 없다
  </acceptance_criteria>
  <done>저장 경로가 NFC 를 강제하고, tagsBodySchema 가 존재하며, archiver 가 설치돼 후속 plan 이 소비 가능하다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → 빌드 | archiver/@types/archiver 설치 (공급망) |
| 마이그레이션 → dev DB | 백필 UPDATE 가 기존 행을 제자리 재작성 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | archiver / @types/archiver 설치 | medium | mitigate | RESEARCH Package Legitimacy Audit 에서 [VERIFIED] OK/Approved, postinstall 스크립트 없음 확인 완료 — TRD §1 잠금 의존성, 신규 미검증 패키지 아님 |
| T-06-01-BACKFILL | Tampering | NFC 백필 UPDATE | low | accept | is_distinct-from 가드로 이미 NFC 인 행은 건드리지 않음. 일회성 배치, one-way 게이트로 승인 |
</threat_model>

<verification>
- `pnpm drizzle-kit migrate` 성공 + gin_trgm_ops 인덱스 2개 확인
- `pnpm vitest run` 전체 green (documentSchema transform 전파 회귀 0)
- `pnpm exec tsc --noEmit` 클린
</verification>

<success_criteria>
document_tag + pg_trgm GIN 인덱스 + NFC 백필이 적용되고, 저장 경로가 NFC 를 강제하며, tagsBodySchema 와 archiver 가 준비돼 06-02/06-03/06-04 가 블록 없이 시작할 수 있다.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-01)
- `src/db/schema.ts` — documentTag 테이블
- `drizzle/00NN_*.sql` (generate) — document_tag CREATE TABLE
- `drizzle/00NN_*.sql` (custom) — pg_trgm 확장 + 2개 gin_trgm_ops GIN 인덱스 + NFC 백필 UPDATE
- `src/lib/validation.ts` — normalizeNFC, documentSchema NFC transform, tagsBodySchema/TagsBodyInput
- `tests/validation/nfc-transform.test.ts`
- `package.json` / `pnpm-lock.yaml` — archiver 8.0.0, @types/archiver 8.0.0
</artifacts_produced>

<output>
Create `.planning/phases/06-tags-search-export/06-01-SUMMARY.md` when done
</output>
