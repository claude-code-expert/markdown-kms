---
phase: 06-tags-search-export
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/lib/documents.ts
  - src/app/api/documents/[id]/tags/route.ts
  - src/components/document/TagBar.tsx
  - src/components/document/TagBar.module.css
  - src/components/document/DocumentWorkspace.tsx
  - src/app/(main)/w/[wsId]/d/[docId]/page.tsx
  - tests/tags/helpers.ts
  - tests/tags/replace.test.ts
  - tests/tags/rbac.test.ts
autonomous: true
requirements: [DOC-03]
user_setup: []
estimate:
  tokens: 62000
  raw_tokens: 33000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "EDITOR 이상이 문서 메타 바에서 태그를 추가하면 PUT /api/documents/:id/tags 로 전체 교체되고 새로고침 후에도 남는다 (DOC-03)"
    - "4번째 태그를 직접 API 로 보내면 서버가 단일 트랜잭션 COUNT 검증으로 400 을 내고 DB 에 3개만 남는다 — 서버가 최종 권위 (NFR-3.2)"
    - "대소문자만 다른 중복 태그는 서버가 dedup 한다"
    - "다른 워크스페이스 문서의 태그를 수정하려 하면 403 (IDOR), EDITOR 미만도 403"
    - "메타 바가 titleRow 와 body 사이에 렌더되고 3개 도달 시 입력이 비활성된다"
  artifacts:
    - "src/lib/documents.ts (replaceTags, getTags, TagLimitError)"
    - "src/app/api/documents/[id]/tags/route.ts (PUT)"
    - "src/components/document/TagBar.tsx (+ .module.css)"
    - "tests/tags/replace.test.ts, tests/tags/rbac.test.ts, tests/tags/helpers.ts"
  key_links:
    - "TagBar → PUT /tags → replaceTags 트랜잭션 → document_tag (06-01 테이블)"
    - "d/[docId]/page.tsx getTags → DocumentWorkspace initialTags → TagBar 초기 chip"
    - "replaceTags COUNT>3 throw → 라우트 catch → 400 (트랜잭션 자동 롤백)"
---

<objective>
TRACER: 태그 한 축을 데이터→서비스→라우트→UI→마운트 전 계층에 걸쳐 end-to-end 로 관통시켜 Phase 6 의 척추(신규 라우트 + 트랜잭션 서비스 + 신규 UI 표면 + RSC 초기값 배선)를 증명한다. 이 슬라이스가 검증되면 06-03(검색)·06-04(export)는 동일한 라우트/서비스/마운트 패턴을 확장만 하면 된다.

Purpose: 태그는 이 phase 에서 유일하게 "신규 테이블 쓰기 트랜잭션 + 신규 API + 신규 컴포넌트 + 기존 화면 마운트 + RSC 초기값"을 한 번에 요구하는 축이라, 아키텍처 막다른 길을 가장 먼저 드러낸다. 3개 제한(DOC-03)의 서버 권위(COUNT 트랜잭션)를 실패 테스트로 먼저 못박고 구현한다.
Output: 동작하는 태그 입력/저장 + 4번째 거부 + RBAC/IDOR 방어.
</objective>

<execution_context>
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/workflows/execute-plan.md
@/Users/codevillain/Claude-Code-Expert/markdown-kms/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tags-search-export/06-UI-SPEC.md
@.planning/phases/06-tags-search-export/06-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 태그 end-to-end 한 경로 — 서비스 + 라우트 + TagBar + 마운트 + RSC 초기값</name>
  <files>src/lib/documents.ts, src/app/api/documents/[id]/tags/route.ts, src/components/document/TagBar.tsx, src/components/document/TagBar.module.css, src/components/document/DocumentWorkspace.tsx, src/app/(main)/w/[wsId]/d/[docId]/page.tsx, tests/tags/helpers.ts, tests/tags/replace.test.ts</files>
  <read_first>
    - src/lib/documents.ts (DbClient 별칭 라인 8, resolveWorkspaceIdForDocument 라인 36-46, restoreDocument 트랜잭션 라인 75-97 = client.transaction 관례)
    - src/lib/closure.ts (softDeleteFolder 라인 145-164 = 트랜잭션 내 throw=자동 롤백)
    - src/app/api/documents/[id]/route.ts (PUT 라인 14-56 = z.uuid→resolveWorkspaceIdForDocument→requireRole→zod parse 4단계 IDOR shape)
    - 06-PATTERNS.md 라인 86-206 (replaceTags 확정 코드 + tags/route.ts IDOR 4단계 + TagLimitError→400 매핑)
    - src/components/document/DocumentWorkspace.tsx (라인 105-125 titleRow↔body 마운트 지점, handleTitleChange 라인 77-81 = 즉시 로컬 갱신+백그라운드 저장 패턴)
    - src/app/(main)/w/[wsId]/d/[docId]/page.tsx (라인 31-63 Promise.all 로 초기값 조회 후 DocumentWorkspace 로 prop 전달)
    - 06-UI-SPEC.md "Tag Meta Bar Contract" (chip 레이아웃, X 제거, empty placeholder, 즉시 로컬 반영+PUT)
    - tests/documents/idor.test.ts + tests/documents/helpers.ts + tests/rbac/helpers.ts (통합 테스트 setup: vi.mock @/auth, createTestWorkspace/addMember/mockSessionFor)
  </read_first>
  <behavior>
    - replaceTags(docId, ["a","b","c"]) 후 getTags(docId) 가 3개를 반환한다
    - replaceTags(docId, ["a","b","c","d"]) 는 TagLimitError 를 던지고 트랜잭션이 롤백돼 document_tag 에 이전 상태만 남는다 (COUNT 는 삽입 후 트랜잭션 안에서 센다)
    - replaceTags 는 replace-all 이다 — 두 번째 호출이 첫 번째 태그 집합을 완전히 대체한다
    - replaceTags(docId, ["React","react"]) 는 대소문자 무시 dedup 으로 1개만 남긴다 (첫 등장 원형 보존)
    - PUT /api/documents/:id/tags 가 3개 이하 tags 로 200, 4개로 400, 잘못된 uuid 로 400 을 낸다
  </behavior>
  <action>먼저 tests/tags/replace.test.ts 를 RED 로 작성한다(위 behavior, tests/documents/helpers.ts + tests/rbac/helpers.ts 재사용, 신규 tests/tags/helpers.ts 로 태그 삽입/조회 팩토리). 그다음 src/lib/documents.ts 에 TagLimitError 클래스 + replaceTags(트랜잭션: 기존 태그 DELETE → trim+NFC+대소문자무시 dedup → INSERT → COUNT(*)>3 이면 throw) + getTags 를 추가한다(06-PATTERNS 확정 코드 채택, import { sql } + documentTag 추가). src/app/api/documents/[id]/tags/route.ts 에 PUT 을 신설한다 — documents/[id]/route.ts PUT 의 4단계 IDOR shape 를 그대로 복제하되 requireRole 을 "EDITOR" 로, body 는 tagsBodySchema 로 파싱, replaceTags 결과 200 / TagLimitError catch → 400. src/components/document/TagBar.tsx 를 신설한다: initialTags/documentId props, chip 목록 + bare 입력, Enter/comma 로 추가·X 로 제거 시 로컬 즉시 반영 후 PUT 전송(제목 입력의 즉시-로컬+백그라운드 패턴), empty placeholder. TagBar.module.css 는 순수 var(--token) 참조만(다크 자동, UI-SPEC chip 스타일: 높이 24px, --surface-2, radius 999px). DocumentWorkspace.tsx 의 titleRow 와 body 사이에 TagBar 를 마운트하고 initialTags prop 을 추가한다. d/[docId]/page.tsx 의 Promise.all 에 getTags(docId) 를 넣어 initialTags 로 내려보낸다.</action>
  <verify>
    <automated>pnpm vitest run tests/tags/replace.test.ts && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - tests/tags/replace.test.ts 전 케이스 green (3개 저장, 4번째 롤백, replace-all, dedup, 라우트 200/400)
    - 4번째 태그 400 이후 DB 의 document_tag 행 수가 3 이하다 (트랜잭션 롤백 증명)
    - TagBar 가 DocumentWorkspace 의 titleRow 와 body 사이에 렌더되고 초기 chip 이 getTags 결과로 채워진다
    - tsc --noEmit 클린
  </acceptance_criteria>
  <done>태그를 추가/제거하면 서버에 replace-all 로 저장되고, 4번째는 서버가 400 으로 거부하며, 메타 바가 문서 화면에 배선된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RBAC/IDOR 방어 + TagBar 전체 상태 커버리지</name>
  <files>tests/tags/rbac.test.ts, src/components/document/TagBar.tsx, src/components/document/TagBar.module.css</files>
  <read_first>
    - tests/documents/idor.test.ts (cross-workspace IDOR 통합 테스트 형태 그대로 복제 — 403 + DB 미변경 assert)
    - tests/rbac/matrix.test.ts (역할별 403 assert 패턴)
    - 06-UI-SPEC.md "Tag Meta Bar Contract" 전 행 (at-limit-disabled, duplicate-rejected, tag save fail 롤백) + "Copywriting Contract" (placeholder/중복/실패 카피)
    - src/components/tree/FolderTree.tsx (라인 104-114 = fetch !res.ok 시 상태 되돌림/에러 세팅 관례)
    - src/components/tree/FolderTreeNode.module.css (.error 클래스 재사용 — 태그 에러 텍스트)
  </read_first>
  <behavior>
    - EDITOR 미만(VIEWER) 이 PUT /tags 호출 시 403 이고 document_tag 는 변경되지 않는다
    - 다른 워크스페이스 문서 id 로 태그를 수정하려 하면 403 (resolveWorkspaceIdForDocument 로 workspace_id 서버 재유도)
    - 잘못된(비-uuid) 문서 id 는 400
  </behavior>
  <action>tests/tags/rbac.test.ts 를 작성한다(idor.test.ts 형태: VIEWER 403, 타 워크스페이스 문서 403 + DB 미변경, 비-uuid 400). TagBar.tsx 에 UI-SPEC 잠금 상태를 채운다: 3개 도달 시 입력 disabled + placeholder "최대 3개까지 추가할 수 있어요", 대소문자 무시 중복 시 chip 을 만들지 않고 입력만 비운 뒤 .error 텍스트 "이미 추가된 태그예요"(다음 keystroke 에 소거, 타이머 없음), X hover 시 X 아이콘만 --destructive, PUT 실패 시 방금의 로컬 변경을 되돌리고 .error "태그를 저장하지 못했어요. 다시 시도해 주세요." 표시(다음 성공 조작이 자동 소거). 경쟁 상태 없이 단순 순차 전송으로 충분(태그 조작은 저빈도).</action>
  <verify>
    <automated>pnpm vitest run tests/tags && pnpm exec tsc --noEmit</automated>
    <human-check>문서 화면에서 태그 3개 추가 → 입력 비활성 확인, 중복 태그 입력 → 에러 문구, 태그 삭제 → chip 제거, 새로고침 후 유지 (다크/라이트 모두)</human-check>
  </verify>
  <acceptance_criteria>
    - tests/tags/rbac.test.ts 전 케이스 green (VIEWER 403, cross-ws IDOR 403 + DB 미변경, 비-uuid 400)
    - TagBar 가 UI-SPEC 의 at-limit-disabled / duplicate-rejected / tag save fail 세 상태를 모두 렌더한다
    - TagBar.module.css 에 하드코딩 색상 hex 가 없고 전부 var(--token) 참조다 (다크 자동 전환)
  </acceptance_criteria>
  <done>태그 라우트가 EDITOR+/IDOR 로 방어되고, TagBar 가 3제한·중복·저장실패 상태를 UI-SPEC 대로 표시한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → PUT /tags | 태그 배열이 untrusted 입력으로 서버에 도달 |
| 문서 id(URL) → workspace 권한 | 클라가 암시하는 워크스페이스를 신뢰하지 않고 서버 재유도 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-TAGCAP | Tampering | 태그 3개 제한 우회(직접 API 4개 전송) | high | mitigate | replaceTags 단일 트랜잭션 안에서 INSERT 후 COUNT(*)>3 throw → 라우트 400, 롤백. UI 비활성화는 보안 경계 아님(NFR-3.2). tests/tags/replace.test.ts 가 증명 |
| T-06-TAG-IDOR | Elevation of Privilege | 타 워크스페이스 문서 태그 수정 | high | mitigate | resolveWorkspaceIdForDocument 로 workspace_id 서버 재유도 후 requireRole(wsId,"EDITOR"). tests/tags/rbac.test.ts 가 증명 |
| T-06-TAG-MASS | Tampering | mass-assignment(임의 필드 주입) | medium | mitigate | tagsBodySchema 가 tags:string[] 만 허용, replaceTags 는 documentId/tag 만 INSERT |
| T-06-TAG-XSS | Information Disclosure | 태그 텍스트가 미리보기/검색결과에 렌더 | low | accept | 태그는 chip 텍스트 노드로만 렌더(HTML 주입 아님), 마크다운 파이프라인 미경유 |
</threat_model>

<verification>
- `pnpm vitest run tests/tags` 전체 green
- `pnpm exec tsc --noEmit` 클린
- 라우트가 EDITOR+ 이고 4번째 태그 400
</verification>

<success_criteria>
태그 축이 데이터→서비스→라우트→UI→RSC 전 계층에서 동작하고 서버가 3개 제한의 최종 권위를 가진다. 이 tracer 로 06-03/06-04 가 복제할 라우트/서비스/마운트 패턴이 증명된다.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (06-02)
- `src/lib/documents.ts` — replaceTags, getTags, TagLimitError
- `src/app/api/documents/[id]/tags/route.ts` — PUT (EDITOR, COUNT 400)
- `src/components/document/TagBar.tsx` + `TagBar.module.css`
- `src/components/document/DocumentWorkspace.tsx` — TagBar 마운트 + initialTags prop
- `src/app/(main)/w/[wsId]/d/[docId]/page.tsx` — getTags 초기값 배선
- `tests/tags/helpers.ts`, `tests/tags/replace.test.ts`, `tests/tags/rbac.test.ts`
</artifacts_produced>

<output>
Create `.planning/phases/06-tags-search-export/06-02-SUMMARY.md` when done
</output>
